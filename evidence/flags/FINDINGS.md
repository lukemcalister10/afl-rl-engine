# FLAG-CLUSTER PER-PLAYER DIAGNOSTICS — INDEX

**State:** `[BAKED c47cb43d]` · main `389ac39` · engine md5 `c47cb43d` · READ-ONLY on engine/data.
Every number here is state-labelled `[BAKED c47cb43d]`; every single-player analysis prints
identity as **ID(key)·pick·cohort(debutyr)** — never surname alone (the store holds four
Picketts, three "darcy"-matching rucks, two Serongs). No engine/data change; no bake.

STEP 0 asserts (blocking): main `389ac39` ✓ · engine `_merged_recover.py` md5 `c47cb43d` ✓
(the bootstrap's "expect 8aed420a" is the stale pre-bake stamp; the task pins the baked head).

| Flag | Folder | Headline BEFORE-number `[BAKED c47cb43d]` |
|---|---|---|
| **(a) M1 breakout-crediting** | `m1/` | 46 up-branch hits; 34 SUSTAINED / 12 SPIKE. Only 46% of each recent-over-career gap is credited; **$10,604 withheld from the 34 sustained risers**. Pickett exemplar (kysaiah-pickett) withholds **$711** vs $158 banked. |
| **(b) DPP forward-eligibility** | `dpp/` | 49 young DPPs (30 fwd-eligible). Baked engine prices max-weight position only; forward-eligibility credit ≈ **$0–10** (Serong anchor: old-engine +$275 lift **dropped**, baked strip even mis-signs +$80). |
| **(c) ruck derivation** | `ruck/` | 1.4×PVC cap binds 46/50 active rucks; top-of-ruck **plateau is talent-blind** (gawn≈edwards ~$838). Proven cap-bound rucks (sean-darcy −$163, reilly-o-brien −$257) **ride the legacy PVC ladder**, not output. |

Each folder holds the script(s), the raw `.txt` output, a machine-readable `.json`, and a
per-flag `FINDINGS.md` with the full decompositions and sum-to-total guards.

## Decomposition discipline

- **(a)** level decomposition `gap = 0.46·credited + 0.54·withheld` — sum-to-total guard
  **PASS 46/46**; dollar leg `(base−zero)+(full−base)=(full−zero)` reconciles.
- **(c)** full-pipeline `e_prod + Δcap+stale + ΔM3 + Δfloor = ev` — guard **PASS** all four
  named rucks; sit-out sub-term `(1−λ)·R·v0_start + λ·e_full = click` reconciles.
- **(b)** no cross-sum claimed: per-player gaps live on two engines/scales that must never
  be subtracted across; kept in separate columns and labelled.
