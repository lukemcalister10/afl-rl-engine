# LEG F4 — CHECKPOINT (mandatory; no engine edit before the answer) · seat 13 · 2026-07-19
**Status: HALT-AND-RETURN for the fence grant + MEMO addendum v1.2. Diagnosis read-only, board-verified,
k=0/backward dormancy proven byte-identical. Nothing edited; ±5% reproduced OUT (−25.8%), not bent.**

1. **MECHANISM (measured, φ=0 mid/veteran):** the forward decline is ~2× the realized backward — an
   L-SYMMETRY breach. It lives ENTIRELY in `raw_ev`'s production price `pr=price6(b6)` (iso flat, pole≈0).
   The forward lens advances the AGE clock through TWO reads F3's form-anchor never touches, then LEVERS the
   result ~4× through `price6`→`v_at_peak`'s `posval(level−REPL)` nonlinearity (a 6–7% level drop → 23–28% pr):
   • **veteran** = the `_dev_advance` AGE_CURVE roll — neutralizing it: veteran −24.8%→−10.6%, MID+VET
     −20.9%→−14.5% ≈ backward −14.4% (symmetry restored, 1.01×). Dominant driver.
   • **mid** = the demonstrated-level band `b6` reading AGE_REF — decline IDENTICAL with/without the
     form-anchor (−5.93% vs −5.86%); a separate age term the `_off` construction cannot reach.
2. **ADDRESSES:** `_merged_recover.py:820-821` (`_proj_w4`: `cur=level_now` left advancing) · `:287-288`
   (`b6`=`cond_prior_band`+q97, read AGE_REF) · `rl_model.py:361` (`_dev_advance` roll — **HARD-OUT**) ·
   `distribution_pricing.py::v_at_peak` via `price6@289` (the ~4× leverage — **OUTSIDE any fence**).
3. **PROPOSED §-LAW-COMPLIANT FIX (for your grant):** a forward-lens-gated **L-SYMMETRY damper in
   `_merged_recover.py`** (the F3 `_form_anchor_clock` pattern): temper the AGE_REF advance of the two
   production-price age reads (the `_dev_advance` output consumed at `:821`/`:851` and the `b6` band age at
   `:287-288`) so the forward age-decline magnitude matches the SAME players' REALIZED backward-transition
   rate (measured off the −2/−1/now boards F3 committed). This is a CALIBRATION of the existing curve to the
   realized bar — no new lens-only decay term (Reid), no cohort hand-tune, gated RL_LEGF + forward-only +
   k=0-inert (byte-exact by construction). One site class: the production-price age-read wrapper.
4. **WHAT IT CANNOT REACH without bending a law:** the `posval(level−REPL)` leverage is in
   `distribution_pricing.py::v_at_peak` — unfenced; the roll's own source `_dev_advance` is `rl_model.py`
   HARD-OUT (so the damper must wrap the CONSUMPTION in `_merged_recover.py`, not edit the roll). If the
   honest realized-transition rate still cannot bridge forward −19.9%→−9.0% within ±5% without cohort
   hand-tuning, that is the OWNER's tension (the ±5% expectation itself), returned — not forced.

**Requesting:** (a) fence grant = `_merged_recover.py` production-price age-read wrapper (`:287-288` + the
`:821`/`:851` `level_now` consumption); (b) MEMO_LEGF v1.2 carrying the damper construction + its seal.
