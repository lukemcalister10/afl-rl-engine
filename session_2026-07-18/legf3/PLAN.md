# LEG F3 — THE PROJECTION FIX · PLAN + SEALS · seat 13 · 2026-07-18
Cures the item-352 verdict per MEMO_LEGF ADDENDUM v1.1 (§2.vi projection compliance + §2.iii distributed
retirement). WRITER build. Supervisor ruling **item 353** governs scope (fence extended to the 4 named
`_merged_recover` clock sites; V0/`_iso_dec` :1121-1171 absolutely HARD-OUT; rl_export:96 / proj_from_peak "if
still implicated"). Base: code-lineage tip `7b6dfc52` (F1). Store `968de0c7` · curve `56dd7a7b` (absolute, matched).

## 0 — ENTRY PROOF (container-relative doctrine item 347; CORRECTED)
Filed hashes reproduce BYTE-EXACT single-thread on this container (3/3): balanced `06d8af60`. (An early cold
build read `30d96f1f` — a transient; NOT reproduced since. Withdrawn. Lesson: warm the build; never generalize a
reproducibility claim from one cold run.) Numeric-stack annex: py3.12.3 · numpy2.4.4 · scipy1.17.1 · sklearn1.8.0
· scipy-openblas 0.3.31 DYNAMIC_ARCH · threads pinned 1 (OPENBLAS/OMP/MKL/NUMEXPR — item 349).

## 1 — THE MEASURED MECHANISM (measure-first; probes in scripts/, board-verified)
item-352 reproduced on this container: developing ≤23 −30.5% (steepest = inverted gradient), young high-pick
−34%. Located: the forward lens de-pedigrees young rows via (a) the pedigree/tenure CLOCKS advancing with
AGE_REF (pole `wage`/`tfade` in `raw_ev`; the `_par_prior` pedigree par in `_coreM1`; the staleness gate in
`ev`; proj_from_peak's runway) and (b) the form anchor at `rl_export:96` NOT re-supplying the pedigree anchor
at +k. The band (`cp.cond_prior_band`) is INERT to the age clock (measured: form-anchoring it = byte-identical).

## 2 — THE DERIVED FENCE (touched set ⊆ this; F1 standard)
IN: `_merged_recover.py` — the 4 ruled clock sites (`_form_anchor_clock`/`_fa_year` helper · `_par_prior` ·
`ev` staleness · `PR.tenure` keying via `_fa_year`; `raw_ev` pole fade) + `_proj_w4` (proj_from_peak, ruling
pt 4, still-implicated: horizon age-shape + runway form-anchored) · `rl_export.py` (form-anchor pedigree-carry
at :96, ruling pt 4 + original fence; §2.iii exit construction) · `session_2026-07-18/legf3/`.
HARD-OUT (untouched, verified): the store · the curve · `_merged_recover` V0/`_iso_dec` :1121-1171 · q97m ·
pins/acceptance · docs · SEASON_PROG · ui.
**Every §2.vi edit is gated on RL_LEGF (default ON) AND forward-only (MA._LENS_FORM / offset>0) ⇒ RL_LEGF=0 OR
k=0 ⇒ INERT ⇒ the pre-F3 chain byte-exact BY CONSTRUCTION** (proven: `06d8af60`/`d85901af`/`9829d01a`).

## 3 — THE FIX (§2.vi projection compliance; Reid: same map at the projected evidence state, no new
## multiplier / lens-only growth term)
(i) **Clock cure** (`_merged_recover`, the 4 ruled sites): the pedigree/tenure clocks (pole `wage`/`tfade`,
    `_par_prior`, staleness `el`, `PR.tenure`) evaluate at the FORM ANCHOR (BASE_REF) in the forward lens —
    "one more year of thin production does not erase draft pedigree." `proj_from_peak` (`_proj_w4`): horizon
    age-shape + young-runway form-anchored (`ah=a−offset`); growth flows through the advancing level.
(ii) **Re-supply the pedigree anchor at +k** (`rl_export:96`, the findings' exact prescription): floor the +k
    value at the now value `_v` decayed by PROJECTED EVIDENCE — **the SEALED φ blend-decay**:
    **`φ(g) = (1 − g/G0)²`, G0 = 46 career games** — the engine's OWN L1c young-credit decay (`_merged` _ycred),
    NOT a new constant. `vP_k = max(vP_k, round(φ(g)·_v))`. Smooth in games, inert once evidence completes
    (g≥46 ⇒ φ=0 ⇒ byte-exact), k=0-safe (φ≤1). **SEAL: φ = (1−g/46)² — sha256_8 `fd92b6fc`.**

## 4 — §2.iii DISTRIBUTED RETIREMENT LIABILITY (replaces the discrete X-bar exit, `rl_export` ~:576)
No named exits. At +k each age-eligible player carries `h(p) = P(retire|age(p),+k)·vP(p)`; aggregate liability
= Σh; expected slots = ΣP; phantom intake re-sized to the aggregate liability. **P(retire|age)** measured from
F2 recorded exits (pooled −2/−1/now, ±1yr, age≥26 eligible, youth floored ≤0.02, isotonic-increasing per rule 7,
no wide bins). **SEALED TABLE** (age→P, +1 horizon; +k cumulative `1−(1−P)^k`):
`{24:.02,25:.02,26:.073,27:.087,28:.104,29:.123,30:.151,31:.177,32:.208,33:.267,34:.388,35:.388,36:.388,37:.388,38:.50,39:.50}`
— **sha256_8 `6100f121`.** [OWNER] ratifies at the viewing (§6 Leg-E workflow).

## 5 — ACCEPTANCE (the item-352 harness, frozen-form; scripts/backtest.py)
(1) F2 −1 board projected forward reproduces now 752,427 ±5% ([714,806 , 790,048]); −2→−1 vs 771,152.
(2) per-cohort signed-mean gradient un-inverted: developing ≥ mid ≥ veteran.
(3) k=0 `06d8af60` byte-exact · RL_LEGF=0 chain byte-exact · store `968de0c7` — all single-thread.
RESULTS filled in EXIT_PROOF.md.

## 6 — EXIT
k=0 dormancy unit test (clock identity at each edited site) · frozen suite · guard-5 = the known pre-bake red
(flag, never self-pin) · numeric-stack annex · PR stacked on #115 · RETURN ≤30 lines.
