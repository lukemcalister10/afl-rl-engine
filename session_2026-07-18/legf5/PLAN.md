# LEG F5 — THE ENTRANT LAYER + THE CONSERVATION GATE · PLAN + SEALS · seat 13 · 2026-07-19
Base: F4 exit `a9570cb5` (F3/F4's cures are the floor, never re-opened). WRITER build; the chapter's LAST.
Owner-ruled construction MEMO_LEGF v1.3 §2.viii + §2.x (item 359). Single-thread OPENBLAS/OMP/MKL/NUMEXPR=1
on every hash-bearing run.

## 0 — GIT ENTRY (item-334 law; this-session fetch)
`git fetch origin main claude/legf4-midvet-calibration-o6x36j` — F4 ls-remote returns
`a9570cb5d567300cc1969328018035e700f88963` STRICT (matched). Branch
`claude/legf5-entrant-layer-conservation-p4susl` re-pointed onto F4 exit `a9570cb5` (was sitting at the
disjoint docs-lineage `main`; every commit it carried is a docs register/directive already present on
`origin/main`, so NOTHING is discarded — the exact F4 seam precedent). Docs stay on MAIN — never joined.

## 1 — ENTRY PROOF (container-faithful; structural per the F1 discriminator doctrine, item 347)
DATA anchors ABSOLUTE + matched: store `rl_model_data.json`=**968de0c7** · curve `pvc_curve_v2.json` file
**56dd7a7b** (self-stamped payload **89c14729**). F4 seals PRESENT + unmoved: r_pop **c62b5ee8** ·
s(age) **efe97ee3** (sealed_rate_pop.json @ a9570cb5). This container instance is DETERMINISTIC (two warm
RL_LEGF=0 builds byte-identical, stable balanced md5 `83a4b21d`, Σv 750,159) but does NOT byte-reproduce the
filed `06d8af60` — a container-instance weather delta living ENTIRELY in the unedited F4 base (one panel row,
Sheezel, at a −95 container delta; the other 9 within ±3). Per the F1 discriminator (the filed bytes are not
in-repo and are container-instance dependent — item 347 forbids CORETYPE archaeology), the k=0 invariant is
proved STRUCTURALLY as a byte-exact DIFF against the F4 base IN THIS CONTAINER, not by reproducing filed bytes:
- **RL_LEGF=0 chain byte-exact**: edited `rl_export.py` == original F4 `rl_export.py` at RL_LEGF=0 (the
  balanced/Leg-E board is byte-identical — the §2.viii block is entirely inside the `RL_LEGF!=0` branch).
- **k=0 phantom = NONE**: RL_LEGF=1 balanced `v` == RL_LEGF=0 `v`, 0/804 diffs (additive lens-scoped arrays;
  engine ev() untouched — the balanced board cannot move by construction).

## 2 — SEALS (job 1; committed BEFORE any render — the §6 seal-first law; LAW: no tuning against the gate)
`sealed_entrant_structure.json` (seal sha256_8 **a17aafed**), measured from recorded store intake history:
- **Window** 2019–2025 (7 complete draft years; the partial 2026 mid-season cohort excluded). Board-eligible
  (GRP-priced) entrants only — the population that surfaces in the realized league total.
- **Pricing** = the engine's OWN effective pick `effpk(p)`: ND = pick · RD/PSD = chained after the national
  draft (`_NDC[year]+pick`) · pickless mechanisms = measured pick-equivalents PICKEQ (MSD **90**, all others
  **92** — item 341). Each expected slot credited at the v2-curve PVC of its effective pick.
- **Smoothing (rule 7)** = per-effective-pick MEAN per-year occupancy over the window (no wide/decile bins).
- **Result**: expected **103.4 slots/yr** (draft 73.1 + mech 30.3) · entrant PVC **draft 69,266 + mech 14,272
  = TOTAL 83,538**. Measured ONCE, sealed pre-render, NOT iterated against the gate.

## 3 — §2.viii THE ENTRANT LAYER (rl_export.py phantom block; replaces F1's 30-pick + flat-207 sizing)
The `RL_LEGF!=0` phantom block emits the FULL sealed intake per forward lens: draft slots at PVC[effpk],
mechanisms at PVC[90/92], `phantom=true`, per-club allocation by natural draft order (mechanisms split even).
The F1 pick-slot strawman (`_LF_R=207` free-intake, natural-order picks 1..30, exit bar X) is SUPERSEDED
(obituary in-block; the owner's ratification list updates). Report/view only · never at k=0 · never gates/bakes.

## 4 — §2.x THE CONSERVATION GATE (job 3; the owner's law)
- **F5 LEAGUE-LEVEL, ±5%, BOTH transitions**: project each committed historical roster forward under the
  sealed r_pop, ADD the §2.viii entrant layer at PVC, vs the realized league total (F2 bridge_totals:
  minus2 770,987 · minus1 771,152 · now 752,427 — ~flat).
- **F4 ROSTER-MATCHED gate re-run** (must stay IN ±5%): −2.8% / +0.1%.
- **Survivor-only DIAGNOSTIC** committed (ageing-quality isolation; never a gate).

## 5 — EXIT (job 5)
k=0 byte-exact (edited==original RL_LEGF=0) · RL_LEGF=0 chain · dormancy suites PASS · store untouched ·
guard-5 = the known pre-bake red · per-lens totals report (bal/+1/+2, with/without phantom, per club + league)
· annex · PR stacked on #118. RETURN ≤30 lines, THE TWO GATE TABLES FIRST.

## FENCE: IN = `rl_export.py` phantom block + `session_2026-07-18/legf5/` (+ `ui/app/board.js` display ONLY
if mechanically required — justified). HARD-OUT: everything else, absolute — store · curve · rl_model.py ·
_merged_recover.py · v_at_peak · V0/`_iso_dec` · q97m · pins/acceptance · docs · SEASON_PROG. HALT rather
than bend; any k=0 change ⇒ HALT.
