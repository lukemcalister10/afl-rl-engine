# RETURN — THE EVIDENCE WEIGHT: FOUR REGIMES → ONE CONTINUOUS OBJECT
- **Branch** `claude/intelligent-lovelace-6v863q` · base pin `62352729` STRICT (ls-remote+rev-parse verified) · merge line `#82→#83→#85→this`.
- **Head SHA** `<HEAD>` · **PR** `<PR>`.
- **The job — ALL FOUR TOGETHER, delete-don't-disable.** One continuous evidence weight on effective qualifying seasons `E_q` replaces: the 10-game bar (`_nqual`), the nqual ramp (`c=n/4`), the PROVEN_N n=3→4 cliff (pedigree par VANISHING), the exposure regime (`expgate` n≥4 gate). Engine `113a4223 → edc47017`.
- **Movers** 403 / 2652 priced · **ΣΔ −5714 num-SCAR**. Separable by evidence state (thin ramp −1922 · proven residual −4710 · unqualified +202 · just-proven +716); one lever, path-additive; 0 movers lack an evidence explanation. AFFECTED_ROWS committed.
- **A8 (Berry/Tsatas) HOLDS** — Berry `2836→2868`, Tsatas `1243→1256` (raw ev), ratio `2.282×→2.283×` (need ≥2.00). The trap's own anchor: the steep qualifying bar keeps Tsatas' sub-10-game seasons at ~0 evidence, so production keeps carrying him (Tsatas +12 num). The recorded A8 break did NOT recur.
- **G-COHORT (July-8) PASS×3, IMPROVED** — y4 `1.2660→1.2413` (margin to 1.30 **0.0587**), y5 `1.2477→1.2164` (**0.0836**), y6 `1.1588→1.1266` (**0.1734**).
- **A-PAIRS scored** — pair 2 reid/bont **+1.3%** (band ±10% PASS); pair 3 sanders/bont **+13.7%** (the canonical standing-failed read, sanders 3960; EXPECTED until the PVC cure — scored, not skipped). **PICK 1 = 3000**.
- **Kill-switch proof** — `RL_EVW=0` → board md5 **`24159c49`** (== base board of record, BYTE-EXACT at both per-player and export level); `RL_EVW=1` (default) → **`9a9889f8`**. Declared exception (the #85 pattern), NOT a manifest dial → **config_sha256 `c2d233ae` UNMOVED**.
- **Three narrowest margins overall** — A10 Curnow 0.55 vs 0.50 (**0.05**) · B1 y4 (**0.059**) · B1 y5 (**0.084**).
- **⚠ ONE NEW GATE FAIL — A9 (Ginnivan > Ward) FLIPS: `1955 vs 2016`** (base Ginnivan 1941 > Ward 1735). This is the **T5 pedigree residual (R98.5) itself**: pick-1 Ward's par (83.7) pulls him UP at weight 0.11, low-pick Ginnivan's par (67.0) pulls him DOWN. Verified NOT tunable — holding A9 requires r ≤ 0.012, i.e. essentially zeroing the residual, which contradicts the T5-measured 0.11 and R98.5's "never vanishes". **A law-vs-anchor conflict for OWNER RULING** (like pair 3): re-rule A9, or move the residual. Reported by name with values; not silently smoothed.
- **Snapshot (DELIVERABLE)** `data/gates_snapshots/gates_edc47017.json` md5 `a6da870a`. Full ship-gates LOG committed (`ship_gates_EVW_final.log`). VERDICT FAIL=4 (A2/A3/A12 base + A9 new) · PASS=16 · PENDING=4.
- **Guards** — fresh-bootstrap Guard 5 PASS; SSI GUARDS 1/2/3/5 green (board read-only+stamped, board==engine 0 mismatches); GUARD 4 canary `<CANARY>`. Book RE-SEALED (head `113a4223→edc47017`, stable_sha256 `92e401a5→3152fa06`, n=2649). expected_boot RE-PINNED (board + engine_head only; **store `340a7a32` UNCHANGED**; config UNMOVED).

## OBITUARY — the four evidence regimes (deleted, not disabled; RL_EVW=0 resurrects them byte-exact)
1. **THE 10-GAME QUALIFYING BAR** (`_nqual`: `games>=10`, census 551 within one game) — the hard 0/1 season-qualifying step. → continuous soft bar `q(g)=logistic((g−11)/1.1)` summed to `E_q`; a 9-game and a 10-game season no longer jump.
2. **THE NQUAL RAMP** (`c=n/PROVEN_N`: 0.75/0.5/0.25 integer steps, census 545 at n=1–3) → the continuous pedigree weight `pw(E_q)`.
3. **THE PROVEN_N n=3→4 CLIFF** (the pedigree par dropping to 0 at n=4, census 253) → `pw(E_q)` fades to the T5 residual **0.11** (never 0, R98.5); the production reference blends `Lc→est` continuously so no cliff is left "at the top of the staircase".
4. **THE EXPOSURE REGIME** (`expgate = 1.0 if nqual≥4 else exposure/POLE_RAMP`) → `expgate = ramp + est(E_q)·(1−ramp)`: base ramp for the unproven, → 1.0 as evidence establishes; the D10 `_playable` proration preserved.

## Close (plain terms)
The engine stopped judging a player by four hard counters — did he clear the 10-game line, how many seasons, has he crossed four, has he banked enough exposure — and now reads ONE smooth thing: how much qualifying football he has actually played. Nobody's price jumps because a counter ticked over. A draftee is priced on his pedigree; as he plays, his own football takes over; and even a proven veteran keeps a small, measured trace of his draft billing — it fades, it never vanishes. The trap anchor (Berry must be worth twice Tsatas) held because a man who has had four years and never held down a season is judged on that record, not his draft ticket. The one place it bit: a former no.1 pick who has been solid-not-great (Josh Ward) now edges just ahead of a mid-pick who has been solid (Jack Ginnivan) — the residual pedigree working exactly as ruled, colliding with an old anchor that predates it. That is the owner's to weigh.

## TIME
Band 2–4 h; actual ~`<TIME>`. Within band (no >2×/<½× flag).
