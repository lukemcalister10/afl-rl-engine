# RETURN — Walk-Forward Book Calibration v1.1 (Tier 3, READ-ONLY)

**Base** `9be07b8e` STRICT · Guard-5 PASS (store `b1fd0bce` · engine `fc7045d6` · rl_model `f79fc740` ·
config `c2d233ae`). **Book** regenerated (frozen `s4_matrix_M1v7.py`, gate mode): **N=2649 ✓, all seal
identity stamps ✓.** Stable-sha not bit-exact across runners (`1b3c856a` vs sealed `99be9b36`) — known
cross-CPU float drift; **bounded 99.6 % EXACT vs the committed canonical board (≤4 SCAR on 0.4 % of
cells)**. Scored book = the sealed book; drift far below cohort signal. DECLARED, not hidden.

**Method fixed in PLAN before any number.** `cur` is a runway stock, output a flow → age-conditioned
throughout; "under-priced" is reference-dependent (marginal β vs population `m`) — both reported.

**THE THREE AXES (measured fact):**
- **Compression (131) CONFIRMED:** price ~ output^β, β=**0.36** peak (n=598, CI[.32,.41]); **0.68**
  proven-27+ (CI excl 1) — matches 131's ~0.69. English/Briggs: output **5.8×** → price **1.56×**.
- **Mid-round trough (132) CONFIRMED, RUC-severe, untouched by trio:** `iso_corr` = evidence-blind
  pos×pick tax; RUC picks **19–33 = −10 to −19 %** while pick-34 = 1.000 (English −11.5 %, Grundy −18 %,
  Gawn −18 %). Structural — the trio was a store patch; **the whole table remains.** 38 contributors in it.
- **Young (130):** star-track **under-priced** vs realisation (~1.4× top-pick → ~7.7× late-pick), by
  design; unconditional (busts kept) over-priced on pedigree (Sanders pick-6, realised −2.5, cur 4220).
  ⇒ **young net-cut is wrong-direction as measured law.**

**Signed mispricing surface** (`cohort_calibration.csv`, under-priced +): most cohorts fair/uncertain;
significant = prime GEN_FWD 23–26 under, faded/early-pick 27+ over. The owner's gap lives in the **curve
shape + pick tax**, not any cohort's price level (English `m`<0 vs population yet under-rewarded at margin).

**Committed** (`session_2026-07-15/book_calibration/`): PLAN.md · calibrate.py · FINDING.md · OWNER_READ.md ·
CSVs (cohort_calibration, output_price_map, iso_corr_structural_table, iso_corr_trough_summary,
iso_pickband_residual, young_cohort_walkforward, worked_rows). Regenerated book = scratch (not committed).
**NO cure proposals** — PVC spec rework is the supervisor's, on this baseline.

**Branch** `claude/walk-forward-book-calibration-pwojve` · **head SHA** `__HEAD__` · **PR** #__PR__

**IN PLAIN TERMS:** the book pays elite output sub-proportionally and taxes rucks drafted at picks 19–33
forever regardless of what they've proven — while the young side it's blamed for is already priced below
what those players realised.
