# D7 ASK 5 — CLARIFICATIONS (one-line answers first; support beneath)
_2026-07-02 · canonical head `8aed420a` store `644d1254`._

**(i) n=807 vs the 805-player universe:** NOT containment — the two populations are different sets that overlap in 746 players: sweep 807 = 746 shared + **61 sweep-only** (59 stale never-played store rows that fail the board's `active()` recency/unplayed test + **2 `_double_count` rows**), export 805 = 746 shared + **59 export-only** (the pre-debut draftee `extra` list, which lives outside `MA.data`); the visible "+2" is 61−59 near-cancellation, and the 2 double-count rows are the literal difference-makers. (Measured this session at canonical: overlap 746 / sweep-only 61 / export-only 59.)

**(ii) D6 clamp-run new-B1 160.2 vs head 160.5 under a pure lower bound:** CORRECT AND EXPLAINED — a lower bound on VALUES is not a lower bound on INDEXED RATIOS. new-B1 averages cohort value INDEXED to yr1=100, and the floor's biggest schedule value (0.45) lands on yr-1 craters: reproduced on the head matrix, the clamp adds +9,028 SCAR at depth 1 (the index DENOMINATOR, mean cohort lift **+1.04%**) vs only +534 at depth 4 (numerator lift **+0.04%**) — so every indexed average past d1 falls ~1% while every underlying value stays ≥ its unclamped self. No base drift, no transcription error. (Fingerprint-rule reproduction gives d4 158.9; D6's printed 160.2 used a slightly narrower cell-exclusion — both sit inside D6's own printed bracket [155.5 incl-delisted, 160.5 unclamped], and B1 PASSES everywhere in it. The same arithmetic is why v2's board B1 reads 156.2 vs 156.6 at minus-cB.)

**(iii) Distributional/quantile pricing output: NIL — nothing anywhere in the engine outputs a value distribution or quantile.** The one distributional object is INTERNAL: the conditional-prior band `bb = [q10,q30,q50,q70,q90,q97m]` built in `b6()` (`_merged_recover.py:90-93`, q97m appended from the GBR) and immediately integrated to a scalar by `price6()`'s `WQ6` dot product (`_merged_recover.py:94-99`). The export emits point values only — `v/vM1/vM2/vP1/vP2 = ev(p, 2024..2028)` (`rl_export.py:28-33`); the exported `band`/`bands` fields are DRAFT-PICK bands (`bandof(effpk)`), not value quantiles. The board-side band machinery that once existed (`tail_restore.rband`, `wire_redesign._capture_bands`) was deleted at D4 (obituary rows 1/3). The U21-4 quantile-output spec is paper-only, confirmed from primary source.

**(iv) BASE effort rule:** registered verbatim in `docs/process/PROCESS_CHANGES_2026-07-02.md` §11 beneath the D6 amendment (high default · xhigh with justification · max with explicit sign-off · every directive carries level + wall-clock band + walk-away note); CHANGELOG carries the registration in the D7 session entry.
