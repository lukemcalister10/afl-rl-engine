# DIRECTIVE 4 — session notes — Luke's ruling package
_2026-07-02 · BUILD · branch `claude/lukes-ruling-package-krhxq7` (PR) + `claude/d4-bake-candidate-m2-m1v7` (candidate)._
_State: canonical head `8aed420a` / store `644d1254` / band `34faa865` UNTOUCHED on the PR branch · post-merge main `e8688a72` (PR #6 merged as STEP 0) · CANDIDATE fingerprint: engine `fb39d88a` · cp `5ac8b162` · store/band unchanged._

## ASK 1 — INSTRUMENT AUDIT (PASS)
- **A8 resolved on the printout** (`d4_a8_audit.txt`): scripted expression WAS the literal 2× (`b > 2*t`); raw values Berry=3473.0 / Tsatas=1083.0 → both reported passes GENUINE (engine 3.21×, board-path 3.87×). Neither pre-named candidate (missing-2× / ±20% leak) present — defect = **display ambiguity** ("vs 2x Tsatas=2166" reads as raw Tsatas; Luke's 2×2166=4332>3473 is that reading). Fixed: raw values + explicit ratio printed; `>` → `>=` per frozen "at least 2x".
- **Full 23-line audit** (`d4_instrument_audit.md`): 20 MATCH (6 in Luke-amended form) · 3 MISMATCH (A8 display/strict-ineq · A10 amendment unwired · B5 signed schedule unwired) — all fixed this session.
- Amendments wired: A2 (already in, D3) · **A10 0.70→0.50** (Luke, data-caused, PROVISIONAL + season-complete review note) · **A3 "evaluated PRE-LTI-layer"** annotation · **B5 signed schedule** (ASK 2). CHANGELOG carries all, per the amendment process.
- **Corrected board at canonical head: 9 PASS / 8 FAIL / 5 PENDING / 1 STRUCK** (was 8/9/5/1). Deltas vs last board: A8 PASS (display fixed, ratio printed) · A10 FAIL→**PASS at 0.51 vs 0.50** (the knife-edge, 0.01 headroom — pre-flagged, not a surprise) · B5 FAIL 9 → FAIL 51 (see ASK 2) · all other gates unchanged.

## ASK 2 — B5 WIRING (wired exactly as signed; boundary flag raised)
- Wired: floors by years-in-system yrs1-7+ = .45/.35/.28/.21/.13/.09/.05 × draftval · population = NATIONAL-DRAFT entrants only, post `PRESENT_ID_OVERRIDES` `type=='ND'` (the overrides re-type re-entries — Perez is SSP-2025 in `MA.data`, so the plain filter is exact) · delisted/retired excluded · **generating rule recorded beside the constants** (floor ≈ 0.9 × smoothed clean p5, ND-only) · **RE-BASE-AT-PVC reminder written into SHIP_GATES.md** on B5 beside A5's sibling note (Luke approved in writing; new SHIP_GATES.md md5 in the return).
- **Prediction verified: the 9 MSD/SSP/RD offenders VANISH.**
- **New status: FAIL with 51 offenders at head** (`d4_b5_decomp_3e_spot.txt`): 23 in the development window (yr2 ×1, yr3 ×9, yr6 ×6, yr7 ×2, yr8 ×5 — Ugle-Hagan yr6 213<270, Chayce Jones yr8 19<80…) = the gate doing its founding job pre-M2/M3; **28 deep veterans (yr9–18: Sidebottom yr18 3<69, Bews yr15…) = the .05-forever tail binding far beyond the generating data** (yr8+ clean p5 runs .01–.09). At the candidate the count grows to 82 (v7 compresses fading veterans further). **LUKE FLAG: uphold .05-for-all-7+ or bound the guard at the development window the generating rule supports (e.g. yrs 1–8) — one-word ruling; the script takes either.**

## ASK 3 — ONE-PRICE EXECUTION (delete done; obituary committed)
- 3a(i): replacement netting in **BOTH paths, once each, never stacked — double application = ZERO** (engine `price6()` and board `_price_repl` did the identical `REPL_DROP` save/restore around `dp.v_at_peak`). 3a(ii): `TR.production_value` consumed **no ev()** (independent chain; off the export dep graph). → 3b: **nothing to promote, no HOLD** — delete proceeded directly.
- 3c: deletion commit **`f2bb22b`** — wire()/router/tail-restore/RUC-pool/redesign_value layers/soft floor/Brodie×0.5/lens-tilt/RUCK_TAX all deleted; `verify_anchors.py` deleted. **`BOARD_LAYERS_OBITUARY.md` (md5 `27af6b41`)**: per-layer plain description · D3 1b measured magnitude · rationale · deletion commit · resurrection ref.
- 3d: `rl_export.py` renders **engine ev() — one universal source**; parity verified byte-exact on named rows (Berry 3473 / Tsatas 1083 / Reid 3523 / Duursma 4179 / Uwland 1781 / Ginnivan 1578; 805 active). Regenerated bundle md5 `1c94348e` at canonical (`4d0584a3` at candidate) vs shipped `b8f9e998` → B4 stays red until Luke's ONE new board (bake-gated, as directed).
- 3e: view re-plumbed — `vM2/vM1/v/vP1/vP2 = ev(p, 2024/2025/2026/2027/2028)` baked at export; JS switches fields, owns no math. Spot rows sane (Daicos 7917/7874/7059/6118/4830; Rozee vM1=3874 == engine-2025). **Held piece (spec'd, flagged):** retired back-rows price flat (`delisted()` hardcodes 2026 in the frozen head) — Y-aware delisting is an engine edit → candidate-branch item.
- Post-delete verification: restore-verify 9/9 · panel 10/10 · engine md5 unchanged `8aed420a`.

## ASK 4 — BAKE CANDIDATE (wired + verified; HOLDS for cold audit + Luke's bake word)
- Branch `claude/d4-bake-candidate-m2-m1v7`, commit `0806d90`. M2 in `conditional_prior._exposure` (f=0.545, s=clip(1−g₂₆/11,0,1), RL_EXPO_F kill-switch); M1+v7 transplanted verbatim from the read-pass-endorsed matrix prototype into `_merged_recover.py` (real-store-players-only v7; ISO/pole frozen on originals).
- **Pre-logged expectations, all landed:** A3 0.642 (M1v7-only, == D3 reconstruction) → **0.658 with M2** (pre-logged ~0.659 ✓); Rozee 2679→2460 under M1v7 (== the UNRESOLVED flag) · **Ward M1-TOL knife-edge stated: gap = +3.0 vs TOL_M1=5** → no M1 lift; one strong fortnight flips it (5.9→3.0 as D3 flagged) · **Curnow one-liner:** Lo 77.7 → Lc 73.1 (gap −4.5, partial down-shed); A10 = **0.549 — the 0.50 knife-edge did NOT flip, headroom grew** (0.51→0.55) · **Tsatas one-liner:** Lo 66.7 → Lc 75.7 (+9.0, M1-lift candidate) while v7 compresses Berry −36.7% → A8 ratio 3.21×→**2.24×, still ≥2×**.
- Kill-switch identity: `RL_EXPO_F=1` reproduces M1v7-only exactly (A3 0.642, Curtis/Ward 0.873 — D3's reconstruction values).
- **Candidate board: 11 PASS / 6 FAIL / 5 PENDING / 1 STRUCK** (`d4c_board.txt`): A5+A9+A10 green (A5/A9 flips predicted by D2 scratch); A2 closes 0.652→**0.875** vs 0.90 (Curtis leg; Weddle leg green); A3 0.66 red; A12 red (both legs); B4 red (expected); B5 red 82 (boundary flag); B6 red (shape unchanged ±3pts — cliff-blend directive's seam). Restore-verify at candidate: exactly the axes that must move moved (head md5 fb39d88a; Maric 1409→1178, Langdon 593→575); store/band/harness PASS; panel tail matches (Smillie 896, Green 741 — pre-debut v7-inert).
- **Walk-forward book rebuilt at candidate** (`data/s4_matrix_candidate_fb39d88a.json`, plain 7147 builder): **B1 pooled PASS-shape (peak N=4, R=148 >100, path_ok) but per-cohort backstop 16/17 — cohort 2020 fails the rise-above-yr1 (yrs4-6 R=97/98/95)**. **ATTRIBUTION RESOLVED — ENGINE-CAUSED**: same-builder control on canonical (`data/s4_matrix_control_8aed420a.json`, `d4_control_B1.txt`) gives R=160, 17/17, cohort 2020 at 109/110/110 → the M1+v7 wiring breaks the 2020 cohort's backstop (M2 byte-exact on those completed-season cells). A live bake-blocker under frozen B1 for the cold audit + Luke's read. A13/A14 advisory: candidate brings Burgoyne INTO ±20% (1858 lineball=True; all five advisory names now lineball).
- **NOTHING MERGED AS CANONICAL** — candidate holds per BAKE_CHECKLIST for the external cold audit + Luke's explicit bake word.

## ASK 5 — M3 DERIVATION (design + backtest; NOTHING wired)
Design: `m3_design_proportional_tenure.md` — value-space interpolation between full-click and clock-pinned evaluations (pin = cp._age_asof/MA.age/PR.tenure/_eo-N/_feat-ten at Y−1; evidence windows untouched — avoids double-prorating M2's channel), scoped by M2's s (zero on-pace collateral BY CONSTRUCTION), fE = SEASON_PROG = 0.58, byte-exact inert at fE=1 and on completed seasons; B1 interaction stated (historical cells untouched — in-progress-year only).
Backtest (`d4_ask5_m3_out.json` `7419c3d0` + `d4_ask5_m3_preoverlay_out.json` `9c6b409e`): Rozee +10.7% (age axis dominates: age-pin +24.7% vs tenure-pin +5.0%); on-pace collateral **0/288** ✓; B5 82→63; B6 ramp holds (≥11g identical, no new dips); A10 untouched (0.549, s=0); spot rows sane (Green +6.2% young sat-out; Maric +2.8% at 9g). **A3: candidate 0.658 → 0.728 @fE=0.58 (0.742 @0.50; full-pin ceiling 0.863) — pre-registered ≥0.80 NOT REACHED.** Pre-overlay decomposition: plain head 0.692 → +M2 0.706 → +M2+M3 **0.780** (0.794 @0.50) — **the M1+v7 overlay costs A3 ~0.05; the clock pair alone lands a whisker under the bar.** Luke rules on the A3-vs-overlay tension; fE NOT tuned to the gate.

## ASK 6 — LTI GROUNDWORK
- `LTI_REGISTER_2026-07-02.md` committed as schema + guards (Maxwell King rename guard; key-by-store-key) — **CONTENT GAP: the directive said supervisor supplies entries; none arrived** — flagged per REQUIRED_INPUTS discipline, nothing invented.
- (i) **No LTI identification/return-haircut machinery exists** — nearest is the B2 present-unavailability haircut `_b2hc` (`rl_model.py:800-812`: established ≥3-season peak≥90 player, 0 games in 2026, season >⅓ done → age-banded present haircut 8.8%/3.9%/0, Now-board only, applied at k=0 in `proj_from_peak:316` and `prod_floor:330`); `_role_decay_hc` (`rl_model.py:184`) explicitly excludes injury. No injury cause, no return window, no multi-year haircut anywhere.
- (ii) **Yes — value is already a per-season sum**: `proj_from_peak` (`rl_model.py:304`) accumulates k=0..17 seasons of `posval(level_age_k − REPL)·21/(1+d)^k` — a win-now view = exposing the k-term partial sums; feasible without redesign.
- (iii) MSD/SSP pick-equivalents: pickless mechanisms load with a placeholder `_eff=75`, replaced post-PVC by each mechanism's empirically-derived equivalent (MECH_STATS/PICKEQ: MSD→59, SSP→94), with `PRESENT_ID_OVERRIDES` re-typing re-entries (e.g. Perez→SSP-2025). The equivalent is ONE shared object feeding both live valuation inputs (pedigree/pole/band via `effpk`) and `draftval = PVC[equivalent]` (B5's denominator) — a register-driven identity change moves price and floor together.

## Hypothesis register (D4)
- h-A8-missing-2× **FALSIFIED** · h-A8-tolerance-leak **FALSIFIED** · new: A8 display ambiguity **CONFIRMED** (fixed).
- B5 signed schedule: 9-offender vanish **CONFIRMED**; NEW OBSERVATION veteran-tail over-binding (51→82 at candidate) — cause = schedule boundary, not engine; Luke ruling requested.
- One-price presumption (Luke): **EXECUTED** — netting double-application hypothesis falsified (zero).
- M1v7+M2 candidate: pre-logged A3/A10/A8/Ward expectations **ALL CONFIRMED**; B1-on-rebuilt-book 16/17 — **cause UNLABELED pending same-builder control** (engine vs builder confound).
