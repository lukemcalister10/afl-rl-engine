# BOARD_LAYERS_OBITUARY — the ONE-price deletion record
_2026-07-02 · D4 ASK 3 · Luke's ruling (in writing): **delete, not disable; earn-back only as engine features.**_
_Deletion commit: `f2bb22b` (branch `claude/lukes-ruling-package-krhxq7`). Magnitudes are the D3 ASK-1b measured toggles (`session_2026-07-02/board_layers_pack_D3.md`), verbatim. Resurrection ref = `git show b6c4003:<path>` (the last commit carrying the full code)._

## The ruling executed
The traded board was priced by `TR.production_value` (the cont.27 wire router) — a second, disagreeing price (board −7.6% vs engine, median player |Δ| 24.9%, three gates flipping PASS↔FAIL between paths — D3). As of the deletion commit, `rl_export.py` renders **engine `ev()` — one universal source**; the router and every board-side layer below are deleted. The re-cut of the live trading board file itself waits for the bake decision (ONE new board total).

| # | layer | plain description | measured magnitude (D3 1b) | recorded rationale | deleted from | resurrection ref |
|---|---|---|---|---|---|---|
| 1 | the WIRE overwrite | the cont.27 router that overwrote every board value with `TR.production_value` — it WAS the board | 800/805 players, **31.6%** of board value; Uwland 834→3274 (+293%), Newcombe 3668→1906 (−48%), Max King 420→1979 with layer off | cont.27 board switch, 2026-06-26 | `wire_redesign.py` (`wire()`, `_capture_bands()`, `__main__`); router in `tail_restore.py` (`production_value`) | `git show b6c4003:engine/rl_after/wire_redesign.py` |
| 2 | REPL −3 (board application) | priced everyone against a slightly weaker "free replacement player" | 802/805 players, **19.6%**; Logan Morris −22%, Ginbey −20%, O'Sullivan −20% if off | cont.25 dial; Luke reverted to uniform −3 on 2026-06-28 | `dist_redesign.py` (`redesign_value` → `_price_repl` call) | `git show b6c4003:engine/forward_valuation/dist_redesign.py` — **NOT an earn-back item: the ENGINE already applies the identical `REPL_DROP` inside `price6()` (`_merged_recover.py`); only the redundant board-side application died** |
| 3 | tail restoration | restored the star-outcome ceiling (p70/p90) for high-pick pre-debut MID/KEY_FWD/GEN_DEF toward the empirical at-draft tail | 29 players, **1.4%**; Smillie −52%, DeMattia −66%, Annable −37% if off | cont.27–28, empirical p90 under-dispersion (cont.27 Check 2) | `tail_restore.py` (`restore/rband/rval` + empirical-curve machinery) | `git show b6c4003:engine/forward_valuation/tail_restore.py` — the under-dispersion EVIDENCE stands; earn-back = engine band construction (conditional-prior/q97m top quantile), never a board layer |
| 4 | RUC draft pool | priced pre-debut rucks off the scorer-pool shape anchored to ruck level (n=69 too thin for a per-pick ruck curve) | 19 players, **1.1%**; Carr +318%, Uhr-Henry +281% if off (the pool held them DOWN) | cont.27, fixed the pk20-30 ruck inversion | `tail_restore.py` (`synth/_plvl/SCORERS` + RUC branch of `production_value`) | same file ref — the pk20-30 ruck-inversion risk returns to the engine's court: **watch A6** at every board |
| 5 | RUCK TAX 0.25 | taxed the speculative (non-production) part of established rucks | 16 players, **0.3%**; Madden +16%, McAndrew +12%, L. Jackson +4% if off | cont.24-25, Luke-approved, rucks-only. D3 reconciled: LIVE on the board path only; DEAD on the engine path | `dist_redesign.py` (`RUCK_TAX` + `_realised` + tax branch of `redesign_value`) | same file ref — `RL_RUCK_TAX=0.25` env pin is now INERT (no live consumer; pin kept for env byte-compatibility); earn-back as an engine feature needs Luke's re-ruling |
| 6 | pedigree soft floor | stopped young high-pedigree strugglers falling below a fading draft floor (`value = production + w·max(0, pole − production)`) | 35 players, **0.1%**; Tunstill −64%, Goater −60% if off | cont.25, Luke's spec form | `dist_redesign.py` (`_floor_w/_adraft_band/KPP_SCHED/FLOOR_*`) | same file ref — the ENGINE carries its own pedigree-pole lift (`par_pole` + `recover` blend in `raw_ev`); B5's signed year-schedule now guards the crater dimension |
| 7 | Brodie ×0.5 | halved one specific role-risk profile | exactly 1 player (Will Brodie 508; 1015 if off), **0.1%** | ported 2026-06-21 | `dist_redesign.py` (`redesign_value`'s `brodie_sig` branch) | `MA.brodie_sig` survives in `rl_model.py` (signal only, exported as the `brodieBase` display knob) — **NO live path halves anyone**; pricing Brodie down again must be earned as an engine feature |
| 8 | lens tilt | now/balanced/future slider tilt on the board value | 0 players moved (inert at the shipped 'bal' lens) | cont.21 | `dist_redesign.py` (`redesign_value`'s `lens_tilt` application) | VIEW machinery KEPT and re-plumbed (below); the value-tilt math is gone |
| 9 | JS legacy pricing chain | surv, P, cvx, vP1/vM1 recompute, pedDecay, losd, brodieBase, CAPT/TILT, JS PVC/SCALE rebuild — the old in-browser pricing | 0% (DEAD — fires only in a fallback that never runs; RELEASE note `_engine_block_v23.js:97`) | superseded by the baked `v` | **not yet excised** — the JS excision belongs to the ONE-board re-cut (bake-gated); knobs still exported for display compatibility | `_engine_block_v23.js` in tree |

Also deleted: `verify_anchors.py` (anchor-drift checker for the deleted `redesign_value` path — verifies machinery that no longer exists). Resurrection: `git show b6c4003:engine/rl_after/verify_anchors.py`.

## What replaced it (3d + 3e)
- **3d — ONE price:** `rl_export.py` execs the head engine once and writes `p['_v'] = ev(p, 2026)` for every active + back row. The gates and the board now read the SAME number by construction. B4's byte-compare vs the orphaned shipped bundle (`b8f9e998`) stays red until Luke's board re-cut — expected and correct.
- **3e — engine-backed view (implemented for active rows):** the forward/backward season view asks the engine the as-of-year question — `vM2/vM1/v/vP1/vP2 = ev(p, 2024/2025/2026/2027/2028)` baked at export; the JS slider switches fields and owns no math. Spot rows: Daicos 7917/7874/7059/6118/4830 · Rozee 3847/3874/2679/2296/2112 — sane trajectories both directions.
- **3e caveat (spec'd, HELD):** retired back-rows price flat at engine-2026 (≈0.02×draftval) because `delisted()` hardcodes 2026 in the frozen head; an honest backward view needs Y-aware delisting (`p['_last_listed'] < Y`) — an ENGINE edit, so it rides the candidate branch, not this delete. Flagged, not silent.

## Replacement-netting lookups (3a/3b — the valve, answered before the delete)
- **(i)** Replacement-level netting lives in **BOTH paths, applied exactly once per path, never stacked**: engine `price6()` lowers `MA.REPL` by `rd.REPL_DROP` around `dp.v_at_peak` (save/restore); the board's `redesign_value` did the identical save/restore via `_price_repl`. The two paths were parallel alternatives, not composed — **double application = ZERO**. (The engine also uses a hardcoded `REPL−3` bar inside `_lvl_eff_infer`'s upside-fade target — a feature target, not value netting; noted for completeness.)
- **(ii)** `TR.production_value` consumed **no `ev()` anywhere** — it priced independently through `rval`/`redesign_value` (D3 already proved `_merged_recover` off the export dependency graph; re-verified at the delete).
- **3b consequence:** netting was NOT board-only → **no promotion needed, nothing to HOLD** — the delete proceeded directly.

---

# ENGINE-TERM DELETIONS (same record discipline; these are engine terms, not board layers)
_2026-07-02 · D7 ASK 1 · Luke's ruling: v7-cB **deleted, not disabled** — earn-back only as a re-derived engine feature._

| # | term | plain description | measured magnitude (D5 ASK-2 term table, verbatim) | recorded rationale | deleted from | resurrection ref |
|---|---|---|---|---|---|---|
| E1 | v7-cB (upper-quantile band compression) | `cB = 0.47·clip((effs−1)/3, 0, 1)` squeezed the band's q90/q95 (bb[3]/bb[4]) toward the median for players with effective-season count ≥1 — the more career a player had banked, the harder his upside quantiles were compressed, regardless of how good he was | A2 Curtis/Ward 0.875→0.822 when off (cB squeezed **Curtis −195/−14.4%, Ward −324/−19.6%**); new-B1 avg peak 147.6→156.6 when off; B5 offenders 82→76 when off; 2020-cohort d4-6 markdown **−8.0%, spread top-quartile −6.8% / middle −15.1% / bottom-half −8.8%** | INDISCRIMINATE markdown: Spearman(value, Δ%) = **−0.024 (p=0.87)** — zero correlation with quality (v7-asc, kept, is mediocrity-concentrated at ρ=+0.706); the **Curtis squeezer** (the A2 red gate's motion was cB, not Ward-side truth); artifact-shaped, not signal-shaped | `_v7` in `engine/rl_after/_merged_recover.py` (BAKE CANDIDATE v2 lineage): cB factor + `_effs` feed (no other consumer) + `GCAP` constant — deletion commit **`c16b970`** (branch `claude/d7-bake-candidate-v2`) | `git show 0806d90:engine/rl_after/_merged_recover.py` (the D4 candidate — the last commit carrying full cB code); D5 decomposition: `session_2026-07-02/d5_ask2_term_table.md` |

Earn-back condition (standing rule): a band-compression term returns only as an engine feature with a demonstrated quality signal (the concentration test v7-asc passes and cB failed), never as a blanket squeeze.

---

# ENGINE-TERM DELETION — D10 2026-07-03 · the FLAT 50% SIT-OUT ANCHOR (retired-PVC basis)

| # | term | plain description | measured magnitude | recorded rationale | deleted from | resurrection ref |
|---|---|---|---|---|---|---|
| E2 | SITOUT_RETAIN × draftval (the "flat 50%") | a still-listed player with no ≥6-game season EVER was priced at a flat position-classed fraction of his OWN old-PVC draft value stepping down only by years-on-list (nonKPP yr1-2 = **0.50**, KPP 0.70, RUC 0.85) — games this season, actual scoring, season progress (R14/24) and the position adjustment were never read; 0 games ≡ 5 games; game six paid a +2551 jackpot on the B6 synth | fired on **49/64 (77%) of the 2025 cohort at R14** (DIAG-B); 54 under-seam players under-credited ≈ +4,096 board points vs a locally-smooth curve (DIAG-A); ~46% of the 2025 cohort's gap vs the recent-5 mean; Luke's book: Patterson 982 / Annable 936 / X.Taylor 690 = exactly 0.50×draftval, position adjustment erased; B6 red on all three clauses | Luke's D10 ruling (verbatim, 2026-07-03): "I do not accept the 'half of draft value' as that is derived from the old PVC, which I believe is a relic and retired… that penalty should smooth in between games 0-6"; the retain levels were DESIGNED placeholders sitting above the KPP/RUC measurement (2026-06-30 audit), never derived; the draftval basis was the retired 3000-down PVC | `SITOUT_RETAIN` table + the `ns==0 → dv·retain[N]` branch of `ev()` in `engine/rl_after/_merged_recover.py`, deleted on candidate branch `claude/games-ramp-engine-change-qt7824` (v2.1, engine `e15bafa9`) | `git show 4a134d05-carrying commit (branch claude/d7-bake-candidate-v2):engine/rl_after/_merged_recover.py` lines 216-236; wired replacement + derivation: `session_2026-07-03/d10_ask2_derivation.md` |

Replacement: the derived GAMES-RAMP treatment — LIVE start value V0 (pick+position band price) held through pre-season, kernel-derived retention R_SIT (still-listed sit-out realization RELATIVE to same-depth norms, the locked "0.76 form"), measured evidence-credit blend λ toward the live production path (continuous at the prorated graduation bar), all games bars prorated to season progress. Earn-back condition: a flat class-fraction anchor returns only with a derivation that beats the kernel curves out-of-sample — never as a designed placeholder.
