# PACKET — <ACT NAME>

**Seat:** <seat> · **Date:** <YYYY-MM-DD> · **Base:** `<branch>` @ `<sha>` · **Prereg:** `<path>`, committed **before** the engine edit
**Owner word:** "<verbatim>"

<!--
THE FIXED-SLOT DECISION PACKET (PLAN_v6 2a.2). Every `## ` heading below is a REQUIRED SLOT and the
validator refuses a packet that is missing one, leaves one empty, or fills one with a dash.

    tools/land packet --check <this file>

The slot list is closed. A question earns a permanent slot only when the owner has asked it twice —
so a new slot is an amendment to this template, made once, with the two askings cited in the
amendment. Anything else a packet needs goes in `## Findings this seat did not go looking for`,
which is the one open slot and is deliberately last.

Delete these comments in a real packet. Do not delete the headings.
-->

## 1. What changes

<!-- One paragraph, then the mechanism. Name the file and the hunk if it is an engine edit. State
     what is NOT touched with the same precision as what is. -->

## 2. Who moves — by name

<!-- The movers list. Names, not counts alone: a count is a summary of an answer, not the answer.
     Include the direction and the size. If the list is long, the full list goes to evidence and the
     head of it goes here — say which. -->

## 3. Who does NOT move — by name

<!-- THE SLOT THAT IS EASIEST TO LEAVE OUT AND HARDEST TO RECONSTRUCT LATER. The rows, totals,
     invariants and identities this act leaves byte-unmoved, named individually, each with the
     measurement that says so. "Nothing else moved" is not a filling of this slot. -->

## 4. Cost

<!-- MEASURED, then quoted (the M2 ruling). Build minutes, landing minutes, gate minutes, and the
     seat time. A number here that was not measured is the thing this slot exists to prevent. -->

## 5. Standing-table impacts

<!-- Which standing tables, ladders, bands or published surfaces move, and by how much. If none: say
     which ones were checked and found unmoved — the check, not the conclusion. -->

## 6. What would make this silly

<!-- The honest case against. The reading under which this act is not worth doing, or is the wrong
     shape, or is a cure for a symptom. Written by the seat that wants to do it. -->

## 7. Recommendation

<!-- One paragraph. What the seat recommends, and what it is NOT asking for. -->

## 8. Falsifiers

<!-- Numbered, decidable, and measured. Each one names what would have to be true for this act to be
     wrong, and how that was tested. A falsifier that cannot fire is a decoration; the estate has a
     recorded case of exactly that (`reconciled_to_f5`, an algebraic tautology that printed PASS on
     the board whose cross-check was failing). -->

## 9. Findings this seat did not go looking for

<!-- The open slot. Anything the act turned up that is not part of it. Referred, not absorbed. -->
