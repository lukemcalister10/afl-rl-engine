# L2 (decoupled at dial 14) + L5 (completion per v30) — record · 2026-07-12

## L2 — DIAL 14, DECOUPLED (owner steer 2026-07-12)
- **Dial 14 stands on its own merits.** The banked discount sweep proves the young-ruck prior-cap
  artifact bites **only at ≤13%** (louis-emmett −22.4% at 13%); at **14%** it is above the artifact
  zone and no guard breaks (sweep: 15→12 breaks neither A-GAWN nor A-BONT; G-COHORT y4 margin widens).
  Mechanism = `LENS['bal'] 0.15 → 0.14` (verified, `L2_FINDINGS.md`).
- **The refuted rescue claim is RECORDED:** L1(b) adoption does NOT remove the emmett cap artifact
  (emmett = 913 at 0.13 with and without L1(b)). "Option (b) removes the mechanism" is refuted.
- **Standing condition:** any future **sub-14** drop requires the cap dealt with FIRST. The
  `RUC_PRIOR_CAP` lift is a **PROPOSED mechanism for the L6 / scarcity ruling — NOT shipped unruled**
  (it lifts the young-ruck cap so cap-bound rucks rise with their cohort under re-normalisation).

## L5 — LATER-ENTRY RECORD, completion per v30 fields
Register v30 (ii): the later-entry record = **type SSP + re-entry year + pick None** (SSP is pickless;
a later SSP entry carries no pick). Original-entry facts stay in the identity evidence; training
membership follows the facts-based law, **out on the debut-window leg (named)**.

| player | type (now) | re-entry year (now) | pick (now) | → v30 target |
|---|---|---|---|---|
| flynn-perez | SSP ✓ | 2025 ✓ | **35** (retained) | **None** + `_pickless=True` |
| lachlan-mcandrew | SSP ✓ | 2024 ✓ | **12** (retained) | **None** + `_pickless=True` |
| mark-keane | SSP ✓ | 2022 ✓ | None ✓ | already complete |

`PRESENT_ID_OVERRIDES` (`rl_model.py:806`) already sets type+year+`_eff`=92; **completion = also null
`pick` and set `_pickless=True`** for perez + mcandrew (keane already pickless). This is the permanent
engine/store change that **rides the permanent refit** (with L1's `_PVC0` swap + L3's `RL_AGE` + L4's
pool exclusion — all deferred to the one-refit stage to keep the engine md5 / boot-pin moving once).
Effect (v30 + `L4_AND_TRIO_FINDINGS.md`): post-L4 the MSD→SSP relabel is **pool-neutral for
mcandrew**; residual ≈ **perez ~7 rows**. Training membership: the trio stay OUT of the calibration
pool via the **debut≤2021 window** (SSP debut = re-entry year+1 > 2021) — the named load-bearing leg;
the edit tripwire (L4) HALTs any data edit that would re-admit them.
