# CONTINUATION PLAN — v2.9 CANDIDATE, continuation seat · 2026-07-13 · branch `claude/v2-9-candidate-integration-kq4dae`

MODE auto; this is the first committed artifact of the continuation seat (directive: "first commit =
your CONTINUATION PLAN — inherit + delta, no re-derivation of the done"). EFFORT max. HALT-not-warn.
The retiring seat's committed findings on `…-vtsnhr@a00c782` are the inheritance; they are NOT
re-derived here — this PLAN records only what is inherited-as-true and what this seat adds.

## BASE + CONTINUATION PIN (first act — done)
Full-URL ls-remote `github.com/lukemcalister10/afl-rl-engine`:
- `main = 40f7a845…` — **ADVANCED past the directive's `0888faa8` (v32)** to **v33** ("PROVISION ✓
  PRESCREENED PASS — PR #66, zero movers; retiree-ID wrinkle; merge word asked"). Reported, not a blocker.
- `refs/tags/v2.8 = 9bd0cfdbf9faff…` ✓ EXACT (unmoved).
- branch `…-vtsnhr = a00c7829aee4…` ✓ EXACT — the retiring seat's 7 candidate commits
  (PLAN→L1→L4+L5→L2→L7→L3a→L3b), diverging from main at merge-base `f93de227`.
- Boot store `b0c39d78` == pinned (Guard 5 PASS on entry + on run_panel). Engine `7a07e369`,
  cm_400 `34faa865`, register `652d83e8` asserted. Panel reproduces **PASS 10/10** (Daicos 8050 ·
  Bont 3721 · Gawn 2538 · Sheezel 8116 · Reid 3726 …).

**BRANCH RECONCILIATION (harness assigned `…-kq4dae`, directive named `…-vtsnhr`):** kq4dae is set to
the candidate tip `a00c782` EXACT (every inherited commit SHA preserved — no rebase), then `origin/main`
merged in (register v28→v33; main touched ONLY docs/OPEN_ITEMS_REGISTER.md since the merge-base, so no
engine conflict). Net: kq4dae = the full candidate lineage + current register, continuing ON it. vtsnhr
on the remote is untouched. The RETURN PR opens from kq4dae (merge-commit policy).

## INHERITED-AS-TRUE (verified by the retiring seat; carried, not re-run)
- **L1 (PVC(b)+smoothing):** smoothing pass authored (`pvc_smooth.py`, h=0.20, monotone re-imposed,
  pick-1=3000); engine `pvc_fit_candidate.json` rebuilt (was the OLD discredited yr-1 W4 fit).
  Verified vs the option-b oracle: anchors byte-identical, 25 movers (24 young-RUC + 1 KEY_DEF),
  board +0.179% @ pin 3000. **Real L1 = the `_PVC0` ev-channel swap + V0/RUC rebuild** (RL_PVCFIT
  alone moves nothing); the permanent path is staged for the one refit.
- **L4 (MSD pool exclusion):** reproduced read-only on store b0c39d78 — pool ND1255/RD640/MSD29→0,
  671 movers, 48≥5%, board +0.19%, **louis-emmett 1178→826 (−29.9%)**, bont −0.35%, gawn +0.71%.
  Facts-based rule (`type==MSD ∧ debut≤2021`) + edit tripwire specified. Load-bearing leg NAMED:
  the trio are held out by the debut≤2021 window, not entry-type.
- **L2 (dial 14):** the prior-cap-gone proof was RUN and **REFUTED** (L1(b) leaves emmett=913 at
  0.13). Dial 14 stands DECOUPLED on its own merits (14% is above the ≤13% artifact zone). Mechanism
  `LENS['bal'] 0.15→0.14`. Any sub-14 drop needs the cap dealt with first (PROPOSED `RUC_PRIOR_CAP`
  lift — unruled, does not ship).
- **L3a (s(age) wiring):** reproduces l7hinr to the number (43 movers 20up/23dn, net +1670, butters
  6060→5997 **−1.04%** inside the G-PEAK 2% tol). `s_clip` table crosses 0.46 at ~age 25.3.
- **L5 (trio):** completion = perez/mcandrew → pick None + `_pickless=True` (keane already pickless);
  pool-neutral for mcandrew post-L4, residual ≈ perez ~7 rows. Rides the refit.
- **L7 (numéraire re-base):** ÷1.0524 uniform (players+picks+anchors+SCAR constants), pick-1=3000,
  ratio-invariant (built + verified; standing assert wired in rl_export.py (g) + BAKE_CHECKLIST §3).
  Applies LAST in the refit.

## THE BLOCKING FINDING — HARNESS RECONCILIATION (directive item 1) — RESOLVED BY CODE READING
The retiring seat's L3b gate read y4 = **137.8%** on an ad-hoc class-sum harness
(`l3_cohort_gate.py`); the audited lineage reads **~128.6** (sweep 15% shipped book:
100/115.3/120.0/**128.6**/127.1/119.0/99.6; v2.6 bake-guard 128.8/127.3/119.6). The **~9 pt gap is a
CONSTRUCTION gap**, pinned by reading the shipped gate:

**The official G-COHORT gate is `ship_gates_check.py` B1** (owner ruling D5, in writing 02/07/2026),
computed by `_b1_rows()` on a matrix regenerated by **`s4_matrix_M1v7.py` in gate mode** (clean
config, `__meta__` engine/store/config hashes must equal the candidate under test, else FAIL). Its
construction, asserted by code reading (`ship_gates_check.py:278-295`):

| convention | official `_b1_rows` (ADOPTED) | ad-hoc `l3_cohort_gate.py` (SUPERSEDED) |
|---|---|---|
| population | `incurve` (type∈{ND,RD}) ∧ **cohort 2004-2020** | `type=='ND'` ∧ **cohort 2014-2020** |
| per-cohort | class-year **SUM** of `Vpath` at depth N | class-year SUM of `value_asof(p,t)` |
| **cross-cohort combine** | **index each cohort to its OWN yr1 (=100), THEN unweighted simple mean** | average RAW sums, then ÷ min(avg₁,avg₂) |
| pricer / anchor | `s4_matrix_M1v7` ASOF; anchor = **end of calendar Yr1 (C+1)**, D10 games-ramp sit-out, retired-stop-at-last-played | `value_asof`; career-yr, `cum_games==0 ∧ t≥3→0` |
| denominator | each cohort's own yr1 ⇒ min(y1,y2)=y1 (=100) | min(avg₁,avg₂) across cohorts |

The dominant driver is the **combine order**: the official INDEXES per cohort (removing cohort-size
and cohort-richness heterogeneity + the ragged-panel column-composition drift) BEFORE averaging;
the ad-hoc averages raw sums across a short recent window (2014-2020) — which inflates the later
depths. Population (ND+RD vs ND-only; 2004-2020 vs 2014-2020) and the sit-out/anchor pricer are
secondary contributors.

**Adoption:** `scripts/cohort_gate_official.py` reuses `_b1_rows` **verbatim** (byte-for-byte copy,
marked do-not-edit) on matrices from `s4_matrix_M1v7.py` (gate mode). No re-derivation — this IS the
shipped B1 gate. The G-COHORT law applied: y4/y5/y6 EACH ≤ 130 (hard; guide 120-125).

## THE BOUNDED SCOPE (this seat, in order)
1. **[DONE — see HARNESS_RECONCILIATION.md]** Reconciled by code reading; baseline reproduced on the
   current base (7a07e369/b0c39d78/69ead79b): AVG row `100/115.3/120.0/128.6/127.1/119.0/105.8`,
   **y4=128.6 y5=127.1 y6=119.0 PASS** — matches the audited 15% shipped book to 0.0 pt on y1–y6
   (137.8 refuted). The convention gap = combine-order (index-then-average) + population (ND+RD
   2004–2020) + pricer, dominant driver the combine order.
2. **THE ONE COMBINED GATE** — patch ALL levers at once into a clean workspace copy (L1 `_PVC0`
   swap + V0/RUC rebuild · L4 MSD pool exclusion · L2 `LENS['bal']=0.14` · L3 s(age)), regenerate
   the matrix via `s4_matrix_M1v7`, run `_b1_rows` ONCE → y4/y5/y6 triple + margins. Assert L7 is
   ratio-invariant (uniform scalar ⇒ per-cohort index unchanged) and L5 is cohort-neutral (trio are
   SSP, not ND/RD cohort members) — by argument, not assumption. A breach HALTS the candidate.
3. **WIRE THE PERMANENT REFIT (only on gate PASS)** — all levers on disk as ONE refit; engine-md5 +
   boot-pin + panel/seal re-pins move IN-COMMIT; full G-ATTR per-lever attribution (base→+L1→+L4→
   +L2→+L3), louis-emmett named; all-levers-off reproduces base byte-exact.
4. **SPEC_1-4 + seam table** on the post-L4 world — reports + PROPOSED corrections only; ADVISORY;
   L6 STOP at the SSP decision point STANDS (return scarcity, don't touch the 92 pedestal).
5. **EXPORT BUNDLE** (rides the refit) — vPrev/vRaw/levers fields · barker/thredgold lti_reg wiring
   fix · +1/+2 phantom-pick lens entries + rolling label years (adopted curve, dial 14, numéraire) ·
   lens-conservation diagnostic.

## GUARDS & FENCE (unchanged)
Five data guards + panel + Guard 5 from fresh bootstrap; acceptance v1.7; expected reds EXACTLY
{A2,A3,A12}; halt-not-warn; no blanket multipliers; delete-don't-disable; SSI. Candidate ONLY —
nothing tags/bakes/merges to main. No force-push of the shared refs. The PR opens at the RETURN.
Owner-word gates (return, do not decide): the L3 cohort-gate PASS/BREACH verdict feeds the ladder;
L6 SSP 92→~51; every SPEC PROPOSED correction; the L5 full-switch. Thin budget → committed
checkpoint + say so; a further continuation is normal.
