# v2.9 BAKE — RETURN (the L-STOP) · 2026-07-13

Branch: `claude/v2-9-bake-execution-dysldl`. Base-verify: `export-display@0654897` UNMOVED at the word.

## What the bake did (fenced; store b0c39d78 identical; engine ev() untouched)
The L7 numéraire re-base is a **display-layer** change — ev()/engine 2030e5df/store b0c39d78/config
69ead79b are all frozen. ship_gates acceptance reads engine ev() ratios, so the reds are invariant.

1. **OVERRIDE RESTORATION (S1 fix set).** `owner_overrides.py` HALTS (never silent []) in gate/bake when
   the file is unresolvable/absent; a POST-EXPORT PRESENCE ASSERTION HALTs if any listed override lacks its
   `ov` on the board; `boot_guard.py` now ASSERTS the board pin at FULL hash (the decorative-pin hole
   closed); `RL_NO_OWNER_OVERRIDES` removed from `INFRA_ALLOW` (rejected in gate/bake). Three red paths
   PROVEN (missing file · listed-unapplied · wrong board pin). ci_guard_brodie exclusion stays GREEN.
2. **L7 NUMÉRAIRE + ADOPTED-CURVE REPOINT.** Every displayed value ÷1.0524; shipped pick curve repointed to
   the adopted `pvc_curve_L1b.json` (pick-1=3000, monotone). Numéraire assert UNCONDITIONAL (dormant legacy
   branch retired). Order preserved (0 strict inversions), 10/10 anchor ratios <0.2%. Brodie ov = ×0.50 of
   the REBASED value (511; 511×1.0524=538, the owner's by-eye read).
3. **REGENERATE + ACCEPT.** Board f2d6e3f5 → **81e48293**. Diff = the uniform ÷1.0524 rebase + adopted pick
   curve + exactly one row (will-brodie) gaining `ov` + nothing else; 0 relative movers (804/804, 198/198).
   §5 numéraire anchors reproduced to the dollar (bont 3482 · gawn 2393 · darcy 3865 · emmett 809 · briggs 2105).
4. **RE-PIN + PROSE.** expected_boot board = 81e48293 (full, asserted); PANEL_EXPECTED + run_panel.sh to the
   numéraire (10/10); all stale `8a66b4ba` prose refreshed (expected_boot, PANEL_EXPECTED, run_panel;
   gates_2030e5df.json regenerated by ship_gates).
5. **BOOK RE-SEAL (B3).** `book_stable_seal.json` re-sealed at the refit head 2030e5df, store b0c39d78,
   n=2649, stable_sha256 c7825f1b… — B3 now PASS (was differs-by-design).
6. **FULL SUITE (fresh bootstrap).** ship_gates: B1–B6 green (B3 re-seal PASS · B4 parity 81e48293==81e48293),
   acceptance reds EXACTLY {A2,A3,A12} (no unexpected reds), VERDICT PASS=17/FAIL=3/FEATURE=1. Five SSI guards
   + Guard 5 + correction canary PASSED; owner-override exclusion GREEN; the three red-path proofs PROVEN;
   numéraire panel 10/10. NOTE: the L7 display re-base changes the board==ev==book relation to
   board==round(ev/F); six parity checks were made numéraire-aware (rl_export gate, run_panel, selftest F1/F2,
   canary, s4_matrix book<->board) — the guards still catch mispricing, only the unit relation moved. Plus one
   real fix surfaced by the bake: ship_gates B4 now passes RL_REPO so its regen carries the override (the S1
   root cause; without it B4 was blind to the override).
7. **UI RE-EXTRACT.** extract_board_view.py ring-fence PASS (srcmd5 == pinned board 81e48293); working + public
   bundles regenerated; structure unchanged (804/198/30); the four UI letters' rulings hold.

## FLAGGED FINDING (UI render — owner call)
The override DATA now reaches the UI working bundle (ov.dispv=511, mark), and ui/app/config.js
EXPECTED_BOARD is re-pinned to 81e48293 (ring-fence PASS). BUT the UI RENDER still shows `p.v` (brodie
1022, the model figure) on the board rail (board.js:86 r.val=p.v) + card "Lands at" — the "Owner override"
tag appears but the halved 511 is not the displayed number, and card.js's override delta computes
p.v−vRaw = 0 (rl_export keeps v=engine and puts the overridden figure in ov.dispv, vRaw=v). Honoring the
override in the UI = render `p.ov ? p.ov.dispv : p.v` on the rail (and decide whether it touches the
lens/Δ-vs-bake/power-bar) — an owner-facing display decision, flagged for the L-STOP rather than changed
unbidden. Engine/board/guards are unaffected (override is display-only; v never moved).

## THE L-STOP
Everything above is committed + pushed. Tag `v2.9` + main fast-forward + PR #68 are the OWNER'S in-chat
word only. No tag, no main promote performed.

## One-screen bake summary
- board md5: **f2d6e3f5 → 81e48293** (uniform ÷1.0524 + adopted picks + 1 ov row; 0 relative movers)
- pick-1 == 3000: asserted UNCONDITIONALLY (adopted curve; legacy branch retired)
- brodie ov: 511 = round(1022 × 0.50), OWNER OVERRIDE ×0.50 (rebased value; 511×1.0524=538)
- suite: [filled] — B1–B6, panel 10/10 numéraire, acceptance reds EXACTLY {A2,A3,A12}, 5 SSI + Guard 5
- B3 re-seal: PASS at head 2030e5df (n=2649, c7825f1b)
