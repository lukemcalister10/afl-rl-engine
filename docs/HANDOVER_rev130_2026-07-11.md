# HANDOVER — rev130 · 2026-07-11 · supersedes rev129
### State + the SINGLE action order. Read after CORE (v2.3). Open items live in
### docs/OPEN_ITEMS_REGISTER.md (the only list). Rulings detail: DECISIONS v94.

## ONE LINE
**BAKE v2.8 IN FLIGHT** (owner fired 2026-07-11): candidate = claude/chapter-lever-discount-sweep-amqpfx
@ bc2929fe (twice-verified: supervisor prescreens + full cold audit, end-to-end reproduction) ·
rulings incorporated in DIRECTIVE_bake_v2_8_v1a (L1 defender credit ADOPTED · 15% held · travaglia +
kysaiah reads WAIVED · PVC letter DEFERRED to v2.9) · the bake build stops at the two OWNER-ONLY
commands (tag v2.8 · main FF promote). Baked reference until then: tag v2.7 == 8f8c00b.

## STATE
- Candidate contents vs baked v2.7: the pick-convention corrections + ×1.0524 re-denomination
  (store 04f38dad · the one engine band-pool line · picks scaled) + the L1 young-GDEF transition
  credit (ycred daaf5e49 · 56 up / 16 waived dips / 9 blessed +1-2 veterans) · board 9ecbe0fa ·
  seal a19b3cb8. Expected reds exactly {A2, A3, A12} (A12's travaglia leg = read WAIVED, red honest).
- Research banked (read-only, rulings pending at v2.9): PVC derived curve (icbhpu @ 3c1d610f —
  founding question answered; options a/b/c; supervisor rec (b)) · age-persistence s(age) curve
  (l7hinr @ 394cd16 — adoption gated on the walk-forward G-COHORT re-measure).
- SUPERVISOR PEN LIVE: fine-grained token, docs/-only, every push SHA-cited, incoming seat audits
  pushes first. Repo-is-home operative; Project knowledge = Tier-0 only (manifest + CORE).
- Owner ledger: token ✓ · branch sweep done (48→36; live chapter + evidence branches intact) ·
  max-king closed · reads sealed on paper · UI answered by the four repo docs + 4 open letters
  (Q-GHOST · Q-DELTA-BASE · Q-VERDICT · Q-THEME).

## OPEN — THE ACTION ORDER
1. **BAKE v2.8 completes:** build return → supervisor prescreen → owner runs the TAG + PROMOTE →
   supervisor verifies live → CONSTRAINTS v1.7 + final-identity amendments filed (supervisor push).
2. **THE PARALLEL COMPLETION RUN (day after; three lanes):** ID-primary migration (owner couriers
   authoritative_universe.csv; named DOB validation rows incl. onley/kickett/rodriguez/driscoll;
   name→ID resolver at the score boundary) ∥ UI build (Matchday LOCK + DESIGN_DIRECTION + flex-spec
   display law + ±1/2-yr lens; the 4 owner letters) ∥ (read-only lane free). Then ROUND-SCORE
   INGESTION. UI + ingestion never bake (no value change; tier-2/3).
3. **THE v2.9 BATCH (one lever build + ONE bake):** per docs/OPEN_ITEMS_REGISTER.md — PVC letter +
   seam/G-Y0 identity table · discount dial (fresh) · age-curve adoption (post re-measure) · SSP
   line · ruck-scarcity/backup-insurance · F-MID13 re-grade · flex (strip first: June-28 Stage-2
   owner gate; owner registers couriered) · the owner-read investigations (JUH/cleary ·
   ryan/moore down-persistence · newcombe production-side).
4. Standing lines every job: FULL-URL ls-remote base verification · boot-store assertion · merge-
   commit policy · FEED = DECISIONS + CONSTRAINTS + acceptance JSON as documents + STOP-and-ask on
   any missing input · research directives carry IMPLICATIONS.

## PROVENANCE
VERIFIED (this seat, live, through 2026-07-11): every SHA above · the audit reconciliation · the
register at docs/OPEN_ITEMS_REGISTER.md (v5). OWNER rulings ledger: DECISIONS v94.
