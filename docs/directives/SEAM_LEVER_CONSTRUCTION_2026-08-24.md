# THE SEAM LEVER — CONSTRUCTION AND VALIDATION · directive 2026-08-24

**Status: PRE-RULING.** The owner has NOT green-lit any seam fix. This directive commissions the
construction and its validation so his ruling can be made on a measured candidate, not a sketch.
Nothing here touches the repository: scratch prototypes and a data hand-back only.

**The evidence base:** the seam study (register v838, scratchpad `seam_study/`) as CORRECTED by the
owner's four pushbacks (2026-08-24, on the record at the next pen): (1) the sitter side is
outcome-calibrated listed-conditionally and STANDS — no sitter rebuild; unconditional cohorts are
the wrong yardstick for listed players; (2) cameo STRENGTH is the missing axis — the level
vocabulary drops sub-6-game seasons ("none"), and the supervisor's strength cut (picks 12-30) shows
the gradient: t2 mean eventual best-3 45.0 (<45 avg) / 62.7 (45-65) / t3 20.5 (<45, 69% bust) /
48.4 (45-65); (3) validation on MEANS, bust-inclusive — the engine's own basis; (4) the earlier
"Dattoli over-priced" reading was a strength-pooling artifact and is WITHDRAWN — the construction
must price strong recent cameos on their form.

## THE CONSTRUCTION TO BUILD (prototype, scratch)

A **cameo-strength-aware conditional prior** for early-career played states (1–8 career games,
tenure 1–4): expected bust-inclusive eventual best-3 conditional on (position-group, effective
pick, tenure, games so far, RAW games-weighted cameo average — the axis the level vocabulary
cannot see). Constraints: monotone in pick (better pick never worth less), monotone in cameo
average (a better cameo never worth less), smooth (L-SMOOTH — no plateau cliffs), and it must
PRESERVE the measured harshness of weak-late cameos (t3 <45: mean 20.5, 69% bust — the owner's
"negative performance can be worse than none" principle, measured true). Blend it into pricing the
way the engine already blends pedigree par with evidence (the pw(E) machinery) — propose the
integration site precisely, but BUILD nothing into the engine.

## VALIDATION (means, out of sample, declared before running)

Cohort-split OOS (declare the split rule first): the conditional prior's mean prediction against
realized bust-inclusive outcomes, per cell, versus TWO baselines — the current engine price at the
same states (via the seam study's M1 pricing-through, its limits restated) and the draft-day
bust-prior alone. Report where the candidate wins, loses, and is indistinguishable. Sensitivity to
the strength-band edges. Thin cells declared UNRESOLVED, never smoothed into confidence.

## THE HAND-BACK

1. The construction, exactly specified (formula, constants, fit procedure, integration site).
2. The validation tables (means, OOS, both baselines, ns everywhere).
3. Repricing preview of the named rows (james-leake, harrison-oliver, jesse-dattoli, plus the full
   1-8-game live census from the study) — current price vs candidate price, with CIs.
4. SEPARATELY, parameterized and not blended in: the year-4 sitter-credit option — what
   probability-weighting the depth-4 credit at the measured bracket (0.20–0.53) would do to the
   live sitter rows, DeMattia named, so the owner can rule that timing question on its own. The
   sitter fade LEVELS are out of scope (they stand calibrated).
5. Anything found off-list.

Rules: read-only on the repo; scratch under the session scratchpad (`seam_lever/`); never
`docs/register/`; no design authority — the owner rules on the hand-back.
