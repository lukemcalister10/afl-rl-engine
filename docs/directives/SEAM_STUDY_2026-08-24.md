# THE PLAYED/UNPLAYED PRICING-SEAM STUDY · directive 2026-08-24

**Owner word (2026-08-24): "Yes, commission the seam study alongside the design arm."**

**The three sightings that forced this study** (register v834–v836): (1) DeMattia — the year-4
listed-conditional sitter-fade value pays out at the end of year 3, before the off-season resolves
survival (~104 of his 340 is pre-paid credit; the year-4 cell was measured on n=11); (2)
Leake/Oliver — modest early cameos price BELOW never-played same-vintage sitters, and 3–4x below
draft-day; (3) the supervisor's base-rate spot measurement — 56 historical picks 12–30 with weak
≤8-game cameos by end of season 2: median eventual best-3 = 55, **21% became 70+ players**, 25%
busts — optionality the current prices do not appear to carry.

**READ-ONLY THROUGHOUT.** No repository writes, no builds that touch any carrier, scratch under
the session scratchpad only, data hand-back to the supervisor who files it.

## MEASUREMENTS (the deliverable is calibration tables, not opinions)

1. **The conditional outcome table** — from the store (`engine/rl_after/rl_model_data.json`),
   resolved cohorts: bust-inclusive eventual best-3 distributions conditional on EARLY-CAREER
   EVIDENCE STATE — pick band × career games so far × demonstrated level so far × tenure year —
   with n per cell, pooled deliberately where thin and declared (PART 3 measurement standards).
   This generalizes the recovered bust-prior construction (draft-day, zero evidence) one step, to
   states along the early career. Include the unplayed states (0 games at tenure 1..4) so the
   sitter fade and the cameo prices sit on ONE surface.
2. **The engine's current price at each state** vs the value the outcome distribution implies when
   priced through today's board scale (state the pricing-through method explicitly and its
   limits). The GAP MAP is the study's centerpiece: where the seam over- and under-prices, by how
   much, with the named rows placed on it — Leake, Harrison Oliver, DeMattia, Dattoli, plus the
   day-0 cohort as a class.
3. **The sitter side re-measured on current data:** the third-year → fourth-year survival base
   rate (the probability DeMattia's year-4 credit prices at 1.0); the listed-conditional fade row
   re-derived (the June cells were n=464/100/11); what the depth-3→4 kink measures now.
4. **What the conditional prior would price the named rows at**, so the owner sees the fix's size
   before ruling anything.

## GUARDRAILS
Trace every figure to the artifact; disclose every pooling choice; no design recommendation beyond
laying the options and their measured sizes side by side — the construction choice is the owner's,
likely as a design-arm lever. The engine's own vocabulary for evidence (`best-3 of ≥6-game
seasons`, `effpk`, `MA.GRP`, T1) is the vocabulary to use; read `bustprior` context at
`docs/register/entries/v835.md` and the recovered producer's construction there.

## RETURN
The tables (machine-readable JSON + a readable summary), the gap map with named rows, the sitter
base rates, the conditional-prior repricing of the named rows, ns and pooling disclosures
throughout, and anything found off-list.
