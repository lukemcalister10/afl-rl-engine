# DOB COURIER STAGING — 2026-07-31 · consumed by #290's courier act; the STORE is the destination truth

**What this is.** The staged, seam-validated input for the one atomic DOB store-write (#290 runbook §5,
Addendum 4): 302 rows — every store row missing `_by` — each with provenance. Assembled from the owner's two
uploaded sheets (186 played/scored · 114 never-scored) plus two owner-chat values (the pickless
internationals), normalized (Excel float-years → int; dates ISO). The owner's uploads are the source
records; this file is the courier INPUT and is retained afterward as the act's evidence — it is never an
alternative source of truth once the store write lands.

**Verification record (durable copies on #290: Addenda 4–5, the year-only ruling comment):**
186/186 verified day-exact vs AFL Tables (namesake hazards resolved by club/era index; 94 third-sourced) ·
112/114 verified (111 independently; all 114 match Draftguru character-exact, so Draftguru = the sheet's
source, not a check) · 2 rows RULED YEAR-ONLY by owner word (Kirkby `_by=1986`, Looby `_by=1987`, `_bd`
deliberately empty — days contested across published sources; the engine consumes `_by` only) · 2
internationals seam-verified vs public records. Join integrity 302/302 by store key; the full row-by-row
cross-check tables are beside this file.

**Retention:** cited by #290's sealed record — RETENTION-PROTECTED (charter D2).
