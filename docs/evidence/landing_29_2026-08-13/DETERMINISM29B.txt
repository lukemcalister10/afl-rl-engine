ORDER 29B -- DETERMINISTIC DOUBLE-BUILD, AND THE DECLARED KILL-SWITCH PROOF (P29B-19)
Two independent fresh staged workspaces, strictly sequential, five-var thread pinning, RL_GRACE unset
(the landed code default), RL_V0SURF_PKL pointed at the checkout's re-baked surface.

  build W2    rl_app_data.json  36d5dfc73e2b508ece530bc7dfae2090
  build DET2  rl_app_data.json  36d5dfc73e2b508ece530bc7dfae2090      IDENTICAL -- P29B-19 HELD

  rl_model.py         14000af2a46f7a3c4cdfde303f5a1aff   (both)
  rl_model_data.json  cb38ef1171dcf20aae66ebf12682be0d   (both -- the store does NOT move)

KILL-SWITCH PROOF (the declared RL_ENTRY29B lever), same tree, same workspace recipe:

  build KILL  RL_ENTRY29B=0     86c8d5d9ba5b95e2cba05c78fbc31f78     BYTE-EXACT vs the ORDER 29 board

This is the strongest single control in the act. It is run on the FULL 29B tree -- the signed artifact
with both borrowed cells installed, the replaced pool-v0 guard, the day-0 branch in ev() and the
printed-day-0 assert in rl_export all present -- and it reproduces the ORDER 29 landing board to the
byte. It therefore proves three things at once:

  1. the entry wiring is the ONLY thing that moves the board in this act;
  2. the two borrowed cells are INERT except through the wiring (nothing else reads pool_v0);
  3. the replaced guard and its non-vacuity probe (which temporarily nulls RD|MID and restores it)
     leave no residue -- the restore is exact, or this build would not be byte-identical.

ENTRY CONTROL (P29B-18), run before PREREG_29B was committed, on the untouched tree:
  build CTRL0                   86c8d5d9ba5b95e2cba05c78fbc31f78     BYTE-EXACT
