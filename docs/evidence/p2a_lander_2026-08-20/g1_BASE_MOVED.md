# THE BASE MOVED UNDER THIS SEAT — recorded, not absorbed

This seat's brief names base `main` @ `9787a3b`. `g1_BEFORE.txt` was captured there, at 14:13.

While P2a was being built, ANOTHER SEAT landed the RULEBOOK v3 act on the same tree:

| commit | what |
|---|---|
| `455d593` | register v806 — the staircase diagnosis (docs only) |
| `427985e` | RULEBOOK v3 APPLIED — 12 laws, PART 4 process laws, the derived twin removed |
| `246af1c` | the 1b amendment file's STATUS line + the mid-act HEAD move recorded in that act's claims |

`g1_BEFORE_REBASED.txt` is the same G1 capture re-taken at `246af1c`, and it is the baseline this
package's G1 falsifier is measured against. The complete delta between the two captures is:

    commit                    9787a3b -> 246af1c
    docs/RULEBOOK.md          8dc13a5d -> fa3caf31      (the v3 amendment, that seat's act)
    docs/acceptance_v2_0.json 9fdea7ff -> REMOVED       (the 1b in-tree violator, disposed of)

**No value-bearing artifact moved between the two captures**: the board `68be10c7`, the store
`b745002e`, `_merged_recover.py` `1867e953`, `rl_model.py` `6fe7c415`, `model_config.json`, all three
fitted pickles, `LTI_REGISTER.md`, both UI bundles, every `expected_boot` pin, the contract seal
`cde9f70a` and the book seal `9f46aba3` are byte-identical across it.

Recorded here rather than quietly re-baselined, per the F5 seat's precedent (its FINAL_STATE §6.8
records the same class of event). This seat shares no file with that act.
