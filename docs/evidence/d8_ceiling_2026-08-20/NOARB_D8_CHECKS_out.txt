======================================================================================================================
ORDER D8 MEASUREMENT — THE COMPARISON COLUMN IS THE D7B TABLE, PROVED
======================================================================================================================

CHECK 1 — matrix identity of the comparison column
  D8CAND    recs md5 2f7738f148a03060   store cc02567f  engine 338a790b  n=2648
  D8CAND2   recs md5 2f7738f148a03060   store cc02567f  engine 338a790b  n=2648
  D8BASE    recs md5 e244d914b7e220fb   store cc02567f  engine 338a790b  n=2648
  D7BCAND   recs md5 e244d914b7e220fb   store cb38ef11  engine 5f434b95  n=2648
  BK        recs md5 e244d914b7e220fb   store cb38ef11  engine 5ac6780f  n=2648
  D8BASE.recs == D7BCAND.recs (the D7B tables' own matrix)   PASS 
  D8BASE.recs == BK.recs (the post-flip bare bake re-emit)   PASS 
  NOTE, and it is a finding worth stating plainly: D8BASE carries store cc02567f while D7BCAND and
  BK carry cb38ef11. The store identity moved at commit de9b8eb (the owner's 112 ownership + 23
  pick-owner moves), whose message records "board byte-identical". The equality above is the
  measured proof of that claim on the walk-forward payload: the apply moved the store identity and
  NOT ONE PRICED RECORD. It is also why harness_pvc_REPINNED_pass3.py's EXPECT_STORE = cb38ef11
  now fails closed on every freshly emitted matrix, and why d8_noarb_bands.py re-points that pin at
  the call site (declared change 4 in its header) instead of editing the instrument.

CHECK 2 — ND band cells, D8BASE vs the on-file D7B tables
  every ND band cell agrees in every field (32 cells, 11 fields) PASS 

CHECK 3 — pool arm cells, D8BASE vs the on-file D7B tables
  every pool arm cell agrees in every field (17 cells, 7 fields) PASS 

CHECK 4 — the class mark of the comparison column
  D8BASE W2 == D7BCAND W2 == the registered 1.0672           PASS (1.0672 / 1.0672)
  D8BASE per-class == D7BCAND per-class, every cohort        PASS 
  the instrument self-validated on ORDER K before any candidate number was read PASS (W2 1.0513 vs 1.0513 · cohort 1.0324 vs 1.0324)
  candidate class mark: W2 1.0751  cohort 1.0503  max class 1.1820 (2016)

CHECK 5/6 — determinism and non-vacuity of the priced matrix
  D8CAND.recs == D8CAND2.recs (two independent emits, byte-identical) PASS 
  D8CAND.recs != D8BASE.recs (the pass-through fired; the dial is not inert) PASS 
  the two matrices cover the same key set                    PASS 
  rows whose walk-forward path moves: 1653 of 2648
  NOT ONE row's day-0 v0 moves (the taper is inert at age<=20; day-0 rows are entrants) PASS (0 moved)

======================================================================================================================
RESULT: ALL CHECKS PASS
======================================================================================================================
