# FINAL STATE — PACKAGE 3a: DATA PINS OUT OF THE ENGINE

**LANDED 2026-08-21** by `tools/land lever` at commit **`e840b9ac16fda034f744c626c0392d4ab059dcc5`**,
first flight to open a transaction, every step's postcondition held. **NOT PUSHED.**

## 1. THE FALSIFIER HELD

| | value |
|---|---|
| **board BEFORE** | `b3e8da99bc7f632e5d1eebc732f9cf01` |
| **board BUILT by the landing, from this tree's source** | `b3e8da99bc7f632e5d1eebc732f9cf01` |
| **board AFTER** | **`b3e8da99bc7f632e5d1eebc732f9cf01` — BYTE-IDENTICAL** |
| store | `b745002eb0a0fbb1c34fa44f1ef708d6` — **byte-unmoved**, claimed as such in `P3A_CLAIMS.json` |
| `as_of_round` | 23, HELD |
| `balanced_board_md5` | `7c32a540578b799922daea41d8acdfa2` — unmoved (the sibling rebuilt to the same board) |
| **engine_head** | `8f59180547a584c981ccba39fa018708` → **`3af8c1f7d61275c198a5df70c34608c7`** |
| contract seal | `55a890820b91…` → `1b435ff6f988…` |
| `data/sheet_pins.json` | **NEW**, md5 `e6e687e61edca56973a36a5754e000dd` — equal to the prereg's §6 prediction |

The board prediction was an INPUT to the lander (`prereg.board_after` in `ACT_SPEC_P3A.json`,
committed at `8230526` before the engine was touched) and was asserted against the build, not learned
from it. `acceptance.runner` full profile after the landing: **GREEN, 18 checks, 18 PASS / 0 FAIL /
0 BLOCKED / 0 RULED-RED** (`RUNNER_AFTER_LANDING.txt`).

## 2. THE COMMITS

| commit | what |
|---|---|
| `8230526` | **PREREG** — committed BEFORE the engine edit (process law P9) |
| `e4a48dc` | **THE ENGINE EDIT** — pins out, `data/sheet_pins.json` in, `carriers.py` entry, runbook ERRATUM E7, guard-regime proof |
| `e840b9a` | **THE LANDING** — `tools/land lever`: engine_head restamp, contract re-seal, UI writers, gates, claims |

## 3. STEP TIMING, MACHINE-RECORDED (the M2 measure-then-quote ruling)

    preflight   0.09s · build_proofs 206.50s · pins 0.02s · lineage 0.00s · contract 0.10s
    sibling   406.80s · ui 1.92s · gates 435.23s · claims 440.67s · commit 0.23s
    TOTAL    1491.56s  (24m 52s)

Pre-transaction lander self-test (outside the transaction, sandbox cut from the last coherent base
`8230526`): **17 PASS / 0 FAIL; 10 steps broken, 10 caught, 10 aborted BYTE-EXACT**
(`preflight_lander_selftest/`).

## 4. PREREG CORRECTIONS — the tree wins (P9)

Three corrections, recorded rather than quietly satisfied. **None of them moves a value.**

1. **The base commit named in the prereg is wrong.** §Base commit says `a258b67` (register v810). The
   prereg's actual parent is `67993a9` (register v811), which another seat landed between this seat's
   first read of the tree and its prereg commit. The prereg was written against the earlier read.
   Nothing in the act depends on the value; it is corrected here.
2. **`data/expected_boot.json` gained one carrier this prereg did not enumerate:**
   `engine/rl_after/ingestion/sibling_repin_state.json` — the sibling provenance sidecar. **§4 of the
   prereg lists sibling-repin targets under "NOT touched, and a halt if they move".** It moved. WHY:
   the sidecar records the release contract's seal, the seal moved *because the enumerated
   engine_head restamp moved it*, and the lander's sibling step is the writer of record for exactly
   that. Its `committed_targets` is `["sidecar"]` and nothing else: the balanced board was rebuilt
   and came back `7c32a540578b799922daea41d8acdfa2`, unmoved. **This is an ENUMERATION MISS in the
   prereg, not a value move** — the enumeration should have said "the sibling sidecar follows the
   seal" and said the opposite. Named here, and reported to the supervisor, rather than tidied away.
3. **`ui/data/board_view_public.js` did NOT move**, though §4 B.8 predicted it would track the
   restamp along with the working bundle. The public bundle carries no engine stamp — that
   keylessness is the two-tier law made structural — so only `ui/data/board_view_working.js` moved.
   An over-prediction, corrected downward.

## 5. THE MEASURED CHURN, AGAINST THE ENUMERATION

Non-evidence files touched by the landing commit — **four**, all administrative:

    data/expected_boot.json                            engine_head only (the pins step: 1 moved, 15 unmoved asserted)
    data/release_contract.json                         identities.engine_head + contract_sha256 (10 frozen fields asserted unmoved)
    ui/data/board_view_working.js                      the engine stamp, regenerated by UI writers 1-2
    engine/rl_after/ingestion/sibling_repin_state.json the seal-following sidecar — see correction 2

Byte-unmoved, asserted by the landing rather than assumed: the store, the board (both copies and both
sidecars), `data/release_lineage.json` (no board move ⇒ no transition entry owed; the lineage step
refused to write one), `ui/data/movers.js` and `ui/data/movers_transition.js` (writers 3 and 4 ran
unconditionally and produced byte-identical files), the three history JSONs (no column owed),
`docs/OPEN_ITEMS_REGISTER.md` and the new `docs/register/` (the supervisor's pen), `docs/RULEBOOK.md`.

## 6. THE GUARD MOVED INTACT

`GUARD_REGIME_PROOF.txt` — **18 checks, 18 PASS**:

- **STRUCTURAL (AST):** `_sheet_pins()` is called from exactly two sites, one lexically inside
  `if _O41_INJ:`, one inside `def _o42_state`; ORDER 41's read precedes ORDER 42's, so it still halts
  first; no pin literal survives anywhere in the engine.
- **TEXTUAL (vs the pre-edit file at `8230526`):** all 27 `ORDER 41`/`ORDER 42` halt-message fragments
  byte-identical; every pinned value byte-equal to the literal it replaced; and the declaration still
  matches the sheet in the tree (md5 `21361291…`, 219 rows, 35 injured=Y).
- **FUNCTIONAL:** the loader HALTS on an absent, malformed, holed, or re-pointed declaration, and
  passes a drifted md5 THROUGH to the ORDER 41/42 compare that has always been what halts on it.

**A dial-off build reads the pin file not at all.** That is the property PLAN_v6 3a insists on — the
two blocks live under different kill-switch families and unifying their halt behaviour would change
semantics in exactly the regime P4's collapse proofs exercise — and it is why the pins are resolved
inside each dial's own branch rather than at module import.

## 7. THE RUNBOOK, AND THE INTERIM WRITER

`docs/runbooks/R23_RUNBOOK.md` gains **ERRATUM E7**. The old instruction — "move six literals in two
engine blocks in one commit with the sheet" — was made WRONG by this act, and a manual advance in the
3a→2b window would have followed it. E7 gives the correct procedure (measure the sheet, write the
three measured values into `data/sheet_pins.json`, one explicit-path commit, then **verify engine_head
did NOT move**), and states explicitly:

- the amended manual path is the **INTERIM WRITER** of `data/sheet_pins.json` until `land round`
  (PACKAGE 2b) takes over as **SOLE writer**;
- a sheet change carries **PREREG-LITE** — predicted `sheet_md5`, `sheet_rows`, `sheet_injured_y`,
  the **disclosed movers** by name, and the owner word — **committed WITH the data change**;
- **`engine_head` moving on a sheet update is a RED, not a chore.**

E4 is kept as history, marked superseded on the pins' LOCATION only. The ADVANCE-REPIN section (§2) is
marked too: a sheet re-cut and an advance-repin are now two commits, not one.

The interim-writer RULE's durable home is `data/sheet_pins.json`'s own `_writer_of_record` header and
the engine's `_sheet_pins()` header. The runbook repeats it — a runbook is a procedure, not a law's
home; this one needed five errata inside a single round.

## 8. OWED, DECLARED, NOT TAKEN

1. **The RULEBOOK line.** PLAN_v6 3a wants the interim-writer rule in the pin file's header **and a
   RULEBOOK line**. Law 10(a) makes any change to `docs/RULEBOOK.md` an owner-only word and none
   covers this act. **Outstanding, and it needs the owner's word.**
2. **The manifest-checked carrier FIELDS.** 3a also asks that the sheet md5 and the pin file become
   manifest-checked carrier fields. This act registers the pin file as a lander **carrier**
   (`tools/landing/carriers.py`) but adds no field to `data/expected_boot.json`: that is outside this
   act's enumerated churn, and a manifest field with no writer before 2b would be a second
   hand-maintained pin of the same fact — the defect 3a exists to remove. **Remaining 3a scope.**

## 9. NOTED IN PASSING — CONCURRENT WRITERS IN THE SAME TREE

This seat shared `/home/user/afl-rl-engine` with at least one other active seat for the whole act (the
v0-public-tier UI act `1d4d426`, and the 3b register act `a8bd952`, both landed under this seat's
feet). Consequences, all recorded because none of them was a defect in this act:

- The first landing attempt **ABORTED AT PREFLIGHT** on the other seat's dirty tree —
  `ABORT_preflight_concurrent_writer.json`. The abort preceded the restore point, so nothing was
  written and the lander said exactly that. This is the abort ladder working.
- Between the engine edit `e4a48dc` and the landing `e840b9a`, the tree was **mid-act by design**
  (P9 puts the prereg and the edit ahead of the landing) and `release_manifest_check.py` therefore
  read **FAIL on four engine_head carriers** for that window. That window is now closed and the check
  reads PASS.
- The `coherent_base` slot in the act spec is `8230526` for this reason, and the pre-transaction
  self-test ran on that tree.
