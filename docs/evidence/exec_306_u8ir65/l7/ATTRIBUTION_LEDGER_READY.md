# L8 ATTRIBUTION LEDGER — READIED AT L7 (#306, seat u8ir65)

L8 builds the candidate board beside shipped and requires **every mover to carry a named cause; an
unattributed mover HALTs**. This file readies that ledger — the cause set, the widened channel it must
cover, and the pinned before/after identities L8 diffs against. It is prep, not the L8 run.

## THE CAUSE SET — every mover maps to exactly one
1. `ruled_curve` — the adopted national ladder `01f27f02` (was `e69a3f38`). Total 54,722 -> 53,497
   (-2.2%); band means -0.8% to -3.2% through pick 45; band 46-64 -8.0%; pick 64 221 -> 185 (-16.3%).
2. `year_zero_lens` — the refit surface `ebc3d330` (was `84fb0cde`/`b540833b`-lineage). Isolated
   surface effect; the engine contributes exactly zero (proven at L6).
3. `pool_levels` — N43 signed per-division levels (ship AS SIGNED; the owner's at the landing).
4. `entrant_layer` — the re-sealed §2.viii structure `ed5b7fcc` (was `a17aafed`): entrant PVC
   83538 -> 62726, re-measured on the live store 81d24704 at the adopted curve.
5. `lander` — any per-player landing re-stamp (identity/display), named individually.

## THE WIDENED CHANNEL — the ledger must cover it, not the narrow story
The feed-back channel is wide: 55.78% of teaching-value movement rides the 71 counted-fallback rows,
44.22% rides the other 1,126 evidence-backed rows. A mover in an evidence-backed row is therefore
NOT automatically a data change — it can be the ruled curve / lens reaching that row's ev() where its
own record is thin. L8 attribution must admit `ruled_curve` and `year_zero_lens` as causes for movers
in concluded careers, or it will mis-HALT on legitimate movers. Wholesale belief is 6.18% of teaching
value; evidence-backed level moved -0.63% under the surface swap (a FLOOR, not the share).

## PINNED IDENTITIES L8 DIFFS AGAINST (from the L7 substrate, capture below)
- store `81d24704` · engine `15525b03` · adopted curve `01f27f02` · surface `ebc3d330`
- entrant seal `ed5b7fcc` (62726) · board `c8116a16`-lineage (L7 render) · G-Y0 0.033% <= 2.000%
- contract_sha256 re-stamped (G-Y0 dated exception retired; held_checks now empty)

## GATE STATE HANDED TO L8
F5 league + F4 roster gates PASS (±5%); the reconciliation is now a HARD assert (board==sealed) in
both rl_export and gate_f5; seal-drift and reconciliation both proven able to fail (nonvacuity_l7.py).
The G-Y0 hard bar stands unchanged at 2.000%; only its dated exception is retired.
