# CONSISTENCY INVENTORY — 2026-07-31 · commissioned by owner word (#279, the full-internal-consistency ruling)

**What this is.** The complete map of live pricing inputs and their derivation bases, swept by three
parallel read-only agents (A: engine value path · B: data artifacts · C: UI and gates), each row carrying
an evidence pointer. Seam-verified: the deciding claims of all three reports were independently re-run
before filing (board GAMMA/PVC · model_config vars · frozen-SCALE comment · par window · F5 identity
arithmetic · FROZEN-RULER comment · FHV sites · trade-desk range). The full tables are the three files
beside this index. Durable summary: #279 (the inventory comment). Ruling authority: the owner's
full-internal-consistency ruling, #279 issuecomment-5137582245.

**Counts (pre-dedup, per lane):** A: 34 STALE-ROOTED of 62 rows · B: 19 of 47 · C: 18 of 94 (with 20
EXPECTED-TO-MOVE-AT-ADOPTION). Overlaps exist across lanes (the curve artifact, the pool triplet, the
contract blocks appear in two lanes each); the directive de-duplicates into work items.

**Seam corrections at consolidation (recorded so the reports are read right):**
1. Report A finding 5's tail is DOWNGRADED: the pick-1 guards (`rl_model.py:916`, `rl_export.py:163-167`)
   check the PUBLISHED curve head, and the ruled pooled-numeraire ladder publishes exactly 3000 — those
   guards are the numeraire law working, compatible by design. (The γ pin stack in the same finding stands.)
2. Report B's tree-level note ("no artifact carries e69a3f38") is EXPLAINED, not a defect: the ruled pack
   lives on `claude/pre-referee-baseline-shaping-4ql38z` (unmerged by design) and its engine-side landing is
   exactly the re-derivation's job.
3. Report B's board-of-record row (and every row whose staleness the already-planned propagation/adoption
   replaces) carries the disposition REPLACED-BY-PROPAGATION rather than being new work.

**The headline structure (deduped):** the pick side is ruled and converged; EVERYTHING the player side
prices with still derives from superseded bases — the v3.4 kernel anchor (SCALE, deliberately frozen at
`rl_model.py:940`; plus the draftval/ycred chain), the SCAR γ=0.85 stack (model_config + config_sha +
five artifacts + build_peak_model_v4:8 hard pins + 5 rendered UI labels), the prior-blend curve artifact
(`08ea9375`, store 265f55d5), the gfut-keyed 2003-2018 par spine feeding `_par_prior`/cm inference, the
frozen peak/cm/q97m pickles WITHOUT training-store stamps, the curve-keyed v0surf freeze, and the
old-basis measurements sealed into gates (G-Y0 ceiling 3.5 · F5 65,925/77,611 · FROZEN-RULER pins
265f55d5/2f8b4bd4 with a comment instructing they "must NOT move" — its premise dies at #279).

**Two NEW defects found by the sweep (independent of the re-derivation, docketed):**
- **F5 reconciliation is vacuous under change** (hazard class 5): `residual_nd_tail` is defined as the
  difference, so `reconciled: true` survives any ladder; the re-derivation must give F5 a real assertion
  and re-seal at the new totals.
- **The trade desk prices picks 66–80 at 0 and pick 65 as an ordinal** (`ui/app/trade.js:23,66` vs
  RULEBOOK v2.1 law 4's split, which `ingest_inputs.price_pick` implements correctly) — a live display
  defect on the shipped board TODAY, predating everything; UI-lane fix.

**Known blocker carried:** the peak-model retrain gap — pickle frozen, `dob_corrected.json` absent
repo-wide; the cm_400/q97m pickles likewise carry no training-store stamp. The re-derivation directive
must resolve the input source (possibly an owner-provided data file) before that leg bakes.

**Retention:** this directory is cited by the #279 record — RETENTION-PROTECTED under charter D2.
