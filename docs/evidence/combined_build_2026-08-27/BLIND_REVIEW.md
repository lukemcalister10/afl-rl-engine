# BLIND REVIEW RECORD (law 11) — the combined build's final movers document · 2026-08-27

Independent reviewer (fresh agent, no build context, instructed to attack). Two rounds.

## Round 1 — VERDICT RED (findings accepted, all remediated)
The board itself was CLEAN on the reviewer's own arithmetic: all 287 movers close exactly
(live + five deltas == candidate), the diff is complete (exactly the 287 differing keys), the
candidate matches the owner-approved waterfall (249/259 exact, the six |diff|>5 rows all carrying
matching step-up/easing attribution), the owner's named rules hold (no surface mover above v0;
burton unchanged; oliver 360 / mccabe 325 / leake 617 / goad 737 / edwards 614 / riak 259 exact).
The RED was the DOCUMENT layer:
1. **13 rows misattributed EASING** — the classifier read the board's `g` (career games) as
   current-season; the O48 scope (one banked season AND it is 2026) reaches exactly 3 rows
   (pickett/dawson/croft), agreeing with the battery's own F2/F4 instruments. FIXED: the easing
   class is now derived from the store's scoring records in both the movers generator and the
   verifier.
2. **Ripple disclosure misstated** (19/−32 vs the true 35/−3 after re-attribution). FIXED:
   computed from the residual column itself.
3. **F3-as-preregistered fired (pairwise census 345→399) and the re-instrumentation was not
   owner-visible.** FIXED: a dedicated headline disclosure states the growth verbatim, names the
   ceiling-gate re-instrumentation, and characterizes the +54 pairs.

## Round 2 — VERDICT GREEN
All 287 rows re-verified arithmetically (column nets: smoothing −277, surface +3225, step-up
+239, easing +276, residual −3 = +3460); completeness exact; every disclosure recomputed and
accurate. The reviewer independently re-derived BOTH censuses from the store and boards
(class sizes match the battery exactly; pairs_cand 399 exact) and corroborated the +54-pairs
characterization: ~51 of ~55 new pairs are surface-floored thin rows above resolved-down banked
rows (median banked loading 0.50); the tail (step-up/smoothing thin sides; the newton standing
row) is now named in the disclosure. Minor methods note: BATTERY_1's pairs_drill is
iteration-order head, not a new-pairs drill — the corroboration rests on the reviewer's own
base-vs-candidate recomputation.

Reviewer transcripts: the two verdict texts are preserved verbatim in the session task records;
the substance above is the full findings list.
