# #306 ACT (2) — RECONSTRUCTION AND THE COMPUTE-PATH ASSERT · **PASS**

**2026-08-04, the #306 cold execution seat `zlaarm`.** Executed under the seam's go
([#306 comment 5174786873](https://github.com/lukemcalister10/afl-rl-engine/issues/306#issuecomment-5174786873)).
Every figure was measured here. Rehearsal posture: **nothing landed**, no gate moved, no pin outside the
lane's recorded bytes, the carrier `claude/exec-seat-290-handoff-d7bnaa` untouched at `7e9d7f9`, the frozen
ancestors and HOLD branches untouched, main untouched. **The EXECUTION word remains WITHHELD.**

---

## ACT (1) — THE MERGE FORWARD, measured before the act

`42a98d4`, a TRUE MERGE on the `bf6596b` pattern, two parents (`f169204`, `7e9d7f9`).

```
merge-base            98fe397 (register v555 / CURRENT_STATE v47)
commits merged        44
conflicts             0            (git merge-tree --write-tree, exit 0, tree b9e0046)
carrier side          177 files, +2,206,824 / -0
product files moved   NONE — no data/, engine/ or ui/ path on the carrier side
```

The single non-evidence path is `tools/preboot_assert.sh`, carrying the harness re-pin the halt filing moved
into history. Without this act no engine act is lawful: D.3's non-vacuous preboot guard lives only on the
record branch.

## THE ENVIRONMENT — pins re-verified independently, not from the script's PASS line

`bash setup_env.sh` → venv at `/root/rl_venv312`, outside the repo tree. Re-read from the interpreter:

| pin | got | want | |
|---|---|---|---|
| python | 3.12.3 | 3.12.3 | EXACT |
| numpy | 2.4.4 | 2.4.4 | EXACT |
| scipy | 1.17.1 | 1.17.1 | EXACT |
| scikit-learn | 1.8.0 | 1.8.0 | EXACT |
| openpyxl | 3.1.5 | 3.1.5 | EXACT |

**5/5 exact**, `sys.version_info[:2]==(3,12)` asserted before any engine import. `bootstrap.sh` reported the
item-392 ENV PIN — numpy 2.4.4 + bundled OpenBLAS `05c9f9eb`, byte-exact to the pin — and **Guard 5 PASS**
(store `81d24704` · rl_model `3b011802` · fv `d920557e`, checkout + loaded-path). `git status` clean of any
write by either act.

## THE RECONSTRUCTION — from committed bytes only, per the seam's ruling

`L6_pass0_state.diff` (`13b71c26934d8a5b62c4f3ac1fa22137`) applied at the merged tip. Its declared base
`f0128d6` is an ancestor of the tip **because of act (1)** — verified, not assumed. Applied with plain
`git apply --binary`, **no `--3way`**, so every one of the 19 pre-image blobs matched exactly or the apply
would have failed. 19 sections, 19 modified paths.

**The ruled verify triple, byte-exact:**

```
data/v0surf.pkl                    fb9efdec4d669d389fe3beef2bca3092    <- the seam's ruled target
engine/rl_after/peak_model_v4.pkl  f305fe5330222f4fa14d3654a0e91ef7
engine/rl_after/pvc_snapshot.json  ade79790efc8ad4585c2c6800a935eaa
```

**The installed curve is `e69a3f38` and no install act was needed**, exactly as the ruling anticipated. Its
identity was **re-derived, not read**: from `engine/rl_after/pvc_curve_v2.json` (file md5 `cdc50a2f`,
64 entries, keys stored as strings) under N32's recipe —

```
STRING-keyed payload md5 -> e69a3f38    == ui/release_pick_curve.json pick_curve_curve_md5
```

`data/expected_boot.json`'s `v0surf` pin reads `fb9efdec`, coherent with the surface on disk.

## THE TWO `.srcmd5` — N33, zero compute, byte-exact

Regenerated by calling the engine's own writer `single_source.stamp_tier2_frozen`, a pure function of the
artifact's own bytes (`{derived, tier:2, frozen:true, own_md5, note}` — no source md5, nothing
machine-dependent). **No peak rebuild**: rebuilding the model on a travelling substrate is a modelling act
and R-A forbids it.

```
engine/rl_after/peak_model_v4.pkl.srcmd5     d14f0f126a3e7682318745c80e346ea2    == N33's expectation
engine/rl_after/pvc_snapshot.json.srcmd5     aaccad1c9908e262c15fd7addfe79f50    == N33's expectation
```

## THE ROUND-TRIP PROOF, taken BEFORE the substrate was disturbed

Regenerating `git diff --binary` against the reconstructed tree reproduced the ruled capture
**`13b71c26934d8a5b62c4f3ac1fa22137` byte-identically**. Taken first, deliberately: it establishes that the
restore check used after the control swap is itself valid, rather than asserting the restore against a
target never shown to be reachable.

## THE COMPUTE-PATH ASSERT — **PASS**, board `92e397bd` byte-exact

The control was **rebuilt, not inherited**, by the method `BOARD_DELTA_L4.txt` records and `L6_PASS1.md`
re-ran: pre-L4 fitted artifacts restored from git with the L4-era surface, all three pins field-level
re-stamped (each target value verified unique in the file before substitution, so the edit could not touch a
neighbour), the tier-2 sidecars re-stamped for the swapped artifacts, then the full seed + board build.

| | control substrate installed |
|---|---|
| `data/v0surf.pkl` | `84fb0cde29f36c1a91d440e63b753c3c` (from the sealed `v0surf_frozen_2026-07-31/`) |
| `engine/rl_after/peak_model_v4.pkl` | `b763f59e6a6b8aa20946211674e37259` (pre-L4, from git) |
| `engine/rl_after/pvc_snapshot.json` | `735d2dec60cee285382a5d7a17c36a6f` (pre-L4, from git) |
| pins re-stamped | `v0surf` · `peak_model` · `pvc_snapshot` |
| unchanged | store `81d24704` · curve `e69a3f38` · γ 1.0 · band `34faa865` · q97m `cfdc7321` |

Chain: `preboot_assert` PASS → `bootstrap.sh` (1.2s, Guard 5 PASS) → `RL_CONFIG_MODE=bake`,
`cd /home/claude/rl_workspace/rl_after`, `python3 rl_export.py` (**96.7s**). Gates green on the way through:
NUMÉRAIRE GUARD PASS (shipped pick-1 = 3000) · FUT-LABEL PASS (87 dual rows) · ZERO-EMPTY-CLUB PASS
(0 blank across 1,002 rows) · **PARITY GATE PASS — all 804 active board values == engine gated `ev()`,
eps = 0**.

```
GOT :  92e397bdf8269c2bb346e6650ad8c5bd
WANT:  92e397bdf8269c2bb346e6650ad8c5bd

COMPUTE-PATH ASSERT: PASS
```

**This box reproduces the host that produced the record's figures.** R-H's gate (a) is satisfied; a fit may
lawfully run here.

## THE RESTORE — proven, not asserted

The pass-0 substrate was restored from the byte backup taken before the swap, and proven by round trip:
regenerating `git diff --binary` reproduced **`13b71c26`** byte-identically a second time. Every touched
file back to its pass-0 md5, both `.srcmd5` back to `d14f0f12` / `aaccad1c`.

---

## A FINDING THIS ACT PRODUCED, AND IT BEARS ON L-C: **THE HOST MOVED UNDER ME, MID-SESSION**

Recorded because it is a fact a successor needs and because it is the strongest corroboration of L-C's
holding this job is likely to get.

| when | `/proc/cpuinfo` `model name` |
|---|---|
| 04:38:54Z, at the read-back | `Intel(R) Xeon(R) Processor @ 2.10GHz` |
| 05:13:48Z, immediately after the assert | `Intel(R) Xeon(R) Processor @ 2.80GHz` — family 6, model 85, stepping 7 (Skylake-SP), AVX-512 F/DQ/CD/BW/VL/VNNI |

Not a misreading: re-read three times, stable at 2.80GHz across all 4 cores, and `uptime` reports **up 8
minutes** against a session then ~45 minutes old. **The container restarted onto a different host between
the read-back and this act.** The record already notes this container migrated hosts mid-session once
before; it has now done so again, in front of the measurement.

**Three consequences, stated plainly:**

1. **My read-back's box observation described a host that is no longer the one running the work.** It was
   true when taken and is superseded by this line. The read-back's *posture* — that the CPU string is an
   observation and not a gate — is what survives, and this is why.
2. **The CPU string is not merely a weak gate, it is not even stable within one session.** It changed under
   me; the board bytes did not. *"The pin was never the guard"* now has an in-session demonstration
   alongside the OpenBLAS `DYNAMIC_ARCH` precedent, from a completely different direction.
3. **The assert's PASS is a point-in-time fact about the host now running.** If the container migrates
   again, R-H gate (a) must be re-run before the next fit rather than inherited from this filing. I will
   re-run it at every act that follows a restart, and name the result.

**What this does NOT do: it does not discharge L-C.** L-C asks for a byte-assert across machines wired into
the lane. An accidental host swap between two acts is not that, and a same-box proxy is explicitly forbidden.
It is recorded as an observation with a hypothesis attached — *if* the migration recurs across a fit, the
second host L-C has been missing may be obtainable rather than DEFERRED — and it will be reported as
UNMEASURED by name unless a real cross-machine comparison of **fitted output bytes** is actually taken.

---

## POSTURE

Acts (1) and (2) of the seam's go are complete and verified. **Nothing landed.** The rehearsal substrate
remains an uncommitted working tree whose exact bytes travel as the committed capture `13b71c26`, proven
reproducible by round trip twice in this act. Next: act (3), the redesign legs — beginning with **L-B's
non-vacuity demonstration in the FAILING direction**, which must reproduce today's defect on today's lane
before any acceptance can be claimed.

**The EXECUTION word remains WITHHELD.**
