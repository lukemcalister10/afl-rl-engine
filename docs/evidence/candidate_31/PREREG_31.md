# PREREG 31 — THE COMPLETE CANDIDATE. FILED BEFORE ANY ORDER-31 QUANTITY EXISTS.

**ORDER 31 build seat · `land/order-29` · 2026-08-17 · brief = #334 comment 5310338355.**

> **THE ONLY THING THAT EXISTED WHEN THIS FILE WAS WRITTEN IS THE STEP-0 ENTRY CONTROL** — the committed
> board `9298203135202a0c707bb0977ba38c31` reproduced on the untouched tree
> (`docs/evidence/candidate_31/CONTROL_ENTRY_31.txt`). That is a *control*, not a quantity of this order.
> No fit has been run, no law has been wired, no price of this candidate exists.

Every prediction below is numbered, has a **band or a binary verdict able to fail**, and is scored by
number in `SHIPPING_PACKET_31.md` with breaches owned. Bands were set from the committed 30B / 30B-M /
30B-P / 30B-R / 30B-C artifacts and the arithmetic of the declared law — never from a build.

---

## 0 · THE LAW BEING WIRED (declared here, before it is fitted)

For **every** row, every pathway, every games count — **no lanes, no bridge, no threshold ownership**:

```
price(p, Y)  =  ρ(g) · P̂(p, Y)   +   π(g, c_u, s) · V0(p)
```

| object | declaration |
|---|---|
| **`P̂`** | the finished production leg at the blend site: **pole DELETED · par-built ISO DELETED**; `Q` and the mediocre-for-years decay gate **RETAINED with their denominators re-referenced to the position-level, pick-blind v0-language bars** (`_O30BP_BARS`); `_a_blend`, `sitout_ev`'s `ns==0` arm and the year-zero floor **REPLACED, not wrapped** (STOP §5 Q4 reading, ruled by the brief). |
| **`V0`** | the **Step-1 positional `v0`** (brief item 7), in board currency, converted to engine currency by `_PL_F` — the same one conversion ORDER 29B's day-0 branch performs. |
| **`ρ(g)`** | `ρ(g) = 1 − exp(−(g/τ_ρ)^b_ρ)`; **`ρ(0) = 0` exactly by construction**, strictly increasing, → 1. **FITTED so the thin-cohort aggregate `ρ(g)·1 + D(2)·κ(g)` matches the R1 re-derived cumulative backbone** `B(≤0)=0.5502 · B(≤2)=0.6485 · B(≤5)=0.6887 · B(≤10)=0.8223` (RAW(1)-normalised, so `P̂` reads 1.0 there). **The backbone constrains the TOTAL. It never replaces production.** |
| **`π`** | `π(g, c_u, s) = D(c_u) · κ(g) · Φ(g, s)` — three **disjoint measured channels**, never multiplied against each other's population: |
| — `D(c_u)` | the **ruled sitter fade**, `D(1)=1 · D(2)=0.5501935857356868 · D(3)=0.26278629823610156 · D(4+)=0.3460004697526451 FLAT`, log-linear between integer depths, on a clock `c_u` that counts **UNPLAYED time only** (a played season advances `g`, not the sitter clock — the brief's Mraz clause). |
| — `κ(g)` | `κ(g) = exp(−(g/τ_π)^b_π)`, **`κ(0) = 1` exactly**; `τ_π, b_π` fitted **n-weighted to the measured pooled `β` band midpoints** (2.5→0.2968279384332228 · 10.5→0.362259307264279 · 25.5→0.22329587551741345 · 53.0→0.15314862603013868 · 85.5→0.020068021140596692) with a free amplitude that is then **discarded** — only the decay *shape* is transplanted, because the *level* at `g=0` is pinned by `D(c)`. |
| — `Φ(g, s)` | the **30B-C stall conditioning**. `Φ(g, 0) = 1` exactly. `s` = the count of **PLAYED-but-not-delivered seasons** (a season with `games > 0` whose `avg` is below the position bar, or `games < 10`). Gameless seasons are NOT counted here — they are `D(c_u)`'s job, and counting them twice is the double-discount the no-stacking constraint forbids. `Φ` ramps to the measured ratio `β_stall(g)/β_pooled(g)` over the measurement's own two-season definition of a continued staller. |

**`π(0, c, 0) = D(c) · 1 · 1 = D(c)` EXACTLY**, so the Step-2 sitter price `v0 × D(c)` is reproduced by the
ONE law with no interception, no lane and no special case — and the printed-day-0 identity survives by
construction rather than by assertion.

---

## 1 · MOVER CLASSES — PREDICTED BEFORE THE BUILD

**P1 · THE AT-BAR VETERANS (named in advance, per the brief's item 12).** Rows with `cg ≥ 71` whose
production leg has decayed to at or below half their `v0`. **141 such rows exist on the Step-2 board.** The
twenty largest by `v0`, named here **before any Order-31 price exists**:

`jaeger-o-meara` · `joshua-kelly` · `paddy-dow` · `stephen-coniglio` · `jacob-weitering` · `adam-cerra` ·
`darcy-parish` · `dylan-stephens` · `scott-pendlebury` · `will-setterfield` · `jacob-hopper` ·
`jackson-macrae` · `dion-prestia` · `jack-bowes` · `oliver-wines` · `steele-sidebottom` · `cameron-rayner` ·
`jack-lukosius` · `jack-martin` · `ben-ainsworth`.

**PREDICTION:** every one of them **rises** against Step-2 by the new pedigree top-up `π·V0` net of the
pole/ISO deletion, and the *median* absolute move of the class is **≤ 250 board points** because `κ` at
71+ games is small. **Fails if** the class median move exceeds 250 points, or if more than 25% of the class
moves down.

**P2 · THE PURE SITTERS (`cg == 0`, ND in-curve, 89 printed-day-0 rows).** **PREDICTION: byte-identical to
Step-2**, because `ρ(0)=0` and `π(0,c,0)=D(c)`. **Fails on a single point of movement.**

**P3 · THE THIN LANE (`1 ≤ cg ≤ 15`, 147 rows).** **PREDICTION: this class moves DOWN against Step-2 in
aggregate** (the backbone calibration is a *cut* to the thin lane: the resolution packet measured the join
at −1.86% of book while the additive reading added +11.12%), by **between 2,000 and 14,000 points in total**.

**P4 · THE ESTABLISHED BOOK (`cg ≥ 16`, 568 rows).** **PREDICTION: this class moves DOWN against Step-2 in
aggregate** — the pole+ISO deletion is worth −16,907 to −19,273 there and is only partly offset by the
pedigree top-up. Band: **−25,000 to −5,000**.

**P5 · THE STALL-CONDITIONED CLASS (30B-C).** Rows carrying `s ≥ 2` played-but-not-delivered seasons.
**PREDICTION: at least 40 rows carry `Φ < 1`, and the named 30B-C overpaid paths that are still on the
board price BELOW their unconditioned counterfactual by at least 15% of their pedigree leg.**

**P6 · NO UNDECLARED MOVER CLASS.** **PREDICTION: every row that moves is attributable, to the point, to
one or more of the declared mechanisms** (v0 head shrink · pool parameters · ρ · π · Φ · pole/ISO deletion ·
supersession · position gate · numéraire). **Fails if any row's move fails to reconcile to the sum of its
attributed mechanisms, at tolerance ±1 point.**

---

## 2 · NAMED ROWS — BANDS FILED BLIND

Bands are **wide on purpose**: they are honest priors from the declared algebra, not targets. Step-2 price
in brackets.

| # | row | Step-2 | **predicted band** | reason the band is where it is |
|---|---|---:|---|---|
| **P7** | `isaac-kako` | 1320 | **850 – 1150** | `ρ(36)≈0.9` on `P̂ 744` plus `π·760`; the additive reading put him at 886 |
| **P8** | `josh-smillie` | 471 | **471 exactly** | pure sitter, `ρ(0)=0` |
| **P9** | `harry-demattia` | 301 | **301 exactly** | pure sitter |
| **P10** | `max-knobel` | 287 | **287 exactly** | pure sitter |
| **P11** | `noah-mraz` | 1665 | **1400 – 2200** | `P̂ = 5016` at `cg 4`; `ρ(4)` is small but **non-zero — the brief's "never again priced at zero weight"** |
| **P12** | `nick-madden` | 1650 | **1000 – 1900** | `P̂ = 1854` at `cg 10`, pool pedigree cell thin |
| **P13** | `dyson-sharp` | 3269 | **2400 – 3400** | `cg 13`, `ρ` well below 1 on a large `P̂` |
| **P14** | `willem-duursma` | 4223 | **3900 – 5300** | `cg 19`, `P̂ 4198`, `v0 3879` |
| **P15** | `jacob-farrow` | 2765 | **2400 – 3200** | `cg 18` |
| **P16** | `toby-conway` | 433 | **450 – 1300** | `cg 6` on `P̂ 1549` with a **6.00 clock**: the row where `D(c_u)` and `Φ` both bite |
| **P17** | `luke-beecken` | 164 | **100 – 500** | MSD `cg 1`, `P̂ 0.2`; his price is **almost entirely `π·V0_pool`** — the row Step 2's pool derivation is for |
| **P18** | `liam-reidy` | 328 | **200 – 500** | RD `cg 7` |
| **P19** | `cooper-trembath` | 2055 | **1900 – 2600** | MSD `cg 24` |
| **P20** | `chris-scerri` | 467 | **150 – 550** | SSP `cg 7`; **the least trustworthy row in the packet** (thin pool cell) |
| **P21** | `jack-martin` | 97 | **0 – 200** | at-bar veteran, `P̂ = 5.3` |
| **P22** | `harry-sheezel` | 10987 | **10200 – 11500** | deep, `ρ≈1`, `P̂ 10983` |
| **P23** | `jai-newcombe` | 4561 | **4300 – 5000** | MSD deep |
| **P24** | `marcus-herbert` | 846 | **700 – 1300** | MSD `cg 8` |
| **P25** | `jaxon-artemis` | 500 | **400 – 800** | MSD `cg 4` |
| **P26** | `nicholas-martin` | 3281 | **3100 – 3600** | SSP deep |

---

## 3 · BOARD TOTAL

**P27 · THE CANDIDATE'S BOARD TOTAL, PRE-NUMÉRAIRE, lands in `[690,000 , 760,000]`.** Anchors: live
**752,429** · Step-2 **706,672** · 30B-P preview **679,875** · the 30B-R derived resolved configuration
**715,229**. The one law sits above the preview (production is no longer shrunk by `(1−σ)`) and below the
un-joined additive reading (the backbone calibration cuts the thin lane and `Φ` cuts the stall paths).

**P28 · THE NUMÉRAIRE RE-PIN moves the board by a pure scale `s`, and `|s − 1| ≤ 0.15`**, with picks and
players moving together and the E6 identity holding.

---

## 4 · THE CONTINUITY AND COMPLETENESS PASSES

**P29 · THE CONTINUITY CURVE (BUILD-FAILING).** Price vs games `0 → 20` at **fixed output** is, for every
continuity row tested: **(a)** monotone non-decreasing; **(b)** free of any step exceeding **+40%** at
`g: 0→1`; **(c)** free of any dead zone (two consecutive equal values with the output held). Emitted as a
packet artifact. *(The 30B-R join measured +37.1% under a construction that had a lane boundary at 1; the
one law has no boundary there, so the band is set at the join's own measured value plus rounding.)*

**P30 · THE COMPLETENESS ASSERT (BUILD-FAILING).** Every printed price decomposes into v0-language objects
only. The **26A forbidden set is unreachable from any printed price**: `par_pole` returns no live value on
the price path, and the par-built ISO multiplier is 1.0 on the price path. Audited and committed.

**P31 · PRINTED-DAY-0 IDENTITY: 89 of 89 at tolerance 0**, re-verified against the **re-fitted** Step-1 v0
cells (not the pre-refit ones).

**P32 · CELL COVERAGE:** every priced row resolves a `V0` object with **zero fallbacks and zero halts**.

---

## 5 · STEP-1 (THE v0 RELATIVITY HEAD) AND STEP-2 (THE POOL)

**P33 · THE HEAD SHRINK.** All **six** positions are audited at the curve head, not only RUCK.
**PREDICTION: RUCK is the largest head inflation and at least one other position also carries a head
anomaly.** The known case is RUCK `1,968 → 3,477`. **Fails if RUCK is not the largest, or if the audit is
run on fewer than six positions.**

**P34 · CONSERVATION.** The head shrink is **conservative**: the position-summed `v0` mass is preserved to
**≤ 0.5%**, the floor-100 tail is retained, and per-position monotonicity in pick is retained. The
reconciliation residual is **printed, not absorbed**.

**P35 · THE POOL PARAMETERS.** `D_pool` and `β_pool` are derived by the **same constructions** as the ND
laws (30A-2 and 30B-M harnesses transplanted), with `n` and dispersion on every published cell, thin
pathways K-shrunk toward the pooled pool row and the borrowing disclosed. **PREDICTION: `D_pool(2)` lands
in `[0.35, 0.80]`** — the pool population is more selected than the ND draftee population, so its fade
should be *shallower* than ND's 0.5502, but the direction is not certain and the band admits both.

**P36 · THE MSD CLOCK.** MSD's first season **is season 1**, on games-of-12, per owner ruling 5; PDN/PDA and
the other pool clocks run on true entry timing. Asserted in the harness, not assumed.

---

## 6 · CONTROLS

| # | control | verdict able to fail |
|---|---|---|
| **P37** | **entry byte-identity** | `9298203135202a0c707bb0977ba38c31` on the untouched tree. **CLOSED AT FILING TIME — PASS.** |
| **P38** | **deterministic double-build** | the final board built twice, byte-identical md5. |
| **P39** | **dial-off byte-identity** | with the Order-31 dial unset, the board reproduces `9298203135202a0c707bb0977ba38c31` byte-exact. |
| **P40** | **identity gate** | the `o29_gate` lineage re-point is **DECLARED** before it is made; no identity literal moves undeclared. |
| **P41** | **boot guard** | passes; `expected_boot` fv pin stays stale **by design** and is stated. |
| **P42** | **pins** | every pin's old→new printed, and the **moved-set asserted** against the declaration — an undeclared pin move is a build failure. |
| **P43** | **`noarb_table_338.py` byte-identical** everywhere it appears. |
| **P44** | **active-provenance guard** | no foreign `rl_model.py` copy is installed for any board that ships a number to the owner. |
| **P45** | **nothing merges** | PR #510 title stays `[HELD — DO NOT MERGE]`; `main` untouched. |

---

## 7 · THE DISCLOSURES THAT ARE OWED WHATEVER THE NUMBERS SAY

Filed here so they cannot be presented later as discoveries:

1. **The deep-β CI spans zero** (71+: `t = 0.49`, CI −4.6…10.5%). Everything `π` does past 71 games rests on
   a coefficient indistinguishable from zero. The point estimate is wired; **the CI is printed on the packet**.
2. **The additive scale assumption** (30B-R §1.6) is **asserted, not proven**: `β` is measured in remaining
   6-season delivered value per unit `v0`, and wiring it against the engine's production leg assumes those
   are the same ruler.
3. **`ρ` is fitted on four backbone knots.** Four points, two parameters. The residual is published.
4. **`Φ` transplants a FORWARD-window measurement onto a BACKWARD-looking observable.** 30B-C measured the
   stall cohort by what happened *next*; at pricing time the seat can only see what happened *before*. The
   substitution is declared, and it is the weakest joint in the conditioning.
5. **The pool `v0` cells are thin** and their K-shrink borrows across pathways.
6. **The ruck-head reconciliation residual** is disclosed, not absorbed.
7. **The position gate's residuals** are flagged precisely rather than papered over.
8. **Pre/post numéraire is stated on every table.**

---

## 8 · THE LINE

Nothing merges. `main` is untouched. Engine runs are strictly sequential under the pinned five-var
environment on the pinned venv. `RL_V0SURF_PKL` is set for all surface work. The active-provenance guard
refuses foreign `rl_model.py` copies. The `expected_boot` fv pin stays stale. `noarb_table_338.py` is
byte-identical everywhere.

**Nothing in this file will be edited after a reading. Breaches are owned by number in the packet.**
