
DAY-0 EMITTER HALT — NOTHING WAS WRITTEN
  REFUSING TO WRITE: the standing day-0 reference is ABSENT at docs/evidence/NO_SUCH_DIRECTORY/DAY0_ABSENT.json. The printed row diff is MANDATORY (M1b), so there is no lawful first write: a regeneration nobody can compare to the reference it replaces is exactly the unreviewable re-base the ruling exists to prevent. Name the standing reference, or install one deliberately in its own act.
