# L6 PASS 1 under R-H — the install executed · **G-Y0 8.084% → 11.030%, the wrong way** · not a fixed point

**#290, 2026-08-04, the fresh execution seat.** Every figure names its substrate. Nothing outside the
lane's recorded bytes was installed; no gate moved; the EXECUTION word remains withheld.

---

## THE BOX — both R-H pre-loop gates RE-ESTABLISHED here, because the prior container is gone

| gate | measured | verdict |
|---|---|---|
| **(a) compute-path assert** | the pre-L4 control rebuild reproduced board **`92e397bd`** byte-exact (116s) | **PASS** |
| **(b) double-fit byte-compare** | two fresh processes from an identical reset → **`aaf45964`** twice (62s, 68s) | **PASS** |

The control was rebuilt, not inherited: pre-L4 fitted artifacts restored from git
(`peak_model_v4.pkl` `b763f59e`, `pvc_snapshot.json` `735d2dec`) with the L4-era surface `84fb0cde`
and all three pins field-level re-stamped, exactly as `BOARD_DELTA_L4.txt` records the method. The
pass-0 substrate was then restored and **proven byte-exact by round trip** — regenerating
`git diff --binary` reproduced the ruled capture `13b71c26934d8a5b62c4f3ac1fa22137` byte-identically.

**The CPU string is not the gate and was not treated as one.** This box reports the same
Xeon @2.80GHz family 6 model 85 (Skylake-SP) with the same AVX-512 tiers as the recorded host; that
is context. The board bytes are the gate, and they matched.

## THE SUBSTRATE RECONSTRUCTION — from committed bytes only

`L6_pass0_state.diff` (`13b71c26`) applied at tip, all 19 pre-image blobs matching the carrier-tip
blobs before it was applied. Verified byte-exact afterwards: `data/v0surf.pkl` `fb9efdec` ·
`peak_model_v4.pkl` `f305fe53` · `pvc_snapshot.json` `ade79790` · plus `cm_400` `34faa865`,
`q97m` `cfdc7321`, store `81d24704`.

**The two `.srcmd5` — the cannot-carry exclusion — regenerated BYTE-IDENTICALLY to the R-A freeze
copies (`d14f0f12` / `aaccad1c`) at ZERO compute**, by calling the engine's own writer
`single_source.stamp_tier2_frozen`. The ruled step prices this as a 31s rebuild; measured, no rebuild
is needed or wanted. The sidecar is `{derived, tier:2, frozen:true, own_md5:md5(file), note}` — a
**pure function of the artifact's own bytes**, with no source md5 and nothing machine-dependent in it.
Re-running `build_peak_model_v4.py` would have re-fitted the peak model **on this box**, which is
precisely what R-A froze the fitted stack to prevent; `single_source.py`'s own docstring calls
rebuilding them *"a modelling action"*. Recorded as a deviation from the step's wording, taken to obey
the ruling the wording serves, with the result proven rather than asserted.

## THE INSTALL — `PASS1_INSTALL_SET.md` executed atomically

Four files, in the order the derived hashes force, field-level throughout (C.2). Every pre-assert
passed before any byte was written.

| | from | to |
|---|---|---|
| `pvc_curve_v2.json` `curve` + `curve_md5` | `e69a3f38` | **`1a8db02b`** (Σ 54,350 · 63/63 strict · pick 64 = 215) |
| `numeraire.pooled_head_pre_scale` | 3068.4647 | **3005.3384** |
| `numeraire.s` | 0.9776876364261254 | **0.9982236942102759** (DERIVED; E6 coherence \|pin/head − s\| = **0.0e+00**) |
| `pool_value` (both artifacts) | 234.3 | **233.4** |
| `stamp.per_entrant_md5` (all three carriers) | `77eba4d3` | **`9c4bca53`** |
| `stamp.store_md5` | `81d24704` | **unchanged — the store did not move** |
| `ui/release_pick_curve.json` `pick_curve_file_md5` | `cdc50a2f` | **`785ff06e`** = md5 of the file written one step earlier |
| `release_contract` `curve_payload_md5` · `contract_sha256` | `e69a3f38` · `3ede10d3` | **`1a8db02b`** · **`ef0f2fb2`** (recomputed by `release_contract.contract_hash`) |
| `one_source_selftest.py` `_contract_md5` (:490) | `ca2f2e87` | **`3701caa0`** = md5 of the file written one step earlier |
| `one_source_selftest.py` `_curve_source_store` (:499) | — | **unchanged**, as specified |

Two proofs were taken **before** the renderer was trusted, not after: the payload recipe reproduces
the **installed** `e69a3f38` from the installed curve on this box; and re-rendering the **installed**
curve with the writer reproduces its bytes exactly. The first attempt HALTED on its own assert —
`b43a4231` instead of `1a8db02b` — because the ladder had been keyed by **int** rather than **string**,
and the recipe string-sorts. Nothing was written; the bug was mine and the assert caught it. **That is
E.5 finding 4 biting a second time, and it is worth a docket line: the recipe's key type is
load-bearing on identity and is still written down nowhere in code.**

### The §4 discipline, enforced and proven — and it fired twice, not once

`ui/release_pick_curve.json`'s `supersedes` block holds historical twins of three edited tokens. Every
edit was made **by JSON path outside that block**, and the block was asserted **byte-identical before
and after** (`1554b98e` / `f14a6622` / `968de0c7`).

**The same shape then appeared in a file nobody had flagged.** In `pvc_curve_v2.json` the literal
`3068.4647` occurs **twice** — once as the `numeraire` field, once inside that field's own `_doc`
prose. A token-level replace would have moved both. Moved by field; the prose was rewritten
separately and deliberately.

**Two disclosed additions to the enumerated set,** both of the same class — *a pin's own label must
not lie once the pin has moved*: `numeraire._doc` (which stated `3068.4647` as the measured head) and
`stamp.per_entrant_path` (which named `per_entrant_271.json` while `per_entrant_md5` now points at the
committed pass-0 matrix). Neither is read by a gate. Named here rather than done silently.

**Sealed history untouched at every step:** the ten historical `45b207c0` stay **4** (`release_lineage`)
and **6** (`finalization_state`).

## THE REFIT — R-H clause 2 obeyed to the letter

The curve moved, so the shipped signature key moved with it: `96d671c952c8` → **`6afe678c73dc`**.
Surface `fb9efdec` → **`aaf45964`**, and the lane re-pinned `expected_boot.v0surf` itself (both were
reset together between the two runs — the predecessor's recorded slip, not repeated).
`pass1_surface/v0surf.pkl` + `IDENTITY.json` were **committed as bytes + md5 + signature key at
`627ce53`, before any board build loaded them.** `data/v0surf.pkl`'s load-or-HALT is untouched; the
dict gained no unrecorded key. Surfaces frozen: **2**, so a normal build performs no fit at all.

## THE MEASUREMENT — G-Y0 rose

```
selftest 96 PASS / 1 FAIL      board f69f0c27      book f8e5d136
  the single FAIL = G-Y0 11.030% vs the 3.500% ceiling
```

All six FROZEN-RULER checks green on the new pins. F1 and F2 parity green, **mismatches = 0**.

| state | surface | payload | ladder | `s` | pooled head | **G-Y0** |
|---|---|---|---|---|---|---|
| L3–L5 record | `84fb0cde` | `e69a3f38` | 54,722 | 0.977688 | 3068.4647 | **13.919%** |
| pass 0 — surface catch-up | `fb9efdec` | `e69a3f38` | 54,722 | 0.977688 | 3068.4647 | **8.084%** |
| **pass 1 — curve 1 installed** | **`aaf45964`** | **`1a8db02b`** | **54,350** | **0.998224** | **3005.3384** | **11.030%** |
| → curve 2, derived on the pass-1 matrix | — | **`ca662051`** | **54,336** | **0.977686** | **3068.47** | pass 2 would measure |

**I am not going to call this converging.** Two things reversed at this pass, and both were moving the
other way when the loop was described as convergent:

1. **G-Y0 rose 8.084% → 11.030%**, +2.9 points, against a 2.000% acceptance bar.
2. **The pooled head and `s` went back**, 3005.34 → 3068.47 and 0.9982 → 0.9777 — almost exactly the
   L3–L5 record's own values (3068.4647 / 0.977688). The driver is visible in the derivation: raw
   pick 1 moved **2936.23 → 2997.90** and the PAVA-pooled head **3005.34 → 3068.47** when the surface
   was re-fit. The head tracks the surface, and the surface tracks the curve.

The trajectory `s` 0.9777 → 0.9962 → 0.9982 was read as a march toward 1.0. At pass 1 it returned to
0.9777. **A three-point sequence is not a trend, and this is the pass that shows it.** What the record
can say is what it has measured: this is a **two-variable fixed point** (curve ↔ surface) and pass 1
moved both for the first time — pass 0 moved only the surface, which is why it looked monotone.

**Not a verdict against the 2.000% bar.** The bar judges the fixed point, and the fixed point is not
reached.

## THE LEVELS — intervals and denominators, no point estimates

Re-measured on the pass-1 matrix: **POOL 233.3, n = 1,005**, relative SE 6.1%, provenance fallback
6.07% — stable across three measurements now (233.0 → 233.4 → 233.3, n = 1,005 every time), which
**strengthens N5 rather than re-opening it**. MSD 293.8, n = 44, relative SE 36.3%. SSP 321.7, n = 31,
relative SE 39.6%. The MSD/SSP posture is binding and is kept: read the interval, not the mean.

## THE MATRIX RE-EMIT AND THE DERIVATION

```
PRE 2f8b4bd4 -> emit (167s) -> CAPTURED 143dcbad -> RESTORED 2f8b4bd4    RESTORE PROVEN, git clean
```

The pass-1 matrix is **committed** beside its identity record per the generalised freeze law:
`pass1_matrix.json`, md5 `143dcbad8bef81264a79ca748c9a7823`, store `81d24704`, sig `6afe678c73dc`,
2,646 records.

**The harness was re-pinned, disclosed, and its assert proven able to fire in BOTH directions both
before and after the move** — old pin vs the pass-1 matrix HALTs and names both values; old pin vs the
pass-0 matrix loads at 1,197 rows; after the move, the two swap. `EXPECT_N` was re-measured on the new
matrix, not assumed. An instrument pinned to a substrate that has moved is exactly as stale as a
capture that has: R-J applied to an instrument.

## R-I — NOT A FIXED POINT

```
installed payload  1a8db02b
derived  payload   ca662051      ->  NOT EQUAL. The loop continues.
```

**A reading I am flagging rather than assuming.** R-I sets the bound at **4 passes** and does not say
whether pass 0 is one of them. Pass 0 installed no curve — it was a surface catch-up — so I am
counting **install passes**, i.e. passes 1–4, which gives three more. If the seam intends pass 0 to
count, the bound is passes 1–3 and I have two more. **Say the word if my reading is wrong; I will not
quietly pick the generous one at pass 4.**

## COSTS — all serial, preboot assert PASS before every act

compute-path assert **116s** · double-fit **62s + 68s** · chain **375s** (board 114 · book 172 ·
selftest 89) · matrix re-emit **167s** · derivation **1s** · bootstrap ~1s each.
**≈ 13 minutes of engine compute this pass.**

## POSTURE

Rehearsal. R-C holds. Nothing lands, no gate moved, no pin outside the lane's recorded bytes, the
carrier and the three HOLD branches untouched, and **the EXECUTION word remains WITHHELD.**
