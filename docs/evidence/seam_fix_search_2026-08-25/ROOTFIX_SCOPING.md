# CANDIDATE 2 — LEVEL-VOCABULARY ROOT FIX · COST SCOPING (v848(b)) · measured, no prototype · 2026-08-25

THE PROPOSAL: sub-6-game seasons bank a heavily-damped level, so cameo strength flows through all
existing machinery (the true root cause per v840 finding 3 — cameo cells read level "none").

## MEASURED BLAST RADIUS
grep census (main checkout, live pricing path + fitted-artifact producers):
- `games >= 6` qualifying-season rule: **67 sites** across engine/rl_after + engine/forward_valuation,
  of which 11 in _merged_recover.py alone (including line 1815's D10 PRORATED in-progress bar — the
  rule is not even one constant, it is a family of variants).
- The rule DEFINES, not merely filters:
  1. THE BAND'S TRAINING CORPUS — conditional_prior.py qualifying seasons build the LEVEL feature for
     all 13,220 training rows. Changing the vocabulary moves every historical cohort's features ->
     full cm_400 refit AND the design contract's level semantics.
  2. THE OUTCOME CURRENCY — build_peak_model_v4.py / build_cohort_book.py target = best-3 of >=6g
     seasons (T_bp). Every outcome-calibrated object in the estate (bust prior, cohort book, the seam
     program's own outcome tables, the modernized table just ruled) is denominated in T_bp. A
     vocabulary change either moves the target (re-derive EVERYTHING) or creates a two-vocabulary
     estate (features see damped levels, targets do not — a new seam).
  3. RULED DEFINITIONS — rl_model.py QUAL_336 (establishment = a season of >=6 games, the ruled
     definition) and the T1 fabricated-zero interplay ("only >=6-game seasons aggregate", measured
     not-applicable at the bust-prior rederivation on exactly that premise; a vocabulary change
     reopens that measurement).
  4. THE GATES — the _gate1 family and the book computations carry the same rule; every one re-reads.
- THE DAMPING CONSTANT IS A NEW FITTED PARAMETER whose evidence base is exactly the thin cameo cells
  v843 proved binding (seam model v2 lost 0-60 ON SAMPLE SIZE; STACK/D1 proved no feature list
  repairs it). "How much level does a 3-game season bank" is fitted from the same n.

## THE DECISIVE POINT
Even fully paid, the root fix DOES NOT GUARANTEE the settled requirement: a damped level flows into
the band, but the thin zone's response is uncalibrated (dBoard/dLevel spans 1.03-48.79, v840(4)) —
nothing forces a cameo>=45 row to its never-played self. The inequality guarantee still needs the
constraint. So the root fix is COMPLEMENTARY OFF-SEASON WORK (it would make cameo strength visible
to the machinery), not a substitute for the ordering fix, and cannot be the week's answer.

## VERDICT FOR THE PACKET
Root fix: honestly costed at full-rebake scale (both arms' constructions + every outcome-calibrated
table + at least two ruled definitions reopened) with a new thin-sample fitted constant, and it
still would not deliver the guarantee on its own. RECOMMENDED DISPOSITION: named for the off-season
conversation, not this week's fix.
