# AMENDED DISCLOSURE — THE DAY-0 PRINT REFERENCE IS REGENERATED ON THE PARITY BOARD `a05fe951`

**Branch `land/order-29`, from `origin/land/order-29` at `9b93fba`. Engine
`29376d5a73a3e8274fcebe5cd90ada0b` — UNCHANGED by this seat, and no engine edit is made or needed.
This is a REFERENCE/DATA change and nothing else.**

**This file is pushed BEFORE the reference is touched.** Nothing below is measured after the fact.

> ## THE BOARD IS **PRICED, NOT ADOPTED.**
> Nothing here is adopted, merged, tagged or promoted. Those are owner-only acts. Nothing is on
> `main`. The tag and the `main` promote are OWNER-ONLY.

---

## 0 · THIS FILE SUPERSEDES `REBASE_DAY0.md` AT `d5c37da`, ON THE RECORD

`docs/evidence/final_candidate_2026-08-19/REBASE_DAY0.md`, committed at **`d5c37da`**, is a
disclosure written for a board that **no longer exists as the candidate**. It names **three** rows —
`sam-allen` (−22), `ollie-murphy` (+4), `kobe-mcdonald` (−3) — measured against the superseded board
**`daa16812`**.

**That regeneration was never executed.** The register v771 owner ruling stopped it in time and the
reference was left untouched; `DAY0_FC.json` was never written, and the absence of that file in this
directory is the physical evidence that the stop held.

**This file replaces it.** `REBASE_DAY0.md` is **NOT deleted** — it stays as filed history, the way
`DAY0_K.json` stays. What changes is which document is operative:

| | file | status |
|---|---|---|
| **superseded** | `REBASE_DAY0.md` (`d5c37da`, three rows, board `daa16812`) | **filed history. Not operative. Not acted on.** |
| **operative** | `REBASE_DAY0_AMENDED.md` (this file, **six rows**, board `a05fe951`) | **the disclosure for the regeneration that is executed** |

**The three-row table in `REBASE_DAY0.md` is wrong for the priced board and is superseded here rather
than quietly overwritten.** Two of its three rows — `sam-allen` and `kobe-mcdonald` — **no longer move
at all**, because the parity guard restores them exactly; and five rows it never named now do move.

---

## 1 · THE AUTHORITY CHAIN

| register | act | bearing on this file |
|---|---|---|
| **v763** | bake item: *"sitter print reference regeneration if day-0 moved — disclose as Orders D/K did."* | the regeneration was **ruled lawful before this seat existed**. It was queued for the bake. |
| **v769** | the supervisor ruling on the final-candidate assembly **pulls that item forward** and orders it executed now, with full disclosure, so the class mark and the no-arb page can be produced. | the item is **live now**, not at the bake. |
| **v771** | the owner's parity ruling: *"Being marked as injured shouldn't all of a sudden enrol you to a mechanism that doesn't affect your peers."* The D7 seat is commissioned; **the day-0 rebase is STOPPED and the reference left untouched** until the parity table is shown. | the reason `REBASE_DAY0.md` was never acted on. **The stop held.** |
| **v773** | D7 delivered and raw-verified on board `a05fe951`. Falsifier **D7-F8 FIRED**: **six** day-0 entrants move against the frozen reference, not the predicted one. Reported wrong-as-predicted, not smoothed. | **the six rows this file discloses.** |
| **v774** | the owner accepts the parity table — *"Completion pass please."* — and orders the completion pass on `a05fe951` / 664,949, item (1) of which is **"the AMENDED day-0 reference regeneration — SIX rows … 83/89 asserted BYTE-IDENTICAL, the stale `d5c37da` three-row disclosure superseded on the record."** | **the order this file executes.** |

This seat is not deciding that a regeneration is lawful, and is not deciding that the amendment is
owed. Both were ruled. What this seat owes is **the disclosure, the assertions, and the honest
count.**

---

## 2 · WHAT IS BEING REGENERATED, AND WHAT IS NOT

| | file | status |
|---|---|---|
| **the ORDER K reference** | `docs/evidence/order_k_2026-08-18/DAY0_K.json` (board `f3101883`) | **NOT TOUCHED.** Filed history. It stays, byte-for-byte. |
| **the NEW reference** | `docs/evidence/final_candidate_2026-08-19/DAY0_CP.json` (board `a05fe951`) | written by `cprb_day0.py`, **only if every assertion passes** |
| the superseded disclosure | `REBASE_DAY0.md` | **NOT DELETED.** Superseded by §0 of this file. |
| the emitter | `docs/evidence/candidate_31f/emit_matrix_31f.py` | **NOT TOUCHED.** Byte-carried, as always. |
| the engine | `engine/rl_after/_merged_recover.py` `29376d5a` | **NOT TOUCHED.** No engine edit in this pass. |
| the guard | the ORDER 31-F replication proof inside the emitter | **NOT WEAKENED.** Same two legs, same tolerance 0. |

The re-point is `RL_DAY0_FINAL`, which the emitter already reads as an environment variable and which
the `run_emit_*` scripts already set. **No code change is made to make the guard pass.** The guard is
pointed at the reference for the law it is guarding, which is what it was built to do — the emitter's
own header calls this *"a declared re-point in the disclosed-copy convention, at each candidate's
reading"* (supervisor's filed basis resolution, #334 comment 5310447449).

### 2a · ONE SCRIPT CHANGE, DISCLOSED

`docs/evidence/consolidation_2026-08-19/run_emit_D6.sh` carries the COMPLETED dial pass-through
through `RL_O42`. **`RL_O43` is absent from that list** — it did not exist when the file was written.
The completion pass adds `RL_O43` to the pass-through and re-points the staging, as
`run_emit_CP.sh`. **That is a SCRIPT change, allowed by the order and disclosed here.** No engine
file, no emitter file, and no guard is modified. Without it the emit would price the D7-off board
while the reference carries the D7-on prints — i.e. the omission would have caused a silent
mismatch, which is exactly the failure the pass-through list exists to prevent (register v767:
**never `run_emit_ASM.sh`**, which drops the newer dials).

---

## 3 · THE SIX ROWS, WITH THE OLD AND NEW PRINTED DAY-0

Straight off the D7 parity harness's own raw output
(`docs/evidence/parity_2026-08-19/PARITY_TABLE_out.txt`, committed at `9b93fba`, unedited):

```
  wired entrants on the frozen reference: 89
  MOVERS vs the frozen prints: 6
  key                          frozen     priced    delta  annotated injured=Y
  harley-barker                   481        504      +23  Y
  blake-thredgold                 372        381       +9  Y
  max-king-syd                    129        138       +9  Y
  liam-hetherton                   66         70       +4  Y
  ollie-murphy                    196        200       +4  Y
  noah-chamberlain                 37         40       +3  Y
```

Written out, with the pathway, the depth as a **clock value**, and the entry object:

| row | pathway | depth (clock) | **printed day-0 OLD** (`DAY0_K.json`) | **printed day-0 NEW** (`a05fe951`) | move | `derived_v0` old → new |
|---|---|---:|---:|---:|---:|---|
| `harley-barker` | ND pick 24, MID | 1.58 | **481** | **504** | **+23** | 843.1284944911190 → **unchanged** |
| `blake-thredgold` | ND pick 26, KPD | 1.58 | **372** | **381** | **+9** | 503.3749976437530 → **unchanged** |
| `max-king-syd` | ND pick 49, SF | 1.58 | **129** | **138** | **+9** | 278.3691443496010 → **unchanged** |
| `liam-hetherton` | PDA pool, `PDA\|KPF` | 1.58 | **66** | **70** | **+4** | 117.3045071268168 → **unchanged** |
| `ollie-murphy` | ND pick 41, KPD | 3.58 | **196** | **200** | **+4** | 398.3582851316144 → **unchanged** |
| `noah-chamberlain` | PDA pool, `PDA\|SF` | 1.58 | **37** | **40** | **+3** | 86.6490308603825 → **unchanged** |

**All six are annotated `injured=Y`** on `docs/owner_annotations/SITTER_2026_v1.csv`
(md5 `b26798c35adcd9bda5cef50ff2c884da`, the value the engine itself pins and asserts):

```
Harley Barker,Geelong,MID,19,ND,2025,0,0,never,1.58,0.707,never,677,Y,
Blake Thredgold,North Melbourne,KPD,19,ND,2025,0,0,never,1.58,0.707,never,373,Y,
Maxwell King,Sydney,SF,19,ND,2025,0,0,never,1.58,0.707,never,156,Y,
Liam Hetherton,Sydney,KPF,19,PDA,2025,0,0,never,1.58,0.707,never,168,Y,
Ollie Murphy,Fremantle,KPD,21,ND,2023,0,0,never,3.58,0.308,never,168,Y,
Noah Chamberlain,Sydney,SF,19,PDA,2025,0,0,never,1.58,0.707,never,86,Y,
```

**`Maxwell King` (Sydney) resolves to the engine key `max-king-syd`, not `max-king`.** That is the
membership-join artifact the D7 seat caught and disclosed (`PACKET_D7.md` §4.2); membership here is
read off the **engine's own** resolution, never off a normalised sheet name.

**EVERY ONE OF THE SIX MOVES UP. There is no downward mover.** That is a property of the encoding,
not a promise: `RL_O43` is a per-row `max`, so it can only raise a row.

### 3a · THE TWO ROWS THAT STOP MOVING — WHY THE COUNT IS SIX AND NOT EIGHT

`sam-allen` and `kobe-mcdonald` moved **DOWN** against the frozen prints on the superseded board
`daa16812` (450→428, 40→37). On `a05fe951` they **do not move at all**:

```
  D7-F3  sam-allen        frozen  450 | base 428 | PRICED 450 | RESTORES
         kobe-mcdonald    frozen   40 | base 37  | PRICED  40 | RESTORES
```

The parity guard restores them **exactly** to their frozen values. So the amendment is not "three
rows became six": it is **two downward movers removed and five upward movers added**, all six inside
the sheet-annotated set. `ollie-murphy` is the one row on both lists, and his +4 is unchanged — he is
a **riser**, the guard leaves him alone (`D7-F4`: base 200, priced 200, untouched).

**These are named to illustrate the mechanism. They do not gate anything.** The gate is the
assertion block in §5, which is keyed on counts and on the sheet, not on any person.

---

## 4 · THE MECHANISM — TWO LAYERS MOVE THE **FADE**, NEITHER MOVES THE ENTRY VALUE

**A day-0 price for a man who has never played is `round(day0_v0(p) × D(c_u))` — an entry value
multiplied by the sitting fade.** Two objects, and only one of them moves.

**`RL_O42=1`** re-keys the availability layer onto the owner's annotation sheet, re-basing the
availability haircut from `1 − g/22` to `1 − g/18`. `_AVAIL_STATE` feeds `_fe_p_one` / `_fEy`, and
`_fEy` is an input to the UNPLAYED CLOCK `c_u` (`o31_cu`), the single argument of the sitter fade
`o31_D`. So `c_u` moves, `D(c_u)` moves, and the printed integer moves with it.

**`RL_O43=1`**, the parity guard, then applies `max(v_injury_regime, v_healthy_counterpart)` per
treated row. For a zero-games row the one law collapses to `v = v0 × D(c_u)` exactly (`rho(0) = 0`,
`o32_age_credit(…, 0) = 0`), **so a max on the value is identically a max on the fade.** The D7 seat
wired the same `max`, the same dial gate, into the engine's own `_entry30b_price` predicate for
precisely this reason — in the predicate's own units, so no numéraire conversion can be got wrong
(`PACKET_D7.md` §5). Result on the priced board: **89 of 89 at tolerance 0.**

**The entry object does not move, and the guard itself proves it.** The ORDER 31-F guard has TWO legs
at tolerance 0:

- **(a)** `int(round(_landed_v0_board(q) × o31_D(q, BASE_REF)))` against `::printed` — **this is the
  leg that moves on the six rows.**
- **(b)** `abs(_landed_v0_board(q) − ::derived_v0) == 0.0` — **this leg is expected to pass 89 of
  89**, and assertion **A2** below halts the regeneration if it does not.

**CONSEQUENCE, AND IT IS THE ONE THAT MATTERS: the walk-forward matrix's year-0 column does not move
at all.** The regeneration touches the *printed* day-0 column of the reference and nothing else.

### 4a · THE `PACKET_FINAL.md` §4a CORRECTION IS CARRIED FORWARD, NOT DROPPED

`REBASE_DAY0.md` §4a corrected `PACKET_FINAL.md` §4a against that seat's own convenience: the three
"base" `derived_v0` numbers printed there (`833.3 → 791.8` etc.) **are not measured anywhere in that
evidence set** — they appear only in a docstring. That correction stands and is restated here so it
does not die with the superseded file. **`derived_v0` is bit-identical across `f3101883`,
`ff936186`, `daa16812` and `a05fe951`. What moves is the sitter fade `D(c_u)`.**

---

## 5 · THE ASSERTION PLAN — WHAT `cprb_day0.py` WILL REFUSE TO WRITE THROUGH

`cprb_day0.py` is `fcrb_day0.py` carried, with four declared changes and no others: **the board tag**,
**the dial line** (`RL_O43=1` added), **the output name**, and **the assertion block re-keyed to the
six rows plus a new direction assertion A8**. Every assertion is HARD — it raises and writes no
reference — and every one is PRINTED into `REBASE_DAY0_AMENDED_out.txt` whether it passes or fails.

| | assertion | HALTS ON |
|---|---|---|
| **A1** | same 89-key set as `DAY0_K.json` | the wired-entrant population changing |
| **A2** | `derived_v0` **bit-identical on 89 of 89** | **any** movement in the matrix year-0 column |
| **A3** | **exactly six** rows move on `printed`, and they are exactly `harley-barker`, `blake-thredgold`, `max-king-syd`, `liam-hetherton`, `ollie-murphy`, `noah-chamberlain` | **a seventh mover, OR a named row that fails to move** |
| **A4** | each mover's old and new printed integers equal the values the D7 parity harness itself published (481→504 · 372→381 · 129→138 · 66→70 · 196→200 · 37→40) | any disagreement with the D7 raw output |
| **A5** | **every moved row is `injured=Y`** on the pinned sheet `b26798c3…` | **a mover that is not sheet-annotated** |
| **A6** | the **83** non-movers are byte-identical on **every** field — `ty pos pick cell printed derived_v0 fade_D day0_price` — not merely on `printed` | a non-mover moving on any field |
| **A7** | the printed-day-0 identity holds **89 of 89 at tolerance 0 on the WRITTEN board** `a05fe951` | the identity failing on the board itself |
| **A8** | **NEW — every moved row moves UP** (`printed_new > printed_old`, strictly) | **any downward mover.** `RL_O43` is a `max`; a fall would mean the dial is not what it claims |
| **A9** | **NEW —** `sam-allen` and `kobe-mcdonald` are **byte-identical** to the frozen reference (450 / 40) | the parity restoration failing to hold on the day-0 print |

**83 of 89 BYTE-IDENTICAL, six moving, all annotated, all up** — the headline the owner's order names,
asserted rather than asserted-away.

Only **10 of the 89** wired entrants are annotated on the sheet at all, so A5 has real teeth: it is a
subset test against a set of 10, not against the whole board.

**After the reference is written, the emit is re-run against it and must read 89 of 89.** If it reads
anything else, that is reported as a failure and no matrix is used.

---

## 6 · WHAT THIS DISCLOSURE DOES NOT CLAIM

- **It does not claim the candidate is adopted.** **Priced, not adopted.** The tag and the `main`
  promote are OWNER-ONLY acts.
- **It does not claim Guard 5 is green.** Guard 5 is **RED** on this branch, pre-existing (register
  v767 item **C3**, the stale six-pin `rl_model` staleness). It is **not re-pinned** here and it is
  **not scored as a pass** anywhere. Its output is recorded as a named red.
- **It does not claim the class mark or the no-arb numbers before they are measured.** They are
  produced after this, on the regenerated reference, and reported at whatever they read.
- **It does not claim the regeneration is small because small is better.** Six of 89 is what the D7
  harness measured. If a seventh row moves, this pass HALTS and says seven.
- **It does not claim `D7-F8` did not fire.** It **FIRED**. The prediction of "only `ollie-murphy`"
  was wrong and is carried as wrong.
- **U0 = 7 return games is carried as OWNER-RULED, DATA-SUPPORTED**, both halves, wherever quoted.
- **Depths are quoted as depths (clock values)**, never as "Nth year" prose.

---

## 7 · FILES

| file | what |
|---|---|
| `REBASE_DAY0_AMENDED.md` | this disclosure — **pushed before the reference is touched** |
| `REBASE_DAY0.md` | the **superseded** three-row disclosure at `d5c37da` — kept, not deleted |
| `cprb_day0.py` | the regenerator: `fcrb_day0.py` carried, plus the re-keyed assertion block |
| `REBASE_DAY0_AMENDED_out.txt` | the raw run, all nine assertions printed pass or fail |
| `DAY0_CP.json` | the regenerated reference, board `a05fe951` — written **only** if every assertion passes |
| `docs/evidence/order_k_2026-08-18/DAY0_K.json` | the ORDER K reference — untouched, filed history |

**Priced, not adopted.**

---

## 8 · APPENDIX — ADDED AFTER THE RUN: TWO INSTRUMENT DEFECTS THE FIRST RUN EXPOSED

**This section is appended after `cprb_day0.py` was run. §§0–7 above are as pushed at `355152c`,
before the reference was touched, and are not edited.** The first run of `cprb_day0.py` **HALTED with
three assertion failures**. Both root causes were in the **carried instrument**, not in the board, and
both are recorded here rather than quietly corrected.

### 8.1 · The carried generator pre-dated the D7 second wiring site — A7 and A6

`ok_day0.py` → `fcrb_day0.py` forms the day-0 price as `d0 × o31_D(p, Y)` — the **LIVE (injury)**
fade. Under `RL_O43` the engine's own `_entry30b_price` no longer does that: it multiplies by the
fade ratio `_dh/_dl` out of `_D7_DFADE`, so the price is formed on the **healthy** fade wherever the
healthy counterpart wins (`PACKET_D7.md` §5, the second wiring site). **A generator that keeps the old
formula measures a different law from the one the board was written with.**

First run, with the stale formula:

```
  A7  printed-day-0 identity on the WRITTEN board a05fe951 : 82 of 89 at tolerance 0
      MISMATCHES: harley-barker, blake-thredgold, sam-allen, max-king-syd,
                  noah-chamberlain, liam-hetherton, kobe-mcdonald
  A6  non-movers : 81 of 83 byte-identical  (sam-allen, kobe-mcdonald moved on fade_D / day0_price)
```

Those seven are exactly the rows where the healthy fade won — the six movers **less** `ollie-murphy`
(a riser, whose injury-regime fade is already the max, so no ratio applies) **plus** `sam-allen` and
`kobe-mcdonald`. **The board was self-consistent the whole time**; the engine's own in-board assert
read **89 of 89** on this same board. The fix reads the engine's own ratio, character for character
as the engine applies it — **the assertion is not relaxed and the engine is not touched.** A7 then
reads **89 of 89**, agreeing with the board's own assert.

**A6 then reads 83 of 83 byte-identical on EVERY field**, `fade_D` and `day0_price` included: under
the guard the healthy fade for `sam-allen` and `kobe-mcdonald` reproduces ORDER K's fade **exactly**,
not merely to the rounded integer. The assertion as disclosed in §5 holds in full.

### 8.2 · The membership join — the artifact `PACKET_D7.md` §4.2 already named

A5 tested annotation by **re-normalising the sheet's `player` column**. That mis-maps the three rows
the D7 seat already caught: `Maxwell King` (Sydney) → naive `maxwell-king` → **engine key
`max-king-syd`**. So A5 fired on `max-king-syd` — **a row that IS annotated `injured=Y`, sheet line
126.** The row was never unannotated; **the join was wrong.**

The fix reads the annotated set out of the engine's own `_AVAIL_STATE`, which the ORDER 42 builder
constructs by matching the normalised sheet name against **both** the record's `key` and its `player`
field, and which it **asserts** is a 37 → 37 distinct-single-record correspondence (halting on a miss,
a duplicate or an ambiguity). **This is strictly stronger than the naive test, not weaker.**

### 8.3 · A corrected count, against this seat's own convenience

§5 above says *"Only 10 of the 89 wired entrants are annotated on the sheet at all."* That number was
carried from the superseded `REBASE_DAY0.md` and it is **wrong** — it is the naive join's count.
**The engine resolves 11 of the 89 wired entrants as annotated**, the eleventh being `max-king-syd`:

```
  A5  annotated wired entrants: blake-thredgold, harley-barker, kobe-mcdonald, liam-hetherton,
      max-king-syd, nathan-wardius, noah-chamberlain, ollie-murphy, ricky-mentha, rob-monahan,
      sam-allen
      every moved row is annotated injured=Y. 5 annotated wired entrants did NOT move:
      kobe-mcdonald, nathan-wardius, ricky-mentha, rob-monahan, sam-allen
```

A5 is therefore a subset test against a set of **11**, not 10. **It still has real teeth**: 5 of the
11 annotated wired entrants do **not** move, so annotation alone does not carry a row through.

### 8.4 · What did NOT change

**None of the order's four halt conditions fired, on either run.** No seventh row moved; no named row
failed to move; no mover was unannotated once the join was read off the engine; nothing moved down.
`A3`, `A4`, `A8` and `A9` passed **on the first run too**, before either fix — the six-row finding
never depended on the two defects. `DAY0_CP.json` was written only after all nine assertions passed.

Both fixes are **SCRIPT changes to an evidence instrument**, of the same class as the `RL_O43`
pass-through disclosed in §2a. **No engine file, no emitter file, and no guard was modified.**
