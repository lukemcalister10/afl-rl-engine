# SEAM HANDOVER — session of 2026-08-05 · for the incoming seam · read AFTER the charter/primer/CURRENT_STATE order

**Who writes this.** The seam seat of 2026-08-05 (read current to register v571 / CURRENT_STATE v62), rotating at ~95% context. This file is the session-delta on top of CURRENT_STATE v62 — everything that moved today, both tracks, with every ruling's durable id. The register pen was NOT held this session (the owner never re-provided the token), so the register lines owed below await the successor + token. CURRENT_STATE Part B is due a wholesale v63 rewrite at the next pen; §5 below is the proposed content.

## 1 · TRACK A — the #306 convergence loop, live state

- **Payload chain (all FULL-md5 verified by this seam's own re-derivations on committed bytes):** installed `e69a3f38` → derived `9f7848f4` (pass 0) → derived `b61c01b0` (pass 1) → derived `3f5875b5...` full `3f5875b57be8e59066993af834f98866` (pass 2). No repeats — no proven cycle.
- **Trend, quantities named:** ladder totals 54,722 → 53,678 → 53,511 → 53,526 (stopped moving); pooled heads 3068.46 → 3010.12 → 3064.37 → 3014.14 (~±50 swing between two neighbourhoods); pick 64: 221 → 189 → 185 → 185. Reads as integer-level flutter around a converged curve — recorded, never interpreted; R-I decides.
- **PASS 3 AUTHORISED (comment 5187760033) — final pass of bound 4.** Install `3f5875b5…` via the L1(b) set; outcomes: equal = CONVERGED (N19 exit) · equal-to-prior = named repeat (cycle proven) · else = BOUND EXHAUSTED → HALT-and-report with the four-pass table + per-pick amplitude map. **If HALT: the closure ruling is the OWNER'S** — the prepared shape (owner pre-briefed in-channel): "a pass moving no pick by more than N points is converged; take the midpoint/last ladder." Expect this branch.
- **Seats:** active = `u8ir65` on `claude/exec-306-pass-2-u8ir65` (tip `a8add22` at writing; read-back audited 5187605206; tree posture ratified 5187760033 — 19 modified files are the substrate held uncommitted under R-C, ~220 restored files are the endorsed base reconstruction, not re-authored). Stood down honourably: `2a1xa4` (`489587b`, hand-off + 9 captures) and `zlaarm` (`472c39d`) before it.
- **Seam rulings this session, by durable id:** 5186820918 (pass-0 audit + separating-run-first re-sequencing + pass-1 auth) · 5187094806 (separating run + belief-mass + pass-1 audited; owed items ordered; rotation honoured; pass-2 pre-auth) · 5187347144 (owed items audited; stand-down accepted) · 5187605206 (u8ir65 read-back cleared; 39-file base-composition finding endorsed) · 5187760033 (pass-2 audit; pass-3 auth).
- **Key measured facts now on the record:** the engine contributes ZERO to teaching-value movement (separating matrix byte-identical to July's, `9c4bca53`, seam-re-hashed) · wholesale-belief mass = 6.18% of teaching value (the −0.63% move in evidence-backed rows is a FLOOR, not the share — caption travels with every use) · channel width 55.78%/44.22% (narrow story never told).
- **ENVIRONMENT HAZARD, live:** execution-seat chats repeatedly trip a model safety false-positive ("sensitive biology") when they echo repo jargon (surgical/twin/excision metaphors). Mitigations that work: silent-mode output hygiene (no file contents/docstrings echoed; replies < 5 lines), model-switch to resume. **Safety valve:** all deciding derivations reproduce on the seam's own box from committed bytes (proven at passes 0/1/2) — machinery: `panel279/` (#279 panel from `9914c4d`) + the pass's `harness_pvc_REPINNED.*.py` as `harness_pvc.py` in a lane dir + `pooled_numeraire.py MATRIX PANEL OUTDIR`; payload = md5 of json.dumps({str(pick): int(round(v))}, sort_keys=True).
- **This box's N35 class (re-verify on any restart):** OFF-CLASS on the old fit lane (produced `969dba06` ≠ `fb9efdec` — third distinct off-class byte pattern, same Xeon@2.80GHz stepping-7 label as fit-class boxes); the REDESIGNED lane reproduces `b540833b` byte-identically here — the first divergent-host confirmation of L-B/L-C. Produce no old-lane fit figures on this box.
- **After the loop closes/halts+rules:** L7–L8 build-out + full rehearsal hand-back → owner's EXECUTION word → the landing (curve + surface + N43 signed pool levels + the #323 store batch + SCAR-text hygiene) → the owner's review set (per-stage attribution · honest backtest book · year-0–7 no-arbitrage table, order 5186108632) → owner satisfaction → round-21 ingest + movers page.

## 2 · TRACK B — the recipe audit (#322): COMPLETE, delivery on `claude/afl-valuation-coordinator-4k9vy5`

Everything lives in `docs/recipe/` on that branch (deliberately not merged; merge is record-keeping, owner's word when wanted):
- `RECIPE.md` — 122 numbered brandless rules (store 51 · prediction 33 · valuation 38), extraction + two independent adversarial verification passes + seam rulings applied; ~117 lines survived as plain rules — **the owner's failure test PASSES with named exceptions.**
- `FINDINGS.md` — the exceptions, one classification each (headliners: the self-teaching loop; the two frozen tree ensembles incl. a silent cold-cache retrain; the currency-anchor chain; the correction-ladder order; store traps).
- `CONSTANTS.json` (39 groups, adversarially verified, corrections applied) + `CONSTANTS_SOURCES.md` + `verify_constants.md` (appendix-side).
- `VERIFICATION_APPENDIX.md` — citations + full verdict trail. NEVER given to a rebuilder.
- `HANDOVER_MANIFEST.md` — the ingredient file list. **Owner-sequenced: the package cuts ONCE, from landed main, after Track A lands.** Checksums refresh then; the manifest's curve note already says so.
- `handover_store/` — **the #323 acceptance fixture**: corrected store copy, 2,650 records, md5 `81c8c58f…`, every owner ruling applied and verified (`CHANGES.md` accounts for all 730 changed records). The LIVE store stays `81d24704` until the landing.

**Open flecks, all small:** Rockliff/Bell 2021 season *averages* unverified over their corrected game counts (owner source, two numbers) · the frozen-models ship-or-withhold decision (manifest recommends withhold; owner leaning agreed but not sealed) · footywire is CAPTCHA-walled to agents (AFL Tables substitutes for games data, carries no fantasy scores).

## 3 · #323 — the store-corrections batch (fires at the landing)

Owner-worded and seam-verified, addenda 1–3: career games = derived sum of season rows, **home-and-away only, finals excluded** (uniform, no exceptions) · Leigh Brown removed + 2008 ND slide · Mzungu re-keyed ND 2010 pick 78, Mohr ND 2011 pick 81 (rookie entries removed, classes slid; `type` field must move with the re-key — engine classifies on `type`) · blank-equals-zero standing rule (explicit 0-game rows; 152 added for 2026) · Bobby Hill 2019 restored + 2023 avg 57.1 · Crameri ×6 + 2016 zero row · Podsiadly ×2 · Post/Keilty/Tohill backfills · Rockliff 2021=2g, Bell 2021=11g, De Koning debut row 2019→2018 · birth years Hickey 1991, Post 1989, Tohill 1999 · last-national-pick table delete-or-derive · SCAR-era text hygiene (dead 0.85 fallback + false comment + ~10 stale pins; behaviour is γ=1.0 VOR everywhere, verified).

## 4 · STANDING LAWS THE OWNER RESTATED THIS SESSION (binding)

Plain English, short sentences, direct answers FIRST, every number names its quantity, ~one screen. **Subagents on OPUS ONLY** — this session burned ~1.7M Fable tokens on inherited-model subagents before the owner caught it; never repeat. Don't bury answers mid-turn before tool calls (text there may not render — it happened and the owner lost a full answer set). The owner's casual questions are load-bearing QC — three of today's findings came from them (finals convention, bust-table fossil, type-field classify).

## 5 · PROPOSED CURRENT_STATE v63 PART B (for the token-holding pen, wholesale replacement)

THE ERA: Track A at the bound's edge — pass 3 authorised (5187760033), outcomes converged/repeat/HALT; if HALT the closure ruling is the owner's (pre-briefed). Track B COMPLETE and landing-sequenced (#322 delivered on the coordinator branch; #323 fixture verified; package cuts from landed main). The measured-facts trio (engine-zero, belief-mass 6.18% with floor caption, wide channel) carried on every number. Environment hazard: safety-flag false positives on exec seats — silent-mode hygiene + the seam's committed-bytes derivation valve. Seats: u8ir65 active; this seam rotated at ~95% context with the owner's word. OWNER ACTS OUTSTANDING: pen token to the incoming seam · the pass-3/HALT closure ruling if it comes · EXECUTION word after the hand-back · #323 fires at the landing · Rockliff/Bell 2021 avgs · frozen-models ship decision · review set then round-21.
