# ROADMAP — AFL SuperCoach RL valuation engine — v6 · 2026-07-06
### Full rewrite from BAKED v2.5. Supersedes v5.2 (which described a pre-bake future that has now happened). Companion to DECISIONS (live tracker) + HANDOVER rev111 (current state).

## STATUS (read first)
**v2.5 is BAKED and LIVE on `main`.** The consolidation is done: one source of truth, everything derived and stamped, both divergence halves sealed by five guards. The pipeline is now HONEST — the board, the book, and the engine agree, and a stale copy can no longer ship silently. That was the whole prerequisite. **From here the work is football, not plumbing.**

## THE CONCEPT (unchanged)
Price every player and draft pick as an expected-future-value asset under the growth law, in one SCAR currency. Now that the pipeline is honest, model quality is the frontier.

## WHERE v2.5 LANDED (the base you build on)
DPP stripped to single positions (present = future for all 805; a dormant `future_position` field is wired for a future transition model). Phantoms scrubbed. Kako's history fixed and guarded. The 34 present-position reassignments applied. Prices are now what the gated engine actually produces — no export inflation, no stale book.

## OWNER'S BINDING RULINGS (carry)
- **Overhaul before the weekly loop.** A per-round rating log is only meaningful if ratings move for football reasons, not model reasons. So: overhaul → engine stable → THEN weekly loop + cockpit start their history clean.
- **PVC start-year = 2004 draft** (verify 2005-season-complete + no-other-reason in the overhaul).

## THE SEQUENCE

### PHASE 0 — FINISH THE TIDY (nearly done)
Guard 5 landed on main ✓. Prune the dead build branches (list-first, protect lineage + the 14 roadmap branches). Refresh the doc pack (this + KICKOFF v8 + DECISIONS v70). Then the repo is clean and the pack is current.

### PHASE 1 — THE V0/PVC OVERHAUL (the big one; the real quality work)
This is where the parked evidence branches pay off. Opening evidence, now trustworthy because it can be RE-RUN on the honest board:
- **EVID1 growth-curve validation** (branch `growth-curve-validation`, 9,565-row walk-forward frame) — the age/decline curve vs realised. The engine's 5–6% over-optimism on players aged 19–22 is the highest-leverage target.
- **Compression/spread** (branch `compression-spread-quantification`) — re-measure on the honest board (the old read was export-contaminated).
- **The FABLE cold design review** (branch `engine-cold-design-review`) — 26-entry risk register + design critique, the map of what to fix.
- **PVC derivation spec** (branch `pvc-derivation-spec`) — the methodology, built to a decision register.
Flag cluster to work (re-confirm each on the honest board): aging-elite decline/retention curve, elite KPF separation, pedigree-floor ordering, ruck V0 derivation (the 1.4× cap is a stopgap), establishment-P (delete if inert). Ends in a re-bake via the owner's codespace.

### PHASE 2 — CALIBRATION / BEAT-BASELINE GATE
Forecast skill is currently ungated. Build C1/C2 so the overhaul's improvements are measured, not asserted.

### PHASE 3 — WEEKLY LOOP + COCKPIT
The HTML cockpit shell exists (branch `html-cockpit-viewer`). Wire it on the stable post-overhaul model; round-history starts clean.

### PHASE 4 — CHAPTER 3
Transition model build-out (populate the dormant `future_position` where present should differ from future). LTI greenfield (branch `lti-availability-audit`). Graded staleness dial. Distribution pricing.

## PARALLEL DATA-MODEL TIDY (not blocking; do when convenient)
- **Fold the 50 `_force_active` "on-an-AFL-list" flags into the store as DATA** (audit done: 63 flags = 53 load-bearing, 3 rule-fixable [Jakob Ryan/Blanck/Graham — recency rule too strict], 10 redundant, 0 wrongly-active). Then who's on the board stops being hand-maintained.
- **Close the Kako write-path loop** — where the owner's corrections get typed, so the guard has a clean front door.

## THE 14 ROADMAP BRANCHES = THE BACKLOG
Every KEEP branch from the prune audit holds unmerged work that feeds a phase above: growth-curve validation, compression/spread, FABLE review, cockpit, PVC spec, Berry attribution, owner ruling R5, LTI audit, flag-cluster diagnostics, Pickett trace, data-flow/dead-feature map, establishment-P/PVC checks, rl-model-data diff, consolidation-source-map. Pull each in as its phase comes up; don't let them rot.

## PATH (one line)
BAKED v2.5 (done) → finish tidy → **V0/PVC OVERHAUL** (re-bake) → calibration gate → weekly loop + cockpit → chapter 3. The plumbing is sealed; the model is the frontier.
