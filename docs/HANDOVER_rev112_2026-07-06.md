# SUPERVISOR HANDOVER — rev112 · supersedes rev111 · v2.5 LIVE ON main · REPO TIDIED · CLEAN ROTATION SEAM
### 2026-07-06. Read KICKOFF v8 (§0 = the single-source invariant) + POSTMORTEM + SINGLE_SOURCE_INVARIANT first, then this. This is a clean rotation point: v2.5 is the live board, both divergence halves are sealed, and the repo is pruned. No hard judgment pending.

## ONE LINE
v2.5 is BAKED and PROMOTED — `main` == the live board. Both the data-duplication AND stale-boot problems are sealed by five guards. Repo pruned to only meaningful branches. Remaining work is the V0/PVC overhaul (real football), on a now-honest board.

## CANONICAL STATE (verified)
- **`main` == `bbf64b4`** (v2.5 bake `e1d8d78` + Guard 5 `9dd8fec` + ruling `bbf64b4`). A default clone lands on the real board.
- Tags: `baked-v2.5-2026-07-05` @ `e1d8d78`; rollback anchor `prebake-v2.5-anchor` @ `4180f06` (both pushed).
- Store `e1b4d8bf` · engine `efea88e5` · board `7d1eeef8` · book seal `5799a9ce`. Byte-identical through all hardening.
- PR #35 open (not merged) as the v2.5 bake record. Merge/close at owner's discretion — main already carries v2.5.

## WHAT v2.5 CONTAINS
DPP stripped (0 blends, 0 live transitions; every player one position, present==future for all 805; dormant `future_position` field wired for a later transition model). Phantoms scrubbed (4 fake rows + MSD/Ireland lift + boosts; real McAndrew/Keane kept). Kako 2025 folded (23@55.2) — root cause: it only ever lived as a book-code patch, never in the store. Off-by-one games fixed. The 34 present-position reassignments applied (Petracca 3033->2414, Marshall ->431). pvc_snapshot pinned read-only. "forward-eligibility ~ $0" retired: true for the median dual, false for ~20 swingmen.

## DIVERGENCE — BOTH HALVES SEALED (live on main)
- **Data half (4 guards):** source-hash stamp on derived files, startup assertion, lookalike tripwire, correction-sticks canary.
- **Base half (Guard 5, `boot_guard.py` + `data/expected_boot.json`):** the four data guards validate a directory against itself, so a stale *workspace* passed them while the board was wrong (the gremlin that ate 2026-07-05). Guard 5 asserts the boot store matches the pinned manifest on ENTRY of every gate/bake/panel/self-test script; HALTs, never warns. Owner-approved as a pre-flight addition, not a frozen-gate change (verdicts byte-identical on the correct store; recorded inline in the repo per the A7 precedent).
- Full doctrine: `SINGLE_SOURCE_INVARIANT.md` (5 guards).

## REPO STATE (pruned 2026-07-06)
origin holds ONLY: `main` · lineage (`a7-amendment-frozen-gate`, `dpp-strip-final-consolidation`) · 2 rescued (`d17-board-walkforward-view` = walk-forward viewer tooling, `bake-v2-5-candidate` = A7 halt record) · `f1-f2-pipeline-divergence` (unique DO-NOT-MERGE evidence, guardrail-rescued) · the 14 roadmap branches. 25 dead branches deleted. (Cosmetic leftovers, harmless: the prune scratch branch, the stale `prebake-v2.4-anchor`.)

## FIRST JOBS FOR THE FRESH SEAT (in order)
1. **Commit the refreshed doc pack to the repo** — KICKOFF v8, ROADMAP v6, DECISIONS v70, updated SINGLE_SOURCE_INVARIANT (5 guards), this rev112. They currently exist as files from the outgoing session; land them in the repo so builds/seats read the current versions, not stale ones. (A small build commit, or from the owner's codespace.)
2. **Start the V0/PVC OVERHAUL** (ROADMAP v6 Phase 1) — the real quality work on the honest board. Begin with EVID1 (engine's 5-6% over-optimism on players aged 19-22, the top lever), re-run on the fixed board. The 14 roadmap branches feed this.

## DEFERRED (data-model + loose ends; not blocking)
- Fold the 50 "on-an-AFL-list" `_force_active` flags into the store as DATA (audit done: 63 flags = 53 load-bearing, 3 rule-fixable [Jakob Ryan/Blanck/Graham], 10 redundant, 0 wrongly-active). Then who's on the board stops being hand-maintained.
- Close the Kako write-path loop (where owner corrections get typed).
- Transition model build-out (populate the dormant `future_position`).
- Calibration/beat-baseline gate; weekly loop + cockpit; Chapter 3.

## HOW THIS SEAT OPERATES (hard-won this session)
- **Verify against real artifacts; navigate by branch NAME + git SHA, never a store md5.** Every win came from direct inspection + git assertions; every loss from trusting a base. (Confusing a store hash for a git ref cost real time — don't repeat it.)
- **Journals every turn + keep the handover current — write them AS you finish the turn.** The outgoing seat lapsed on both under load; under load is exactly when it matters. (The guards now enforce the data discipline; the journal discipline is still on the supervisor.)
- Physical-action-first, plain language, flag consequences before a directive fires, frozen-gate changes need written owner sign-off (A7 + Guard-5 precedent). Owner reads are ground truth. Don't over-produce; do produce the real deliverables.

## STATE / SAFETY
main = v2.5, both divergence halves sealed and live, repo pruned, rollback anchor + tags in place, PR #35 as record, doc pack refreshed (commit to repo = first job). Nothing half-baked, nothing in flight needing the outgoing chat's context. The hard judgment calls (A7 stale-instrument, the transition premise, the stale-boot diagnosis, main-vs-v2.5) are all resolved and recorded. Clean seam — safe to hand over.

## SEED ORDER (fresh chat)
KICKOFF v8 · **HANDOVER rev112 (this)** · POSTMORTEM · SINGLE_SOURCE_INVARIANT · DECISIONS v70 · ROADMAP v6 · OPERATING_CARD · LTI_REGISTER.
