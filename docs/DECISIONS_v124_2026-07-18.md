# DECISIONS — v124 · 2026-07-18 · supersedes v123
### v124 (item 368): the seat-13 chapter rulings fold (R108.x). The register (v340 head at this
### filing) carries every verbatim — read its OWN header.

## §0 — CARRIED FORWARD (BINDING)
R98.x–R107.x in force as at v123 §0. PLUS the seat-13 owner rulings:
- **R108.1 (items 345/346):** Option B — the chapter extended for the full forward-lens feature
  set (phantom intake + retrospective −1/−2) before the bake; consequences accepted.
- **R108.2 (items 351/359, "Rule it"):** THE CONSERVATION CONSTRUCTION — ~100 entrant slots at
  pick PVC (structure sealed from history) · exits never named: ONE population carrier
  (r_pop incl. exiters; §2.iii retired) · everyone stays at +k · totals ~conserve · MEMO_LEGF
  v1.0–v1.3 = the construction of record.
- **R108.3 (item 354/357-359):** the ±5% calibration bar NEVER widened; the TESTS corrected to
  measure the claim (roster-matched + league-level conservation gates); HALT-over-bend held
  throughout (twice to checkpoint, once to the owner).
- **R108.4 (item 342):** the v−R view is FIRST-CLASS at the viewing (the owner's replacement
  steer); v2.11 still bakes GROSS (R107.7 stands).
- **R108.5 (items 363/364):** the v−R BOARD TOGGLE, post-v2.11: R = ONE FLAT DERIVED scalar
  (rider-iv R_realized 207, re-derived at bake data; NOT positional; 220 was illustrative);
  rank-preserving re-valuation.
- **R108.6 (items 361/362):** the weekly tracking loop rides go-live (track/dRound feeds into
  the built card + Δ toggle; rank-Δ card element = explicit small UI add).

## §1 — OPEN OWNER READS (operative until HE rules; the natural point = THE VIEWING)
- **THE TAIL READ (325/332/343):** gross p1/p60 ≈5.5× in curve AND reality; his 7.5×+ lives in
  the replacement adjustment (at R_realized 207: curve ≈8.6×, realized ≈9.5×). Rule at the
  viewing with rider (iv) in front of him: STAND (⇒ the post-v2.11 entry-pricing chapter) or
  RETIRE (resolved-by-the-replacement-frame).

## §2 — THE VIEWING RATIFICATION LIST (his calls, all sealed/flagged, none presumed)
tail read · R=207 free-intake basis · entrant structure `a17aafed` · φ pedigree-carry
`fd92b6fc` · r_pop `c62b5ee8` · posture presets `c2e17c49` · the raw-pick benefit-of-doubt flag
(item 344; largely cured by F3 — shown with movers) · the knife-edge brittleness note (366).

## §3 — OWNER TO-DOs
**DO:** run the five audit shards (incognito, outside the Project; seed =
docs/AUDIT_SEED_15a9abd) and courier returns. **WHY:** the ladder's independent rung; everything
after waits on it. **DO (then):** the viewing + the ratification list + the sealed-reads scoring
+ the written bake word (it carries the guard-5 re-pin + the three INFRA_ALLOW entries). **DO
(at the bake seam):** sync PK (manifest v4.53 · HANDOVER rev160 · DECISIONS v124; CORE/SSI
unchanged) — optional now, mandatory at the seam. **DO (post-delivery, your word):** round-entry
GO-LIVE.
