# DIRECTIVE — RIDER (iv): THE REPLACEMENT-ADJUSTED VIEW · seat 13 · 2026-07-18
### Ruled R107.6 (adopted) · R107.7 BINDING (gross stays gross; R is a VIEW; production bars ≠
### list-space R) · owner steer item 342 ("consider worth over walking to the free pool more").
### REPORT-ONLY · read-only Tier-3 · runs PARALLEL to the Leg-E writer (S3; disjoint files).

## GIT ENTRY (read-only variant — the item-338 precision law)
`git fetch origin main claude/five-migration-pvc-consumers-kxykfd`
`git ls-remote https://github.com/lukemcalister10/afl-rl-engine.git claude/five-migration-pvc-consumers-kxykfd`
must return exactly `a90052add8570c014626196cc2e3e13eece02548` (HALT on mismatch). Branch parent =
MAIN (expected for read-only); **provenance = the stamps, asserted at load, HALT-on-mismatch:**
store `968de0c7` · curve payload `89c14729` · the R-inputs artifact read from the git object
`a90052a:session_2026-07-18/five_migration/out/rider_R_inputs.json`. Never edit engine files;
never join lineages; every repo claim cites a this-session fetch.

## EFFORT: High. Why not Medium: this is the artifact the owner's central open read (item
325/332/342) is ruled on — era-matching and censoring handling are where it silently goes wrong
(§5.3/§5.4 of the filed failure-mode list). Why not Extra: read-only, stamped, re-runnable; the
cold audit recomputes it. MODE: auto, PLAN first (method per job + derived fence). TIME: ~1–2 h;
flag >2×/<½×; report actual.

## FEED: DECISIONS v123 (R107.6/.7) · the filed §5 list (`docs/inputs/CLEANROOM_GPT_section5_…`)
· the riders-i–iii outputs @ `03cecdec` (PR #110; reuse machinery, S5) · the R-inputs report +
census @ their pinned objects · CONSTRAINTS v1.19 + acceptance JSON (context; nothing gates).
DELIBERATELY NOT FED: the owner's prior (220) as anything but a labelled reference constant; no
expectation language.

## THE JOB LIST (one commit each; census-grade stamps on every artifact)
1. **THREE R CANDIDATES, constructed and labelled:**
   (a) **R_curve** = the v2 curve evaluated at the measured free-intake pick-equivalents (90–92),
   WITH the circularity note ON the artifact: the v2 deep tail was partly anchored on pickless
   pk90 outcomes, so R_curve is partly self-referential.
   (b) **R_realized** = era-matched realized-outcome value at the same entry points: mechanism
   cohorts vs national cohorts of the SAME entry years; censoring handled by continuous evidence
   weighting (NO hard maturity cutoff — our law); busts full weight.
   (c) **R_owner = 220**, carried as a labelled reference line only (his stated prior, item 332).
2. **UNCERTAINTY ON R ITSELF:** cohort-bootstrap the mechanism samples (they are thin — MSD 44/17
   played, SSP 31/16; say so on the artifact); an honest grade per R candidate.
3. **THE VIEW:** per exact pick, smoothed, full board: GROSS beside v−R for each R candidate; the
   p1/p60 and p1/p90 ratio table per candidate; the deep-tail premium-over-free-pool curve
   (v−R past ~p50) with rider-(iii)'s uncertainty grade overlaid.
4. EXIT: README mapping artifacts→stamps · PR, REPORT-ONLY / DO-NOT-MERGE · RETURN ≤25 lines
   (branch · head SHA · PR # · actual vs band · per-candidate R values + the one most
   decision-relevant sentence each, findings not verdicts).

## THE LAW ON EVERY ARTIFACT
Finest supported resolution, smoothed, NEVER decile/wide bins (item 325). SYMMETRIC presentation:
three candidates, no verdict, no steer — the reading is the OWNER's at the ladder. The axes note
verbatim on the summary: "production bars and list-space R are orthogonal (R107.7); v2.11 bakes
GROSS either way; making v−R the traded currency is the named post-v2.11 chapter, on the owner's
word."

## FENCE: IN = `session_2026-07-18/rider_iv/` only. HARD-OUT: every engine file · the store · the
curve · `docs/`. HALT on any HARD-OUT need. S1–S6 · SILENCE IS A RED.
