· v882 (2026-08-29): **THE ORDER 49 AVAILABILITY EXPOSURE BLEND (TAU 2.5) IS LANDED — the Taylor/Taylor cliff is removed at the one law; and the S4 WALK-FORWARD PARALLELIZATION IS PROVEN AND ADOPTED.** Owner words 2026-08-28/29, verbatim at each site: "Please lock in tau 2.5 and build that", then "Please land tau" — ruled on the tabled Variant A of the availability-smoothing study after reviewing TAU 4 / TAU 3 / TAU 2.5 / cut-cap / 8-game-floor alternatives, with the study itself opened by his fairness probe ("Do we really think it's fair that Dylan Patterson as a pick 5 is ranked at 0.98x of his v0 ... while sitters like Oskar Taylor drop 0.81x from not playing"). THE FINDING BEHIND IT: the availability discount was a BOOLEAN of games>0 — a 2-game cameo fully shielded the draft prior while zero games took the sitter channel whole; the ruled repair is the exposure blend final = w·played + (1−w)·sitter, w = 1−exp(−games/2.5), the sitter leg being the row's OWN scoring-stripped counterfactual through the full wrapped chain (ORDER 45 net + ORDER 46 final guard), symmetric so thin evidence counts partially whichever way it points. THE LANDING: engine 17243c16 (flip commit 7b5efc2, RL_O49 default '0'→'1' with the engine_head restamp riding it), config f233d160 → d4f3c3cf (90 vars), board 530a4053 → 4a52cc44, one flight of tools/land lever (c9271f00) — preflight 13/13 CLEAN, the pre-transaction self-test's full abort ladder proven, ON arm PREDICTED BOARD MET (4a52cc44 == prereg, 594s), kill-switch arm byte-exact to the retired 530a4053, sibling reconciled, movers column order49-avail-blend-28-8 (MC-15) written, register/state/contract re-stamped, 2,393s total. THE MOVERS ARE THE APPROVED TABLE: 78 direct movers all within the prereg's ±1 rounding bar, 0 extras, 0 off-by->1, net −1,467 vs the tabled −1,465 (toby-murray's tabled −1 rounds to 0 — the study rounds each leg to board currency then blends, the engine blends then rounds once); named anchors xavier-taylor 894→873, oskar-taylor 624 unmoved, lachy-dovaston 680→655, max-kondogiannis 414→411, dylan-patterson 1125→1134, daniel-annable 1260→1286. THE S4 PROOF: sequential and 4-worker parallel stable-key shas byte-identical (619fe67f…, 2650 rows), overlap agreement clean — bar RESTATED from the old book seal (which binds the pre-O49 world) to sequential-vs-parallel in the current world; book RE-SEALED on that double-derived stamp, closing both SEALED-LAG notes, manifest coherence fully PASS. INCIDENT, THE ROOT CAUSE OF A LOST DAY: the first proof attempts burned ~10 hours without finishing one arm; measured on the live pickle, a single GBM predict cost **145 ms under OpenMP oversubscription vs 8.7 ms with OMP_NUM_THREADS=1 — 17×** — every multi-process engine run had been spinning ~10 threads per process on 4 cores. The supervisor had sampled stacks and called it "healthy but slow" instead of timing one call; the launchers now pin the threads and the class is: measure a per-call latency before narrating a slowness. SECOND CLASS, five instances: the flip commit's identity restamps were incomplete and each was found BY RUNNING THE GATES, not by reading the diff — engine_head pin, release_contract (identities + self-hash), the board-view stamp, the ingest projection, and the sibling-repin-state contract binding (this last one discovered by the lander's own SELFTEST, whose late fault arms were each burning a doomed 20-minute reconcile against a stale seal). B5 re-instrumented for the O49 world on the O46 precedent: in-class blend rows report on their own line ("ORDER 49 blend rows=N") and the purity bar keeps measuring the floor's own scope at 0. Ship-gates on the landed board: B4 byte-agree, B2 0.000, shrink strikes (A9/A15/B1) and B3 bake-scoping live, A2/A3/A12 the carried ruled reds. Elsewhere: the UI de-bloat redesign landed on his critique (one transparent tier, process off every user surface, ui/MAINTAINER.md as its home, Round Δ the one movement column, club picks restored, 16 suites restated green); the movers scope lifted to default at the latest round on his word; three pick-owner corrections applied in the workbook's own bytes. QUEUED: the walk-forward retrospective (R14–R24 re-priced under the live engine, his ask of 2026-08-29) — driver + emitter + UI wiring + tests committed, pricing restructured onto ONE engine load after eleven per-round loads could not outrun the container-reclaim cadence; S6 pin-collapse; S13 slice 2.

incident: yes
