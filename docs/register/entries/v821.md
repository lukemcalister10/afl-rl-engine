· v821 (2026-08-21): **PACKAGE 3c IS LANDED AND VERIFIED — the live-state document is machine-generated, and CURRENT_STATE.md is retired.** docs/STATE.md is written by tools/landing/state.py as a step in BOTH landing sequences (lever 11 steps, round 15) and by a standalone entry point; content all COMPUTED (identities re-hashed beside their pins, rulebook lint verdict, register LATEST line quoted, lineage tip, generating-commit stamp, deliberately no clock); freshness gate registered as runner check state_file (0.109s, negative controls fired both ways); lander self-test now 29/29 with the new step broken/caught/aborted byte-exact. docs/CURRENT_STATE.md — the hand-maintained predecessor that once sat 156 versions stale — is a TOMBSTONE at its path (G4 retirement of record); its load-bearing content REHOMED BY NAME: the governing test + the owner's product laws (verbatim) + the sixteen hazard classes (numbering preserved) -> docs/ENGINE_PRIMER.md section 0; standing norms/roles/housing/budget/environment-carries -> docs/runbooks/STANDING_PROCEDURES.md (new); Part B not rehomed (a v642 snapshot; the register holds the rulings). Eight primer pointers repointed; the seam charter amended in its own convention (the pen's replace-Part-B-wholesale duty ABOLISHED). ALSO IN THE ACT: orient.sh's dead pack-doc section RETIRED with a dated note (exit 1 -> 0, sample regenerated; live governing headers print instead — RULEBOOK/SSI/PRIMER/STATE); playwright-core DECLARED and installed (ui/tests/package.json), both Chromium suites RUN AGAIN: responsive_layout 71/72 (one 320px name-wrap red), ui_222_items stale-fixture reds then a strict-mode crash at :217 leaving its tail clusters uncovered — REPORTED, not fixed, both queued. CARRIED FROM THE SEAT: (1) the governing test's RULEBOOK home is OWED TO THE OWNER (law 10a — a seat signs nothing; it lives in the primer meanwhile); (2) responsive_layout WRITES INTO a retention-protected evidence tree when run (restored byte-exact here; the suite needs its output repointed); (3) the ruled-but-not-live ledger (5 items, v642) is 178 versions unadjudicated. Supervisor verification: state check PASS, orient exit 0, runner 16/16 GREEN under the supervisor's own run; committer identities repaired by rebase before push (build@seam.local -> the configured identity; content untouched, verified by the same runner after the rewrite). Board b3e8da99 / engine 3af8c1f7 / store b745002e byte-unmoved throughout. Study B remains in flight; ownership refresh is the next lane.
incident: no
