· v828 (2026-08-21): **P4b THE ROLLBACK FIRE DRILL — PASS: TAGS ARE RESTORABLE BYTES (docs/evidence/p4b_fire_drill_2026-08-21/, evidence cherry-picked from the seat's isolated worktree at e423080).** Target baked-v2.11-2026-08-20 @ 463e53d (latest owner release; the annotated tag carries the owner word per law 10b; the expected_boot 'tag: v2.9' field is stale NARRATIVE from 2026-07-13, still printed by Guard 5 — noted). PRIMARY LEG: 13 pins declared, 13 verified byte-exact; the tag's own guards accept the restored world (boot_guard PASS, fv provenance PASS, ruling_config 13/13); git-archive to a self-certified tree in 1.93s, full re-certification ~7.5 min. SECONDARY LEG EXCEEDED ITS BAR: rl_export at the tag rebuilt the board BYTE-EXACT to a05fe951 three times (~85s each) — no reproducibility finding owed; the env pin held. THE DRILL'S REAL VALUE = FOUR MATERIAL FINDINGS about the naive rollback, none a byte failure: (F1) the tag's release_contract was never re-stamped at its landing — the tag's own verifier REJECTS the tag's own bytes while passing on today's head, and rl_export never calls contract verify; (F2) a repo restore is HALF a rollback — the shared workspace still carries the live engine and Guard 5 halts (correctly); the out-of-repo pickles matched by luck, not construction; (F3) `git checkout <tag> -- .` is a DELETION — the tag predates acceptance/, tools/landing/, docs/register/, sheet_pins; rollback must be scoped to the carrier enumeration; (F4) verify_restore.sh is BROKEN TODAY, identically at tag and head — import muzzled fail-closed, stale hand-typed numbers (a live P4 violator); (F5) engine/rl_after/s4_matrix.json does not satisfy its own seal AT EITHER END — keyed by CPython id(), non-byte-reproducible, silently drops rows (2,647 committed vs 2,650 generated); a freshly regenerated book seals GREEN — after any rollback the book is REGENERATED, never trusted as restored bytes; A LIVE EXAMPLE of the silent-deletion class P4c exists to catch; plus (F6) the live book seal one engine-move stale and (F7/F8) tag-era self-test reds mapped. EMERGENCY_ROLLBACK_RUNBOOK.md written (owner word + prereg first; verify-in-scratch before touching live; scoped carrier restore; bootstrap re-seed; contract re-stamp; gates) — WRITTEN NOT EXECUTED, its own unproven steps named. QUEUE FROM THE FINDINGS: verify_restore.sh repair-or-retire (F4, a P4 violator standing) · the s4_matrix id()-keying determinism defect (F5 — candidate rebake-week item, it touches the book's reproducibility story) · the contract-restamp-at-tag gap (F1 — the round/lever landers already restamp, so this is historical-tag hygiene + a tag-time check) · retire the stale expected_boot 'tag' narrative field at the next natural carrier edit. The retirement era's two prerequisites are now one: P4-zero (the declared kill_switches block) remains, queued for a fresh window.
incident: no
