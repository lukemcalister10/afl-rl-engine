· v883 (2026-08-29): **THE WALK-FORWARD RETROSPECTIVE IS LIVE — every round since R14 re-priced under the current engine and selectable in Movers; and the container-reclaim failure that had blocked it for eleven attempts was the supervisor's own launch pattern, not the infrastructure.** Owner ask 2026-08-29, verbatim: "What about the movers list having each round since round 14 as an option under the currently approved and live engine?", following his earlier "it would be beneficial if the movers list was round 23 to 24, under the current model ... what it should not be is round 22 to 23, different models."

WHAT SHIPPED: eleven points `retro-r14 … retro-r24` in `ui/data/movers.js`, each the live engine's answer for that round's football — the season clock set to the round's derived `calendar_progress` / `exposure_pace` from the immutable repo-root policy, every 2026 scoring row truncated to as-at-that-round, the whole 804-row active board priced through the engine's own `ev()` at `ev/_PL_F` with the display layer's owner overrides, and every mutated row restored and the restoration asserted (the D7-F6 discipline: a probe that corrupts the board it prices is worthless). THE CONTROL HOLDS: R24's truncation is a no-op, so `retro-r24` must reproduce the live board exactly, and it does — **0 diffs, 0 missing over 804 rows** — which validates the clock handling, the currency mapping and the display layer in one assertion.

INCIDENT, AND IT WAS MINE: ten prior attempts across eight hours banked zero rounds, and the supervisor had reported this to the owner as container reclaims landing every ~2–17 minutes against a ~12-minute engine load — infrastructure noise to be waited out, with an opportunistic retry gated on `uptime > 20 min` that never once opened. The gate never opened because it could not: every watchdog wake read an uptime of 1–4 minutes, which is not a container dying mid-job but a container that **is not running between turns at all**. The session goes idle when a turn ends, the container is reclaimed, and a `setsid nohup … &` process the harness knows nothing about goes with it. The supervisor's own launch pattern was killing the job every single time, and framing it as an environment fault delayed the fix by a day. THE CLASS: a long job must run as harness-tracked work inside an active turn (background task + armed monitor), never detached across a turn boundary; and a repeated "environment" failure with a perfectly regular signature is a hypothesis to test, not a condition to wait out. Relaunched that way the series completed in one pass — 12 min load, ~130 s per round, 2,006 s total — on the ONE-LOAD harness the owner's standing "load things once" directive had already called for.

THREE EXCLUSIONS THE GATES FOUND, each a live defect without it. A retro point is a re-pricing, not a board this application ever served, and three places read "the newest / every point" as the record: (1) `core.lineage`'s ONE ASSERT — `retro-r24`'s in-process pricing became "the newest stored point", never equalled the served board, and **fail-closed the entire Movers tab** the moment the series was emitted; (2) `round_finalize._newest_point_vs_live_board`, the same assert on the producer side, which would have halted the NEXT round's finalisation; (3) the player-card history, where eleven re-pricings interleaved into every card would have doubled the table with movement that never happened. All three now exclude `kind === "retro"`. The evidence the semantics are right rather than the tests bent to fit: the assertions that failed on the naive emission went green on the exclusion itself, with no test restated.

THE DEFAULT PAIR MOVED, and this is the owner's "round 23 to 24, under the current model" delivered literally. The old default was the newest stored round report's own pair, which had two faults the retrospective exposes: a stored report is priced under the model that was live that week — R24's was finalised before ORDER 45/46/47/48 and before the ORDER 49 blend, so it is a superseded model's answer — and its `previous_round` is the last out-of-round column, so the tab opened on `MODEL CHANGE (MC-11) → ROUND 24`. It now opens on `retro-r23 → retro-r24`: one model on both ends, one round of football between them, terminating on a re-pricing that IS the live board. PARTICIPATION IS NOT LOST TO THE SYNTHETIC PATH: a one-round retro pair is that round, so played / DNP / score are read from that round's own stored report — whether a man played and what he scored does not depend on which engine priced him — and the chips, the scores and both filters survive (played 409 / DNP 395). Every other range keeps the honest "not recorded", asserted in both directions so the enrichment cannot silently widen.

ALSO CORRECTED: a retro point in the meta strip read "R24 · post-model", which means the opposite of what a retro point is; and MC-15 (the ORDER 49 blend) was present in the bundle but missing from the maintainer ledger, a gap the TAU landing left. All fifteen UI suites green. Evidence at `docs/evidence/walkforward_retro_2026-08-29/` (pass, emitter, eleven `values_rN.json`, `RETRO_ONELOAD_VERDICT.json`); the retro-point contract, the control and the three exclusions are written up in `ui/MAINTAINER.md`. STILL QUEUED: S6 pin-collapse, S13 slice 2, and the shrink-completion entry.

incident: yes
