· v843 (2026-08-24): **SEAM MODEL V2 IS MEASURED AND THE CONDITIONAL-MODEL ROAD IS CLOSED — the engine passes its seam audit a second time, 0-60; the binding constraint is SAMPLE SIZE, proven, not construction; THE SEAM ORDERING FLOOR is what remains and goes to the owner's word.** THE V2 BUILD (owner's direction "is there a way to iron out the imperfections to make that viable?"; prereg'd, scratch-only, screened): all three declared repairs delivered — monotone-boosted construction on the full state incl. AGE (0 violations of 41,400 pick + 86,400 cameo segments; sitters coded by NaN-sentinel so the disputed ordering was not imposed), direct value-target fitting (kills the Psi seam — and made things WORSE: the zero-spike relocated into the leaves, 11 hard zeros on live rows; the Psi arm with its declared defect is the better route), and a genuine row-level engine incumbent (ev(as_of(p,Y),Y), validated inert on live rows and exact on all 48 live sitters). VERDICT under the pre-registered rule: **MODEL LOSES — 0-60 splits raw, +45.6 MAE [+31.0,+60.2] vs the engine; the level-Psi arm 4-56; every hyperparameter setting 0/20; the engine also wins ordering (Spearman 0.479 vs 0.416)**. THE DIAGNOSIS, definitive: the STACK diagnostic handed the learner THE ENGINE'S OWN PRICE as a feature and it still lost 0-60 (no measured addition); D1 showed the learner cannot even RE-EXPRESS a predictor it is given (+18.6 MAE noise floor) — at 596 players against a zero-spiked target with sd 1132, LEARNER VARIANCE dominates; no feature list repairs that; further engineering on this population is the wrong axis. WHAT SURVIVES, narrow and real: (1) the t3 ORDERING INVERSION reproduced at row level on one surface (engine prices t3 sitters at mean 146 vs realized 37, ABOVE t3 played at richer realized value) — the engine wins MAE while ordering the seam wrong; (2) the live-board floor set re-confirmed independently (the same 6 rows; the v2 surface would lift 3 of 6 unaided but is not shipping); (3) LEAKE'S ABSOLUTE PRICE now spans 154/229/566 across three constructions — unresolvable, thrice; (4) one resolvable subset (t4: model beats engine 51-9 — read honestly, both over-price late-tenure, the engine by 5x, the model by 4x; the whole late-tenure surface including the engine's is over-priced, calibration 4.15x at t4); (5) engine facts found: ev() is not fully as-of (33 of 338 probed states move if post-Y seasons stay on the row) and _O41_PRED8/_PE_CACHE are id()-keyed (copied rows can read a recycled id's stale value — the book's id()-class again, in the pricing cache). ALSO: 60-split census repricing DISAGREES with v1 in aggregate sign (+964 vs −233; median CI width 1.72x the row's own price — capacity bought variance) — further proof no point-repricing of this class can carry a ruling. THE REMEDY THAT REMAINS: **THE SEAM ORDERING FLOOR** (cameo avg >= threshold → price never below the row's own never-played counterfactual, computed by the engine itself with scoring stripped, validated exact against all 48 live sitters): binds on 6 rows +649 points at threshold 45 (goad at 43.5/−235 is the one nearby row a threshold of 40 would add); zero fitted constants; law-3's counterfactual logic across the seam; weak-late rows (barnett-class) untouched, per the outcome data and the owner's instinct. ON THE OWNER'S DESK: the floor — adopt as a design-arm lever (prereg, self-test, movers) yes/no, and the threshold (45 or 40). Word B (fit-both) already ruled and instructed to the design arm; arm-2 six-row reconciliation still in flight.

incident: no
