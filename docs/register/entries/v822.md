· v822 (2026-08-21): **THE OWNERSHIP MIRROR IS CURRENT AND HAS A WRITER OF RECORD — and its seat caught the supervisor's own gate red.** THE DIAGNOSIS, better than "stale data": the mirror's 804 rows were never wrong — only its STAMP was (generated at a05fe951/cc02567f/R22), and because the UI refuses an unauthenticated mirror, the live ownership lane had been SILENTLY OFF since the R23 advance (all 13 sidecar reds were the fallback path, not wrong clubs). Root cause = the missing-writer class, fifth member: the mirror's only writer was the owner-move transaction, so a round advance moved the store and nothing regenerated it, and it was not a declared carrier. CLOSED THE SAME WAY AS THE OTHER FOUR: regenerated THROUGH ui/tools/ingest_inputs.py (--mirror-only fence: club_valuation discarded, the store apply never runs; deterministic, twice byte-identical, md5 8e9bc43d); declared carrier; WRITER 5 of the shared ui step, both sequences inherit, drift guard + the reader's own pin predicate asserted in-step; self-test 30/30 (19 steps broken/caught/aborted byte-exact, +1 case where the writer is silenced AND the pin falsified). Suites: ownership_sidecar 22/35 -> 35/35; single_source 15/17 -> 16/17; store_apply 27/28 unchanged — BOTH residual reds are STALE TEST FIXTURES named as P4-class (a hand-typed ed-langdon club from July; an exactly-one-#283-entry count that is now legitimately two), queued, not fixed. THE SEAT'S CATCH, owned by the pen: register v821 was penned WITHOUT regenerating docs/STATE.md, so the state_file gate reds — proven pre-existing by the seat on a pre-act worktree, exactly the drift that gate was built to catch, one day old. FIXED IN THIS PEN and made procedure: docs/register/README.md step 3 now names both regenerations (incident index + STATE) as part of every pen. ALSO CARRIED: ui/data/club_valuation.js is stale the same way but carries a WALL-CLOCK field, so it cannot be byte-proved or made a carrier as it stands — dropping its timestamp is a design call, queued with the carrier-consolidation review. Board/store/engine byte-unmoved throughout; runner GREEN under the supervisor's re-run after the STATE regeneration.
incident: yes
