· v829 (2026-08-21): **THE PICK-SURFACE CLASS IS CLOSED — guard + writer 6 landed and verified (worktree seat, cherry-picked e61a51f/8aae62a, supervisor-verified on main: parity 41/41, runner 16/16 GREEN, lander self-test GREEN, board b3e8da99 byte-unmoved).** (1) THE WALL CLOCK IS GONE from ui/data/club_valuation.js — the one field that made the bundle non-byte-provable; three full ingests produced the identical md5 2277fa53; new fenced lanes --clubs-only (no store apply) and --clubs-check in the --mirror-only style. (2) THE IDENTITY GUARD (the #232 mirror law): MD.clubTotals.pin() refuses a bundle whose board/store stamp disagrees with the loaded app, reason naming both identities; centralised through the seam so all three reading surfaces fail closed — a stale pick price can no longer be silently shown; parity suite 17->41 assertions proving both directions. (3) WRITER 6 wired in the shared ui step, both landing sequences inherit; self-test 38/38, 20 steps broken/caught/aborted byte-exact; the carriers header records this bundle could not have been a carrier before the wall clock went. THE SIX UI MIRRORS NOW ALL HAVE WRITERS OF RECORD INSIDE THE LANDING — the missing-writer class is closed by enumeration again, one wider. CARRIED FROM THE SEAT (not its fault, measured precisely): tools/inbox_manifest.py derives its 'arrived' column from file MTIME, which a fresh checkout/worktree rewrites — the seat correctly REFUSED to regenerate an owner-input provenance date from a checkout timestamp (P4); queue = derive 'arrived' from git history or a declared field. The isolated-worktree commissioning practice is now proven twice (fire drill + this act) and is STANDING PRACTICE for build seats — the concurrent-writer item closes procedurally with it.
incident: no
