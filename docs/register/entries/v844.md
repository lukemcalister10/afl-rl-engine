· v844 (2026-08-24): **ARM 2 IS FULLY RECONCILED AND PRESENTED — the supervisor's build was the canonical one, the cause was a THIRD instance of the loaded-path class, and word B's fit-both lands with the conditioned feature better OOS and board-invisible.** THE SIX-ROW RECONCILIATION (seat, screened): the seat eliminated threading, hashing, concurrency and CPU-kernel selection by measurement (the ARM 2 estimator stack is KERNEL-INVARIANT — Haswell/Nehalem/Prescott/SkylakeX byte-agree, the property expected_boot records for the incumbent), then found the cause: **rl_model.py:1232-1234 loads peak_model_v4.pkl and pvc_snapshot.json BY RELATIVE PATH FROM THE WORKSPACE CWD, not from RL_REPO — the candidate root's peak/pvc swap was silently INERT** and the seat's board carried candidate band+ceiling with LIVE peak/pvc; re-seeding the workspace from the root reproduced the supervisor's 6bcf61b7 EXACTLY, and ship-gates B4 independently regenerates it. Boards corrected (a6c47317→6bcf61b7, noT1 10608b74→32732153); NOT ONE mover count shifted; mint −1.6076→−1.6068%; every battery instrument confirmed board-file-independent and standing. THE CLASS LEDGER: this is the third loaded-path incident (item 91 q97m → F7 rl_export-never-asserts → this); TWO REPAIRS OWED AND QUEUED: widen the coherence halt to the peak model, and the F7 fix must assert THE LOADED PATHS of all five artifacts, not the checkout copies. WORD B FIT-BOTH (owner "Fit both and show me", delivered): same table/store/settings, only the conditioning differs; day-0 byte-exact asserted (max delta 0.0 over 2,650 draft rows; fit 2 nests fit 1); OOS on the clean whole-career protocol the CONDITIONED feature WINS −0.97% RMSE at all three folds (on the leaky split the static wins +0.63% — the FB2 split-dependence again, same direction); AND THE BOARD CANNOT TELL: 67 rows move, all down 1-2 points, −0.0099% pool; the 51-row cameo census — the lever's own target — moves ONE point on ONE row; leake/oliver/dattoli move ZERO. Scope note: fitted on tenure 1-4; outside it the feature IS the static B (2,315 of 7,117 training rows touched). MOVERS PAGES DELIVERED to the owner (supervisor's own recomputation from the corrected boards agrees exactly: vs live 648 move 301/347, pool −11,245; vs arm 1 639 move 290/349, −17,563 — the graham −262 caveat printed on the page). ARM 2 NOW STANDS FULLY VERIFIED; the owner's word-list is open: the −1.61% BURN (he waived a mint, has not seen a burn) · THE EXACTNESS PRICE (no declared grid point beats the incumbent on the clean protocol — full design ~−1.1..1.9%, level-only ~−0.3%; the incumbent+ratchet delivers law-3 at the read site at zero prediction cost, so the choice is now guarantee-at-the-fit vs guarantee-at-the-read, priced) · a* 21.5 vs his 23 (0.06% apart) · the W2 floor margin (1.0321 vs 1.03, margin +0.0021 vs live's +0.0628) · O33 (asc*=1 still binds, margins narrowed) · the packet choice on the conditioned feature (better science, invisible board) · THE SEAM FLOOR (v843, threshold 45 or 40) · bust_prior producer landing (word A ruled, rides the design-arm refit pass which is now DONE via word-B fit 1: the modernized-table peak model exists as wordb_static ab11a10e).

incident: yes
