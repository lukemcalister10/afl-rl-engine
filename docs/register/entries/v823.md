· v823 (2026-08-21): **THE R24 DRESS REHEARSAL FLEW — GO-WITH-NOTES (docs/evidence/r24_rehearsal_2026-08-21/, commit cd09bb5).** Six flights of the REAL tools/land round in a worktree sandbox against a deterministic synthetic R24 courier file (411 listed / 393 DNP, the owner's exact cp1252/CRLF shape); RUN F completed exit 0 in 278s. FIRST-EVER coverage: an ARMED round applied through the lander (the self-test never arms; three of the five default gates had never executed inside a landing) — store/board advanced in-sandbox, guard5 held, ledger +411 with 0 dupes, ADVANCE-REPIN in-transaction, all five UI writers, claims 9/9, the abort ladder restored 30 carriers AND a mid-flight commit byte-exact on a real applied round, and two independent armed runs produced the SAME synthetic board (an unplanned determinism proof). Movers sanity 20/20 right-direction. TWO HALTS THAT WOULD MEET THE REAL R24, found in a sandbox instead of on the day: (1) THE INPUT-COMMIT PINCER (machinery): catchup_preflight wants the score file uncommitted-then-committed-by-itself, but declared_dirt() covers only the sheet — so the armed path halts EITHER way; fix = declare the score file + overrides as expected dirt when round.scores is declared; (2) THE WEEKLY ROUND PINS (procedural): both movers suites' pins (as_of_round, future-append fixture, prod.rounds, report count, lineage-state) go red the moment R24 lands and no step moves them — remedy measured and listed in the report: pre-bump and commit before the flight. ALSO: RL_BUILD_LOCK_FILE in the self-test sandbox env is the FOURTH BURN of the never-RL_-prefix class (it kills an armed advance via the config policy; unarmed self-tests never noticed) — rename due; NO RUNNABLE DAY-0 EMITTER exists for R24 (cprb_day0.py is act-pinned to a vanished scratch dir) — R24 owes a carried emitter or the owner's word to hold day-0 OFF; two owner-facing page notes (M0 baseline = the-staircase-adoption-21-8, and R24 closes the season calendar at 1.0). THE OWNER'S R24 WORDS, so the flight is not stopped three times: the score-write arming word (law 10c, his file + sentence + token into round.arming) + the injury-sheet word (H2 will trip on the planted-class rows) + the day-0 word (regenerate at the advance per v810, or hold OFF). REPAIR SEAT LAUNCHES ON THIS PEN: the pincer fix + the env-var rename + a CARRIED day-0 emitter, each with self-test coverage. Live tree byte-unmoved (board/store/engine + sheet + pins + suites all asserted); sandbox destroyed so the synthetic board cannot be mistaken for record.
incident: yes
