· v878 (2026-08-27): **BUILD BLOCK 3 LANDED ON THE CANDIDATE BRANCH (4bfdb76): the levers are BUILT, F1 is GREEN on the final bytes, and the candidate board f8d6ff9c is VERIFIED with EVERY mover attributed.** Owner authority: the banked words v873-v875 + the fold-everything and land-if-possible directives; no new word taken this block. THE BOARD: leg A (dials 0, kill-switch world) reproduces live 3167cba6 BYTE-EXACT; leg B (dials 1) = f8d6ff9c, net +3,254 pre-level. Named rows EXACT against the approved v872/v875 waterfall: oliver 283→360 · mccabe 231→325 · leake 421→617 · goad 667→737 · edwards 404→614 · riak-andrew 118→259 · burton 239 (unmoved, the standing-above-draft rule) · dodson 85→101 (inside the registered ~100-117 window). Engine 856e8328 · config 7b47ee7f (89 vars, dials RL_O46/47/48 + RL_O48_W=0.25 pending the F4 grid) · curve 1c3b22d1 (pin1=3000, strict descent) · v0surf d9f5d15d · entrant seal bd2d091b · pvc_snapshot UNMOVED 4704b829. **THE INCIDENT LEDGER — seven catches this block, zero corrupt artifacts, every one now a committed instrument:** (1) the bake's first draft re-derived the FROZEN pvc_snapshot from the live curve — caught by the artifact's own key range before a wrong number existed; the freeze is now a bake comment + the untouched pin. (2) v0surf FROZEN-SIGNATURE halt — the curve move invalidates the fitted surface; refit --bake run (5dd34ca8→d9f5d15d), and the halt fired AGAIN on leg A proving the kill-switch world needs the OLD surface: the two-world swap (set_v0surf.py, pre_bake originals) is now the committed machinery and the flip spec's kill-switch carries it. (3) PRINTED-DAY-0 exporter halt — 25 in-class cameo rows priced through the new floor; _entry30b_price restated ONE CLAUSE WIDER in the 29B→30B→31 lineage (the final-guard construct mirrored, tolerance 0; the import wall π(0)=D still holds — gameless cells sit under the stepped fade everywhere). (4) LEG F5 entrant-seal halt — the smoothed curve re-prices the frozen intake counts; re-sealed ccc26a9e→bd2d091b with counts BYTE-IDENTICAL (the ORDER 29 discipline), exporter pin restamped, old seal+exporter in the kill-switch world. (5) THE CLASS CONFLATION — the first build used the <2 sat word as the SURFACE class boundary; oliver/mccabe/leake priced out of their own mechanism (oliver +0). Caught by the board verification instrument, not a guard: the surface class is the prereg L1 ≥6 boundary; the <2 word scopes the STEP-UP only (_o46_class/_o47_class now separate predicates). (6) THE nsat TRANSCRIPTION — the first step-up implementation counted sat seasons instead of the derivation's CLOCK LAW; the O45 scoring-stripped counterfactual twin lost its history and drew a fresh-survivor D(2)=0.60, leaking +58/+55 into goad/green through the net. Caught by the residual probe (one harness load, per-lever dial toggles); the law is now L3a verbatim — c_u snaps to the next integer level with a HIGHER table value — twin-safe because it reads the clock, and dodson's benchmark held (101). (7) THE MRAZ FLIP — the blend-level draft cap left floored π visible to a downstream unproven-π relief: mraz (standing 943, draft 452) fell to 739 — a floor pricing a row DOWN. The surface is now the ORDER 46 FINAL GUARD at the finished-price level: max(v_off, min(v_on, max(v_off, day0×F))) — no-decrease and the cap by construction; mraz 943 exactly, unwin healed. Also tightened pre-emptively: the easing scope reads the law's "during" — only a first banked season IN PROGRESS tapers. **THE RESIDUAL CLASSES, disclosed:** step-up movers 4 (smillie +180 — the clock law on a pick-3 pedigree, the derivation's own "~5 movers" with size left to the built lever; bowman +39, dodson +17, camporeale +3); easing candidates 16 (dawson +28, pickett +34, croft +8 the real eased class; F4's declared grid sizes W in the battery); v0surf-refit ripple 19 veteran rows net −32 (the studies predate the mandatory refit — this channel is why the FINAL movers list derives from the BUILT board, prereg §2). Arithmetic closes: 2,948 (waterfall) + 245 (step) + 67 (ease) − 6 (ripple) ≈ +3,254. **ALSO THIS BLOCK:** B2 RE-BASED — the v863 red's root cause was the dead RL_PRIOR_TREES=150 pin in _gate1_wf.py re-pointing cm_load_path at the stale pre-rebake /home/claude/cm_150.pkl; under ARM-2 both gate legs build from the bound design spec (capacity-matched by construction), the pin is removed, the gate re-runs in the battery. SPEED ITEMS FILED (the owner's machine-time challenge): S1 bake-class preflight (the five-halt retry-tax killer — every halt this block was discoverable in seconds) and S2 walk-forward parallelization (40→~8 min), at SPEED_ITEMS_FILED.md. MACHINE LEDGER, honest: five full build-chain runs (~10 min/board leg) — the class fix, the guard redesign and the two frozen-artifact swaps each cost one chain; the residual probe cost ONE load (353s) and closed three defects at once — the S1 preflight would have saved roughly half of today's chain reruns. NEXT: block 4 — the battery on the harness (F2-F10, the F4 easing grid sizing W with the manifest re-declared if ≠0.25, censuses as gates, no-arb re-emit on f8d6ff9c, ship-gates with the re-based B2), then re-level, blind review (law 11), THE FINAL JOINT MOVERS LIST for the landing word.

incident: yes
