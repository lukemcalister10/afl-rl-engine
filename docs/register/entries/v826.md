· v826 (2026-08-21): **THE THREE R24 PRE-FLIGHT REPAIRS ARE LANDED AND VERIFIED (commit 1870cde, one explicit-path commit, supervisor-verified: identities byte-unmoved, runner 16/16 GREEN and lander self-test GREEN under the supervisor's own runs).** (1) THE INPUT-COMMIT PINCER is dead both jaws: declared_dirt() enumerates the score file + override record when round.scores is declared, and the input commit skips-if-unchanged (output commits deliberately do NOT — a step that believes it wrote and staged nothing still dies); the rehearsal's RUN A and RUN C halts are now standing self-test regression cases, plus a byte-exact abort of the mid-flight input commit. (2) THE FOURTH RL_-PREFIX BURN is fixed the hard way: the lock flags renamed to BUILD_LOCK_* end-to-end, LEGACY NAMES HALT rather than being ignored, and the name was deliberately NOT allow-listed into the config policy (admitting tooling variables into the engine's namespace is the habit that produced all four burns); one survivor named for the next seat that opens those files (RL_BUILD_LOCK_HELD, currently triple-popped before any engine child). (3) THE CARRIED DAY-0 EMITTER exists (tools/landing/day0_emit.py): generic, fail-closed, refuses unactivated and refuses a missing reference (the M1b printed diff is mandatory, so no lawful silent first write), byte-stable twice, reads the engine's own pricing accessor rather than mirroring it; steps.day0 defaults to it; NO reference installed on the live tree — that is R24's owner-worded act. Self-test 36/36 (was 30), 19 steps broken/caught/aborted byte-exact. COST DISCLOSED: the pre-flight self-test now ~110s (was ~34s; the emitter's positive case loads the engine) — the R24 flight estimate gains ~1.5 min. SEAT IDENTITY SLIP CAUGHT AT VERIFICATION: the seat's commit landed under the OWNER'S email — amended --reset-author before push (content untouched, re-verified); the committer-identity rule goes into the standing seat directive boilerplate from the next commission. THE PRE-R24 MACHINERY LIST IS NOW EMPTY: what remains for the flight is procedural (the five weekly pin pre-bumps per the rehearsal report) plus the owner's three words (arming · sheet · day-0). Also carried: season_total_rounds=24 — R24 closes the season calendar; an R25/finals question is the owner's, flagged for the R24 conversation.
incident: yes
