· v887 (2026-08-30): **SHRINK S6 IS LANDED — one stamped carrier, every other stamp derived from the tree and checked against it; THE CUT LIST IS NOW COMPLETE.** Owner word 2026-08-29 then 2026-08-30: "S6 we can do after this is all locked away", then "Alright, so now s6?".

S6 CHANGED SHAPE ONCE IT WAS SCOPED, AND THAT IS THE FINDING. The cut list called for collapsing the identity-pin triplication (expected_boot / release_contract / mirror stamps) to one carrier plus derived agreement. Scoping it showed that inside `tools/land` two of the three were ALREADY derived: the contract step MEASURES config / engine_head / rl_model / fv / store / board from the tree and refuses if a carrier disagrees, so a landing cannot leave them out of step. The gap was never inside the transaction — it was every change made OUTSIDE one, which is exactly what the ORDER 49 dial flip was: a hand commit that left five stamps behind (the engine_head pin, the contract identities and self-hash, the board-view stamp, the club-valuation projection, the sibling sidecar's contract binding), each found by RUNNING A GATE at minutes apiece rather than by noticing (v882's "five instances" of one class). Rewriting the carrier structure would have been a large change to the mechanism that guards everything else, in service of a defect that was somewhere else.

WHAT WAS BUILT, THREE PARTS. (1) ONE IMPLEMENTATION OF THE CONTRACT RESTAMP: `release_contract.restamp_bake_identities` now owns the bake-lane write — `config_sha256` plus `identities.{engine_head,rl_model,fv}`, the re-seal, the frozen-field assertion, and the trailing-newline preservation that is what makes a no-op landing genuinely byte-exact. It had lived INLINE in the landing step, which is precisely why a hand commit could not reach it; the lander and the new tool are now two callers of one function, with one definition of what may move while those four fields are written. (2) `tools/restamp` — `check` (read-only) and `apply`. The tree is the truth, as the transaction already treats it, and a stale stamp is rewritten THROUGH ITS OWN WRITER OF RECORD, never by the tool editing another writer's bytes. It refuses two things outright: `expected_boot`, because moving the CARRIER is a landing act with a prereg and an abort ladder behind it, and the sibling sidecar, because its writer REBUILDS before it writes and a build belongs inside a transaction that can restore. (3) THE FENCE, which is the real design content: the tool ENUMERATES rather than searches, because `release_lineage.json` names superseded engines on purpose, the gate snapshots are keyed to the head each recorded, `movers.js` records what the board was at each point, and the value/rank histories ARE the record — a tool that "updated every file carrying the old id" would falsify all of them. Three named tables (stamps · history-or-bake · carries-no-identity-by-design), a reason per entry, and a refusal if any two ever overlap.

A VACUITY GUARD THE FIRST RUN EARNED. `board_view_public` reported AGREES while claiming no identity at all — the public bundle carries none by design, the standing law keeping process off user surfaces. A stamp that claims nothing passes every comparison forever, which is the one way this table can rot unseen: a writer stops emitting its stamp and the row stays green. A zero-claim stamp is now REFUSED as vacuous and the file moved to the by-design list, so its absence is a stated decision.

PROOF. 44 assertions (`tools/test_restamp.py`), and they are mostly non-vacuity because agreeing WRONGLY is this tool's failure mode, not crashing: every claimed identity of every stamp corrupted in turn and caught — done per identity rather than by staling one fixed id, because each stamp claims a different subset and a fixed id would silently skip the stamps that never claim it (the sibling sidecar carries store and fv but no engine id, and the first draft of the test passed by not testing it); prefix agreement discriminating in both directions; the vacuity guard firing; an unreadable stamp failing closed; both refusals; the tables never overlapping; and the one that matters most — this tool's measurers asserted equal VALUE BY VALUE to the lander's own `PIN_MEASURERS`, so the two can never drift into disagreeing about what "the tree" means. `land selftest --only contract` on the committed tree: 10 PASS / 0 FAIL, control clean, the injected seal fault caught with the abort byte-exact, the depth arm restoring 11 written carriers, the round rehearsal clean, every live carrier byte-unmoved. Wired into the step-0 preflight at 0.12s — 15/15 CLEAN — so the divergence that cost five gate runs is now visible before the lock.

INCIDENT, MINE, AND KEPT AS EVIDENCE: the first selftest FAILED because the supervisor launched it before committing. The lander runs its own code from the working tree while loading `release_contract.py` from the sandbox, which is cut from the last commit, so the new step called a function the sandbox's copy did not have. That is the incoherent-base case `--base` exists for. The machinery behaved exactly as designed — aborted at `contract`, named the step, restored the one carrier already written byte-exact — and both transcripts are filed, the failure beside the pass, because a ladder demonstrated on a genuinely broken step is the better exhibit. THE CLASS: a change that spans the lander's code AND an artifact the lander loads from the tree is only coherent once committed; commit before you self-test, or name the coherent base.

THE CUT LIST IS COMPLETE. All twelve items plus S13 are ruled and shipped: S1/S4/S11/S12 (v881), S2/S5/S7/S10/S13 (2026-08-28), S3 via S10, S9 via S13 slice 1, S8 (v884), S6 (here). Nothing on it is outstanding.

incident: yes
