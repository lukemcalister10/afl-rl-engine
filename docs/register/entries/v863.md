· v863 (2026-08-26): **THE SPEED ACT IS LANDED — every deliverable proven, the book re-sealed on the landed tree, and falsifier 6 discharged at last.** Owner words: the standing efficiency directive ("No convenient interpretation of my words to be lazy... Parallelise. Load things once"), "We can do B3 in the next set", and this sitting's "please continue, stopping only if there is a blocker to pushing live. Try to land what we've worked on if it's possible." Nothing value-bearing moved — machine-checked: claims note docs/evidence/speed_act_2026-08-26/SPEED_CLAIMS.json, 21/21 GREEN (11 unmoved identities incl. board 3167cba6/store fb640ca0/engine d84031cf, 4 seal fields, 6 re-runnable gates). THE DELIVERABLES, each with wall-clock actuals stated in the P14 spirit even though P14 awaits his signature: (1) **tools/harness — the load-once battery command** (build lock → Guard 5 → gate mode → ONE engine load → instrument passes in-process): validated end-to-end, 772s load then the smoke pass in 1.3s, exit-code green; building it re-hit the RL_BUILD_LOCK_HELD/gate-mode collision (fixed per the recorded thrice-burned convention: pop it from the child env) and this seat's own first invocation piped through tail and masked a non-zero exit — the exact item-38 sin, self-caught, invocation corrected. (2) **land preflight — the sub-second pre-launch battery** (tools/landing/preflight.py + the cli verb, auto-run at the top of lever/round/edit BEFORE the lock, the sandbox and any build): first live run CLEAN, 10 checks in 3.3 seconds — the checks that cost six of ORDER 45's nine failed attempts; two corrupt legs filed firing with non-zero exits. (3) **Typed spec validation** (attempt-1 and attempt-4 classes refused at validation time: kill_switch.env typing, the moves/unmoved SCOPE PARTITION with FLIP_SCOPED/NO_WRITER tables cross-asserted against steps.PIN_MEASURERS at import per P4, the round arming.env latent AttributeError): five corrupt legs proven firing, the landed ORDER 45 spec still green, filed at SPEC_VALIDATOR_CORRUPT_LEGS.log. (4) **Pickle isolation, the attempts-2+5 class fixed at the root**: Sandbox.env pins the selftest sandbox to its OWN tree's cm/q97m and Ctx.child_env pins every transaction child to THIS tree's — both INFRA_ALLOW path vars Guard-5-asserted; the shared mutable /home/claude copies are no longer read by any landing path. INTEGRATION PROOF: the lander sandbox selftest on the hardened machinery — 22 steps broken / 22 caught / 22 aborted byte-exact, 43 PASS / 0 FAIL. (5) **B1/B3 ceiling 1800→5400s at the measured cause** (the arm-2 exact-monotone band made the gate-mode matrix regen ~35 min where the suite's header says ~3) and (6) **THE BOOK RE-SEALED on the landed ORDER 45 tree** via the declared lineage copy reseal_speed.py: head 1867e953→d84031cf · store b745002e→fb640ca0 · config →29fdfd1e · n RE-COUNTED 2650 · stable 9f46aba3→**5b5f65af**, and the stamp REPRODUCES the independently-run 2026-08-25 uncapped matrix byte-stable — two gate-mode regens a day apart, same content seal. **THE FULL SHIP-GATES SUITE THEN RAN COMPLETE ON THE ARM-2 TREE FOR THE FIRST TIME** (SHIP_GATES_FULL_RUN.log): **B3 PASS — the candidate book matches the new seal — FALSIFIER 6 (prereg ORDER45 R-FIX5) IS DISCHARGED** · **B4 PASS — a bare gate-mode rebuild reproduces the shipped board 3167cba6 BYTE-EXACT** · B6, D14a-d, A5-A8, A10-A11 PASS · A12 carried standing-fail as recorded · A9/A15/B1 STRUCK as ruled · A13/A14/C1/C2 PENDING as always. INCIDENTS AND FINDINGS, all disclosed: (i) the first full-suite launch HALTED in the suite's own engine load — the v0surf frozen-load resolver needs RL_REPO bound and this seat's invocation did not bind it (the sealer and harness both do); one ~6-minute run lost, re-launched correctly — an INVOCATION contract, noted for the suite's runbook; (ii) **B2 IS RED ON THE ARM-2 TREE, A NEW FINDING, NOT A REGRESSION OF THIS ACT**: _gate1_wf.py pins RL_PRIOR_TREES=150 for its capacity-matched IS/WF construction, the stale pre-rebake /home/claude/cm_150.pkl (2026-08-20) declares no _rl_design_spec, and the ARM-2 ARTIFACT-COHERENCE HALT correctly refuses to price a mixed band/ceiling — the guard fired exactly as designed (P15's argument in the wild). B2's 2026-08-25 greens were PRE-FLIP roots; the producer has NEVER run on the arm-2 engine, and its repair — re-basing the capacity-matched construction onto the arm-2 artifact family — is QUEUED into the combined build's repairs, not hacked tonight; (iii) the harness wrapper's first cut missed the workspace PYTHONPATH, fixed. ALSO THIS ACT: the P14/P15 + handover-diet + wall-clock-budget amendment proposal REDRAFTED from the register (the scratchpad draft died with a container) and filed at docs/proposals/rulebook/AMENDMENT_P14_P15_2026-08-26.md — NOTHING BINDS until his word (law 10a). MACHINE-TIME LEDGER of the act, honest: harness validation 772s+1.3s · seal regen ~33 min · full suite ~95 min (B1/B3 regen + B2 attempt + B4 export) · selftest ~7 min · every other proof sub-second; keep-alive chain held throughout, zero platform-reclaim losses. NEXT: THE COMBINED BUILD — blocked only on the owner's four packet words (depth-3 cap · ruck relief size · pedigree mechanism after the decomposition · smoothing confirmation) plus the amendment signatures if he gives them; the build's battery now runs on the harness this act just proved.

incident: yes
