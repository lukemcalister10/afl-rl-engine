· v884 (2026-08-29): **SHRINK S8 IS BUILT AND PROVEN — the sibling half of the proof stash; and the shrink cut list's disposition is now on the record, which it was not.** Owner word 2026-08-29, on the item put to him with its risk stated: "Your suggestion makes sense though. S6 we can do after this is all locked away." He also asked the question the machinery had never been made to answer plainly — "Why do we need the sibling?" — and the answer, given to him from the artifact record rather than from prose: the balanced sibling is the reproduction gate, and its recorded catch is the container-#2 divergence `d7a95e8d`, where the SAME store and the SAME recipe built on different hardware yielded Σv 750,171 / sheezel 7,869 against the intended 752,427 / 7,964 — a rank-unsafe board that nothing downstream would have flagged, the artifact map's own note reading "it exists as proof the gate must exist". It is also the fixed lineage anchor every movers report carries, and the carrier of the forward view and its oracle.

WHAT S8 IS: the sibling reconcile is 54% of a landing's wall-clock (2,171s) and the combined-build landing rebuilt it FOUR times to the same identity, three of them deleted byte-exact by aborts. The cost is `build_sibling` — two full engine builds (the balanced/strict present-lens board and the config-of-record forward view) that WRITE NOTHING and return a derived-identity dict. ONLY THAT DERIVATION IS CACHED. Every writing part of the step still runs for real on every flight: the plan, the overlay validation, the release-contract self-seal, the atomic replacement, the journal, the rollback ladder and the verify-after. This was the deliberate answer to the risk the supervisor put to the owner when asking rather than assuming — slice 1 caches a pure computation, while the sibling step writes and commits, so the cache was kept on the side of the step that only computes. THREE CONDITIONS FOR A HIT, any one failing rebuilding in full: not a selftest and not a fault run; the key, over EVERY PIN_MEASURERS identity measured LIVE at that point in the transaction (the step runs after the board and store are committed, so its inputs are final and measurable, and its own output is a DELEGATED pin, correctly absent from its own key); and the entry's OWN recorded provenance — the store, fv and rl_model that `build_sibling` derived from the artifacts it actually built — still equalling live. The third is the one that carries the weight: it is independent of the key, so a collision or an entry that outlived its inputs is refused by the artifact's own record rather than by trust in the key. The patch is scoped to the landing step and restored in a `finally`, so the gate's check verb, the CLI and every other caller of `sibling_repin` are untouched by landing policy.

A GAP SLICE 1 LEFT, now closed: the proof stash had NO TESTS AT ALL. It is the one instrument whose purpose is to not run the thing it stands in for, and the lander selftest disables it by design — so its decision boundary was proved by nothing in the estate. `tools/landing/test_proofstash.py` now asserts all of it, 23 assertions: key sensitivity per input, hit, miss and banking, the independent provenance refusal one field at a time, a tampered entry failing its own md5 and falling through to a real build, never-in-selftest, never-in-fault, the scoped patch being restored, `with_forward=False` never served, and a stash failure never failing a landing. It runs in the step-0 preflight battery at 0.04s, on the principle that an instrument trusted to skip 36 minutes should cost a fraction of a second to check.

PROOF: `land selftest --only sibling` 10 PASS / 0 FAIL — control arm clean and committing only declared paths, the build-lock refusal firing, the injected sibling fault caught with the landing aborted at that step, 11 written carriers restored byte-exact, and every live carrier byte-unmoved across the run. Preflight 14/14 CLEAN. Evidence at `docs/evidence/shrink_s8_2026-08-29/`.

THE DISPOSITION, WHICH WAS THE REAL FINDING: the cut list's twelve per-item `Your word: ____` slots were never filled in when the rulings came, so the document still read as twelve open questions when ten were settled and shipped. Each is now recorded against a citation in the code or the register rather than against memory — S1/S4/S11/S12 from v881; S2, S5, S7, S10 from their own "owner word 2026-08-28" markers in the source; S3 delivered by S10 (the seal is B3's baseline and B3 is bake-scoped, so no landing asserts it); S9 delivered by S13 slice 1. S6 — the identity-pin triplication, the item that would have prevented the five ORDER 49 restamps — stands DEFERRED by his word above, and remains the one open decision on the list.

incident: no
