· v836 (2026-08-24): **THE GRAHAM DUAL CORRECTION IS OWNER-RULED, PREREG'D, PROVEN BUILDABLE — AND PARKED ON A REAL CAPABILITY GAP: the lever lander has NO LANE for an out-of-round store edit.** THE RULING (owner words verbatim, 2026-08-24): "Can we please edit Will Graham to be a 40% only SF then in the store and recalculate?" — given after the supervisor measured the LEG C MAX-law dual benefit on Graham's row (+490.5 ev, +43.7% at p_dual=90; v835). THE ACT AS FLOWN: prereg FIRST (2feaf25, P9) with the prediction measured on a scratch build — store daa93053→fb640ca0 by surgical one-field byte edit (p_dual_stream 90→40), board 6fd0f7de→82fcd8bb, EXACTLY ONE MOVER will-graham 1533→1271 (−262), pool −262, sitters 86/86 unmoved; then the flip commit + pin companion; then tools/land lever. THREE BYTE-EXACT ABORTS, each the lander correctly refusing, each filed in docs/evidence/graham_dual_2026-08-24/: (1) spec validation — the lineage owner-citation fields are lander INPUTS, never composed; (2) lineage base-coherence — a flip without its pin companion leaves HEAD incoherent; (3) with the pin companion, the CHAIN check — the lineage source is measured at HEAD and must equal the register tail's destination or bridge via a SINGLE round report on BOTH legs; a pre-committed store flip makes the source store (fb640ca0) unbridgeable (the R24 report bridges to daa93053), and the lever sequence has NO STEP that edits a store in-transaction. Confirmed against steps.py: pins() re-pins measured movers and lineage() demands in-transaction store movement — the two checks JOINTLY forbid a pre-landed store flip, and the lander (writer of record for lever acts since 2026-08-20) has never been asked to land one: the last out-of-round store writes (item 20, the ID migration) predate it. THE BUILD ITSELF PASSED IDENTICALLY ALL THREE TIMES (~141s, predicted board byte-exact). PER THE LANDER'S OWN RECOVERY LADDER (abort-then-retry, then the act SLIPS — never an improvised manual landing): the flip pair is REVERTED (bbe40be), the tree re-verified fully coherent (store daa93053 == pin, board 6fd0f7de), and the act SLIPS pending THE LANDER EXTENSION — a store-edit lane for lever acts (spec-declared store edit applied in the work dir so source/destination straddle it and the chain stays continuous), to be built as its own prereg'd act WITH self-test coverage, and designed alongside the design-arm landing's artifact-pin choreography (the in-repo cm_400 move poses the same class of question). The Graham prediction STANDS unchanged for the re-flight. ALSO THIS WINDOW: the auto-mode classifier blocked one compound command (known false-positive class; resolved by splitting into plain steps, per the standing rule — never worked around). OPEN OWNER WORDS after this entry: none new — the sitter-fade/regime-seam study (now three sightings: DeMattia year-4 timing, Leake/Oliver severity vs the measured base rates — 56 weak-cameo picks 12-30: median eventual best-3 = 55, 21% became 70+, 25% busts — and the fade calibration) awaits his word; the bust-prior consistency variant and the design arm are the supervisor's next builds.

incident: yes
