· v831 (2026-08-21): **ALL SIX REBAKE DECISIONS ARE NOW RULED — the rebake week is fully specified, pending R24. Owner words verbatim (2026-08-21):** D1 = "Exact it is." (the exact constraint construction, with the private-contract tripwire self-test; the ratchet retires on the must-move proof). D2 = the hill construction accepted WITH THE PEAK CHALLENGED: "I feel like the highest player value point is at year 5-6 - so age 23 or so on mean, not 21-22?" — RULING AS RECORDED: a* is NOT hand-fixed; it is SELECTED OUT OF SAMPLE at the bake over a declared grid that includes 21/22/23/24, with the owner's prior (~23) and the study's fitted response peak (~21.5) both on the record; note for the bake seat: the owner's year-5-6 read likely comes from the no-arb cohort path (peaks year 5 = age ~23), which is a COMPOSITION curve (survival x value) and not the same object as the band's conditional age response — the selection settles it by measurement, and the choice must be reported with the numbers for both candidates. D3 = "Agree on recency weighting" (mild, window-anchored half-life per study B M-60; the exact half-life selected out of sample at the bake, 10-16y grid, declared before the run). D5 = waived (v830). D6 SCOPE = "Happy to rebake the ones you suggest": band + ceiling + peak_model_v4 (+ pvc_snapshot, which CO-EMITS with the peak-model build by design — verified: rl_model.py:1234 loads it as the peak model's FROZEN train-time PVC feature, deliberately not tracking the live curve to avoid train/serve skew, so it regenerates with its model and never separately) + bust_prior_table; v0surf UNTOUCHED (current). D7 = "Sequencing is fine yep": store-alone arm built and SHOWN first (movers list to the owner), the live board moves ONCE at week's end after his rulings on the full design. Every arm carries: the provenance stamp in-repo, T1 applied, the loader HALT, the level census (V3) at zero, the pinball monitor baseline, and the reported (not gated) law-9 reading.
incident: no
