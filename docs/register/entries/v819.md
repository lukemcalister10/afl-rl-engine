· v819 (2026-08-21): **THE B RULING IS ALREADY THE ARCHITECTURE — AND A STALE FIGURE IS CORRECTED (a P4-class slip by the supervisor, named).** Investigation before implementing the claims B ruling found tools/claims.py ALREADY works at hash/parse/count level and reads gate verdicts rather than re-running suites: the claims step measured 0.04s in the P2b dry run. The "~3.8 of 9.4 gate-minutes" figure the supervisor quoted to the owner when presenting the ruling was the v807-era PRE-LANDER measurement, quoted as if current — exactly the this-month's-number error P4 legislates against; corrected here, not smoothed. The B ruling stands SATISFIED by the existing design (independent hashes + negative control + no expensive recompute); no code change was needed and none was made. THE REAL REMAINING REDUNDANCY, measured: the in-transaction gate profile includes build_twice_determinism (~350-420s of the 425s in-transaction runner cost) INSIDE a transaction whose own machinery already builds the board — plus the soak-era supervisor duplicate full run (~430s more). OFFERED TO THE OWNER as a one-word option, not taken unilaterally (it reduces in-transaction gate coverage): exclude build_twice from the in-transaction profile, keeping it in the full profile that CI and every supervisor verification run — would roughly halve landing wall-time; declining keeps maximum in-flight paranoia. G3.iv stand-down remains the other, larger lever and is already his.
incident: yes
