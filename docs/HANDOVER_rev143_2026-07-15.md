# HANDOVER — rev143 · 2026-07-15 · supersedes rev142 (hardened at the owner's request before rotation:
### rev142 was filed AT the seam; this fold adds everything that landed AFTER it — the UI ranking-page
### miss + ruling (item 173) · the open Tier-3 PR dispositions · the still-pending confirmations — so the
### incoming seat starts with ZERO unstated threads. The chapter "evidence over calendars" is BAKED +
### PROMOTED (tag v2.10 = d14efae · main promoted a64c47f, docs line beyond). The merged PVC+FLEX chapter
### opens next. Read after 00_MANIFEST (v4.28) + CORE (v2.8).
### **The register (v158+) is the durable log — trust it over this summary.**

## FIRST ACTS (CORE v2.8, BINDING ORDER)
Freshness check FIRST — full-URL `ls-remote` · register header · docs/ list · PK vs repo DOC BY DOC —
then file the handed pack. **PK behind on ANY doc ⇒ HALT and tell the owner.** Adopt `tools/seat/` from
turn one (orient.sh · prescreen.sh · gates_score.py · pen.py · board_diff.py · gates_brief.py, on main).

## STATE (verify live; cite your own fetches)
- Pack: manifest **v4.28** · CORE **v2.8** · DECISIONS **v104** · CONSTRAINTS **v1.16** +
  acceptance_v1_16.json (REPO-ONLY) · SSI v1.3 (docs/) · **register — READ ITS OWN HEADER** (v158+).
- **THE CHAPTER IS BAKED: tag `v2.10` = `d14efae`** (branch tip; byte-identical shipped state to the
  CI-verified bake `83f945a` — a stray-report relocation is the only delta, register item 169). **main
  promoted = `a64c47f`** (two-parent merge of PR #97; parents 7ba44a3 + d14efae). Board `790136a3` ·
  store `b1fd0bce` · book seal `99be9b36` · pick 1 = 3000. **Prior tag of record v2.9 = `9f8ae76`
  intact.** main CI was IN PROGRESS at this filing — verify it went green (AMD runner printing 790136a3).
- **THE 1.335 WAIVER IS REVERTED — G-COHORT HALTS AT 1.30 AGAIN** (CONSTRAINTS v1.16 / acceptance v1.16).
  The v2.10 y5 was 1.3057 (in the waived zone); a fresh board at 1.3057 would now HALT — the merged
  chapter must bring cohort ratios back under 1.30 or seek a new owner waiver.
- **L-CAPTAIN is OPERATIVE** (all prereqs cleared at the bake).

## THE NEXT CHAPTER — PVC + FLEX, MERGED (R103.2). Its baseline set is COMPLETE and prescreened:
- **CALIBRATION (item 170, the measured spec baseline):** compression β=0.364 peak / 0.683 proven-27+
  (CI excludes 1) · the RUC mid-round iso trough is INTACT and evidence-blind (the trio was a one-player
  patch) · young star-track UNDER-priced vs realisation (net-cut = wrong-direction as measured law) ·
  **the ≤22 slope β=0.111 = the Reid worry, measured.**
- **CENSUS-v2 (item 165):** the unearned-only gauge (125/+15,612; the disease drained ~19% across the
  chapter) is the standing scoring tool. The v2 discontinuity probe reads the LIVE E_q path.
- **RULED LAW carried VERBATIM:** items 130–132 (no young net-strip · cure on the axis where the defect
  lives, LIFT-led · iso_corr evidence-fade = the English leg) · R103.3 (LENS PROJECTION build) ·
  R103.5/R103.6 (watch rows Gawn↑/Bont↑/Reid-relative; the Reid constraint = project projected years
  through the same un-compressed map).
- **OPENS ON:** the AXIS RULE ruling (PENDING-OWNER, CONSTRAINTS) · the flex positioning registers
  (owner couriers at fire time, item 99's Harvey Thomas seam) · a Fable-build earmark for the
  compression+lens lever (the owner authorised Fable use where the gap warrants).

## QUEUED HYGIENE (Tier-3, non-blocking; fold into the merged chapter or run parallel)
- **The SGC_REPORT_DIR auto-write** tripped THREE builds (items 149/151/169) — ship-gates should
  strip/reject SGC_* or write in-fence. A one-shot fix.
- **The BOOK's cross-CPU determinism** was never separately proven (item 170; board proven at item 115;
  book bounded 99.6% exact, sealed book scored). Rides the next determinism window.
- **The dead `if not _EVW:` discrete-regime branch** (_merged_recover.py:358–372) — DELETE-DON'T-DISABLE
  candidate with an obituary (SSI), flag at the PVC chapter.
- **UI v1.1** (fired, self-gated on the v2.10 tag — now live): the owner's feedback batch (item 163) +
  the AFFL ranking page. Owner views before any merge word.

## LANDED AFTER THE SEAM FILING (rev142 predates these — items 173–174)
- **THE UI RANKING-PAGE MISS (item 173, supervisor-owned):** the UI v1.1 directive claimed the AFFL club
  ranking page was "previously specced" — NO SUCH SPEC EXISTS (the build searched everything, refused to
  invent, asked — correctly). RULING GIVEN: the build ships items 1 · 3–8 + item-2's group/filter/ΣSCAR
  and DEFERS the standalone page. **The page is an OPEN OWNER DESIGN ITEM** (what should it rank — club
  ΣSCAR · roster depth · best-XI · positional balance?). Its directive is written when the owner specs it.
  The UI RETURN will flag the deferral — prescreen it against item 173, not the original directive text.
- **OPEN TIER-3 PRs + BRANCHES (all post-promotion mergeable; owner rules on each):** PR **#95** (aging,
  `c27682f`) · PR **#96** (calibration, `cf153fd` — carries a DECLARED lineage caveat: its commits sit on
  the register line, its DATA was `9be07b8e`'s, item 170 note 1; merge normally) · census-v2 branch
  `claude/census-instruments-head-engine-c0rsmv` @ `cfe4a14` (no PR by convention) · cm_400 diagnosis
  `claude/retrain-determinism-diagnosis-i3nbco` @ `796f371` (no PR) · the UI v1.1 build (in flight).
  None blocks anything; they are the chapter's committed evidence.
- **STILL-PENDING CONFIRMATIONS (chase early):** (1) **main CI green** — it was in_progress at every
  check this seat made post-promotion (the docs pushes retrigger it); the R100.7 standard is the AMD
  runner printing board `790136a3`. (2) **PK SYNC** — the owner syncs Project knowledge to THIS pack
  (manifest v4.28 · CORE v2.8 · DECISIONS v104 · HANDOVER rev143 · SSI v1.3); your freshness check HALTs
  on a stale PK, so if PK shows rev142/v4.27, that is the KNOWN one-filing lag, not a disease — say so
  and ask for the sync rather than halting the world.
- **SNAPSHOT-NAMING HYGIENE (carried from item 149):** a seat once grabbed an OLD gates snapshot via a
  `tail -1` alphabetical accident. Read snapshots by ENGINE HASH (`gates_fc7045d6.json`), never by sort
  order.

## OPEN OWNER WORDS (no action until given)
The AXIS RULE (PENDING-OWNER) · the U-curve <20 clamp is CLOSED (R102.6) · the tail CLOSED (0.11).

## HOW TO WORK WITH THE OWNER (empirical record — UNCHANGED, load-bearing)
His reads are ground truth until HE waives them — he has found pricing defects from the board
repeatedly and the override doctrine (R103.5) now formalises when a conviction is a model-defect claim
(in-stats) vs an override (extra-statistical). **Give him numbers, never theories**; symmetric options
WITH a recommendation; DO:/WHY: asks; plain-read openings; no over-production. **The repeating seat
failure: a plausible mechanism written as a measured fact — RUN THE COMMAND.** Provenance-tag every
carried claim; verify before building.
**⚠ CLIENT QUIRK (prior chat): text before/between tool calls did not render — put the full response
AFTER the last tool call.** (Seat 7 did not hit this; verify per client.)

## CONTEXT ECONOMY (owner-agreed)
Chat = plain read + the decisions the owner needs; detail in the register + the per-turn journal
(MANDATORY, crash insurance). Verify in FEW targeted commands; grep, never read wholesale. CONSTRAINTS +
acceptance are REPO-ONLY — fetch and grep when judging a board.

## RESIDUAL RISK MAP
`cm_400.pkl` is FROZEN (R103.7) — re-derivable-at-refit, no longer an ingestion blocker. The out-of-fence
`np.average` site · `_iso_dec` at `_merged_recover.py:801` — look there first if CI reds on a fixed line.
