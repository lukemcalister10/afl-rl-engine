# SUPERVISOR HANDOVER — AFL SuperCoach RL valuation engine — 2026-07-02 (rev93 · ROTATION · SUPERSEDES rev92 AND ALL PRIOR)
### Canonical STATE, fully reconciled and rewritten at rotation (no stale carried bodies). Read `supervisor_KICKOFF_2026-07-02_v7.md` FIRST. The outgoing instance rotated by choice at ~70% so you ingest the D7 return fresh — the same pattern as the previous two rotations.

## §1 — ANCHOR STATE
- **Engine head `8aed420a` · store `644d1254` · band `34faa865` — canonical, unchanged all session except Luke-ruled gate/doc commits** (SHIP_GATES.md now a55921f6 after the new-B1 amendment · checker 5e6e34d9 · LTI register 1b24df4e in repo). Restore-verify 9/9 + panel 10/10 at every session end. Anchors: Maric ev(2026)=1409 · Langdon=593 · Rozee 2026=2679 / 2025=3874 · Tsatas HEAD=1083 (display bug history: 2166 was 2×) · Gothard HEAD=317 (staleness cap).
- **Repo** github.com/lukemcalister10/afl-rl-engine · main @ `b2671355` (post-PR#8) · PR chain #4→#9: all merged except **#9 OPEN** (D6; D7's STEP 0 merges it). **D7 IN FLIGHT or about to fire** (xhigh, ~3–4h; its PR = #10 presumably). Old candidate fb39d88a SUPERSEDED — D7 builds BAKE CANDIDATE v2.
- Engine env per run: `PYTHONHASHSEED=0 RL_GAMMA=0.85 RL_PICK1=3000 RL_RECENCY_DECAY=0.72 RL_PRIOR_TREES=400 PAR_RAMPS=22`. One engine load per process; sequential evals. Store data runs through **R14/24** (no timestamp field; derived cut — answers all "how current" questions).
- **ONE PRICE is real since D4:** board renders engine ev(); board valuation layers deleted with obituary (9 headstones incl. a dead JS chain); lens = engine-backed view. Luke still trades off the STALE b8f9e998 board — remedy = ONE re-cut after the bake word.

## §2 — THE RULED CONFIGURATION (all Luke's words, all this session — D7 assembles it as BAKE CANDIDATE v2)
1. **GROWTH LAW REDEFINED (the constitutional change):** NOT every cohort must rise — the CROSS-COHORT SIMPLE AVERAGE at each year-depth must rise to a peak in years 4–6 (5% pre-peak dip tolerance on the average); individual cohorts UNGATED but their curves PRINT on every board (Luke's eyeball channel). His words: "not all draft cohorts are equal; 2020 is a shocking draft — it should lose value." New-B1 amended into the frozen file (his ruling verbatim in CHANGELOG), scripted, passing at all measured states. THE OLD PER-COHORT LAW IS DEAD — never argue from it.
2. **OVERLAY RULED (turn 17): keep M1 + v7-asc, DELETE v7-cB.** Decomposition provenance: v7 = asc + cB only (source-diff nil residual). asc = mediocrity-concentrated markdown (2020 bottom-half −33.6% vs producers −5.0%, ρ=+0.706 — Luke's read confirmed); cB = indiscriminate (ρ≈0), the Curtis squeezer, deleted with obituary. Known consequences Luke accepted in writing: A2 ships RED (Curtis 0.822 vs the 0.90 bar — "we can look at Curtis down the line"); B1 aggregate peak at minus-cB = 156.6 (head 160.5).
3. **M2 + M3 WIRED at v2.** M3's acceptance RE-REGISTERED at Luke's amended bar: A3 ≥0.75 (was 0.80; DATA-CAUSED — Rozee out for all 2026). KNIFE-EDGE BY DESIGN: expected ratio ~0.74–0.78 at v2 — treat as unknown until measured; do not spin either outcome.
4. **FLOOR = ENGINE FEATURE (hard floor, FLAT TAIL):** ev_final = max(ev, floor×draftval), ND-only, .45/.35/.28/.21/.13/.09/.05 yrs 1–7+; saves-list prints every board (visibility rule: mispricings stay visible, never silently clamped). B5 alarm→feature amendment commits in D7. D6 evidence: flat tail saves 51 (+1287; Haynes +4, Hopper +48), derived tail rejected (27 saves, vets excluded). Distribution pricing (U21-4 spec, paper-only pending D7 ASK-5iii primary-source check) is the PRINCIPLED SUCCESSOR — Luke understands values will change again when it lands.
5. **STALENESS CAP FIX — GO (design read first, M3 discipline).** The Gothard mechanism: ev()'s stalled branch (el≥3 AND ns≤1) overwrites with 0.25×draftval — counts seasons, never asks WHEN. Engine's honest Gothard read ~1790 (his 70.2 avg ≈ replacement 70.9). Luke's calibration: "probably fine, a touch rich, happy to leave it" — no artificial shaving. He requested BOTH lists: current cap victims with before/after + who the new rule still catches (true ghosts). D7 ASK 4 delivers as a design read; wires NOTHING; joins a later candidate only on his endorsement.

## §3 — D7 RETURN: YOUR SCRUTINY SCRIPT (your first heavy ingest)
1. ASK 1: v2 on its own branch; cB DELETED not disabled, with obituary entry; M3 byte-exact at f=1; floor pure-lower-bound re-verified (0 lowered, 0 non-ND).
2. ASK 2: three amendments with Luke's words quoted verbatim in CHANGELOG; A3 note carries the knife-edge language; A2 untouched at 0.90.
3. ASK 3: **the Rozee ratio is the headline number** — vs 0.75, genuinely unknown (0.74–0.78 est). Per-gate deltas vs the D4 candidate board (11P/6F) each attributed to a named ruling. Spot-check ≥3 gate lines' arithmetic yourself. N-reconcile every table (universe 805 — but see the 807 clarification, ASK 5i). Gothard at v2 ≈ 355 (floor-saved; cap fix NOT in v2 — do not confuse).
4. ASK 4: judge the cap fix STRICTLY against its pre-registration (Gothard ~1790 neighbourhood · ghosts stay capped · zero outside-collateral · gates hold). It is a DESIGN READ — bounce any return that wired it.
5. ASK 5: the 160.2-vs-160.5 explanation must be arithmetic-coherent (a pure lower bound cannot lower an average — if the answer hand-waves, bounce); the 807-vs-805 line; the distributional-pricing nil/cite (answers Luke's open GBM question — relay it plainly); base effort rule registered.
6. Template: ≤30 lines + IN PLAIN TERMS · dual stamp · burn · per-task commits · PR (#10).
THEN assemble Luke's package — likely: cold-audit dispatch on v2 · the cap-fix design read (plain) · then his bake word. BAKE CHAIN (do not let steps skip): v2 verifies → COLD EXTERNAL AUDIT → Luke's explicit written bake word → merge canonical → ONE board re-cut.

## §4 — LUKE'S RULINGS LEDGER (this session, all in writing — the authority trail)
Growth law redefined + confirmed (turns 14–15 of his; supervisor turns 4–5) · Tsatas 1083 OK-preferred-lower → v2 expect <1083 (979 at old candidate) · floor-save endorsement names (Goater · B.Campbell · Bowes · O.Henry · Gibcus · Pedlar · Ugle-Hagan; Cleary/Hardeman tolerable; Haynes/Hopper utility vets) · GOTHARD headline worry → mechanism found (cap) → fix GO + ~1790 "touch rich, fine" · hard floor flat tail · overlay keep-two-cut-one · Rozee bar 0.80→0.75 · Curtis down-the-line (A2 ships red) · EFFORT AMENDMENT (turn 8): downward justification on every effort line — "why is this not one level lower?"; specificity test; binds supervisor + directives; in PROCESS_CHANGES §11 (base rule registration rides D7 5iv).

## §5 — SYMPTOM TAXONOMY + HYPOTHESIS REGISTER
FAMILIES: (1) staleness-cap victims — ratio exactly 0.250, single current season; Gothard + yr-3 cluster; fix in D7 ASK 4. (2) absence-punished young prospects — Gibcus (nil-2026 should cost a SLIVER, not 75% — Luke's correction stands), Rozee; owned by LTI workstream + M3; Luke's missed-season-by-age research calibrates the haircut. (3) ordinary players missing tail value — floor now, distribution pricing later. (4) Curtis residual — 0.822 vs 0.90, own thread, queued.
FALSIFIED (do not revive): per-cohort B1 as Luke's law · A8-missing-2× / ±20%-leak (was display ambiguity, fixed) · h-REPL-double · M3-reaches-0.80-at-honest-fE · LTI-machinery-exists · Gothard-is-interaction · floor-breaks-B1 · tail-variant-matters-below-yr8 · cB-mediocrity story. CONFIRMED: 2020-markdown-is-mediocrity (asc) · B5-blowout-is-asc (27/31) · supervisor's A2-at-minus-cB 0.822 · peak-recovery-on-cB-cut (156.6). OPEN: h-staleness-cap-lacks-recency-term (mechanism proven; fix deriving) · the 160.2 clamp-peak anomaly (ASK 5ii) · n=807 (ASK 5i).

## §6 — LUKE'S PLATE (as of handover)
Fire D7 (if not already) · paste its return to YOU with think-extra-deeply · nothing else owed. Coming to him after the D7 ingest, in plain terms: the Rozee number vs his 75 bar · the corrected scoreboard at v2 · the cap-victim lists (his explicit request: current catches with before/after + true ghosts under the new rule) · the cold-audit dispatch · then his bake word. LATER: cap-fix endorsement → joins a v3 or post-bake per his pacing · M3/LTI interplay · Curtis thread.

## §7 — PATH TO SIGN-OFF (objective = the growth law, AS REDEFINED — aggregate average)
(1) D7 returns → v2 verified + cap design read. (2) COLD AUDIT at v2 → Luke's bake word → merge canonical → ONE re-cut (Luke finally trades true values). (3) Cap fix endorsement → wire (v3 or post-bake). (4) GAMES-RAMP job (B6 + A12 Travaglia + 10g seam). (5) LTI design (haircut by age + win-now view; register in repo; feasibility confirmed — proj_from_peak is per-season). (6) C1/C2 baseline → PVC stage (A13/A14 activate · draftval re-bases · 1.19× revisit · B5 generating-rule re-derivation · MSD/SSP pick-equivalents — note price+floor are ONE shared object). (7) Distribution pricing (the q90–97 tail, replaces the floor stopgap) · position/dev-template · hygiene → ship when the suite is green.

## §8 — STANDING RULES DELTA vs rev92 (everything in rev92 §2–§3 still binds; these are NEW or CORRECTED)
- EFFORT downward-justification amendment (see §4) — apply to your own drafting too.
- MACHINERY-DEPTH: breached twice this session (turn-15 plate items written above decision-depth; Luke said "I don't understand" twice). Test every plate item in league-manager language BEFORE sending: player names and values yes; gate IDs/ratios in brackets only.
- Directive-2 pattern (Luke supplies the decisive objection) fired FOUR more times this session: the B1 law itself · Gothard · Gibcus over-grading · the floor-semantics misread ("saved by the floor" assumed a hard floor that didn't exist — always state which semantics a mechanism has before logging a ruling on it).
- Verbatim payloads: anything the build must commit verbatim gets EMBEDDED in the directive between markers (the D4 register-miss lesson; D5 fixed it).
- Build chats never watched/reused post-return (a build offered; declined on discipline).

## §9 — FILES LUKE SEEDS YOU WITH
`supervisor_KICKOFF_2026-07-02_v7.md` (first) · THIS rev93 (second) · `DECISIONS.md` · `LUKES_OPERATING_CARD.md` · `LTI_REGISTER_2026-07-02.md` · `D7_relay_v1.md`. Everything else lives in the repo/PRs; if a load-bearing fact isn't in these files, ASK LUKE — do not invent. Mint your own notepads + revs from turn one; DECISIONS.md is yours immediately; per-turn notepad + doc sends every turn, unprompted.
