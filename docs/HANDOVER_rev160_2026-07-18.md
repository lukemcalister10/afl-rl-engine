# HANDOVER — rev160 · 2026-07-18 · supersedes rev159
### rev160 (item 368): THE SEAT-13 MID-LADDER REFRESH (owner-requested). The v2.11 chapter is
### BUILD-COMPLETE; the cold audit is IN FLIGHT (five shards, owner-couriered). This doc is the
### rotation insurance: an incoming seat continues the ladder from here + the register (v340+).

## 0 — PINS (verify fresh; the pen moves main)
main = verify fresh (docs-only line; last pen commits ≈ `cc5da3c4`+). Tag **v2.10** (`d14efae`)
UNCHANGED — the baked, canonical, owner-usable engine. **THE FINAL CANDIDATE:** …→ `e4177c2` →
`a90052a` (five-migration, PR #111 line) → `cc58570` (Leg E, #113) → `7b6dfc5` (F1 phantom,
#115) → `bccc231` (F3 pedigree fix, #117) → `a9570cb5` (F4 calibration, #118) → **`15a9abd`
(F5 entrant layer, #119) = THE CHAPTER HEAD**, branch `claude/legf5-entrant-layer-conservation-
p4susl`. Report-only PRs: #110 (riders i–iii) · #112 (rider iv) · #114 (retro boards) · #116
(calibration investigation). Store `968de0c7` UNTOUCHED all chapter · curve `56dd7a7b`/payload
`89c14729` · engine at head: rl_model `cc626d7d` · _merged_recover `caf013e2` · SEASON_PROG 0.58.
Boards (single-thread, canonical-container labels): balanced `06d8af60` · +lens `d85901af` ·
RL_PVC2=0 `9829d01a` · +phantom `e613ca58`-era → F5 head recomputed at audit. Seals: presets
`c2e17c49` · φ `fd92b6fc` · r_pop `c62b5ee8` · s(age) `efe97ee3` · entrants `a17aafed`.

## 1 — WHERE THE CHAPTER STANDS
ALL BUILDS GREEN, prescreened (items 341–366): the five-consumer migration · the projection
lens (k=0 byte-exact; Rozee/Reid named rows) · riders (i)–(iv) (R_realized **207 [184–229]**
contains the owner's 220; R_curve 471; gross p1/p60 ≈5.5× in curve AND reality) · retro −1/−2
boards + bridge (771k/771k/752k, the curve clean of backward leakage — evidence at item 348) ·
phantom intake → **the v1.3 conservation construction (owner-worded): full entrant layer 83,538
sealed-from-history; ONE population carrier r_pop (§2.iii retired w/ obituary); the damper at
the _merged_recover consumption sites.** GATES (build-run; audit recomputing): conservation
−1.2%/+1.7% IN · roster-matched −2.6%/+0.3% IN · gradient monotone-correct · G-Y0 1.100%/2%.
**DOCTRINE THIS CHAPTER (CORE v2.9 fold QUEUED post-bake, operative NOW):** the GIT ENTRY block
in every directive (items 333/334) · THREADS=1 on every hash-bearing run (349) · FP board md5s =
container labels, equivalence is the invariant (347) · **THE BAKE-CONTAINER REPRODUCTION GATE**
(366: the bake instance must reproduce filed boards byte-exact or be swapped — the knife-edge
row finding, one player −95 cross-container, audit shard A hunts it) · measured-rate seals
never iterated against acceptance (356/365, proven in-code) · checkpoint-gated fix builds.

## 2 — THE LADDER (in flight; the ordered remainder)
**NOW: five audit shards** (seed `docs/AUDIT_SEED_15a9abd_2026-07-18.md`, leak-guard-proven item
367) in the owner's incognito chats → returns couriered → **RECONCILIATION** (supervisor:
audit-computed vs build-claimed, EVERY number; a mismatch = investigate before anything else) →
**the VIEWING-PACK RENDER** (read-only directive, authored at reconciliation: board+book+movers
re-rendered at 15a9abd incl. Rozee/Reid · per-lens totals w/ phantom split · retro bridge ·
riders · THE RATIFICATION LIST) → **the OWNER'S VIEWING** (sealed reads scored — DONE and held,
unseen; the ratification list: tail read stand/retire · R=207 · entrant structure `a17aafed` ·
φ `fd92b6fc` · r_pop `c62b5ee8` · presets `c2e17c49` · raw-pick benefit-of-doubt · knife-edge
brittleness note) → **his WRITTEN WORD** (cures guard-5 by the re-pin; carries RL_PVC2/RL_LEGE/
RL_LEGF INFRA_ALLOW) → **ONE bake per BAKE_RUNBOOK_v2_11_DRAFT → tag v2.11 → clean FF promote.**
POST-DELIVERY QUEUE: round-entry GO-LIVE (one owner word; + the weekly tracking loop 361/362 +
rank-Δ card element) · the v−R flat-derived toggle (363/364, R=derived 207) · backward-eval
Y-awareness fix (348) · stack-pinning option (347) · 326/327 incremental currency · 307/308 ·
the entry-pricing chapter IF the tail read stands · docs hygiene sweep (330, optional).

## 3 — STANDING CONDUCT (adds to rev159 §3)
Fetch-before-every-pen-append (the non-FF guard fired twice, correctly) · assert-before-write +
&&-chained filing ops (the turn-15 defect, cured, then the cure PROVEN firing at item 365) ·
returns without their SHA are incomplete (365, recovered by ls-remote) · read-only builds:
branch parent = main, provenance = stamps (338) · fences derived-from-jobs kept honest (three
322-misses then a strict-subset run at F1/F5).

## 4 — SEAT-13 CORRECTION LEDGER (owned; the pattern: measurement corrected the supervisor too)
(1) the half-shipped Leg-E filing (swallowed python failure; cured, cure later fired correctly).
(2) the rider-(iv) circularity premise (refuted by the build with code evidence; confirmed +
credited). (3) the item-351/352 backtest construction (roster-vs-league conflation — the owner's
conservation law exposed it; owned at 357). (4) the 357/358 entrant-value conflation (corrected
at 360). (5) the item-339 over-stuffed fence (an 8-h hang; cured by right-sizing + checkpoint
gating). Mechanical: recurring pen non-FF on stale local base (guard held both times).
