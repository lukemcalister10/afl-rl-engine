# DECISIONS.md — AFL RL engine — v74 · supersedes v73
### 2026-07-06. The single live tracker. For "how to operate" see KICKOFF v9; full state = HANDOVER rev114.
### v74 change (minimal): the PVC spec-header STAMP now rides the PVC-phase opener as step 1 (owner-confirmed); systemic
### guard strengthened (durable warnings ride on the ARTIFACT, not only a side tracker). Everything else = v73.

## PLAIN READ
The base is clean (phantom gate passed; Wave-0a dead-code strip in flight). Every owner decision is ruled. The injury study is a
DIRECTIONAL HYPOTHESIS (re-measure against our own aging curve). The parked PVC spec is a valid DESIGN foundation but STALE as an
executable — and its warning now rides ON THE SPEC FILE (stamped by the PVC opener), not just here, so it can't be folded away again.
No new decisions land on you.

## SETTLED — DO NOT RE-OPEN
1. **Injuries (3-arm LTI build, Chapter-3, greenfield):** (a) age with the season clock (freshness fix; site `rl_model.py:61-62`);
   (b) return-from-LTI haircut — **SHAPE IS A HYPOTHESIS TO VERIFY, not settled**: the `INJURY_RETURN_STUDY_v2` numbers DO NOT
   carry (narrative-grade netting, against the OLD aging curve, on a changed engine). Re-measure on the current engine NET OF our
   re-derived aging curve (coupled → lands in the derivation phase). Survives = two hypotheses (young = forfeited growth year not
   ceiling dent · recovery age-dependent + heterogeneous) + one RULE (measure net of aging or it double-counts). (c) present-season
   wipe, games-remaining PRORATED (graded proration net-new; `_b2hc`+`SEASON_PROG` = adjacent scaffolding only). GT = LTI_REGISTER.
2. **PVC:** historical end-of-year-one value of the TYPICAL player at that slot → tightly-banded smoothed curve, loclin at pick 1.
   Primary fit from the **2004 draft** (observable iff draft ≥ 2004; 2003 reference-only). See PVC-SPEC-STATUS below.
3. **Evidence-vs-prior blend:** gradual, evidence-weighted; convergence rate + form-conditioned aging decline MODEL-DERIVED. Pick-1 ≠ pick-50 after one game.
4. **Ruck:** derived beats the dialled 1.4× cap, WITH a cross-position sanity/replacement calibration (owner eyeballs the ruck $).
5. **Establishment-P:** dead. **DPP:** stripped (single positions).
6. **Governance:** frozen gates reviewed as needed (informal) · owner dials → visible `OWNER_OVERRIDES` layer.
7. **Viewer:** ONE unified pure-view app on the cockpit, ring-fenced fixture (PR #36). Real data plugs in last.
8. **Measuring stick:** beat-baseline gate DROPPED. Acceptance = owner board review + frozen PLAYER-ANCHORED gates + time. The
   lean **CHANGE-LEDGER** (descriptive "what moved" — not a score) is CONFIRMED.

## PVC DERIVATION SPEC v1 — STATUS  (in the live tracker; also to be stamped on the file itself)
Branch `claude/pvc-derivation-spec-17l4ly` (PR #20). Written pre-v2.5 (vs CONTROL `8aed420a` / v2.4 `7c199a1f`). Assessed
2026-07-06 from the RETURN SUMMARY, not the 281-line file (report-only; a line-by-line re-stamp is a build job when PVC opens).
**Verdict: valid DESIGN foundation, STALE as an executable. Do NOT run P0–P8 as-is.**
- **CARRY (design core, board-independent):** estimand + methodology — one fitted object over log-pick × position × draft-age on
  the locked ruler, busts in at 0, two-part establishment×value, named censoring/era/re-entry estimators, kernel eff-n≥35,
  pooling tree, channel-split book ruling, abort criteria, P0–P8 shape, the 11-entry decision register.
- **SUPERSEDED — already corrected:** cohort start 2006 → **2004** (SETTLED #2). Gains sample.
- **CONTAMINATED — RE-STAMP on v2.5 before building:** every number read the OLD inflated board — ruck-cap 1.73 (live 1.4 is a
  stopgap), the V0 reconciliation target, all v2.3/v2.4 baselines, any pick prices.
- **COORDINATE RE-CHECK on v2.5:** code archaeology (`SCALE_PVC` / pick exchange-rate, pick-1=3000, `PICKEQ`, `build_pvc_v34`)
  mapped against the OLD engine; the strips moved or may have DELETED some.
- **OWNER FORKS — NOT re-opened:** the 11 "decision-needed" calls are mostly SETTLED (year-one-value ruling) or de-escalated
  (OD-9 batch); the only live one (re-level V0 → re-derive retention+floor) already lives in the integration wave. Don't relay as fresh forks.
- **ACTION (owner-confirmed 2026-07-06):** the PVC-phase opener is a BUILD job whose FIRST step **stamps the spec file's header**
  — *"v1 / pre-v2.5 · valid design core · do NOT run as-is · re-stamp numbers + re-check coordinates on v2.5 first"* — so the
  warning travels WITH the artifact. Then it re-stamps the numbers + re-checks coordinates on v2.5, THEN Opus fits. Not urgent (derivation-phase item).

## OPEN — ACTION ORDER
1. **WAVE-0a (in flight):** DPP dead-code strip, zero-value-delta proof; resolves the local-`main` drift in its preamble.
2. **WAVE-0b (next):** F1/F2 identity/parity gates + change-ledger instrumentation.
3. **RE-FIT WAVES:** measurements (7 confirm-with-a-number; injury %s RE-MEASURED net of aging) → derivations (young-slope ·
   **PVC — opener stamps the spec header + re-stamps numbers + coordinate-checks FIRST, then fits on the 2004 window** ·
   ruck+calibration · aging decline) → ONE integration candidate (retention+floor once; injury meshed with aging, no double-count — FABLE spot) → re-bake.
4. **Viewer track (parallel, non-urgent).**
5. **Docs owed (non-urgent):** ROADMAP → next version (mirror injury-hypothesis wording + PVC re-stamp precondition); correct the
   OVERHAUL_SPEC (PR #37) "all LIVE" inventory at a docs pass; a KICKOFF convention for the systemic guard below at the next KICKOFF touch.

## SUPERSEDED / CLOSED (do not resurrect)
Phantom/credit/DPP gate (RESOLVED) · OVERHAUL_SPEC "all LIVE" inventory (STALE) · beat-baseline gate (DROPPED) · injury haircut as
a "settled shape" (now a hypothesis) · DPP forward-eligibility · establishment-P · F1/F2 bugs (Wave-0b gates carry the fix) ·
Kako history (restored) · OD-2/3/5 + the PVC spec's 11-fork register as "open decisions."

## PROVENANCE
- VERIFIED (owner-seen / re-runnable): v2.5 board + hashes + guards; EVID1/2/3; viewer pure-view + ring-fence.
- BUILD-REPORTED, read-only (cited, not independently re-run): the phantom-gate verdicts + LTI inventory; the PVC-spec-v1 status.
- REPORT-ONLY, do NOT build on without re-measuring: the injury study (`INJURY_RETURN_STUDY_v2` — directional hypothesis).
- FLAG, resolve in the next write job's preamble: local-`main` divergence (`f4a4d34`/`fddab37`); honest board `origin/main@7bc5726`.

## STANDING CONVENTIONS (KICKOFF v9 §3B) + SYSTEMIC GUARD (this thread's lesson)
Effort ladder + mode · build does ALL git (owner: tag-push/branch-del/promote-main) · time estimate + confirm + actual · mode vs
scope separate · returns end "in plain terms" · feed any synthesis the DECIDED-STATE · run the "is this still open?" test on DECISIONS.
**NEW GUARD (from the PVC-status loss):** (1) a warning that must survive rides ON THE ARTIFACT (a header stamp travels with the
file; a side-tracker/delta gets archived and compressed). (2) When folding a delta into a new doc version, DIFF the delta against
the new version — don't trust the compression to carry everything. (This guard would have caught the dropped PVC flags.)
