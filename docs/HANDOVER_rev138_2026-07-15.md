# HANDOVER — rev138 · 2026-07-15 · supersedes rev137
### To the INCOMING FABLE SEAT, from the outgoing Fable seat, at the rotation seam it requested.
### Read after 00_MANIFEST + CORE. **The register is the durable log — trust it over this summary.**
### rev137 and the seat-5/6 letter are ARCHIVED; everything load-bearing from them is either filed
### (laws → CONSTRAINTS v1.13 · process → CORE v2.6/v2.7 · facts → register) or folded below.

## FIRST ACTS (CORE v2.7, BINDING ORDER)
Freshness check FIRST — full-URL `ls-remote` · register header · docs/ list · PK vs repo DOC BY DOC — then
file the handed pack. **PK behind on ANY doc ⇒ HALT and tell the owner.**

## STATE (verify live; cite your own fetches)
- Pack: manifest **v4.20** · CORE **v2.7** · DECISIONS **v101** · CONSTRAINTS **v1.13** + acceptance_v1_13.json ·
  SSI v1.3 (now at `docs/`) · **register — READ ITS OWN HEADER** (never trust a version pinned here).
- Tag of record **v2.9 = `9f8ae76`** · board `81e48293` · **pick 1 = 3000, permanent numéraire law.**
- **MAIN CI IS RED AND THAT IS EXPECTED** — main lacks the determinism fix; it goes green when the candidate
  line merges/promotes, on the owner's word only. **Do not "fix" main.**
- **THE CANDIDATE LINE (one straight history):** `addef03` (q97m freeze) → **PR #82** `e7d980eb` (Fix 1 +
  absence; board `e6a8e6ef`) → **PR #83** `a2a06c7` (determinism fix; board **`800bf461`**) → **the R100.11
  REWORK** (directive issued at this seam; base = `a2a06c7` STRICT; the owner fires it).
  ⚠ **PR #81's commits are NOT ancestors of this line** (merge-base checked, item 116) — reconcile its
  disposition (likely: close as superseded, the #76 pattern) BEFORE any merge word.
- **ITEM-74 (the bake blocker, CONSTRAINTS PART 7): LEG 1 SETTLED VIA THE FIX ROAD** — A2 proven AT THE HEAD:
  the AMD runner's log prints board `800bf461` == the box (supervisor-read from the run log itself, item 115).
  No tolerance ruling needed. **LEG 2 (the reconcile) folds into the chapter ladder** — hardware-independence
  makes any machine the environment of record, so **incognito cold audits are valid instruments again.**
- **PRESCREENS DONE, both independently re-verified by this seat** (items 111/114 + journals): #82 PASS with
  notes (Wilmot −693 owner-flag · Buller +4 unexplained · Smith raw −899 · env dials unpinned — all folded
  into the rework directive) · #83 PASS with notes (mechanism MEASURED at `par_build.py:70`; item 106
  superseded in place; two justified fence deviations; #82's stale panel pin caught and fixed).
- **R100.11's TWO OPEN ASSUMPTIONS (owner has NOT ruled):** gap-length magnitude (measure first, then rule) ·
  the U-curve <20 clamp. **PARKED. The rework must not resolve them.**

## YOUR QUEUE, IN ORDER
1. **Prescreen the rework return** when the owner couriers it — against `acceptance_v1_13.json`, raw board
   moves, three narrowest margins, and the three named rows (Smith · Buller · Wilmot).
2. **The FULL ladder on the line's final head:** cold audit (valid again) folding LEG 2's reconcile — every
   guard, anchor, the flattery census (**123 / +19,168 — verified twice**) and the discontinuity census
   (`3ca74f3`) re-measured on the build of record and reconciled; any mover → register → owner views board +
   book → his written word → bake → promote main (goes green).
3. **The design chapter, standing order:**
   - **THE EVIDENCE WEIGHT `w`** — the biggest prize. One continuous object dissolving the 10-game bar, the
     nqual ramp, the PROVEN_N cliff and the exposure regime. T5 measured its shape (trust saturates 40–70
     games; pedigree fades to ≈0.11, CI [0.04,0.17] excludes zero). ⚠ **The four regimes replace TOGETHER or
     not at all** — a prorated bar alone broke A8/Tsatas (smoothed the counter, left the cliff).
   - **THE IMPROVER FIXES** — wire **L-SYMMETRY** · measure the S_AGE 30+ residual (**L-SAGE-FADE**: nobody
     has ever tested whether the zero is real) · make **`_eo` two-directional** (kill the `min()`, KEEP the
     term — it is the only anti-flattery mechanism in the engine).
   - **THE PVC RE-DERIVATION** — pair 3 (sanders 10.9% above bont, owner reads bont above) and
     English-vs-Briggs (3.0× above replacement for 1.4× the money) are ONE pricing defect, at BASE, before
     machinery. ⚠ **G-Y0's recorded fix direction is INVERTED-STALE — re-derive the CURE, not just numbers.**
   - **COMPLETENESS ARCHITECTURE (item 67)** — FIVE worked cases: three decorative pins · boot_guard's
     silent-skip and pin-length holes · `par_build.py` unpinned (self-declared, item 114).
   - Flattery decay — a symptom of the improver work, not a chapter.
   - **R100.1 remains PROPOSED** — the owner handed the frame to this seat lineage; adopt, replace or discard
     on the evidence. (Its known hole: it cannot explain English, the strongest finding.)

## HOW TO WORK WITH THE OWNER (the empirical record, folded from the letter)
His reads are ground truth until HE waives them — he found the pricing defect twice from the board without
reading code, and killed two wrong supervisor mechanisms with one-sentence arithmetic. **Give him numbers,
never theories**; symmetric options with a recommendation; DO:/WHY: asks; plain-read openings; no
over-production ("97% for 50% is ok"). **The one repeating seat failure (30 ledger items, three seats): a
plausible mechanism written down as a measured fact. RUN THE COMMAND.** Provenance-tag every carried claim
(owner-seen / re-runnable / report-only); never silently upgrade. **Owner-instructed 2026-07-15: prior seats'
judgements are hypotheses; their re-run figures keep their tags. Verify before building.**

## CONTEXT ECONOMY (owner-agreed at this seam — the reason this rotation happened)
- **Chat = plain read + the decisions the owner needs. Detail lives in the register and your per-turn journal
  (MANDATORY, crash insurance).** No dumps.
- Verify in FEW targeted commands; grep files, never read wholesale; trim outputs.
- PENDING OWNER RULINGS from the seam discussion: P1 (slim the seam pack to Tier-0 — his PK curation) ·
  P3 (a Tier-3 build committing standing orientation/prescreen scripts). Fold into CORE when ruled.

## RESIDUAL RISK MAP (if CI ever reds again on the fixed line)
The out-of-fence `np.average` site (never surfaced) · `_iso_dec` at `_merged_recover.py:801`. Look there
FIRST; neither is a failed fix.
