# DIRECTIVE — LEG C RELAY: THE TRUE LINE + THE CURRENT-SEASON DPP LAW · 2026-07-17 · seat 10
### STATUS: **EXECUTED AND RETURNED (2026-07-17, head 6306378, PR #104) — HISTORICAL REFERENCE ONLY;
### DO NOT FIRE** (item 290/GPT-F2). Known text errata, superseded by the register: the header's
### "8/91" reads 8/90 (items 283/290); the job-2 fence was amended in-flight by the owner's Option 1
### (IN += forward_valuation/distribution_pricing.py:v_at_peak, item 281). Original status line:
### fires on the owner's paste. This ONE build closes items 270–275: the topology cure,
### the owner's Accept waiver, the §1b CURRENT-SEASON DPP LAW (owner-confirmed), the Driscoll
### register correction (100% MID), the obsolete future==present guard, the 8/91 fut-label
### defect, and the owner-law deliverables Leg C honestly skipped. THE ONE STORE WRITER.
### The quarantined 2b64630 is a CONTENT REFERENCE ONLY — never a base, never merged.

## EFFORT: High · MODE: auto — PLAN committed FIRST; ONE MINI-CHECKPOINT after the wiring
## commit (the new §1b site + the guard update + the fut-label fix): HALT ≤5 lines with the
## diff SHA for the supervisor's prescreen — the owner is present; the wait is minutes. Then on
## PROCEED the full pipeline runs unattended. TIME: 3–5 h. Confirm; flag >2×/<½×; report actual.

## THE FIRST COMMANDS (item 270 standing law — BEFORE anything else, printed in the PLAN):
## git fetch origin claude/legb-selection-wire-s010-ho4vl8
## git checkout -b <job-branch> 273463e
## git merge-base --is-ancestor 273463e HEAD || exit 1
## "Content moved to match" is NEVER conformance. A wrong base = HALT, not improvisation.

## BASE PIN — full-URL ls-remote against https://github.com/lukemcalister10/afl-rl-engine.git
- **Engine/store base — STRICT: `273463e`** (the prescreened Leg-B selection head; board
  f2f077b2; store b1fd0bce). Docs base: main AT OR AFTER the SHA in the owner's paste,
  docs/-only diff. Docs are FETCHED (the corrected register + spec v1.3 live there), never merged.

## FEED
docs/SPEC_PVC_FLEX_CHAPTER_v1_2026-07-16.md (**header v1.3 — §1 the future blend law + §1b THE
CURRENT-SEASON DPP LAW, both verbatim-binding**) · docs/inputs/AFFL_Future_Positioning.csv
(**CORRECTED at issue: Driscoll 100% MID — blend rows now NINETY, measured**) ·
docs/acceptance_v1_20.json (assert the JSON: leg_c_remeasure · earned gate · census) · register
items 265/266/272/273/274/275 · the quarantined reference `2b64630` (content only) · this directive.

## THE JOB
1. **RE-INGEST from the corrected register** (validate-or-halt per the v1.1 rules: RUCK≡RUC
   normalization; stable-ID resolution ONLY — item 269): the PRIMARY-FUTURE diff recomputed
   (expected ELEVEN, the item-269 list) + the 4-write rider + the **NINETY** blend rows.
2. **THE WIRING COMMIT** (then the mini-checkpoint HALT): (a) the §1b YEAR-0 DPP site — post-
   collapse eligibilities (K-X drops every G — R105.1), remaining-season bar = min of the dual
   pair, level path keyed to present_position, banked component untouched, SEASON_PROG-scaled;
   (b) the futblend years-1+ site re-applied from the verified content; (c) **RL_FLEX gates ALL
   of Leg C incl. §1b** — =0 ⇒ the 273463e board `f2f077b2` BYTE-EXACT; (d) the obsolete
   future==present self-test (one_source_selftest lines ~146–148) REPLACED with the flex-era
   invariants (future in vocab · ≤1 alternate · blend params register-consistent) — a GUARD
   CHANGE, listed by name for the cold audit; (e) the fut-label fix: the board's stream array
   carries the TRUE alternate label regardless of the bar comparison + a per-row assertion
   (board labels == store primary/alternate on all 90).
3. **ON PROCEED — the full pipeline:** store writes → derived regeneration → expected_boot
   re-pin (same commit) → the FULL battery: A/B chain (RL_FLEX=0 ⇒ f2f077b2; +RL_UNCOMP=0 ⇒
   8d90c9ac) · suite GREEN (no red-by-design lines remain) · frozen β · **G-COHORT vs the FULL
   BAND both sides (floor 1.08 · cap 1.30 · ideal 1.15–1.25)** · E/B raw parts · census ·
   pool Δ · net ΣΔ · **the all-804 SINCERITY ledger (ranks; Bontempelli named; SCAR-up-rank-down
   named)** · A-PAIRS pair 2 (±15%) + pair 3 · **the earned-component gate with the item-272
   TWO-ROW NAMED WAIVER (Carroll, Emmett) wired as scored-never-flagged; any OTHER negative
   row = HALT** · the attribution vs `f2f077b2` in stages (+writes · +blend · **+§1b year-0
   DPP**) with per-player deltas — Petracca/Sheezel/Jagga Smith named rows — and the Driscoll
   delta vs the quarantined reference explained (register correction, expected).
4. NOTE: the quarantined board `b4c67bb6` will NOT reproduce (the Driscoll correction + §1b
   both move values) — a mismatch there is EXPECTED, not a halt; the attribution explains it.
5. Candidate PR on the true line; number in the return.

## FENCE
IN: the register-derived store writes · the three engine sites named in job 2 · the guard
update · derived + re-pins · session artifacts `session_2026-07-17/legc_relay/` · the PR.
OUT (touch = HALT): any other store field/row · Leg-B's map/dial/constants · docs/ · ui ·
config manifest · the pick curve · any re-tune. SILENCE IS A RED. Scope growth = a NEW
directive (S2).

## RETURN
≤30 lines + plain close: branch · head · PR · FIRST-COMMANDS ancestry line · new board md5 ·
A/B chain · suite verdict · β · y-band both sides · E/B raw · Bontempelli & Petracca year-0
movement · pairs · sincerity-fail count (named) · earned-gate (waived rows scored) · net ΣΔ ·
actual time.
