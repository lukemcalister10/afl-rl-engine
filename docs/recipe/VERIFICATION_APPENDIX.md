# VERIFICATION APPENDIX — code-level audit trail for THE RECIPE and NAMED FINDINGS

2026-08-05 · **This file is NEVER given to a rebuilder.** It exists so every recipe line can be checked against the code. The recipe is brandless and code-free by design; this appendix is neither.

---

## §1 Method

Extraction was performed by three agents from the pinned redesign tree (working tree `wt-lens`; store `engine/rl_after/rl_model_data.json`, md5 `81d2470440a80f72afea4405e94338c5`, 2,651 records; plus the `panel279` tree for the curve/lens derivation harness). Two independent adversarial verification passes followed: every step was opened against its cited code, every formula re-read from source rather than from the citations files, and a fresh field census of the store was taken. The supervisor then re-ran the deciding figures independently: the stale-games recount (569 mismatched rows = 548 + 21), the gamma dead-text check (the import-time force-set to 1.0 runs before any signature can be computed), the walk-forward board-path grep (no reassignment of the board-path switch anywhere in the teaching emitter), and the channel/derivation reproductions. Verdict vocabulary: CONFIRMED / WRONG / INCOMPLETE per recipe line; STANDS / REFUTED / RECLASSIFY per finding. Every WRONG or INCOMPLETE line was rewritten in the final recipe from the verifier's correction text; every CONFIRMED line was kept verbatim.

---

## §2 Citations — corrected where verification corrected them

Lines touched at verification are marked "[corrected at verification]".

### §2.1 Citations for LIST 1 — THE STORE

All paths relative to the read-only working tree root:
`/tmp/claude-0/-home-user-afl-rl-engine/857170f9-1a0a-5fff-ab5e-32b13cff3f0e/scratchpad/wt-lens/`

The store file is `engine/rl_after/rl_model_data.json` (md5 `81d2470440a80f72afea4405e94338c5`, verified with md5sum; matches the `source_md5` in `data/rl_build/rl_app_data.json.srcmd5` and the "store" pin cited across `docs/evidence/`). Structure verified by direct JSON parse: a flat list of 2,651 dicts; key frequencies and value sets enumerated by script.

1. Store identity/single source: `engine/rl_after/single_source.py:5,21-24` (SOURCE_NAME='rl_model_data.json'); load site `engine/rl_after/rl_model.py:29`. Boot checksum assertion: `boot_guard.py:9-21,131-145`; pin lives in `data/expected_boot.json` ('store'); short pin 81d24704 appears in `data/rl_build/rl_app_data.json.srcmd5` and `docs/evidence/rehearsal_290_2026-07-31/L6_convergence/pass2_matrix.json:3`. Derived-artifact stamping: `engine/rl_after/single_source.py:5-24`; `engine/rl_after/ingestion/round_apply.py` docstring step 6.
2. player: `engine/rl_after/rl_model.py:162,1387`; `engine/rl_after/_merged_recover.py:67-73` (L4 tripwire by name); `engine/rl_after/id_resolver.py`; display `engine/rl_after/rl_export.py:284`.
3. key: `engine/rl_after/rl_model.py:721` (`_dkey`); `engine/rl_after/_merged_recover.py:124` (injury register join); `engine/rl_after/rl_export.py:181,218,284,294-296`; overrides join `engine/rl_after/owner_overrides.py`; `tools/owner/lti_repin.py` docstring ("Name resolution is BY KEY, never by label").
4. stable_player_id: `engine/rl_after/id_resolver.py:37,69,79,100,106`; `engine/rl_after/rl_model.py:162`; active-pool definition `engine/rl_after/ingestion/README.md` ("rows with a durable stable_player_id that are not _retired (804 today)"). Count 804 verified by field census.
5. year: `engine/rl_after/rl_model.py:191,192,195,251,261,283`. Range 2003–2026 verified by census.
6. pick: `engine/rl_after/rl_model.py:214` (`effpk`), `:265-267` (ND 1–64 on-curve vs pool). Range 1–80 / 283 nulls verified by census. Database-universe numbering comment: `rl_model.py:241-250`. [corrected at verification] The store's per-stream per-year pick sequences are all gapless 1..n (verified for ND/RD/MSD, every year); the gapped ends survive only in `national_draft_last_pick.json` — see item 31.
7. type: `engine/rl_after/rl_model.py:192,195,251,262-281`; `engine/rl_after/_merged_recover.py:58-60` (MSD training-pool exclusion); PICKLESS `rl_model.py:179`. Value set {ND,RD,MSD,UNR,IRE,SSP,PDA,PDN,PDS} + counts verified by census; 'PSD' branch `rl_model.py:274-277` (zero rows).
8. drafted_position: `engine/rl_after/rl_model.py:32,42`.
9. present_position: `engine/rl_after/rl_model.py:33,43-44` (`_pos_now`); DPP-error check `:164-165,173-174`. Note: since item 271 Addendum 4 the year-0 bar reads the `eligibilities` column first (`_decl_bar` `:86`); present_position is the fallback and the gfut fallback axis.
10. future_position: `engine/rl_after/rl_model.py:34,45` (`_futpos`), `:110-113` (gfut); "future == present in THIS build" comment `:36-37`. [corrected at verification] The comment is contradicted by the data: 341 of 2,651 rows have `future_position != present_position` (340 retired, e.g. Olli Hotton SF/SD, plus ONE active: Oskar Baker, MID present / SF future, `_retired`=false); bnow==gfut does NOT hold on those rows (materially: back-board pricing of the 340 retired rows, and Oskar Baker's live row). The draft's "Verified: all rows equal" was false.
11. eligibilities: `engine/rl_after/rl_model.py:83-86,98-105,139-152,158-176`. Present on exactly the 804 non-retired rows (census); cross-class data-error combos = {KPD,SF},{KPF,SD},{RUCK,SF},{RUCK,SD}.
12. alternate_position: `engine/rl_after/rl_model.py:49-50` (`_altpos`), `:115-138` (futblend/futstreams). [corrected at verification] 87 players carry a non-null alternate stream (census); the 90 is the `:48` code comment only.
13. p_dual_stream: `engine/rl_after/rl_model.py:49-51` (percent /100), `:124-128` (q fraction on the lower-REPL bar). 85 float + 2 int values (census).
14. games (top-level): `engine/rl_after/rl_model.py:493` (`clamp(p['games']/130.0,0.30,0.85)`). Mismatch vs scoring sum on 569 rows: supervisor-confirmed (finding g-1 / #323).
15. scoring rows (year/avg/games/pos): `engine/rl_after/rl_model.py:229-236,410-415,434-480,497-500,101-104`; `engine/rl_after/_merged_recover.py:53,61-62`; `engine/rl_after/pgrid.py`; `engine/forward_valuation/conditional_prior.py`, `distribution_pricing.py`, `par_build.py`. 11,264 rows, sub-keys {year,avg,games,pos} verified by census; slash-duals in 16 observed pos labels.
16. _by: `engine/rl_after/rl_model.py:186,191` (fallback `p['year']-18`; 302 explicit nulls, census-confirmed).
17. _bd: `engine/forward_valuation/build_peak_model_v4.py:42,141-148` (age_source REAL_DATE). Present on 848 rows (census); repo-wide grep: only this builder reads it.
18. _retired: `engine/rl_after/rl_model.py:714,1329-1338`; `engine/rl_after/_merged_recover.py:1060,1785,1870`. 1,847 true (census).
19. _last_listed: `engine/rl_after/rl_model.py:715,1315-1317,1329`; `engine/rl_after/_merged_recover.py:1060`. 13 rows, values 2024×3/2025×10 (census).
20. _force_active: `engine/rl_after/rl_model.py:716`. 63 rows (census). [corrected at verification] 19 of the 63 flagged rows DO have scoring rows, all with played games (2026 debutants — 19 MSD-class plus PDA/SSP, e.g. Xavier Bamert 7 games); entry years span 2015–2026 (2015×1, 2018×3, 2022×4, 2023×10, 2024×12, 2025×19, 2026×14); only 47/63 actually need the flag to pass `active()`.
21. _has26: `engine/rl_after/rl_model.py:718`. 626 true (census); 27 stale-false rows — finding g-1.
22. _pickless: `engine/rl_after/_merged_recover.py:1025,1785`; `engine/rl_after/rl_model.py:1236` (written at runtime for SSP re-entry); board columns `engine/rl_after/s4_matrix_M1v7.py:84,87`. Exactly the 283 pick-null rows = SSP+IRE+UNR+PDA+PDN+PDS (census cross-tab).
23. _pvc_exclude: `engine/rl_after/rl_model.py:284-296`. 3 rows: Dylan Shiel, Jeremy Cameron, Adam Treloar (census).
24. _cat: `engine/rl_after/rl_export.py:300,346-353` (cat_of → four buckets); `engine/rl_after/s4_matrix_M1v7.py:87`. Not read by any pricing path (grep). Value set incl. one-offs verified by census.
25. _draft: `engine/rl_after/rl_export.py:300` (board 'draft' label only). Value set incl. the 1-row 'Post Draft - Academy' variant (census).
26. _draft_club: `engine/rl_after/rl_export.py:300,402` (CAT_BY_CLUB on draft club).
27. afl_club: `engine/rl_after/rl_export.py:300`; `engine/rl_after/id_resolver.py:80` (club VETO, "two-Max-Kings" comment); `engine/rl_after/ingestion/dry_run_proof.py:38`.
28. affl_team: `engine/rl_after/ingestion/round_movers.py:279,305-308,598,620`; `ui/tools/extract_board_view.py:79,92,111,151` (joined FROM the store); not in board of record; "SEPARATE field, untouched" `engine/rl_after/rl_export.py:304` comment.
29. draft_stream / stream_year / stream_pick / pick_correction_note / _pick_source: zero grep hits in any live .py/.js/.mjs/.sh outside `session_*/`, `docs/`, `evidence/`, backups (repo-wide grep, independently re-run at verification). [corrected at verification] `draft_stream` collapses FIVE routes (IRE, UNR, PDA, PDN, PDS = 231 rows) into 'OTHER'; SSP (52) and MSD (106) keep their own codes — not "the six post-draft routes". `stream_pick` (2,368 rows) equals `pick` on every row where either is present and is null exactly where pick is null — byte-redundant, not merely unread. pick_correction_note 1,041 rows; _pick_source 8 rows.
30. season_state.json: `data/season_state.json` (season_year=2026, season_total_rounds=24, as_of_round=20, calendar_progress=0.83, exposure_pace=0.773); readers `engine/rl_after/rl_model.py:2-26,1011` (SEASON_PROG), `engine/rl_after/_merged_recover.py:6-30,106,1752` (SEASON_FE/M3_FE), `engine/forward_valuation/conditional_prior.py:86` (EXPO_F); bar proration `_merged_recover.py:101-106`; advanced by `engine/rl_after/ingestion/staged_apply.py:78,491-502,781`.
31. national_draft_last_pick.json: file `engine/rl_after/national_draft_last_pick.json` (23 years); loud row-count fallback `rl_model.py:255,259`. [corrected at verification] The "authoritative chaining offset" reading described retired machinery — the RD/PSD chaining onto the national end was DELETED (`rl_model.py:268-277`); the ONLY live consumer of `_NDC` is `rl_export.py:518` (MEDIAN of all years as the display offset `TYPEOFF={'ND':0,'RD':_medNDC}` for the debut projector's pick scale). The table now DISAGREES with the store's own last national ordinal in 4 of 23 years (2009: 67 store vs 68 table; 2010: 77 vs 93; 2011: 80 vs 89; 2019: 64 vs 65) — it records pre-renumber gapped database-universe ends (the ITEM 411 renumbering made the store gapless), so the `rl_model.py:241-250` comment ("The table value is the store's own MAX National ordinal per year") is stale.
32. params.json (PEAK, PEAK_AGE, AGE_CURVE): `engine/rl_after/rl_model.py:29,58-66` (`_smooth_tail` ≥1%/yr post-peak).
33. rl_passmark.json: `engine/rl_after/rl_model.py:29,58,183,359`; used `:216-227,370-372`. pm_pos 48 (= 8 bands × 6 positions), pm_band 8, bands 8, curves length 10, BAND_ANCHOR 8, BUST_BAND 8 (verified).
34. ycred_table.json: `engine/rl_after/_merged_recover.py:1003-1006` (HALT if absent; G0=46, grid_picks 90).
35. bust_prior_table.json: `engine/rl_after/rl_model.py:656`; 6 positions × picks 1–70 (inspected).
36. pvc_curve_v2.json: `engine/rl_after/rl_model.py:859,948,994` (adopted offline curve; curve exactly 1..64, pick1=3000 asserted `rl_model.py:981`; `pool_value` 234.3); pick_redenomination.json factor 1.0524: `engine/rl_after/rl_export.py:130-132`, `engine/rl_after/one_source_selftest.py:62-66` (applied as a DIVISOR: board v = round(ev/F)).
37. LTI register: `LTI_REGISTER.md` (repo root), schema+parser `engine/rl_after/lti_register.py:1-31,47-60` (L = 1 − min(g2026/22,1); `section` gates the return-HAIRCUT arm: A yes / B structurally absent); consumed `engine/rl_after/_merged_recover.py:108-131,1912`.
38. lti_return_table.json: `engine/rl_after/_merged_recover.py:1929-1932` (Part-2 HALT if absent); produced by `engine/rl_after/derive_lti_return.py:3`; file keys young_cut=27, cap=0.15, age_surface (inspected). [corrected at verification] The age surface stores the HAIRCUT h = 1 − gap_ratio/nongap_ratio (values 0–0.0909), not retained output; retained output = 1 − h.
39. owner_overrides.json: `data/owner_overrides.json`; applier `engine/rl_after/owner_overrides.py:1-27` (display-only; never touches v); wired `engine/rl_after/rl_export.py:617-621`.
40. Frozen pickles: q97m `engine/rl_after/_merged_recover.py:74-93`; cm_400 `engine/rl_after/wire_redesign.py:9,14,57` (Guard-5-asserted cache; declared cold-bake retrain fallback WR:53-62 — see §2.2 step 13); v0surf `engine/rl_after/_merged_recover.py:1262-1282,1359`; peak_model_v4 `engine/rl_after/rl_model.py:655`. All pinned in `data/expected_boot.json`. "Never at run time" holds for the shipped/default config; a non-shipped kill-switch config still fits v0surf live by design.
41. Governance: `boot_guard.py:131-145`; `data/expected_boot.json`; `data/model_config.json` LTI-clock pin (`_merged_recover.py:120` + `ruling_config_check.py`); `data/release_contract.json` / `release_lineage.json` (sibling_repin targets).
42. Weekly score entry: `engine/rl_after/ingestion/round_entry.py` + CLI `tools/round_entry/round_entry.py`; law + active-pool + residue loop in `engine/rl_after/ingestion/README.md`; resolver normalisation `engine/rl_after/id_resolver.py` (`_norm`).
43. Weekly apply: merge arithmetic `engine/rl_after/ingestion/score_ingestor.py:255-266`; transaction `engine/rl_after/ingestion/staged_apply.py` docstring (STAGE→VALIDATE→ATOMIC SWAP; gate APPLY_DEFAULT=False + env, ships OFF); predecessor steps `engine/rl_after/ingestion/round_apply.py` docstring; participation ruling `engine/rl_after/ingestion/round_catchup.py` docstring "PARTICIPATION".
44. Multi-round catch-up: `engine/rl_after/ingestion/round_catchup.py` docstring (identity overrides `catchup_identity_overrides.json`).
45. Post-commit finalize: `engine/rl_after/ingestion/round_finalize.py` docstring (CORE_COMMITTED→FINALIZING→FINALIZED).
46. Sibling repin: `engine/rl_after/ingestion/sibling_repin.py` docstring (rebuild-and-compare; pins in lockstep).
47. Ownership apply: `ui/tools/ownership_store_apply.py` docstring (CSV → `affl_team`; header says "TEN TARGETS" while enumerating 11 lines 0–10 — the store write + ten dependent targets; the recipe's "ten dependent checksum pins" reading is the correct one, verified).
48. Injury register repin: `tools/owner/lti_repin.py` docstring (validate-or-halt; moves ONLY the register pin).
49. No standing draft/list-change feed: directory listing + grep over `tools/` and `ui/tools/`; ingestion README "store-write path absent by design"; migration example `session_2026-07-29/item262/migrate_positions.py`.
50. Position set + REPL: `engine/rl_after/rl_model.py:67,145,501` (MID 80.1, SD 78.3, RUCK 78.5, KPD 68.4, SF 70.9, KPF 66.8); PEAK/PEAK_AGE/AGE_CURVE `:58-59`; R105.1 `:150-151`; cross-class errors `:158-159`. Census value-sets match on every position field.
51. Route set + semantics: PICKLESS `rl_model.py:179`; pricing split `:197-215,260-281`; curve-teaching gate `:297-313`; MSD debut/cycle `:192,195`; MSD training-pool exclusion `_merged_recover.py:58-60`; hist cohort `:282-283`; PSD branch `:274-277` (zero rows); the 8 pre-season draftees typed RD with `_pick_source` "pre-season-draft-official (web-verified ...)" (census: Nankervis, Hall, Hibberd, Grimes, Rockliff, Bellchambers, Johnson, Betts). Route counts from the `type` census.

### §2.2 Citations for LIST 2 — PREDICTION

Path prefixes:
- WT  = /tmp/claude-0/-home-user-afl-rl-engine/857170f9-1a0a-5fff-ab5e-32b13cff3f0e/scratchpad/wt-lens
- MR  = WT/engine/rl_after/_merged_recover.py · RL = WT/engine/rl_after/rl_model.py · CP = WT/engine/forward_valuation/conditional_prior.py · DP = WT/engine/forward_valuation/distribution_pricing.py · DR = WT/engine/forward_valuation/dist_redesign.py · PRD = WT/engine/forward_valuation/par_redesign.py · PB = WT/engine/forward_valuation/par_build.py · WR = WT/engine/rl_after/wire_redesign.py · RE = WT/engine/rl_after/rl_export.py · EM = WT/session_2026-07-29/item271/emit_matrix_271.py · HP = /tmp/claude-0/-home-user-afl-rl-engine/857170f9-1a0a-5fff-ab5e-32b13cff3f0e/scratchpad/panel279/harness_pvc.py · EB = WT/docs/evidence/exec_306_zlaarm/basis/emit_structural_basis.py · RV = WT/session_2026-07-18/legf6/scripts/refit_v0surf.py · RQ = WT/refit_q97m.py

Scope note — entanglement: MR:454-466 (raw_ev returns a price), MR:381-387 (price6 = SCALE_DIST × Σ WQ6·v_at_peak), DP:260-283 (v_at_peak priced per quantile level), MR:909-931 (the per-year loop mixes level path and posval/discount/W(k)). ev() returns rounded value units: MR:1698-1740, MR:1783-1790.

1. Store load and row shape: RL:29; scoring rows read e.g. MR:303, RL:436, PB:45-48, EM:169-170; draft fields RL:41-51, RL:191-195; retirement/delist MR:1060, RL:714-715.
2. Soft 10-game qualification: MR:205-208 (`_EVW_Q0=11.0; _EVW_QW=1.1`, `_ev_qual` logistic). [corrected at verification] The hard **10**-bar does NOT prorate: `_nqual` (MR:168) is an unprorated `games>=10` even for the in-progress season; the D10 scope note MR:169-175 records a board-wide prorated 10-bar measured and REJECTED; the f1-credit channel that delivered first-season proration was deleted with the four-regime branch (obituary MR:562-571). The 6/14/22 bars do prorate (MR:1063, CP:117, MR:298); additionally: prorated 12-bar in `_kpf_LD` MR:840-841; deliberately unprorated G_ADQ=12 MR:104-105.
3. Era table: MR:51-55 (per-year mean of ≥6-game season averages 2009-2025; REF = mean of means); applied MR:1063/840/1695/2003; NOT applied in `_lvlcurr` MR:301-305.
4. Replacement bars: RL:501; −3 at pricing DR:35-39, applied MR:382-384; season/eligibility bar RL:83-109 (dual takes lower REPL at RL:85,104), collapse law RL:146-152.
5. Pick ladder vs pool: RL:197-215 (ND_CURVE_LAST=64, POOL_PICK=65), classification RL:260-281, PICKEQ purged RL:1154-1163.
6. Evidence dials: MR:203-210 (`_ev_qual`/`_ev_rec`/`_ev_est`), MR:293-296 (`_ev_pw` = gate×fade). [corrected at verification] The 0.11 "NEVER 0" floor applies to the FADE factor only; `_ev_pw` = gate×fade → 0 at E=0.
7. Season weights in level: MR:185-187 (`_wg(g)=g²/(g+5.8)`), MR:133-134 (LDECAY_G: KEY .40 / GEN .35 / MR .225), MR:301-305.
8. Level blend: blend MR:560-580 (`_coreM1`); `_est` MR:550-559 (DOWN_TOL=3.0, ramp /5, rise × S_AGE, shed × `_agemult2`); S_AGE MR:519-533 (0.9154@20, 0.3793@29, 0@30+); `_agemult`/`_fbump`/`_agemult2` MR:136-166; par prior MR:306-308 → PRD:68-71. [corrected at verification] The conservative level Lo is NOT CP:112-118: WR:52 rebinds `cp._lvl_eff = PR.lvl_par` BEFORE MR:167 saves `cp._lvl_eff_orig`, so Lo = PRD:110-113 `lvl_par` = par + (weighted level − par)·min(1, exposure/22) — PAR-CENTRED, regressing thin records toward the position/pick/tenure par (RL:434-480 `level_demo` is on the floor path, not here). Consequence: the par enters the level twice — inside Lo and again via `_ev_pw·_par_prior`.
9. Opportunity pull: MR:340-351 (`_upS`, `_eo`), MR:588-596 (`_inferM1`; RL_EO2 default ON → two-directional).
10. Absence: MR:629-714 (`_ABS_EFF` age curve /75, cap 0.20; fade (g+5.8)/(g²+g+5.8) MR:711; shortfall-only netting MR:712-713); bind MR:715.
11. Band model: training CP:143-162 (debut ≤2021, one row per (player,Y), GBR quantile Q=[.10,.30,.50,.70,.90], 400 trees; T1 excision CP:154-155); target CP:56-63; inference CP:164-167 called from MR:368; frozen load WR:42-63. [corrected at verification] The level-slot feature at training (PR.retrain PRD:124-125; and q97m's X built at MR:57-62 after WR:52) is par-centred `lvl_par`; at live inference it is the M1/absence-refined level (MR:628, MR:715); CP:112-118's "reliability-shrunk" `_lvl_eff` is never the bound feature on this path.
12. Ceiling model: MR:74-93 (frozen q97m), RQ:40-41 (quantile α=0.97, 200 trees), floored MR:369; WQ6 MR:94; age compression `_v7` MR:597-612 (asc [20,22,24,27]→[1,.76,.58,.40] on the excess over the MEDIAN bb[2]; retention φ=(clip((lcr−4)/26))·min(nq,2)/2·0.6), applied MR:621-627 (real players only).
13. Train/serve asymmetry: MR:99, MR:628, WR:52, PRD:110-113 + PRD:124-125; q97m has no fit path (MR:84-92 HALT). [corrected at verification] cm carries a cold-bake fallback (WR:53-62) that SILENTLY retrains and writes the cache whenever the pickle is absent — no env gate, no HALT, unlike q97m; "never refit at build" is false for cm on a cache miss.
14. Level path: RL:581-599 (`proj_from_peak`); DELTAS/frac RL:502-503; PEAK_AGE RL:58; current-level floor `cur` = `level_now` RL:496; break at age>38 / frac<0.42.
15. Pricing in the same loop: RL:594-598 and live W4 copy MR:909-931; capt bar 105 RL:380; d=0.14 RL:540; −3 REPL drop DR:35-39 applied MR:382-384; W(k) MR:893-907 (context MR:849-892, OVPX depths MR:790); KPF/KPD ×1.05; runway·elite·PMAX=0.25; WQ6=0.18×5+0.10 MR:94 dotted at MR:386; SCALE_DIST=1.0 DP:48; per-quantile pricing DP:260-283.
16. Demonstrated floor: RL:600-627 (`prod_floor`, H=clamp((40−a)/3,1,3)) and proven-player copy MR:939-968 (duplicate-loop hazard MR:947-953). [corrected at verification] Both copies roll `lev = cur·min(1, frac(ag,pa)/frac(a,pa))` — the fixed DELTAS fraction-of-peak table of step 14 (ratio-normalised, capped at 1), per-position only through PEAK_AGE; AGE_CURVE (RL:481-495) is used only inside `_dev_advance`/`level_now`, an identity on the now board (RL:489) that matters only under the forward lens.
17. Availability: MR:108-131 (`_fEy`→1.0, `_avail_hc`=L), register application MR:1908-1975, k=0 haircut MR:923 and RL:593,619; register source WT/engine/rl_after/lti_register.py. [corrected at verification] Return-season haircut gates: proven only (`_nqual>=4`, MR:1937 — young/speculative ship 0), Section-A names only (MR:1935), board path only (`_BOARD_PATH` at MR:924 — the walk-forward book is availability-free).
18. Pedigree pole: MR:454-466; wage MR:461 (RUCK 0), tfade MR:462, expgate MR:297-300 + MR:463; pole prices MR:390-395 (`par_pole` + hand-set `_SCALE` dict); recover curve MR:94 + MR:388; par surface PRD:68-71 → PB:150-232 (taught from ND/RD 2003-2018 season rows ≥6 games, target `_lvl_wt` PB:100-101).
19. Un-compress map: MR:396-453 (`rho_out` d=0.25, `_uncomp_prod` log-blend, w=s·E·ramp), dials RL:523-529 (UNCOMP_S=0.10 owner-set), references MR:1863-1906 (conservation C[pos]). [corrected at verification] The blend applies to the CAPTAIN-FREE price pr0 (MA._CAPT_OFF pass, MR:434-437); the captaincy increment delta=pr−pr0 is added back UNBLENDED (MR:439, 453). The stale MR:405 "INERT by default" comment is superseded by RL:527 — the map is live.
20. Youth re-rating credit: MR:969-1040 (`_ycred_mult`; sat/played blend s=min(g/6,1); clip≥0; leak-free trailing MR:986-989; HALT MR:1003-1005; KPF ×0.92 MR:1038; scope ND/RD non-pickless MR:1025), applied MR:1041-1045; LTI clock advance MR:1010-1022.
21. Pick-order guard: MR:467-497 (ISO per position over synthetic par players; `iso_eff` fade e^(−E_q/1.1); full at zero evidence); applied at MR:1057, 1165, 1199, 1665, 1674. "Frozen" = built once per build on original features before the MR:628 rebind (MR:365, 498-500), not a pinned pickle.
22. Year-zero surface: raw V0 = `raw_ev(p, debut−1)×iso_eff` MR:1192-1201; board value from frozen surface `v0_start` MR:1618-1624; lens fit MR:1393-1581 (anchor MR:1440-1446; kernels+shrinkage MR:1450-1476, constants MR:1246-1253; neutrality MR:1477-1513; bound [0.5,2.0] MR:1502,1513); frozen load/HALT MR:1271-1282, 1360-1389; signature MR:1287-1313; refit lane RV:40-63; basis HALT MR:1426-1432. [corrected at verification] The 1.4×PVC ruck prior cap (MR:1128-1138, 1183-1184) binds `_v0_raw` — the backtest/guard path, the curve-miss fallback, and the superseded pre-#306 free fit (MR:1582-1591) — NOT the shipped board lens value: the #306 lens fit never reads `_v0_raw` (MR:1486-1490) and `v0_start` returns the lens surface for every real ND row (MR:1618-1624); the separate ev-chain ruck ceiling is MR:1702-1704. "Capped first" described the superseded D14 fit comment (MR:1228-1230).
23. Sit-out: MR:1650-1655; retention surface R_SURF MR:1095-1125 (KPP floor O1 MR:1116-1124, board-path-only MR:1123); LAM_SIT MR:1126; trigger ns==0 MR:1066-1067/1729.
24. Staleness: MR:1727-1739; graded release table MR:1680-1697. [corrected at verification] The mediocre cap's base is LARGER than the stalled base — 0.45·max(0.3,1−0.08·(el−onset))·(1.5 key/ruc) vs 0.25·max(0.4,1−0.10·(el−onset))·(1.6 key/ruc) — and lacks the graded release (hard min, no `gr` term); "capped at a smaller fraction" was false.
25. In-progress blend: MR:1741-1767 (`_ev_m3`; clock-pin plumbing MR:716-748); season progress MR:106, RL:1011.
26. Floor and scrap: MR:1768-1790 (FLOOR_YRS {1:.45 … 6:.09}, tail .05; ND-only); delist scrap MR:1698-1700 (0.02·v0_start).
27. Forward views: RE:170-211 (`_LENS_FORM`=2026 at RE:187; young floor RE:201-207); form-anchor clock MR:211-236; L-symmetry damper MR:237-292 (sealed r_pop/s tables; band site MR:370-380, level_now site MR:944-945).
28. Walk-forward as-of paths: EM:98-120 (truncate ≤Y, `ASOF[(id,Y)]=ev(p,Y)`); board-path switch left ON: `_BOARD_PATH=True` MR:1108-1111, no reassignment anywhere in EM (grep verified). Caveat: EM emits the 271 matrix (which taught the shipped curve); the LENS basis reads the 279 matrix (EB:38), whose emitter is absent from the audited tree — verified only through its meta (store 6b9d00a7, v0surf b781ed253bff, v0surf_frozen false, same schema/force-majeure fields).
29. Career teaching value: HP:60-77 (`_es=max(0,1−(pw−0.11)/0.89)`; realised_at=Σe·v/Σe), HP:80-87 (never-established → 0.0), HP:33/44-47 (QUAL_GAMES=6); pw carried for years 1-6 only, later seasons reuse the year-6 weight (HP:73, EM:141-145).
30. Actuarial completion + counted fallback: HP:106-144 (strata (pos,tenure), MIN_STRATUM=20; both fallback classes valued at r['v0'] = the engine's v0_start, EM:163; counts asserted HP:141-142); stamped EB:104/120.
31. Population and hygiene: HP:91-102 (picks 1-64, classes 2004-2022, N=1197 asserted); force-majeure slide EM:52-95 (Boyd/McCartin); basis artifact EB:42-47/93/104 (pins, +4.7%..+8.4% optimism stamp, provenance); consumed as declared input MR:1402-1439.
32. Ruled curve fit: HP:166-193 (Gaussian kernel over log-pick to eff-n≥35), PAVA + strict descent (see §2.3 steps 28-31); adopted artifact loaded, never refit: MR:1828-1850, RL:993-1010, RE:133-165. [corrected at verification] The SHIPPED pvc_curve_v2.json stamp records derivation by derive_271.py from per_entrant_271.json (stage-B: explicit hard pin(1)=3000, time-kernel tau=0.12 fit_year0, window pool [2004,2024] + priors [2006,2020], nd_curve_rows 1325, ×0.6 blend ceiling, min(n_pos/200,1) prior blend; `_working_substrate_note` = the #290 stop-point substrate) — NOT the #279 basis (per_entrant_279_vor, classes 2004-2022, N=1197) that teaches the lens. HP's kernel_raw/pin_and_check is the #279 panel harness, not the shipped curve's derivation. Also: HP:166-182's "effective n" is plain Σw, not (Σw)²/Σw².
33. The dependency: pedigree term MR:293-296 + MR:580; V0 anchors MR:1650-1655, 1727-1739, 1783-1790; counted fallback HP:127-135 + EB:104; lens self-reference bar MR:1400-1408; board-path V0 during teaching walk-forward MR:1108-1111 + EM (see 28). [corrected at verification] 0.11 floors the FADE factor only; pw = gate×fade ≈ 0 at zero evidence (Eq=0.1 → pw≈0.03), peaks ≈0.35–0.37 near Eq≈1, settles at 0.11 for established players — 0.11 is the high-evidence asymptote, not a lower bound on pw.

Supporting: SCALE anchor RL:628-706 (v4 load RL:655), RL:730-734, RL:929-932; GAMMA default RL:504; ONE-price ruling WR:1-6, RE:170-172.

### §2.3 Citations for LIST 3 — VALUATION

Roots: ENG = /tmp/claude-0/-home-user-afl-rl-engine/857170f9-1a0a-5fff-ab5e-32b13cff3f0e/scratchpad/wt-lens · PAN = /tmp/claude-0/-home-user-afl-rl-engine/857170f9-1a0a-5fff-ab5e-32b13cff3f0e/scratchpad/panel279
Shorthand: MR = ENG/engine/rl_after/_merged_recover.py · RM = ENG/engine/rl_after/rl_model.py · RE = ENG/engine/rl_after/rl_export.py · DP = ENG/engine/forward_valuation/distribution_pricing.py · DR = ENG/engine/forward_valuation/dist_redesign.py · CP = ENG/engine/forward_valuation/conditional_prior.py · PRD = ENG/engine/forward_valuation/par_redesign.py · WR = ENG/engine/rl_after/wire_redesign.py · OV = ENG/engine/rl_after/owner_overrides.py · RV = ENG/session_2026-07-18/legf6/scripts/refit_v0surf.py · HP = PAN/harness_pvc.py · FC = PAN/fitter_control.py · PN = PAN/pooled_numeraire.py · LV = PAN/levels_pool_msd_ssp.py · V2 = ENG/engine/rl_after/pvc_curve_v2.json · RD6 = ENG/engine/rl_after/pick_redenomination.json · SS = ENG/data/season_state.json · MC = ENG/data/model_config.json · RP = ENG/run_panel.sh

Step 1 — the six-node band. Quintiles Q=[0.10,0.30,0.50,0.70,0.90]: CP:45 [corrected at verification — cited as CP:44, off by one]; band build CP:164-167; sixth node MR:366-369; q97m frozen-load MR:84-93; v7 age-taper on the tail node for real players MR:597-627 (`asc` 1.0→0.40 by 27; form retention ×0.6); band collapse pin MR:1046-1058; demonstrated current level RM:434-480, RM:496.
Step 2 — path from peak inside the pricer. RM:581-599; DELTAS/frac RM:502-503; W4 rebind MR:909-932.
Step 3 — captaincy premium. RM:373-400 (BAR=105, M=109.5, W=1.85, G=1.00; closed-form softplus integral); applied RM:594 / MR:925.
Step 4 — replacement netting + soft-hinge. REPL RM:501; −3 drop DR:35-40 applied MR:381-387; soft-hinge RM:505,507 (S_SH=3); year-0 vs future bar RM:592-596, g0 RM:705, future dual blend RM:115-128. [corrected at verification] The §1b year-0 dual-eligibility blend (DP:260-277, `y0dpp_bar` RM:166-176, SEASON_PROG RM:1011) is SUPERSEDED AND INERT on the live Now board: RM:72-82 — the year-0 bar takes the dual pair's LOWER REPL at source (`bnow` → `_fit_bar` → `_decl_bar` → `_elig_bar` = min-REPL of the collapsed column, RM:83-109), so `y0dpp_bar` returns None for every Now-board row; the blend can fire only on backward as-of years where a season-row bar exceeds the column bar.
Step 5 — 21×, discount, position multipliers. RM:595-596; LENS RM:540-551 (bal 0.14 under RL_DIAL14 default; now 0.34; fut 0.05; postures 0.18/0.10 STRAWMAN); KPF/KPD ×1.05 RM:597; runway×elite×PMAX=0.25 RM:598/RM:180; W4 copy MR:927-930.
Step 6 — γ and the currency scale. Binding read RM:504 (default '1.0'); SCALE=7000/ref^γ RM:730-732; val RM:734; BOARD_FACTOR RM:929-932; numeraire loader + halts RM:859-904; artifact numeraire block V2 (head 3068.4647, s 0.9776876364261254, pin 3000 — verified). γ SET sites: MC (RL_GAMMA "1.0"), RP:25-26 (gate mode + export 1.0); stale 0.85 pins ENG/ship_gates_check.py:64, ENG/engine/rl_after/verify_anchors.py:8, ENG/verify_restore.sh:8, ENG/verify/d15/_d15_common.py:21, ENG/session_2026-07-21/final_integration/tools/config_inventory.py:102; FV standalone setdefaults '1.0': CP:40, DP:28, DR:24, par_build.py:28, build_cohort_book.py:5, build_peak_model_v4.py:18. γ OTHER READ sites: MR:1287-1306 (signature payload, dead '0.85' default — see §4.2 F5 / §4.3 F1), RE:570, _engine_block_v23.js:40,95, build_peak_model_v4.py:153,176. Historical fork incident: docs/evidence/rehearsal_290_2026-07-31/L5_census/manifest_vs_default.py:9-13.
Step 7 — demonstrated floor. RM:600-627; proven-player copy MR:939-968; consumed DP:283.
Step 8 — band weights and the production price. MR:94 (WQ6 sums to exactly 1.0), MR:381-387, DP:48 (SCALE_DIST=1.0), DP:260-283.
Step 9 — proven-player W(k). MR:849-892 (`_w4_ctx`), MR:893-907 (`_w4_W`, floor 0.05), constants MR:780-822, applied MR:926-928 and MR:966. [corrected at verification] `ovpx` (deep-pick compression, W4_OVPX_D SF/SD/MID, ramp picks 38→46, MR:884-891, 905-906) is set only in the sub-proven `else` branch (MR:886-891) and the partial-proven KPF case (MR:883-885) — a fully proven (n≥4) non-KPF player NEVER carries it; and a partial-proven KPF (2≤nqual<4) with top-tier sustained demonstration earns the near-year credit scaled c=n/4 (MR:863-868, 876, 899-900).
Step 10 — young re-rating credit. MR:998-1040 (table halt :1001-1009; injured-clock advance :1010-1022; s=min(g/6,1); φ=(1−g/46)²; KPF ×0.92), applied MR:1041-1045; G0=46 MR:1000,1007; ND/RD non-pickless scope MR:1025.
Step 11 — pedigree pole recovery. MR:454-466; pole MR:390-395 (`_SCALE` = MID 1.19, SF 0.93, KPF 0.95, SD 1.08, KPD 1.05, RUCK 1.13); recovery MR:94 + MR:388; wage :461, tfade :462, expgate :297-300; par surface PRD:68-71 (fitted in par_build.py).
Step 12 — un-compress map. MR:396-453; dials RM:523-530 (UNCOMP_S_DEFAULT=0.10 owner-set, default on); reference/conservation MR:1863-1906.
Step 13 — pick-order guard. MR:485-497; applied at the value-path sites MR:1057, 1165, 1199, 1665, 1674. [corrected at verification] Five value-path sites, not six: the "LEG A site 6/6" tag is a diagnostic in ENG/engine/rl_after/_ov_angleA.py:39, not on the value path.
Step 14 — veteran read-through. MR:203-296, MR:560-580, MR:933-938, MR:461-462; step 9.
Step 15 — ruck caps. MR:1128-1184 (RUC_PRIOR_CAP=1.4 :1138; RUC_CEIL_HEAD=0.80, RUC_CEIL_REFPK=72, RUC_YRH=0.35 :1156-1159; ceiling grid :1161-1169; headroom :1170-1174); bind condition MR:1702-1704. [corrected at verification] `_ruc_prior_cap` is ALSO applied unconditionally to every real ruck's `_v0_raw` (MR:1183-1184, 1202-1205), feeding v0_start and every start-value consumer; the D14 curve is FITTED on capped V0s (MR:1228-1229); the no-production headroom is draft-age-keyed (`_ruc_head_v0`) while the production ceiling's is as-of-age (`_ruc_head_mult`) — same function, different age key.
Step 16 — KPF compress. MR:1712-1724; `_kpf_LD` MR:823-848; dials MR:791,819-822.
Step 17 — delist scrap. MR:1060, MR:1699-1700 (round(0.02×v0_start)).
Step 18 — sit-out. MR:1650-1655; R_SURF MR:1104-1125 (O1 KPP floor, board-path-only MR:1123); LAM_SIT MR:1126; gate ns==0 MR:1729-1730; continuity at λ=1.
Step 19 — staleness caps. MR:1731-1739 (stalled 0.25 base, ×1.6 key/ruc; mediocre 0.45 base, ×1.5 key/ruc — the mediocre keyruc multiplier is 1.5, not 1.6); grade table MR:1680-1697; form-anchored tenure clock MR:1727.
Step 20 — rounding. MR:1740.
Step 21 — M3 mid-season blend. MR:1741-1767; clock-pin plumbing MR:716-748; season-state single source MR:6-30 + SS (calendar_progress 0.83 verified); fenced halt MR:8-29.
Step 22 — start-value floor. MR:1768-1790 (schedule {45,35,28,21,13,9}% + 5% tail; real-ND, non-delisted/retired/pickless scope).
Step 23 — availability layer. MR:106-131; register application + halts MR:1908-1975 (proven-only `_ret_hc_for` MR:1933-1940); haircuts consumed MR:923-924/:959 and RM:593/:619; non-register bit-parity by construction.
Step 24 — display re-base + parity. RE:132 (factor 1.0524 from RD6, verified), RE:177-181, order/ratio asserts RE:213-239 (<0.2% relative), parity gate RE:596-613 (eps=0, halts).
Step 25 — views. RE:182 backward; forward lens RE:185-189 + MR:367/:385; forward pedigree floor RE:201-207; F4 population damper MR:237-292 (applied at the two forward reads MR:372-379 and MR:944-945; s=0 at ages ≤22 up to 1 at 34+); ÷F on all view columns.
Step 26 — owner overrides. RE:615-633; OV:71-131 (adds only `ov`; `assert_presence` halts in gate/bake).
Step 27 — teaching values input. HP:22-33 (YR_LO 2004, CLASS_CUT 2022, PIN1 3000, ND_LAST 64, MIN_STRATUM 20), loader HP:91-102 (EXPECT_N=1197), structural_values HP:106-144.
Step 28 — the control fitter. HP:166-193; boundary FC:35-36, FC:57-67, FC:70-90, FC:93-113 (interior bit-identical, proof key FC:153); zones FC:20-22 (picks 1-2 and 51-64 on this matrix).
Step 29 — pooling, no override, strict descent. PN:35-50 (weighted PAVA, visible head block), PN:69-83 (NO pick-1 override; EPS=1e-3); superseded hard-set retained for comparison PN:56-66 / HP:213-228.
Step 30 — the global factor s. PN:75-77 (s=PIN1/pooled_head; scaled=fit×s), published PN:137-140; artifact numeraire block V2 (verified); player-side same-s law RM:908-932; coherence halts RM:886-903. [corrected at verification] The shipped ladder's CURVE block is the ITEM-271 stage-B derivation (derive_271.py carrying derive_pvc2.py "function for function": explicit hard pin(1)=3000, tau=0.12 time-kernel fit_year0, window pool 2004-2024, nd_curve_rows 1325, ladder_total 65925 — per the artifact's own `source`/`stamp` fields, verified) and its entries were NEVER rescaled by s (×s≈0.9777 would have moved the total ≈−2.2%); only the `numeraire` block is the #279 ruled economy; on this tree s is applied to the PLAYER side alone (RM:931).
Step 31 — integers + strict descent + domain. PN:78-83; domain law RM:197-215; `_split_ladder` RM:957-992. [corrected at verification] The shipped national ladder is the stage-B artifact of step 30's note, not this pipeline's output; the panel emits fitter_*.json ladders (HP:243-262) that do not ship.
Step 32 — pick board prices. RE:135-155 (pick1==3000 assert RE:146; strict-descent assert RE:149-150), unconditional numeraire halt RE:156-165, picks at face value RE:584 (no ÷F), phantom future picks RE:525-537 & :767-788 (64 per lens at exact PVC[n]), posture discounts RE:589-595 (ONE application).
Step 33 — the pool. RM:197-215, RM:260-281, PICKEQ retirement RM:1154-1163, pool value on ladder MR:1838-1850 / RE:136-138, draftval MR:1806, curve-teaching gate RM:297-332.
Step 34 — pool/MSD/SSP level rule. LV:35-71, LV:74-95 (honest mean, busts at zero, 95% interval), stream subsets LV:102-126, shared-table disclosure LV:110-117, ×s reporting PN:241-250, "nothing is ruled today" LV:105-106, stop-point pool value 234.3 with re-measure-at-L6 note (verified in V2), adoption pending LV:18.
Step 35 — curve × lens. MR:1393-1522 (laws :1410-1422; constants :1246-1253; anchor :1440-1446; barred self-reference :1394-1432; kernel + shrink :1450-1476; neutrality on APPLIED population :1477-1513, λ iterated 200× to worst ≤1e-12; surfaces :1514-1522); lookup :1607-1616; applied :1618; consumed by v0_start :1620-1624. Fit code executes only under the declared refit; normal builds load the frozen output.
Step 36 — freeze/signature. MR:1255-1313; frozen-load/halt/declared-refit MR:1359-1389; triple-fit note MR:1592-1599 (see §4.3 F6 correction: TWO signatures in the shipped pickle, not three); cross-host byte-assert MR:1523-1570; refit entry RV:1-136.
Step 37 — draftee through the machinery. Zero-evidence V0 MR:1192-1201 (E_q=0 ⇒ iso unfaded); board V0 := lens surface MR:1620-1623; pre-season hold MR:1072-1074 + MR:1125; young credit MR:977-985; floor/staleness/scrap anchors MR:1728/:1789/:1700. [corrected at verification] The applied par weight `_ev_pw` = gate×fade (MR:293-296) is ≈0 at zero evidence (gate E_q²/(E_q²+0.55²) ≈ 0), hump-shaped (max ≈0.37 near E_q≈0.8), residual 0.11; only the FADE factor runs 1→0.11; a zero-evidence draftee's level is carried by the conditional-prior band at draft features, not by a near-1 pedigree-par weight.
Step 38 — no-arbitrage enforcement map. ONE price RE:170-172; parity halt RE:596-613; numeraire halt RE:156-165; strict descent RE:149-150 + RM:983-985; two-sided scaling RM:859-932; sitter-never-gains MR:1095-1125 + assert MR:1644-1648; conservation REPORT-ONLY RE:539-559 and :789-804; schedule-keyed terms MR:462, MR:1779-1781, MR:1731-1738, RM:1085; anti-schedule prorations MR:1741-1767, CP:76-99, MR:100-131.

Citation-file notes carried from verification (non-blocking): CP:44 → actual CP:45 (Q list); the "six sites" iso tag lists five value-path sites (site 6/6 is a diagnostic in _ov_angleA.py:39); the MR:1305 comment's internal refs ("rl_model.py:504 and :866") are stale — the RL_PICK1 read is at RM:929.

---

## §3 Verdict tallies (from the three verification passes)

**Store (list 1):** inventory — 45 CONFIRMED · 1 WRONG (item 10) · 5 INCOMPLETE (items 12, 20, 29, 31, 38) · 0 UNVERIFIABLE. Findings — 13 STANDS (4 with corrections: 4, 6, 12, 13) · 1 REFUTED (finding 3) · 0 RECLASSIFY.

**Prediction (list 2):** steps — 22 CONFIRMED · 7 WRONG (2, 8, 16, 22, 24, 32, 33) · 4 INCOMPLETE (11, 13, 17, 19) · 0 UNVERIFIABLE. Findings — 5 STAND (F1, F2, F3, F4, F6 — F1 and F6 with mandatory corrections) · 1 REFUTED (F5).

**Valuation (list 3):** steps — 30 CONFIRMED · 4 WRONG (4, 9, 30, 37 — each wrong in one identified clause, remainder verified) · 4 INCOMPLETE (1, 15, 25, 31). Findings — F1 STANDS (subsequently downgraded by supervisor ruling to the merged hygiene note, FINDINGS h-1) · F2 STANDS · F3 STANDS · F4 STANDS · F5 RECLASSIFY (shouldn't-exist in its current unreproducible form) · F6 STANDS with one factual correction (two signatures, not three).

Supervisor rulings applied on top of the verdicts: store finding 3 dropped; prediction F5 and valuation F1 merged into one hygiene note; valuation F5 reclassified; the shipped-today vs ruled-method framing added at recipe steps L2:32 and L3:30–31.

---

## §4 Audit trail — the verification files, verbatim

The three adversarial verification files are reproduced below without alteration. They are the authority every correction in the final documents was drawn from.

### §4.1 Store verification (verbatim from verify_store.md)

# ADVERSARIAL VERIFICATION — recipe_store.md / citations_store.md / findings_store.md

Verifier: independent re-read of every cited file plus a fresh field census of
`engine/rl_after/rl_model_data.json` (md5 re-verified `81d2470440a80f72afea4405e94338c5`, 2,651 records).
All paths relative to `/tmp/claude-0/-home-user-afl-rl-engine/857170f9-1a0a-5fff-ab5e-32b13cff3f0e/scratchpad/wt-lens/`.
Per instruction, findings on the 569-row stale top-level `games` count and its use as the games/130
trust weight are supervisor-confirmed and only noted here.

## Inventory lines (recipe_store.md items 1–51)

1. CONFIRMED — md5 matches; pin in `data/expected_boot.json` ('store'); short pin in `data/rl_build/rl_app_data.json.srcmd5`; boot assertion `boot_guard.py:131-150`; stamping per `engine/rl_after/single_source.py:1-24` tiers.
2. CONFIRMED — `player` read at `rl_model.py:162,1387`, L4 tripwire `_merged_recover.py:67-73`, resolver, `rl_export.py`.
3. CONFIRMED — all 2,651 keys match `[a-z0-9-]` slug form; `joshua-kelly` exists; joins verified (`_AVAIL_STATE.get(p.get('key'))` `_merged_recover.py:124`; `owner_overrides.py` by player_key; lti_repin "BY KEY, never by label").
4. CONFIRMED — `stable_player_id` on exactly the 804 non-retired rows; resolver attaches by it (`id_resolver.py`); `_DPP_DATA_ERRORS` keyed on it (`rl_model.py:160-165`).
5. CONFIRMED — `year` 2003–2026 (census); reads at `rl_model.py:191,192,195,251,261,283`.
6. CONFIRMED — range 1–80, 283 nulls (census); `effpk` `rl_model.py:214`, split `:265-267`; database-universe law comment `:241-250`. Note: the store's per-stream per-year sequences are all gapless 1..n (verified for ND/RD/MSD, every year) — see item 31 and finding 4 for the table-vs-store divergence.
7. CONFIRMED — 9 codes with exact counts (census); `_ft/_grp/_eff/_pool` classification `rl_model.py:260-281`; PICKLESS `:179`; PSD branch `:274-277`.
8. CONFIRMED — `_p['pos']=_p['drafted_position']` `rl_model.py:42`; cohort axis per `:32,68`.
9. CONFIRMED — `_pos_now` `rl_model.py:43-44`; note (nuance, not an error): since item 271 Addendum 4 the year-0 bar reads the `eligibilities` column first (`_decl_bar` `:86`), present_position is the fallback and the gfut fallback axis — the recipe's own item 11 carries this.
10. WRONG — "In the current data it equals current position for every player" is false: 341 of 2,651 rows have `future_position != present_position` (340 retired, e.g. Olli Hotton SF/SD, plus ONE active player: Oskar Baker, MID present / SF future, `_retired`=false). The citation's "Verified: all rows equal" is not true of this store; the extractor evidently trusted the `rl_model.py:36` comment ("In THIS build future_position == present_position for every player"), which the data itself contradicts. Consequence: bnow==gfut does NOT hold for those rows (materially: back-board pricing of the 340 retired rows, and Oskar Baker's live row).
11. CONFIRMED — on exactly the 804 active rows, max 2 codes (census); `_collapse_elig` R105.1 `rl_model.py:146-152`; the four cross-class combos `:158-159` = {KPD,SF},{KPF,SD},{RUCK,SF},{RUCK,SD}; fall back to single-position via `y0dpp_bar`→None `:166-176`.
12. INCOMPLETE — mechanism confirmed (`rl_model.py:49-51,115-138`) but the count is wrong in the line: 87 players carry a non-null alternate stream, not 90 (the 90 is the `:48` code comment; the drafters' own finding 7 records the contradiction, yet the inventory line asserts 90 as fact).
13. CONFIRMED — percent /100 at `rl_model.py:51`; q fraction on the LOWER-REPL bar `:124-128`; 85 float + 2 int values (census).
14. CONFIRMED (supervisor-confirmed; not re-litigated) — `rl_model.py:493` `clamp(p['games']/130.0,0.30,0.85)`; top-level `games` stale on 569 rows (recount matches: 548 scoring>top, 21 reverse).
15. CONFIRMED — 11,264 rows, sub-keys {year,avg,games,pos} (census); slash-duals in 16 observed pos labels; live-season bar from the column vs closed-season row bar `rl_model.py:99-105`; readers as cited.
16. CONFIRMED — 302 explicit nulls (census); fallback `p['year']-18` `rl_model.py:191`.
17. CONFIRMED — `_bd` on 848 rows; repo-wide grep: only `engine/forward_valuation/build_peak_model_v4.py` reads it (age_source REAL_DATE, `:42` area, census block `:141-148`).
18. CONFIRMED — 1,847 retired (census); `active()` excludes `rl_model.py:714`; back-board recall `:1327-1339`.
19. CONFIRMED — 13 rows, values 2024×3/2025×10 (census); `rl_model.py:715,1315-1317`; `delisted()` `_merged_recover.py` ~:1060.
20. INCOMPLETE — flag and `active()` behaviour confirmed (`rl_model.py:716`, 63 rows), but the stated meaning is too narrow: 19 of the 63 flagged rows DO have scoring rows, all with played games (2026 debutants — 19 MSD-class plus PDA/SSP, e.g. Xavier Bamert 7 games); and entry years span 2015–2026 (2015×1, 2018×3, 2022×4, 2023×10, 2024×12, 2025×19, 2026×14), so it is not only "not-yet-debuted current draftees with no scoring rows at all". Only 47/63 actually need the flag to pass `active()`.
21. CONFIRMED — 626 true; read at `rl_model.py:718`; the 27 stale-false rows are finding 2.
22. CONFIRMED — exactly the 283 pick-null rows = SSP+IRE+UNR+PDA+PDN+PDS (census cross-tab); written at runtime for SSP re-entry `rl_model.py:1236`.
23. CONFIRMED — 3 rows (Shiel, Cameron, Treloar); curve-pool drop + slide-up `rl_model.py:284-296`; still valued/on board.
24. CONFIRMED — free text incl. one-offs (census); four buckets via `cat_of` `rl_export.py:346-353`; not read by any pricing path (grep: `_cat` appears only in export/book rows).
25. CONFIRMED — display only (`rl_export.py:300` 'draft'); value set incl. the 1-row 'Post Draft - Academy' variant (census).
26. CONFIRMED — `rl_export.py:300` club fallback, `:402` CAT_BY_CLUB on draft club.
27. CONFIRMED — board 'club' `rl_export.py:300`; resolver club VETO (`id_resolver.py`, "two-Max-Kings" comment); `dry_run_proof.py:38`.
28. CONFIRMED — read by `round_movers.py:279,305-308,598,620` and `extract_board_view.py:92,111,151` (joined FROM the store); not in board of record; "SEPARATE field, untouched" `rl_export.py` comment.
29. INCOMPLETE — the unread claim itself is fully CONFIRMED (independent repo-wide grep over all live .py/.js/.mjs/.sh excluding sessions/docs/evidence/backups: zero readers of `draft_stream`, `stream_year`, `stream_pick`, `pick_correction_note`, `_pick_source`; counts match). But the description of `draft_stream` is wrong: it collapses FIVE routes (IRE, UNR, PDA, PDN, PDS = 231 rows) into 'OTHER'; SSP (52) and MSD (106) keep their own codes — not "the six post-draft routes". Additional fact the line omits: `stream_pick` equals `pick` on every one of its 2,368 rows (and is null exactly where pick is null), i.e. it is byte-redundant, not merely unread.
30. CONFIRMED — `data/season_state.json` keys/values exact (2026/24/20/0.83/0.773); readers as cited (`SEASON_PROG` `rl_model.py:1011`, `SEASON_FE`/`M3_FE` `_merged_recover.py:106,~1752`, `EXPO_F` `conditional_prior.py:86` reads `exposure_pace`); bar proration `_merged_recover.py:101-106`; advanced by `staged_apply.py` (TARGET + derive at ~:487-505).
31. INCOMPLETE — file exists (23 years) and the loud row-count fallback is real (`rl_model.py:255,259`). Two corrections: (a) "the authoritative chaining offset" describes retired machinery — the RD/PSD chaining onto the national end was DELETED (`rl_model.py:268-277`); the ONLY live consumer of `_NDC` is `rl_export.py:518`, which takes the MEDIAN of all years as a display offset (`TYPEOFF={'ND':0,'RD':_medNDC}`) for the debut projector's pick scale; (b) the table now DISAGREES with the store's own last national ordinal in 4 of 23 years (store-max vs table: 2009 67 vs 68; 2010 77 vs 93; 2011 80 vs 89; 2019 64 vs 65) — the store was renumbered continuous/gapless under ITEM 411 (the 1,041 `pick_correction_note` rows) while the table still records the pre-renumber gapped database-universe ends, so the `rl_model.py:241-250` comment ("The table value is the store's own MAX National ordinal per year") is stale.
32. CONFIRMED — `params.json` PEAK/PEAK_AGE/AGE_CURVE for the 6 positions; `_smooth_tail` ≥1%/yr post-peak `rl_model.py:60-66`.
33. CONFIRMED — pm_pos 48 (=8 bands × 6 positions), pm_band 8, bands 8 ([1,3]...[49,99]), curves length 10 (seasons 1–10, `expected()` clamps s to 1..10), BAND_ANCHOR 8, BUST_BAND 8.
34. CONFIRMED — `ycred_table.json` G0=46, grid_picks 90 (1..90); HALT-if-absent `_merged_recover.py` (~:1002-1005); credit `_ycred_mult`.
35. CONFIRMED — 6 positions × picks 1–70; loaded `rl_model.py:656`.
36. CONFIRMED — `pvc_curve_v2.json` curve exactly 1..64, pick1=3000 (asserted `rl_model.py:981`), `pool_value` 234.3 = the single pool price; factor 1.0524 from `pick_redenomination.json` (note: applied as a DIVISOR — board v = round(ev/F), `rl_export.py:~130-177`, `one_source_selftest.py:~62-66`; "converting raw engine output into board points" is the right meaning, direction unstated).
37. CONFIRMED — schema `lti_register.py:10-21` exact; L = 1 − min(g2026/22,1); join by key. One wording slip: `section` gates the return-HAIRCUT arm (A yes / B "STRUCTURALLY ABSENT"), not a "return credit" — the line's own later clause says haircut.
38. INCOMPLETE — file/gating confirmed (`_merged_recover.py` Part-2 HALT; `derive_lti_return.py`; young_cut=27, cap=0.15). But the surface stores the HAIRCUT h = 1 − gap_ratio/nongap_ratio (age-indexed values 0–0.0909), not "expected retained output"; retained output is 1 − h. The parenthetical "(fraction of prior output; haircut capped at 15%)" half-corrects it, but the line's noun is the wrong quantity.
39. CONFIRMED — `owner_overrides.py`: adds `ov={factor, dispv=round(v*factor),...}`; `v` never touched; wired `rl_export.py:617-621`.
40. CONFIRMED — q97m (quantile-GBR, band[5], frozen `_merged_recover.py:74-93`), cm_400 (`wire_redesign.py` docstring: pinned Guard-5-asserted cache, authoritative, RandomForest forward chain), v0surf (config-signature-keyed frozen surface `_merged_recover.py:~1262-1290`), peak_model_v4 (`rl_model.py:655`); all pinned in `data/expected_boot.json`. Minor caveat: "never at run time" holds for the shipped/default config — a non-shipped kill-switch config still fits v0surf live by design, and cm has a declared cold-bake retrain fallback.
41. CONFIRMED — `boot_guard.py` (checkout store/pin coherence, halts before read), `data/expected_boot.json`, `data/model_config.json` LTI-clock pin (`_merged_recover.py:120` + `ruling_config_check.py`), `data/release_contract.json`/`release_lineage.json` (sibling_repin).
42. CONFIRMED — `round_entry.py` docstring (register item 305 law verbatim), README active pool "804 today", exact-match no fuzzy, residue confirm loop, snapshot-not-store.
43. CONFIRMED — merge arithmetic `score_ingestor.py:255-266`; STAGE→VALIDATE→ATOMIC SWAP + season-state advance + pin moves + ledger (`staged_apply.py`); listed-0-is-played / absent-is-DNP (`round_catchup.py` PARTICIPATION); gate ships OFF (APPLY_DEFAULT=False + env, proofs on scratch copies).
44. CONFIRMED — `round_catchup.py` docstring: one preflight + one approval, per-round sequential staged transactions, identity overrides sidecar.
45. CONFIRMED — `round_finalize.py`: derivatives re-derived from committed state; CORE_COMMITTED→FINALIZING→FINALIZED; writes no store field.
46. CONFIRMED — `sibling_repin.py`: build-and-compare, pins in lockstep; store/board-of-record fenced untouched.
47. CONFIRMED — `ownership_store_apply.py`: CSV→`affl_team`, atomic; the docstring header says "TEN TARGETS" while enumerating 11 lines 0–10 (the store write + ten dependent targets), so the recipe's "ten dependent checksum pins" reading is the correct one.
48. CONFIRMED — `lti_repin.py`: validate-or-halt against store keys; moves ONLY the register pin; never writes the store.
49. CONFIRMED — repo-wide search finds no intake/retire/delist feed; only store writers are the (gated) round lane and the ownership lane; ingestion README states the store-write path is absent by design; dated migration scripts live under `session_*/`.
50. CONFIRMED — REPL exactly {MID 80.1, SD 78.3, RUCK 78.5, KPD 68.4, SF 70.9, KPF 66.8} (`rl_model.py:501`); GRP/_ELIG_MAP identity sets; R105.1 `:150-151`; census value-sets match on every position field.
51. CONFIRMED — all 9 route counts exact; ND-only curve 1–64 with 65+ to pool (`:262-267`); pool teaches nothing (`_teaches_curve` `:313`); MSD debut-in-entry-year + cycle −1 (`:192,195`) and training-pool exclusion (`_merged_recover.py:58-60`); PSD branch zero rows; the 8 pre-season draftees typed RD with `_pick_source` "pre-season-draft-official (web-verified ...)" (census: Nankervis, Hall, Hibberd, Grimes, Rockliff, Bellchambers, Johnson, Betts).

## Findings (findings_store.md 1–14)

1. STANDS (supervisor-confirmed; noted only) — independent recount agrees exactly: 569 mismatched rows = 548 scoring-sum>top-level + 21 reverse; only the season row is updated by the merge; `rl_model.py:493` reads the top-level number.
2. STANDS — 27 rows re-verified by census (names match: Dylan Patterson, Beau Addinsall, Zane Peucker, ...); all carry a 2026 scoring row with `_has26` false; rescue disjunct `any(r['year']>=2024 ...)` at `rl_model.py:718` confirmed.
3. REFUTED — the scoring system IS named: `README.md:1` "afl-rl-engine — AFL SuperCoach RL valuation engine", `SHIP_GATES.md:1` "AFL SuperCoach RL engine", and inside the engine itself `engine/rl_after/rl_export.py:297` ("the SuperCoach toggle"). The store fields don't name it and "avg"'s unit label is implicit, but the claim "never named in the store or the engine" is false, and the brand did not need to be guessed: avg is SuperCoach points per game.
4. STANDS, with one subclaim corrected — the headline (database-universe ordinals; official count not the authority; a rebuilder importing official results diverges) is confirmed (`rl_model.py:241-250` + the 1,041 ITEM-411 correction notes). But "at 2010/2011 the max ordinal != row count" is FALSE of the shipped store: every stream-year pick sequence is gapless 1..n (max==count in all 23 ND years, verified). The gapped ends (2010=93, 2011=89) survive only in `national_draft_last_pick.json`, which consequently now disagrees with the store's own last ND ordinal in 4 years (2009/2010/2011/2019) — see inventory item 31.
5. STANDS — PSD branch `rl_model.py:274-277` live, zero PSD rows; the 8 pre-season draftees typed RD with web-verified `_pick_source` notes (census).
6. STANDS, with two corrections — unreadness re-verified independently (zero live readers). Corrections: `draft_stream` collapses five codes to 'OTHER' (231 = IRE+UNR+PDA+PDN+PDS), not six — SSP keeps its own code; and `stream_pick` is not just unread but fully redundant: it equals `pick` on all 2,368 rows where either is present.
7. STANDS — comment `rl_model.py:48` says 90; census says 87 non-null alternate_position+p_dual_stream pairs.
8. STANDS — 85 float / 2 int (90, 70) re-verified; `float()` coercion at `rl_model.py:51`.
9. STANDS — axes re-verified incl. the 2026-07-30 store-authoritative ruling (`ownership_store_apply.py`) and the resolver's afl_club-not-affl_team veto (`id_resolver.py`).
10. STANDS — census matches exactly ('Academy - Unmatched' 18, 'Priority (West Coast)' 1, 'Compensation (Campbell Brown)' 1; 'Post Draft - Academy' 1 vs 'Post-Draft - Academy' 50).
11. STANDS — 302 nulls; `by()` fallback `rl_model.py:191`; the fallback age also feeds `_v4_age`/hist cohort fits.
12. STANDS, with a count correction — SIX columns sit on exactly the 804 active rows (`afl_club`, `stable_player_id`, `affl_team`, `eligibilities`, `alternate_position`, `p_dual_stream`; verified identical row-set = the non-retired set), plus `_bd` at 848 overlapping 46 retirees — seven columns, not "eight". The two-schemas-in-one-list observation is confirmed.
13. STANDS, with corrections — the dedup code (`rl_model.py:721-728`) and ID-based ingest are real, but: the store's keys are currently globally unique (2,651/2,651 distinct — own census, corroborated by `ownership_store_apply.py`'s "2,651 of 2,651 distinct"); the two Bailey Williams sit under DISTINCT store names/keys (bailey-williams-wb / bailey-williams-wc, display names "Bailey Williams" / "Bailey J. Williams"), the collision was a FootyWire export display-name artifact; and the (round,score) override is round-scoped to R15–R19 and retired from R20 (`catchup_identity_overrides.json` retired_note: "ZERO display names in the store are shared by two active players"). So: name collisions in the FEED are the live risk; store-key collisions are hypothetical defence-in-depth.
14. STANDS — in-memory derivation `rl_model.py:41-51,260-281` confirmed; the SSP runtime overwrite at `:1230-1236` in fact also rewrites `type`, `year` and `_grp` for the four PRESENT_ID_OVERRIDES players (Perez, Hall-Kahan, McAndrew, Keane), i.e. the mutation is broader than the finding states — which strengthens, not weakens, the rebuild-trap point.

## Tallies

Inventory: 45 CONFIRMED, 1 WRONG (item 10), 5 INCOMPLETE (items 12, 20, 29, 31, 38), 0 UNVERIFIABLE.
Findings: 13 STANDS (4 of them with corrections: 4, 6, 12, 13), 1 REFUTED (finding 3), 0 RECLASSIFY.

### §4.2 Prediction verification (verbatim from verify_prediction.md)

# VERIFY — LIST 2: PREDICTION (adversarial pass)
Paths as in citations_prediction.md (MR/RL/CP/DP/DR/PRD/PB/WR/RE/EM/HP/EB/RV/RQ). Every cited file was opened; every formula below was read from source, not from the citations file.

## Step verdicts

1. CONFIRMED — RL:29 (store load), RL:41-51 (position columns), RL:191-195 (by/_age_at/debut), MR:1060 + RL:714-715 (retire/delist). Fields as stated.
2. WRONG — the soft bar is right (MR:205-208: logistic centre 11, width 1.1; the code's own "~4-game ramp 9->13"), and the 6/14/22 bars do prorate (MR:1063, CP:117, MR:298). But the claim that the hard **10**-bar prorates is false: `_nqual` (MR:168) is an unprorated `games>=10` for the in-progress season too; the D10 scope note MR:169-175 records that a board-wide prorated 10-bar was measured and REJECTED, and the f1-credit channel that delivered the first-season proration was deleted with the four-regime branch (obituary MR:562-571). The recipe repeats the MR:101-105 design comment that the code itself corrects. (Also unmentioned: the prorated 12-bar in `_kpf_LD` MR:840-841 and the deliberately unprorated G_ADQ=12, MR:104-105.)
3. CONFIRMED — MR:51-55 (per-year mean of >=6-game season averages 2009-2025, REF = mean of means); applied MR:1063/840/1695/2003; `_lvlcurr` MR:301-305 uses raw `a` (not era-adjusted), as stated.
4. CONFIRMED — RL:501 (REPL: MID 80.1 high, KPF 66.8 low; v3.3-derived + owner dial per the in-code comment), RL:83-109 (`_elig_bar`/`_fit_bar`, dual takes lower REPL), RL:146-152 (collapse law). The −3 belongs to step 15 and is there.
5. CONFIRMED — RL:197-215 (ND_CURVE_LAST=64, POOL_PICK=65), RL:260-281 (classification: ND 65+, RD, PSD, pickless all → pool), RL:1154-1163 (PICKEQ purged; order carries no value in the pool).
6. CONFIRMED — MR:203-210 (`_ev_qual` = Σ logistic((g−11)/1.1); `_ev_rec`=E²/(E²+0.55²); `_ev_est`=E³/(E³+3.6³)), MR:293-296 (`_ev_pw` = gate×(0.11+0.89·e^(−E/1.1))). Formulas exact. (Note for step 33: 0.11 floors the *fade* factor only; pw itself = gate×fade → 0 at E=0.)
7. CONFIRMED — MR:185-187 (w(g)=g²/(g+5.8)), MR:133-134 (LDECAY_G: KEY .40 / GEN .35 / MR .225), MR:301-305 (`_lvlcurr`).
8. WRONG — the blend arithmetic, tolerances, S_AGE (0.9154@20, 0.3793@29, 0@30+; MR:519-533), `_agemult2` (0.85@25→0.55@37 + up-only elder bump; MR:136-166) and the `_ev_pw·_par_prior` mix (MR:560-580) are all as stated. But the "conservative career level" Lo is misidentified: MR:49 runs `cm=W.build()`, and WR:52 rebinds `cp._lvl_eff = PR.lvl_par` **before** MR:167 saves `cp._lvl_eff_orig=cp._lvl_eff`. So Lo = PRD:110-113 `lvl_par` = **par + (weighted level − par)·min(1, exposure/22)** — a PAR-CENTRED level that regresses thin records toward the position/pick/tenure par, not CP:112-118's zero-shrunk level, and not "one-year spikes and dips regressed toward a robust baseline" (that machinery is RL:434-480 `level_demo`, which is on the floor path, not here). The citation (step 8: "conservative level = CP:112-118") is wrong for the same reason. Consequence the recipe misses: the par enters the level twice — inside Lo and again via `_ev_pw·_par_prior`.
9. CONFIRMED — MR:340-351 (`_upS`, `_eo` = clip((N−2)/4)·clip(games/(14·(N−1)))), MR:588-596 (`_inferM1`, T=max(_upS(best−(REPL−3),N), _lvlcurr); RL_EO2 default ON kills the min → two-directional).
10. CONFIRMED — MR:662-670 (`_ABS_EFF` age curve /75, cap 0.20), MR:711 (fade (g+5.8)/(g²+g+5.8)), MR:712-713 (gap-filled counterfactual, pen=max(0,Lb−Lng·(1−frac)) — shortfall only, never lifting), MR:715 (bound as the level fed forward). Gap definition (games==0 year after a played season, most recent only) MR:671-697 matches.
11. INCOMPLETE — training pool/target/rows/quantiles/trees all check (CP:143-162: debut≤2021, one row per (player,Y), Q=[.10,.30,.50,.70,.90], 400 trees; target CP:56-63 with busts→0/low; T1 excision CP:154-155; frozen load WR:42-63). But the feature tuple's "reliability-shrunk level" names the wrong binding: at training (PR.retrain, PRD:124-125; and q97m's X built at MR:57-62 after WR:52) the level slot is par-centred `lvl_par`; at live inference it is the M1/absence-refined level (MR:628, MR:715). CP:112-118's "reliability-shrunk" `_lvl_eff` is never the bound feature on this path. Step 13 states the asymmetry correctly; step 11 should not name the slot "reliability-shrunk".
12. CONFIRMED — MR:74-93 (frozen q97m), RQ:40-41 (quantile α=0.97, 200 trees), MR:369 (floored at b[4]), MR:597-612 (`_v7`: asc [20,22,24,27]→[1,.76,.58,.40] on the excess over the **median** bb[2]; demonstration-earned retention φ=(clip((lcr−4)/26))·min(nq,2)/2·0.6), MR:621-627 (real players only).
13. INCOMPLETE — the asymmetry claim is verified (MR:99, MR:628, WR:52, PRD:110-113) and q97m truly has no fit path (MR:84-92 HALT). But "never refit at build (refits are declared, gated)" is false for cm: WR:53-62 carries a cold-bake fallback that **silently retrains and writes the cache** whenever `/home/claude/cm_<trees>.pkl` is absent — no env gate, no HALT, unlike q97m. The recipe must carry that exception.
14. CONFIRMED — RL:581-599 (`proj_from_peak`: lev=lp·frac(age+k,pa); DELTAS RL:502 gives 1.0@peak, .58@−8, .34@+14; floors `ag<=pa` and `k==0` at cl; 18-year cap). The break at age>38 / frac<0.42 is omitted from the step text but recorded in the citation, so "up to 18 years" stands.
15. CONFIRMED — RL:594-598 / live copy MR:909-931 (base=lev+capt_prem, posval(base−REPL)·21/(1+d)^k), capt bar 105 (RL:380), d=0.14 (RL:540), −3 REPL drop (DR:35-39 applied MR:382-384), Wk (MR:893-907: c_near k∈[0,2]→credit, h_far from k=4; OVPX depths MR:790), KPF/KPD ×1.05, runway·elite·PMAX=0.25, WQ6=0.18×5+0.10 (MR:94) dotted at MR:386, SCALE_DIST=1.0 (DP:48).
16. WRONG — the floor exists as described in horizon (H=clamp((40−a)/3,1,3), RL:615-626 / MR:955-967) but it does **not** roll along "an empirical per-position age curve": both copies roll `lev = cur·min(1, frac(ag,pa)/frac(a,pa))` — the fixed DELTAS fraction-of-peak table of step 14 (ratio-normalised, capped at 1), per-position only through PEAK_AGE. The cited AGE_CURVE (RL:481-495) is used only inside `_dev_advance`/`level_now`, which is an identity on the now board (RL:489) and matters only under the forward lens.
17. INCOMPLETE — Part 1 verified (MR:108-131: `_fEy`→1.0, `_avail_hc`=L applied at k=0 MR:923/RL:593/619; register application MR:1908-1975). But the return-season haircut carries three load-bearing gates the step omits: it applies only to **proven** players (`_nqual>=4`, MR:1937 — young/speculative ship 0), only to Section-A names (MR:1935), and only on the **board path** (`_BOARD_PATH` at MR:924 — the walk-forward book is availability-free).
18. CONFIRMED — MR:454-466 (raw_ev: pr + wage·tfade·expgate·recover·max(0,po−pr); wage zero for RUCK, full ≤20 → 0 at 26; tfade 1.00→0.05 by tenure 5; recover 0.54→1.0 on RECX/RECY MR:94; perf=cp._lvl_wt), MR:390-395 (pole prices ×_SCALE hand-set dict), par surface PRD:68-71→PB (ND/RD-window 2003-2018, ≥6-game rows, target `_lvl_wt` at PB:100-101).
19. INCOMPLETE — MR:428-453 verified (v0p=pr0^(1−w)·t^w, w=s·E·ramp with s=0.10 owner-set RL:527, E=1−e^(−Eq/1.1), ramp=ρ/6; t=V_ref_b·ρ/ρ_den; C[pos] conservation MR:452/1891-1902; ρ decay 0.25 RL:525). Omitted and load-bearing for high scorers: the blend is applied to the **captain-free** price pr0 (MA._CAPT_OFF pass, MR:434-437), with the captaincy increment delta=pr−pr0 added back **unblended** (MR:439, 453). (The stale MR:405 "INERT by default" comment is superseded by RL:527; the recipe correctly treats the map as live.)
20. CONFIRMED — MR:1023-1040 (e′=e·(1+0.9·max(R,0)·(1−g/46)²); R by year/pos/log-pick with sat/played blend s=min(g/6,1); clip≥0; trailing leak-free; HALT if table absent MR:1003-1005; scope ND/RD non-pickless), applied MR:1041-1045; LTI clock advance MR:1019-1021. (KPF ×0.92 sub-trim is carried in the citations file, MR:1038.)
21. CONFIRMED — MR:485-497 (ISO per position over synthetic par players, isotonic non-increasing; multiplier itself monotonized under RL_ISOFADE; `iso_eff` fade e^(−E_q/1.1); full at zero evidence), applied at MR:1057/1165/1199/1665/1674. Caution only: "frozen" means built once per build on original features before the MR:628 rebind (MR:365, 498-500), not a pinned pickle.
22. WRONG (one clause) — the lens construction is verified exactly (MR:1444-1476: anchor(pick)×m; kernels h_pick 0.35 / h_age 1.5, shrink n/(n+25) through (pos,age,pick)→(pos,pick)→(pos)→1.0, bound [0.5,2.0] MR:1502/1513; neutrality on the applied composition MR:1477-1513; frozen load/HALT MR:1360-1389; basis-declared-input HALT MR:1426-1432). But "hot ruck priors are capped first at 1.4× the pick's ladder value" is NOT part of the shipped board year-zero value: the #306 lens fit never reads `_v0_raw` (values barred — only composition weights enter, MR:1486-1490), and on the board path `v0_start` returns the lens surface value for every real ND row (MR:1618, 1620-1624), so no 1.4×PVC cap touches it (the only bound is the lens B=2.0). The ruck cap (MR:1128-1138, 1183-1184) binds `_v0_raw` — i.e. the **backtest/guard path, the curve-miss fallback, and the pre-#306 free fit (MR:1582-1591)** — plus the separate ev-chain ruck ceiling (MR:1702-1704, step F6's rung). "Capped first" describes the superseded D14 fit comment (MR:1228-1230), not the live lens lane.
23. CONFIRMED — MR:1650-1655 (sitout_ev=(1−λ)·R·v0_start+λ·e_full; τ=years+fe^1.5 concave accrual; λ=interp(min(gy/fe,6)) on LAM_SIT reaching 1 at the prorated 6-bar), R_SURF MR:1104-1125 (class×log-pick×depth, isotonic-in-depth, KPP floored at nonKPP — note the floor is board-path-only, MR:1123, a minor omission), trigger ns==0 via `nseas_pro` MR:1066-1067/1729.
24. WRONG — stalled branch verified (MR:1732-1736: cap=0.25·max(0.4,1−0.10·(el−onset))·(1.6 key/ruc)·v0 + graded release `_staleness_grade` MR:1683-1697, keyed on current-season quality/REPL and gap). But "played-but-never-near-par careers of 5+ years capped at a **smaller** fraction" is false: the mediocre cap (MR:1737-1739) is 0.45·max(0.3,1−0.08·(el−onset))·(1.5 key/ruc) — a **larger** base fraction than the stalled 0.25 (e.g. el=5 non-key: 0.378 vs 0.20); what the mediocre branch lacks is the graded release (hard min, no `gr` term).
25. CONFIRMED — MR:1755-1767 (w=1−s(1−fE), s=clip(1−g_Y/11,0,1), value-space blend with the _M3PIN clock-pinned eval; on-pace and completed seasons untouched by construction; clock-pin plumbing MR:716-748).
26. CONFIRMED — MR:1779-1790 (FLOOR_YRS 1:.45…6:.09, tail .05; real-ND-only scope, floor=frac·v0_start), MR:1698-1700 (delisted → round(0.02·v0_start)).
27. CONFIRMED — RE:181-189 (vM2/vM1/v/vP1/vP2 = ev(p,2024..2028); `_LENS_FORM`=2026 for +1/+2), RE:201-207 (young floor φ(g)=(1−g/46)²·now-value, g<46), MR:211-236 (form-anchor clock), MR:237-292 (sealed r_pop/s tables, geometric blend; band site MR:370-380, level_now site MR:944-945). "No separate growth term" holds (Reid constraint; growth via _dev_advance only).
28. CONFIRMED — EM:99-120 (Y in 2003..2026, scoring truncated ≤Y, effective retirement fields, BASE_REF=AGE_REF=Y, ASOF[(id,Y)]=ev(p,Y)); `_BOARD_PATH=True` at MR:1111 with no reassignment anywhere in EM (grep verified: zero hits in session_2026-07-29/item271). Caveat for the record: EM emits the **271** matrix (which taught the shipped curve); the **lens** basis reads the 279 matrix (EB:38), whose emitter is absent from the audited tree — verified only through its meta (store 6b9d00a7, v0surf b781ed253bff, v0surf_frozen **false**, same schema/force-majeure fields).
29. CONFIRMED — HP:60-77 (_es=max(0,1−(pw−0.11)/0.89); realised_at=Σe·v/Σe), HP:80-87 (never-established → 0.0), HP:33/44-47 (QUAL_GAMES=6). Minor unstated detail: pw is carried for years 1-6 only; seasons past year 6 reuse the year-6 weight (HP:73, EM:141-145).
30. CONFIRMED — HP:106-144 (strata (pos,tenure), MIN_STRATUM=20; completion=sofar×(mean full/mean sofar); both fallback classes valued at r['v0']=the engine's v0_start (EM:163); counts asserted to sum HP:141-142); stamped into the artifact EB:104/120.
31. CONFIRMED — HP:91-102 (teaches_curve, picks 1-64, 2004-2022, N=1197 asserted), EM:52-95 (Boyd/McCartin ND-pick-1 identity HALT, slide before the split, membership via the engine's own rule), EB:42-47/93/104 (pins, +4.7%..+8.4% optimism stamp, provenance).
32. WRONG — the mechanical elements are real (Gaussian log-pick kernel to n≥35, PAVA non-increasing, pick-1 hard-set 3000, strict descent: HP:166-228; adopted-never-refit: MR:1838-1850, RL:993-997, RE:144-165), but "the SAME structural values → the ruled pick curve" is false for the shipped artifact: pvc_curve_v2.json's own stamp says it was derived by **derive_271.py from per_entrant_271.json** (store 265f55d5/81d24704, nd_curve_rows 1325, windows pool [2004,2024] + priors [2006,2020], a time kernel tau=0.12, a ×0.6 blend ceiling and min(n_pos/200,1) prior blend), and its `_working_substrate_note` marks it the #290 stop-point substrate — NOT the #279 basis (per_entrant_279_vor, store 6b9d00a7, classes 2004-2022, N=1197) that teaches the lens. HP's kernel_raw/pin_and_check is the #279 *panel* harness ("the six-way alpha evidence rebuilds once, later, on whichever fitter is ruled" — HP docstring), not the shipped curve's derivation. One population/two-siblings is the lens basis's claim about the *class-cut subset*, not about the shipped curve. (Also: HP:166-182's "effective n" is plain Σw, not (Σw)²/Σw².)
33. WRONG (one clause) — the dependency itself is verified (V0 anchors: MR:1650-1655/1727-1739/1783-1790; counted fallback: HP:127-135; lens self-reference bar: MR:1400-1408; board-path-ON walk-forward: MR:1108-1111 + EM; no bound on the residual: confirmed by absence). But "the pedigree term … never falls below weight 0.11 inside the value" misstates MR:293-296: 0.11 floors only the **fade** factor; pw=gate×fade is ~0 at zero evidence (e.g. Eq=0.1 → pw≈0.03) and 0.11 is the **high-evidence asymptote**. The correct plain statement: the pedigree par keeps weight ≈0.11 in every *established* player's level forever, while thin-record seasons carry a much larger pw (peaking ~0.35 near Eq≈1) — the echo is largest exactly where the teaching weights e are smallest, which is the finding's real point.

## Finding verdicts

F1. STANDS — every code citation checks (lens bar MR:1400-1408; fallback rows at r['v0'] HP:127-135; ASOF=ev EM:114; V0 anchors; no bound on the indirect echo anywhere). The aggravator (walk-forward built with `_BOARD_PATH=True`) is verified for the 271 emitter (zero `_BOARD_PATH` hits in EM) against the engine's own contrary comment (MR:1108-1110). Two corrections to carry: (a) the "pedigree weight floors at 0.11" phrasing — see step 33 (0.11 is the fade floor / high-evidence asymptote, not a lower bound on pw); (b) the lens's actual teaching matrix is the 279 emitter, absent from the audited tree — the aggravator is proven for the curve's teaching matrix (271) and inferable but not in-tree-provable for the lens's (its meta shows v0surf_frozen=false, i.e. a live-fitted previous-generation surface, consistent with the finding). Classification nobody-understands-it: defensible — the non-fallback prior share is nowhere measured or bounded.

F2. STANDS — verified: no artifact anywhere is "expected per-season output"; the per-year `lev` is converted to money inside the same k-loop (MR:920-928/RL:590-596); price6 averages PRICES over the band (MR:386, DP:260-283); W(k) is simultaneously belief and payment. Classification earns-its-complexity: defensible (band-of-prices convexity is deliberate; the boundary genuinely does not factor).

F3. STANDS — WR:12-18 (cache ≠ retrain, measured b271ed2e vs 34faa865), WR:52 vs MR:628 (train=lvl_par, serve=refined level), RQ:40-41 (hyperparams only in the refit script) all verified. Supporting detail the audit should add: cm's cold-bake fallback (WR:53-62) will silently fit a NON-canonical forest on a cache miss — strengthening, not weakening, the "unstatable artifact" charge. Classification nobody-understands-it: defensible.

F4. STANDS — RL:730-734 (SCALE=7000/ref^GAMMA on the STBL basis over player_raw), RL:651-706 (peak_model_v4.pkl + frozen pvc_snapshot feature), RL:908-932 (BOARD_FACTOR=(_P1/PVC[1])·s over the pre-anchor v3.4 head 4441; the E6 comment's "agree only because both pins are 3000") all verified; peak_est confirmed absent from ev()'s chain (b6/price6/v_at_peak/prod_floor never call it) yet it sizes every value unit via SCALE. Classification shouldn't-exist: defensible on the code's own E6 record.

F5. REFUTED — the false sentence exists exactly as cited (MR:1306 `'RL_GAMMA':'0.85'` + MR:1305 "Defaults are the engine's own" vs RL:504 default '1.0'), but the armed-trap mechanism is impossible: `_v0surf_sig` reads `os.environ.get(g,d)` (MR:1312), and by the time any `_build_v0_curve` call runs, MR:49's `import wire_redesign` has transitively imported conditional_prior/dist_redesign/par_build/distribution_pricing, each of which executes `os.environ.setdefault('RL_GAMMA','1.0')` (CP:40, DR:24, PB:28, DP:28). So in an unset shell the env var is materialized as '1.0' before signing — the signature signs '1.0' and the engine runs 1.0; set to 0.85, both read 0.85. The '0.85' dict default is unreachable dead text in every in-process build (there is no code path that computes the signature without the FV import chain). Replacement plain rule: "the signature's RL_GAMMA leg always equals the engine's live gamma, because the FV modules' setdefault('RL_GAMMA','1.0') runs before any signature is computed; the '0.85' literal and its comment are dead and false respectively — a comment fix, not a misload hazard." Classification shouldn't-exist accordingly falls away (downgrade to a hygiene note).

F6. STANDS, with three order corrections a supervisor must apply before relying on the recorded ladder (ironic in a finding whose thesis is "the order is the recipe"): (i) un-compress runs BEFORE the pole pull, not after — raw_ev computes pr=price6 → `_uncomp_prod` (MR:455) → then pr+w·recover·(po−pr) (MR:466); (ii) the sit-out branch (`ns==0`, MR:1729-1730) is evaluated and RETURNS before the stalled/mediocre caps (MR:1732-1739), not after them; (iii) the delist override (MR:1700) is the FIRST statement of ev(), not the last rung — it pre-empts the whole ladder rather than closing it. The remaining claims verify: youth credit multiplies before iso and before the ruck cap (MR:1042-1044 → 1665, with MR:992-994 declaring capped rucks keep the cap); the KPF compress re-enters the chain band-pinned (MR:1046-1058); hand-set constants at MR:394 and MR:1733-1738; no invariant in the audited tree asserts the order. Thesis and earns-its-complexity classification: defensible once the order is corrected.

## Tally
Steps: 22 CONFIRMED · 7 WRONG (2, 8, 16, 22, 24, 32, 33) · 4 INCOMPLETE (11, 13, 17, 19) · 0 UNVERIFIABLE.
Findings: 5 STAND (F1, F2, F3, F4, F6 — F1 and F6 with mandatory corrections) · 1 REFUTED (F5).

### §4.3 Valuation verification (verbatim from verify_valuation.md)

# ADVERSARIAL VERIFICATION — LIST 3: VALUATION
Verifier: independent line-by-line check against the code trees (wt-lens = ENG, panel279 = PAN; shorthand MR/RM/RE/DP/DR/CP/PRD/HP/FC/PN/LV/RV/V2/SS/MC/RP as in citations_valuation.md). Nothing was executed except read-only JSON/pickle-opcode inspection of committed artifacts.

## Per-step verdicts

1. INCOMPLETE — Band construction verified (CP:45 quantiles [.10,.30,.50,.70,.90] — cited as CP:44, off by one; CP:164-167 `cond_prior_band`; MR:366-369 sixth node = q97m floored at b[4]; MR:84-93 frozen q97m). OMITTED from the step text: the v7 age-taper (MR:597-612, rebound at MR:621-627) rescales the q97 node for every real player — `asc` falls 1.0→0.40 by age 27, partially restored only for demonstrated producers (lcr>4, nq≥1, ×V7_FORM_W=0.6). The band that prices is NOT "5 quantiles + q97 floored at q90"; its sixth node is age/form-tapered inside the valuation wrapper. (The step's own citation lists the taper; the text doesn't state it.)
2. CONFIRMED — RM:581-599 exactly as stated (peak×frac path, DELTAS −8..+14 at RM:502-503, floor at current level for k=0 and ag≤pa, break at ag>38 or frac<0.42, 18-year cap); live W4 copy MR:909-932 preserves the same loop.
3. CONFIRMED — RM:380 (BAR=105, M=109.5, W=1.85, G=1.00), RM:385-387 softplus-integral credit, clamped ≥0 (zero at/below bar), marginal = logistic P(L) < 1 structurally; applied per season RM:594 / MR:925.
4. WRONG (one clause) — Netting, −3 drop (DR:35-39 applied in price6 MR:381-387), soft-hinge S_SH=3 (RM:505,507), future-leg dual blend with the lower (more valuable) bar on the alternate share (RM:115-128) all CONFIRMED. But the parenthetical "blended toward a declared dual-eligibility lower bar in proportion to the season still remaining" describes the §1b machinery (DP:260-277, RM:614-626/MR:954-966) which is SUPERSEDED AND INERT on the live board: RM:72-82 records that the year-0 bar now takes the dual pair's LOWER REPL at source (`bnow` → `_fit_bar` → `_decl_bar` → `_elig_bar` = min-REPL of the collapsed column, RM:83-109), so `y0dpp_bar` (RM:166-176) returns None for every Now-board row (`REPL[low] < REPL[bnow]` can never hold when bnow IS that min; data-error combos also return None, RM:171-174). The blend can only fire on backward as-of years where a season-row bar exceeds the column bar. Correct plain rule: on the Now board a dual-eligible player's whole year-0 leg nets against the lower bar of his declared pair, at source, with no proportional blend.
5. CONFIRMED — ×21/(1+d)^k RM:595-596; d: bal=0.14 under RL_DIAL14 default (RM:540), now=0.34, fut=0.05, contender/rebuilder 0.18/0.10 marked STRAWMAN (RM:548-551); KPF/KPD ×1.05 RM:597; runway×elite×PMAX=0.25 RM:598/RM:180; W4 copy MR:927-930 identical.
6. CONFIRMED — One binding read RM:504 `GAMMA=float(os.environ.get('RL_GAMMA','1.0'))`; SCALE=7000/ref^γ on the STBL-toggled 99th-pct basis RM:730-732; val RM:734; BOARD_FACTOR=(_P1/PVC[1])×s RM:929-931 with PVC[1] = the v3.4 import-fit head (comment RM:914: measured 4441; overwritten by the artifact before shipping, RM:915-917); s from V2 numeraire block (head 3068.4647, s 0.9776876364261254, pin 3000 — verified in the artifact) via the E6 loader with coherence halts RM:859-904. Effective 1.0 on this tree: MC vars RL_GAMMA "1.0" (verified), RP:26 `RL_GAMMA=1.0` under RL_CONFIG_MODE=gate (RP:25) + config_manifest.enforce(); stale 0.85 pins verified at ship_gates_check.py:64, verify_anchors.py:8, verify_restore.sh:8, verify/d15/_d15_common.py:21, config_inventory.py:102. (The chain's "same measured head" framing on the pick side is qualified under step 30.)
7. CONFIRMED — RM:600-627 (H=clamp((40−a)/3,1,3); decline-only roll cur×min(1, frac ratio); same capt/netting/21/(1+d)^k; returns val(prod)); proven-player parallel copy MR:939-968; consumed as max(prod, prod_floor) at DP:283.
8. CONFIRMED — WQ6=[.18×5, .10] normalized (sum exactly 1.0) MR:94; price6 = SCALE_DIST × det_dot(WQ6, v_at_peak per node) MR:386; SCALE_DIST=1.0 DP:48; each node individually floored via DP:283.
9. WRONG (two details) — Credit/fade verified: MR:893-907 (W += cw·up·gm·dur·sh·interp(k,[0,2,5],[1,1,0]); W −= 0.60·(1−gm)·fadew·interp(k,[4,10],[0,1]); floor 0.05), constants MR:786-822, gates MR:850-892, applied MR:926-928 and in the proven floor MR:966. But (a) the deep-pick over-optimism compression (W4_OVPX_D SF/SD/MID, ramp over picks 38→46, MR:884-891,905-906) is set for SUB-proven (thin-career) players and partial-proven KPFs ONLY — a fully proven (n≥4) non-KPF player NEVER carries `ovpx` (it is set only in the `else` branch MR:886-891 and the `_partial` case MR:883-885); the recipe attributes it to "a PROVEN player". (b) OMITTED: a partial-proven KPF (2≤nqual<4) with top-tier sustained demonstration earns the same near-year credit scaled c=n/4 (MR:863-868, 876, 899-900) — the reweighting is not proven-only.
10. CONFIRMED — MR:998-1040 exactly as stated (×[1 + 0.9·max(R,0)·(1−g/46)²], sat/played blend s=min(g/6,1) with row['1']=sat→row['0']=played, KPF cell ×0.92, table keyed by eval year with pre-2007 zero, halt if table absent, LTI clock advance g += L×SEASON MR:1019-1021); ND/RD non-pickless scope MR:1025; applied in the raw_ev wrapper MR:1041-1045.
11. CONFIRMED — MR:454-466 (pr + w·recover(perf,par)·max(0,po−pr)); pole = price6 of a 2-season at-par synth × _SCALE{MID 1.19…RUCK 1.13} MR:389-395; recover: RECX/RECY MR:94 + MR:388 (0.54 at ≤0.30 of par → 1.0 at ≥0.82); w = wage (1 at 20 → 0 at 26; 0 for RUCK, MR:461) × tfade ([1.00,.76,.40,.16,.05,.05] over tenure 1-6, MR:462, on developmental tenure) × smooth expgate MR:297-300; par surface PRD:68-71.
12. CONFIRMED — MR:412-453 (ρ games×recency d=0.25; captain-off pr0; v0p=pr0^(1−w)·(V_ref_b·ρ/RHO_DEN)^w; w=0.10·E·ramp(Δ=6); C[pos] renorm; captain delta added back untouched); dials live at defaults RM:523-529 (UNCOMP_S_DEFAULT=0.10 owner-set, _UNCOMP default on); reference/conservation build MR:1863-1906.
13. CONFIRMED — MR:485-497 (synth at-par over picks 1-70, IsotonicRegression(increasing=False), ratio fs=iso/max(raw,1e-6), re-monotonized under _ISOFADE, iso_eff = 1+(base−1)·exp(−E_q/1.1)); applied at the board-path sites (MR:1057, 1165, 1199, 1665, 1674; the citation's "six sites" sixth tag is a diagnostic in _ov_angleA.py:39, not a value-path site — citation imprecision only).
14. CONFIRMED — Interpretive summary consistent with MR:203-296 (evidence weights, _ev_pw → 0.11 residual), MR:560-580 (_coreM1), MR:933-938 (floor carries the credit for elders), MR:461-462 (pole fades).
15. INCOMPLETE — Production-leg cap verified exactly (MR:1698-1704 bind iff ceil < e ≤ V0_uncapped; ceiling = 0.80 × standardized-ruck price at refpk 72 at bestlvl, × young headroom 1+0.35·f(pick)·f(age), MR:1156-1182; no-production fallback 1.4×draftval×headroom MR:1180). OMITTED: the same 1.4×draft-slot×headroom prior cap is ALSO applied unconditionally to every real ruck's V0 START VALUE (`_ruc_prior_cap` on `_v0_raw`, MR:1183-1184, 1202-1205), which then feeds v0_start and every start-value consumer (steps 17/18/19/22, and the D14 curve is FITTED on capped V0s per MR:1228-1229). Also minor: the no-production headroom is draft-age-keyed (_ruc_head_v0) while the production ceiling's is as-of-age (_ruc_head_mult) — "the same headroom" is the same function, different age key.
16. CONFIRMED — MR:1712-1724 (gates nqual≥4 & age≥24; split at eD = price of sustained LD clipped to [eP,e]; e′=eP+0.70(min(e,eD)−eP)+0.55(e−max(eD,eP))); _kpf_LD MR:823-848 (top-2 ≥12-game seasons in Y−3..Y prorated, injury-season nuked with window extended cap +2, counted-against fallback).
17. CONFIRMED — MR:1060, MR:1699-1700 (round(0.02×v0_start)).
18. CONFIRMED — MR:1650-1655 (τ=completed seasons + fe^1.5; (1−λ)·R·v0_start+λ·e_full); R_SURF 3 classes × log-pick knots 5/15/30/50 × depths 1-6, isotonic-in-depth by derivation, KPP floored pointwise at nonKPP on the board path (O1, MR:1123-1124); λ isotonic knots MR:1126 over games-at-pace min(gy/fe,6); gate ns==0 MR:1729-1730; continuity at λ=1.
19. CONFIRMED — MR:1731-1739 (stalled onset 3, 4 for KPF/KPD/RUCK; frac=0.25·max(0.4,1−0.10(el−onset))·1.6keyruc; graded release via _staleness_grade MR:1683-1697; mediocre el≥onset+2 & bestlvl/par<0.55, frac=0.45·max(0.3,1−0.08(el−onset))·1.5keyruc — the mediocre keyruc multiplier is 1.5, not the stated stalled-side 1.6; the recipe doesn't misstate it, it just doesn't give it).
20. CONFIRMED — MR:1740.
21. CONFIRMED — MR:1752-1767 (w=1−s(1−M3_FE), s=clip(1−gy/11,0,1), clock-pin _M3PIN plumbing MR:724-748, round(w·v+(1−w)·vpin)); SS carries calendar_progress 0.83 (verified); fenced halt MR:6-30; on-pace s=0 untouched; delisted skipped (identical evals).
22. CONFIRMED — MR:1779-1790 (schedule {45,35,28,21,13,9}% + 5% tail; real-ND, non-delisted/retired/pickless scope; floor vs v0_start; round).
23. CONFIRMED — MR:106-131 (_fEy=1.0 for register out-names), register application + halts MR:1908-1975 (_avail_hc=L, return-haircut table + proven-only _ret_hc_for MR:1933-1940), haircuts consumed MR:923-924/:959 and RM:593/:619; non-register bit-parity by construction (layer touches register keys only).
24. CONFIRMED — RE:132 (_F=1.0524 from pick_redenomination.json, verified), RE:177-181 (round(ev/F)), order/ratio asserts RE:213-239 (halt on strict inversion; anchor-pair ratios <0.2% relative), parity gate RE:596-613 (eps=0, compares board v to round(ev/F), halts).
25. INCOMPLETE — Backward RE:182, forward lens with form anchor 2026 RE:185-189 + MR:367/:385, forward pedigree floor (1−g/46)²×now RE:201-207, ÷F on all view columns — all CONFIRMED. OMITTED from the step text: the Leg-F4 L-SYMMETRY population damper (MR:237-292, applied at the two forward reads MR:372-379 and MR:944-945) geometrically tempers every +1/+2 age-advance toward the form anchor by the sealed s(age) (s=0 at ages ≤22 — fully form-anchored — up to 1 at 34+), reproducing the sealed measured backward-transition rate r_pop; it materially shapes every forward value and appears only in the citations, not the step.
26. CONFIRMED — RE:615-633; OV:71-108 (adds only `ov`={factor,dispv,mark,note,prov}; never touches v), OV:111-131 (assert_presence halts in gate/bake).
27. CONFIRMED — HP:22-33 (YR_LO 2004, CLASS_CUT 2022, PIN1 3000, ND_LAST 64, MIN_STRATUM 20), loader HP:91-102 (teaches_curve, picks 1-64, EXPECT_N=1197), structural_values HP:106-144 (concluded realized incl. busts-at-zero via never_established, actuarial completion, COUNTED v0 fallback — computation itself deferred to the sibling list as declared).
28. CONFIRMED — HP:166-193 (Gaussian kernel over log(pick), h from 0.10 in +0.02 steps until kernel mass ≥35, cap 0.60, weighted mean); FC:35-36/57-67 (boundary = >1% off-domain mass, z=2.3263), FC:70-90 (degree-1 WLS intercept, degenerate→decline), FC:93-113 (max(a,0) only on boundary; interior bit-identical, proof key FC:153); zones picks 1-2 and 51-64 on this matrix (FC:20-22).
29. CONFIRMED — PN:35-50 (weighted PAVA with visible head block), PN:69-83 (plain PAVA, NO pick-1 override, strict descent EPS=1e-3); superseded hard-set retained for comparison PN:56-66 / HP:213-228.
30. WRONG (one load-bearing clause) — The RULE is real and correctly cited: s=3000/pooled_head, whole-curve ×s (PN:75-77), published (PN:137-140), the artifact carries the numeraire block (verified: head 3068.4647, s, pin), and the player side takes the same s under E6 halts (RM:859-932). BUT "multiply the ENTIRE curve by s — pick 1 = 3000 becomes a true measured unit, never an overwrite" is FALSE of the shipped ladder the board actually prices from: pvc_curve_v2.json's own `source`/`stamp` fields (verified) record the curve block as the ITEM-271 stage-B derivation — derive_271.py carrying derive_pvc2.py "function for function" with an explicit hard pin(1)=3000, tau=0.12 time-kernel fit_year0, window pool 2004-2024, nd_curve_rows 1325, ladder_total 65925 — i.e. the OLDER pin-override method on a different window/population than the ruled 2004-2022/n=1197 basis, and its entries were never rescaled by s (×s≈0.9777 would have moved the total ~−2.2%). Only the `numeraire` block is the #279 ruled economy; on this tree s is applied to the PLAYER side alone (RM:931). The recipe nowhere discloses that the section-D derivation is not the derivation behind the shipped curve entries.
31. INCOMPLETE — As the PN publish rule: CONFIRMED (max(1,round), integer strict-descent re-force, 64 entries, PN:78-83; domain law RM:197-215 + _split_ladder RM:957-992). But "the result is the national ladder" overstates: the shipped national ladder is the stage-B artifact of step 30's correction, not this pipeline's output; the panel emits fitter_*.json ladders (HP:243-262) that do not ship.
32. CONFIRMED — RE:135-155 (shipped PVC := adopted pvc_curve_v2.json curve + pool via _split_ladder; pick1==3000 assert RE:146; strict-descent assert RE:149-150), unconditional numeraire halt RE:156-165, picks at face value RE:584 (no ÷F), 64 anonymous placeholders per +1/+2 lens at exact PVC[n] RE:767-788 (RL_LEGF default), posture 10/15/5% applied once RE:589-595.
33. CONFIRMED — RM:197-215 (POOL_PICK=65, "THERE IS NO PRICE FOR PICK 70"), classification RM:260-281 (ND>64/RD/PSD/pickless → pool), PICKEQ retirement RM:1154-1163, pool entry on ladder MR:1838-1850 / RE:136-138, draftval reads the pool entry for pool entrants MR:1806, position differentiation via iso layer.
34. CONFIRMED — LV:35-71 (shared completion table from concluded pool rows; (mean full ÷ mean sofar) ratio; v0 fallback under MIN_STRATUM=20, share disclosed), LV:74-95 (honest mean, busts at zero, 95% interval), stream subsets LV:102-126, shared-table disclosure LV:110-117, "nothing is ruled today" LV:105-106, ×s reporting PN:241-250, adoption pending LV:18; shipped stop-point pool value 234.3 with re-measure-at-L6 note (verified in V2).
35. CONFIRMED — MR:1393-1522 (anchor×m; bounds [1/2.00, 2.00]; kernels 0.35 log-pick / 1.5 age; shrink n/(n+25) through (pos,age,pick)→(pos,pick)→(pos)→1.0; barred self-referential basis with halt MR:1426-1432; λ(pick) iterated 200× on the APPLIED population's anchor-weighted composition to worst ≤1e-12 MR:1500-1513); lookup star MR:1608-1616; consumed by v0_start on the board path MR:1620-1624. (Note: this fit code executes only under the declared refit; a normal build loads the frozen output — consistent with step 36.)
36. CONFIRMED — signature = active curve + roster geometry + value-gate env (MR:1287-1313), frozen-load MR:1362-1367, HALT on unknown signature MR:1368-1389 (no silent refit), declared refit lane RV:22-27/97-100 (RL_V0SURF_REFIT=1, --bake gated by RL_BAKE_V0SURF=1, pin write RV:101-114), cross-host byte-assert at fit time MR:1523-1570.
37. WRONG (one clause) — Start-value hold and decay CONFIRMED (pre-season τ=0 → R=1, λ=0 → price = v0_start, MR:1650-1655/1072-1074/1125; V0 = raw_ev(debutyr−1)×iso_eff with E_q=0 → iso unfaded, MR:1192-1201; young credit at full strength g=0, MR:977-981; floor/staleness/scrap anchored on v0_start MR:1700/1728/1789). BUT "the pedigree-par weight near 1 fading to a permanent 0.11 floor" is wrong: the applied par weight is _ev_pw = gate×fade (MR:293-296) and the qualifying GATE (E_q²/(E_q²+0.55²)) is ≈0 at zero evidence — a zero-evidence draftee's level does NOT lean on the pedigree par through a weight near 1 (it is carried by the conditional-prior band at draft features); pw is hump-shaped (≈0 → max ≈0.37 around E_q≈0.8 → residual 0.11), and only the FADE factor alone runs 1→0.11.
38. CONFIRMED — Every enforcement claim verified at its cited site: one price RE:170-172, parity halt RE:596-613, numeraire halt RE:156-165, strict descent RE:149-150/RM:983-985, two-sided scaling RM:859-932, sitter isotonic + O1 + assert MR:1095-1125/1644-1648; conservation report-only RE:539-559 & RE:789-804 ('report_only': True both); schedule-keyed terms and the anti-schedule prorations all at the cited lines.

Counts: 30 CONFIRMED, 4 WRONG (4, 9, 30, 37 — each wrong in one identified clause, remainder verified), 4 INCOMPLETE (1, 15, 25, 31).

## Finding verdicts

F1 (γ signature fork) — STANDS. RM:504 default '1.0' verified; MR:1306 signature default 'RL_GAMMA':'0.85' verified, with the demonstrably false comment at MR:1305 ("Defaults are the engine's own (rl_model.py:504…)"); every listed 0.85 pin re-verified (ship_gates_check.py:64, verify_anchors.py:8, verify_restore.sh:8, _d15_common.py:21, config_inventory.py:102); the recorded manifest/default fork incident exists (manifest_vs_default.py:9-13). In a bare shell the signature hashes a γ the engine is not running; the unknown-signature halt (MR:1368-1389) converts the wrong-board risk into a build halt but the fork remains. "Shouldn't-exist" is the right class: it is a one-line inconsistency, not irreducible complexity.

F2 (no data boundary between prediction and valuation) — STANDS. All rebinds verified (MR:628 cp._lvl_eff=_inferM1 & cp._feat=_feat_infer; MR:715 cp._lvl_eff=_lvl_eff_abs; MR:748 clock rebinds incl. cp._feat=_m3_feat), the band-pin re-entry (_kpf_prod_efv MR:1046-1058), and the three record-to-price bypasses (prod_floor RM:600-627; ruck ceiling MR:1178-1182; KPF compress MR:1712-1724). "Earns-its-complexity" is defensible: pricing the outcome distribution through the real chain (DP:1-16, TODO#1/#6) genuinely requires re-running the path per hypothetical level.

F3 (dormant twin + duplicate floor loop) — STANDS. MR:319-335 ("DORMANT twin … never bound/called … kept in lock-step"), MR:352-359, and the declared duplicate-loop hazard at MR:947-953 / RM:611-613 ("edit BOTH or neither. Queued hygiene (NOT this build)") all verified; nothing executable enforces lock-step. The tree's own comments queue the deletion/delegation, supporting "shouldn't-exist".

F4 (currency anchor chain) — STANDS. Verified end to end: SCALE=7000/ref^γ on the one-line STBL basis (RM:730-732); v3.4's absolute level anchored to the legacy build_pvc(0.6) top band (RM:852-856, ALPHA RM:769); BOARD_FACTOR divides by the v3.4 pre-anchor head of a curve discarded lines later (RM:929-932, comment RM:908-928 including "measured 4441" and the admission the previous description was measured false); the artifact's SCAR-by-construction note vs the executing γ=1.0 VOR basis (V2 `note` verified; PN:134 'gamma':'1.0 VOR'); E4 "only the description was wrong" MR:1799-1802; coherence maintained by the E6 halts, not construction. On the default path the import-time v3.4 fit's only load-bearing product is the divisor (plus diagnostics and the RL_PVC2=0 fallback). "Nobody-understands-it" is well-supported by the file's own history of falsified prose.

F5 (pedigree-pole re-level constants) — RECLASSIFY. The six constants (MR:394) and the absence of any runnable derivation in either tree are confirmed. But "the rule reduces to 'multiply by these six numbers because they are these six numbers'" is refuted by the tree's own record: docs/CHANGELOG.md:849-856 (entry 2026-06-29, "STEP 3-B POLE RE-LEVEL — APPLIED TO ENGINE") states the derivation rule — _SCALE[pos] = trajectory-integrated pole (mean price6 of the par-synth over development maturities T1-5) ÷ the 2-yr-synth pole — plus its robustness measurement and the passed hard gate. So the finding's plain rule exists; what is missing is a committed, re-runnable derivation script. Proper class: not "nobody-understands-it" but a frozen, documented calibration whose derivation code was never committed (a reproducibility/hygiene defect nearer "shouldn't-exist in its current unreproducible form").

F6 (triple-fit freeze) — STANDS, with one factual correction. The mechanism is verified (MR:1592-1599 comment; swap-and-rebuild blocks MR:1811-1827 and 1838-1850; halt MR:1368-1389; RV:53-63 freezing every built surface). CORRECTION on the requested check: the shipped data/v0surf.pkl contains exactly TWO 32-hex signature keys (96d671c9…, 0589a262…; verified via pickle-opcode scan, no execution), not three. That is consistent with the code, and the MR comment's "three DIFFERENT signatures" overstates on this tree: call 1 (before _PVC0 exists) keys on MA.PVC, which since the RL_PVC2 producer swap (RM:1010) IS the same split v2 ladder call 3 keys on — so calls 1 and 3 share a signature and the freeze needs (and holds) two. The finding's thrust — the freeze must pin every intermediate surface or it silently isn't a freeze — survives; the count in its supporting quote does not. "Earns-its-complexity" stands (the layered-revert design forces the intermediate rebuilds; the halt makes staleness loud).

## Citation-file notes (non-blocking)
- CP:44 → actual CP:45 (Q list).
- Step 13's "six sites … MR:1057, 1165, 1199, 1665, 1674" lists five; site 6/6 is a diagnostic in ENG/engine/rl_after/_ov_angleA.py:39 (not on the value path).
- The MR:1305 comment's "rl_model.py:504 and :866" internal refs are stale (RL_PICK1 read is at RM:929); F1 quotes the comment accurately.
