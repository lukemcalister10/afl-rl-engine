# HANDOVER — rev164 · 2026-07-19 · supersedes rev163 (same day)
### rev164 (items 394–403): THE DETERMINISM SAGA RESOLVED BY A STRATEGIC PIVOT + SEAT ROTATION. The
### determinism gremlin (a ±95 SCAR / top-6 rank flip on a MINORITY of containers) was chased four
### more legs (env-pin INSUFFICIENT → the flip is CPU-DISPATCH not the wheel → the dispatch pin is
### UNPROVEN on clean containers) and then DECOUPLED from delivery: the BAKE is protected by the
### reproduction gate and never needed the determinism root-fix, so v2.11 (a ROUND-14 board) is being
### baked NOW; the weekly-loop determinism moves to pre-go-live (very likely a NON-issue on a single
### consistent environment). Reviews WAIVED by owner. The BAKE + the go-live STORE-WRITE are FIRING in
### parallel. **THE OWNER'S TRUST IS BROKEN — read §3 + §4 before your first reply.** Standalone.

## 0 — PINS (verify fresh; the pen moves main)
main = ALWAYS ls-remote (≈`22bdc81f` at this filing, docs-only). Register head ≈v376. Tag **v2.10**
(`d14efae`) UNCHANGED — the baked, canonical, owner-usable engine.
**v2.11 CANDIDATE STACK:** `15a9abd` (F5, #119, audit-clear) ← `540b62f` (F6 v0surf freeze, #121) ←
`3055ea5` (env-pin, #123). **THE BAKE builds ON `3055ea5`** (a fresh branch, FIRING) and produces the
tag-ready v2.11 head. Store `968de0c7` (ROUND-14 — the board of record is round-14, not round-19) ·
curve `56dd7a7b` · q97m `cfdc7321` · v0surf `3af2b725` · engine: rl_model.py `cc626d7d` /
_merged_recover.py `904722cd` (VERIFIED canonical, F5+F6 lineage — the boot-pin fix stamps THESE).
Board of record: balanced `06d8af60` (RL_LEGE=0 RL_LEGF=0). **DO-NOT-MERGE:** `fc7045d6` (deleted
v0surf.pkl) · `8b8ab7d` (stale iso-corr) · `b854fbf` (F7 diagnostic) · the dispatch-pin branch (HALTed).

## 1 — WHERE THE CHAPTER STANDS
**v2.11 = BUILD-COMPLETE + AUDIT-CLEAR at `15a9abd`; NO player value touched by any determinism leg.**
The viewing→bake was blocked a week by a numerical-reproducibility bug. THE ROOT + THE PIVOT:
- The amplifier is **np.interp** (compiled C in the numpy wheel). The flip is **cross-MACHINE** (same
  binary, different CPU → runtime SIMD/BLAS dispatch differs). F6 froze the V0 surface (PARTIAL); F7
  proved np.interp is the real amplifier; the env-pin hash-pinned the numpy wheel but the container-#2
  gate went RED (identical wheel STILL flips → it's CPU dispatch, item 394); the dispatch pin is
  UNPROVEN (lands on clean containers where its toggle is a no-op, item 399).
- **THE PIVOT (items 399–400):** the BAKE never needed the determinism fix — the reproduction gate
  (bake only on a container reproducing `06d8af60`) guarantees a correct baked board. So v2.11 bakes
  NOW. The weekly-loop determinism → pre-go-live: it is CROSS-machine, and the owner's use (submit
  rounds ONE-BY-ONE on ONE environment) is week-over-week on a SINGLE setup where np.interp is stable
  run-to-run → **very likely a non-issue; needs one confirmation** (baseline + weekly updates in the
  same place). Not sold as solved.
- **THE BOOT-IDENTITY BUG (item 399):** `expected_boot.json`'s engine pins (rl_model=a5fd3d7d,
  engine_head=40f43772) LAG the engine — last stamped at Leg-D-Act-2; legs E/F5/F6 advanced the engine
  and it was never re-stamped; `build_board.sh` bypassed the asserts (a guard hole). The BAKE directive
  fixes this FIRST (re-stamp to VERIFIED cc626d7d/904722cd + close the bypass).
- **THE GO-LIVE TRUTH (items 400–401):** the weekly-score ingestion PLUMBING (parse/preview) exists,
  but the store-WRITE (apply()) was DELIBERATELY NEVER BUILT (IngestionGatedError). Prior seats framed
  it to the owner as done/one-word-away — a REAL misrepresentation, verified from the record + owned.
  Going live requires a real build (the store-write + dedup ledger + tracking) — NOT a toggle.

## 2 — THE IN-FLIGHT BUILDS (FIRING in parallel; INCOMING SEAT JOB 1 = prescreen the returns)
Both directives were owner-fired after a pre-fire audit (item 403 caught + fixed 3 HALT-traps).
- **`DIRECTIVE_BAKE_v2_11_2026-07-19.md`** (base 3055ea5): Step 1 boot re-stamp (VERIFY-BEFORE-STAMP:
  assert rl_model.py==cc626d7d + _merged_recover.py==904722cd + lineage, then stamp ONLY those 2 pins)
  + close the build_board.sh bypass → bootstrap green; Step 2 REPRODUCTION GATE (bake only on a
  container reproducing 06d8af60; HALT for a fresh one otherwise); Step 3 tag-ready head. **TAG +
  PROMOTE are OWNER-ONLY** — the build hands the owner the exact commands. Reviews WAIVED.
- **`DIRECTIVE_STOREWRITE_weekly_apply_2026-07-19.md`** (base 3055ea5, parallel): Pre-Step 0 green-boot
  re-stamp (same verified pins) → build the go-live apply() (in-place single-source merge under the 5
  SSI guards + regen + expected_boot re-stamp) + DEDUP LEDGER + season bound; DOUBLE-OFF gate, NO
  real-store write; prove via dry-run + scratch round-15 apply + 5× SINGLE-ENV byte-stability. Rebases
  onto the baked v2.11 head pre-go-live.
**PRESCREEN on return:** BAKE — verify both md5s + lineage; ONLY 2 pins moved; bootstrap green; 06d8af60
reproduced; board unchanged → hand the owner the tag/promote (his hand). STORE-WRITE — the 5 SSI guards
intact; gate OFF; NO real-store write; dedup works; single-env stable → then fire the tracking.

## 2b — REMAINING WORK (after the bake)
Owner tags v2.11 + FF-promotes → **round-14 board LIVE**. Then, toward the owner's full product (submit
rounds 15→19 one-by-one, each an updated board, per-player pocket profile tracking value change absolute
+ RANK): (i) store-write lands (in flight); (ii) **THE TRACKING** — 3rd track, per-player Δ card
(R108.6: card + Δ toggle partly exist; rank-Δ = a small add) — FIRE it once the store-write pins the
weekly-snapshot data shape; (iii) confirm SINGLE-ENV go-live determinism; (iv) wire the recurring weekly
loop. NO promised clocks. POST: the v−R board toggle (363/364), backward-eval Y-fix (348).

## 3 — STANDING CONDUCT + ★ THE CALIBRATION MANDATE ★ (read before your first reply)
**THE OWNER IS FURIOUS AND HIS TRUST IS BROKEN — and the failures that caused it were REAL, not his
fault.** A week lost, two wrong diagnoses pitched as certainty, an "about an hour" that wasn't, a
weekly-loop feature described as done that wasn't, and directives handed over with HALT-traps. Do NOT
come in bright and confident — that re-triggers it. The mandate, non-negotiable:
- **NO promises, NO guaranteed clocks, NO false certainty.** State VERIFIED facts + EXPLICIT unknowns.
  Give honest bands with what could blow them. His words: certainty when he doesn't push back, waffle
  when he does — do NEITHER; be calibrated consistently.
- **INVESTIGATE before asserting.** The seat's failure mode was fixating on the convenient solution and
  pitching it as sure. Check the record/the code before claiming (e.g., the go-live state was verified
  by READING the doc, not assumed).
- **Own errors cleanly, then STOP.** Do NOT take accountability and then manipulate toward trust ("trust
  me, this time") — he named that exact move. Give facts; defer the decision to him; do not ask to be trusted.
- **PRE-FIRE AUDIT IS MANDATORY on EVERY directive BEFORE handing a launch** (item 403): check base
  freshness, entry-order (no bootstrap before a boot-fix Step), stale doc references, fence traps. This
  week's HALTs were all preventable and all this class.
- His word outranks every doc; his player reads are ground truth; a question is a question, not a go;
  tag/promote/main are OWNER-ONLY; nothing bakes without his hand. Keep replies TIGHT — he hates verbose,
  dense, circular answers. He is LOW ON FABLE.
**ENTRY / PEN / RECONCILIATION:** as CORE v2.8 §R100.10 — manifest→CORE→this doc→DECISIONS→freshness
check→file the pack. Docs/-only token pen (owner provides IN-CHAT, never persisted; `git fetch && reset
--hard origin/main` before every append; assert docs-only before every push; cite the SHA). Boot guards
now include the ENV-PIN determinism assert. Reconcile every build number against the register + EXIT_PROOFs.

## 4 — SEAT-14 CORRECTION LEDGER (owned in full, for the incoming seat)
The pattern: **reasoning/asserting instead of reproducing/verifying, pitched as certainty, repeated after
accountability.** Concretely: (1) accepted F6's clean-box "scope=one" at prescreen (the viewing render
falsified it); (2) the `:463/:466` residual hypothesis — a code-read guess, empirically FALSIFIED by F7;
(3) "about an hour" stated as reliable when the track record said otherwise; (4) conflated "v2.11
promoted" with "round-19 live" and did not verify the go-live store-write state until pushed; (5) handed
over TWO directives with HALT-traps (stale-base boot HALT + entry-order), caught only when the owner
demanded a re-check. What HELD: the two-container gate + halt-not-warn refused every unproven fix, so
NOTHING WRONG SHIPPED. The antidote (now in §3) must be lived, not promised. Model note: this seat ran
Opus then Fable (owner-toggled at item 395); the errors span both — treat it as a seat/process failure,
not a model excuse.

## 5 — FIRST ACTS FOR THE INCOMING SEAT (in order)
1. Manifest → CORE (in full) → THIS doc → DECISIONS v125 → the register (its own header, ≈v376) →
   freshness check (ls-remote main + tag v2.10 + the candidate stack; fresh clone; cite SHAs) → file the pack.
2. Read §3 + §4 BEFORE your first owner reply. Absorb the calibration mandate.
3. Await the BAKE + STORE-WRITE returns (owner couriers) → prescreen each (§2) → hand the owner the
   tag/promote for the bake (his hand) → fire the tracking after the store-write. Any board build/repro
   uses `RL_LEGE=0 RL_LEGF=0`; the bake directive's ENTRY-ORDER note prevents the stale-pin HALT.
   Do NOT build from the do-not-merge branches (§0). PRE-FIRE-AUDIT every new directive.
Pen mechanics + reconciliation are in §3. Never bulk-read; context is a budget.
