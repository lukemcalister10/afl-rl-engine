# HANDOVER — rev132 · 2026-07-12 · supersedes rev131
### State + the SINGLE action order. Read after CORE (v2.3). Open items: docs/OPEN_ITEMS_REGISTER.md
### (v24, the only list). Rulings detail: DECISIONS v96. Versions ride the manifest's next touch.

## ONE LINE
**v2.8 IS THE LAST BAKED BOARD** (tag == 9bd0cfdbf9... unmoved) · **main == 3c65829e** carries the
MIGRATED store (b0c39d78 · board de4baef9 · 804 live players on stable IDs) + the MERGED Matchday
UI (re-pinned, rendering the migrated board) · reds exactly {A2, A3, A12} (pre-existing [DC],
carried) · the Fable wave is COMPLETE and SYNTHESIZED · next station: ROUND-SCORE INGESTION.

## STATE
- MIGRATION ✅ (#65, two-parent merge, supervisor-verified live): 804 stable IDs + team/elig
  (0 movers) · 5 named DOB fixes · adams retired (88-row ≤±2 hairline, declared) · nankervis
  no-import · TRIO DEFERRED to v2.9 (register v20) · resolver on main · boot pin + book seal +
  panel moved with the store in-commit.
- UI ✅ (#62 merged 11f85fb9; bundle refreshed in #65): four letters law · pick-filter
  forward-safe guard (ladder was already players-only) · screens render de4baef9.
- FABLE WAVE ✅ (#63 seven-decision ruling pack + #64 four measurement specs, prescreened PASS):
  synthesis at docs/SYNTHESIS_fable_wave_2026-07-12.md — THE ONE-CANDIDATE LAW (decisions
  1/5/6/7 + trio switch + SSP line = one combined candidate, one refit, one G-COHORT re-measure)
  · the emmett corner (named attribution) · G-Y0 preview (kids 9–14% cheap vs the derived curve).
- The day's rulings (register v7–v24): UI letters · migration pre-flight · item 11 (a) MSD pool
  exclusion + nonsense-review trigger · items 12–16 lens/pick-asset/cycle laws · JUH
  smooth-in-evidence read · PVC-basis assert.
- Seat-3 pen: 20 pushes, all docs/-only, SHA-cited, live-verified.

## OPEN — THE ACTION ORDER
1. **ROUND-SCORE INGESTION (next; Tier 2/3, never bakes):** names in weekly through the resolver
   at the score boundary; owner's word fires the directive.
2. **THE v2.9 BATCH (one lever build + ONE bake):** the seven-decision RULING SLATE (PR #63's
   paper) + this wave's synthesis in one owner sitting → the combined candidate per the
   one-candidate law → full Tier-1 ladder. Register v24 items carry every input.
3. Owner smalls: none outstanding. Standing lines every job: FULL-URL ls-remote base verification
   (main == 3c65829e or newer-verified; tag v2.8 == 9bd0cfd ALWAYS) · boot-store assertion ·
   merge-commit policy · FEED = DECISIONS + CONSTRAINTS + acceptance JSON as documents ·
   STOP-and-ask on missing input.

## PROVENANCE
VERIFIED (this seat, live fetches 2026-07-12): main 3c65829e · both merges' two-parent shapes ·
store md5 b0c39d78 computed independently · UI pin de4baef9 · boot pin · tag unmoved · every
figure in the wave returns against committed evidence. OWNER (2026-07-12): all rulings per
DECISIONS v96. BUILD-REPORTED: gate-suite greens (consistent with committed artifacts;
re-provable on any fresh bootstrap).
