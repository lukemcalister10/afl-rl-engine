# HANDOVER — rev148 · 2026-07-16 · supersedes rev147 · SEAT 9 (Fable) CLOSES HERE
### THE ROTATION FILING. Seat 10 opens on this + the standard prompt: freshness check FIRST
### (R100.10), file the handed pack, then pick up at §2 — the segment-5 MINI-CHECKPOINT is your
### first act (trivial by design). Seat-9 journals (turns 1–59) are the crash record; the register
### (v223, items → 250) is the durable log. The seat-9 token is REVOKED at this seam.

## 0 — PINS (verify fresh; the pen moves main)
main = verify fresh (full-URL ls-remote). Canonical tag **v2.10** (`d14efae`) · shipped board
**790136a3** · store **b1fd0bce** · config `c2d233ae`. UI SHIPPED (#99/#101) · LTI tool MERGED
(#102). Candidate line: Leg-A `8b8ab7d` → seg-3 scaffolding `243106b` → seg-4 branch
`claude/legb-segment4-v1-2-law-grid-2l4dhv` at `ef6bf1a` (the v1.2 law wired + the two decisive
diagnostics) → **SEGMENT 5 IN FLIGHT continuing that branch**. Pack: manifest **v4.41** · CORE
v2.8 · DECISIONS **v115** · CONSTRAINTS v1.18 (repo-only; L-RECENCY is acceptance-carried, the
CONSTRAINTS fold rides the next re-seal) · acceptance **v1.20** · SSI v1.3.

## 1 — LEG B: FOUR ITERATIONS, CLOSED (the design story every seat must know)
**v1.0** wired at the posval component (elasticity ≥1 there ⇒ compresses by construction) aiming at
the smoothed level. **v1.1** fixed the axis to realised output but its hard games-floor MADE 144
PHANTOM ROOKIES (owner-caught; measured item 239). **v1.2** = the owner's WEIGHT-DON'T-GATE law
(R105.4: every played season counts, games×recency; no exclusion/floor/phase test) — measured
λ=0.81 at the seat-default half-fade; the λ pre-gate HALTED it. **v1.3 = FINAL (R105.6, owner:
"Accept")**: fade d = **0.25 OWNER-SET** (a last-season game ≈ a QUARTER of a this-season game) ·
machine selection bar **β ≥ 0.80 OWNER-SET** (the seat's 0.85 sat above the measured never-wipe
ceiling of 0.83 — impossible, retired with its derived λ gate; THE GRID IS THE ARBITER) · R105.5
L-RECENCY (per-game weight monotone in recency — guarded HALT-NOT-WARN self-test) + the ALL-804
MOVEMENT LEDGER are acceptance-carried. **ZERO seat-authored numbers remain in Leg B.** The kernel
table's key fact: the fade choice moves ONLY the injured veteran (negative-ρ ⇒ identity, no uplift,
re-enters on form — NOT a verdict); the young cohort is fade-insensitive.

## 2 — IN FLIGHT (exactly ONE chat): SEGMENT 5
Directive docs/DIRECTIVE_LEGB_segment5 (fired; base `ef6bf1a` STRICT; seals: memo v1.3 `cf6c0080…`
· acceptance v1.20 `6b83e336…`; docs ≥ `7eae8f7`-era main). Its MINI-CHECKPOINT (~15 min in) is
SEAT 10'S FIRST ACT: the diff should show exactly (a) UNCOMP_DECAY 0.5→0.25, (b) the L-RECENCY
self-test, (c) the R105.4 forbidden-list assertion — nothing else; PROCEED on that. Then A/B
(8d90c9ac byte-exact) → the grid {0.55–0.70} at the 0.80 bar (the algebra PREDICTS s=0.65 selects
at β_eff≈0.81, s=0.60 just misses at ≈0.797 — prediction, NOT measurement) → the full item-206
RETURN + the all-804 ledger + E/B vs the owner's HARD 1.75. Empty grid ⇒ HALT with the table
(the owner accepted this risk with open eyes — item 248).

## 3 — THE QUEUE (the path to the ONE bake)
Seg-5 RETURN prescreen (seat 10's first heavy act: the ledger scans — outliers · item-130 young
net-strip over EVERY row · tiny-recent-sample dominance) → **LEG C fires** (pre-drafted,
docs/DIRECTIVE_LEGC_flex_positional; placeholders <LEG-B-HEAD> + <DOCS-SHA>; the owner's four-write
rider incl. Flanders present→GDEF) → D (consumes the w-export; R104.9 strict descent; item-204
candidates) → E (posture presets; movers MUST include named LTI rows — Rozee — item 216) → the
LADDER (cold audit · the owner's sealed reads · his viewing incl. the s-overrule right) → his word
→ **ONE BAKE, chapter end** → tag v2.11. NOTHING BAKES BEFORE THE LADDER (owner-clarified item 249).

## 4 — STANDING CONDUCT (turn-one law; CORE-fold flagged)
NEVER AMEND after embedding a SHA — pins fill in FOLLOW-UP commits (item 249, the third pin lesson).
· Verify the doc CASCADE (handover→decisions→manifest→acceptance rows) AS A UNIT at every filing
(item 242 — three owner reviews found 4+6+5 defects there). · Report the APP counter; self-estimates
run ~40% LOW, treat as floors (item 238). · ONE fireable job = ONE document (233). · RE-VERIFY every
pin at handover (225). · Substantial compute → parallel Opus/Code (209). · Context report + reap
tasks at every HALT/RETURN (222). · EXTRA legs are multi-segment; the supervisor budgets; ~200k ⇒
retire at the next clean halt (212/222/232). · EFFORT + why-not-one-lower on every directive (234).
· PK-sync asks ship the documents same turn (186) · plain reads · DO:/WHY: · journals every turn.

## 5 — LOOSE ENDS CARRIED (seat 10's background queue)
The GO-LIVE directive pre-draft + the weekly score-CSV format proposal (queued, next quiet window) ·
Rollover SPEC v1 filed (build = post-bake chapter) · owner-input class ratification at go-live ·
ingestion store-write gated by the owner's v25 ruling · book cross-CPU hygiene (170n2) · the
owner's Law-2 flex register (his fill, his timing) · CONSTRAINTS fold of L-RECENCY at the next
re-seal · seat-9 token REVOKED at this seam (owner to-do).
