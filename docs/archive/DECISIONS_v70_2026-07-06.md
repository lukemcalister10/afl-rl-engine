# DECISIONS.md — AFL RL engine — v70 · v2.5 BAKED + PROMOTED · both divergence halves sealed
### Supersedes v68 (which was pre-consolidation). This is the live tracker as of 2026-07-06.

## PLAIN READ
v2.5 is baked and it's now the live board on `main` — the one every clone lands on by default. It's the honest board: DPP collapsed so every player has one position (Petracca is MID-only at 2414, Marshall a pure ruck at 431), the fake phantom rows gone, Kako's 2025 season restored (23 games @ 55.2), and the two export bugs that had two-thirds of prices ~12% too high are fixed (Louis Emmett went 1361 → 855). Nothing you eyeballed changed in the bake — Maric 1271, Langdon 567, Daicos 7002, all exactly as reviewed. The old "data keeps forking" disease is now impossible: five guards halt any build that reads the wrong copy or boots on a stale board, instead of it shipping quietly. What's left is cleanup (pruning dead branches) and then the real football work — fixing the model's known quality issues on the now-trustworthy board.

## OPEN — ACTION FIRST
1. **Commit the refreshed doc pack to the repo** — KICKOFF v8, ROADMAP v6, DECISIONS v70, updated SINGLE_SOURCE_INVARIANT (5 guards), and the settled handover. **DO:** land these in the repo so a fresh seat/build reads the current versions, not stale copies. **WHY:** docs only help if they're in the repo, not just in a chat.
2. **Rotate the supervisor** — clean seam reached (v2.5 live + promoted, guards sealed, branches pruned). Hand over on the settled handover + the refreshed pack.
3. **Then: start the V0/PVC overhaul** (ROADMAP Phase 1) — the real quality work, on the honest board. Rec: begin with EVID1 (young-player over-optimism, the top lever), re-run on the fixed board.

**DONE 2026-07-06:** ~~Prune dead branches~~ — deleted 25 dead branches; guardrail rescued `f1-f2-pipeline-divergence` (unique DO-NOT-MERGE evidence). origin now holds: main, lineage (a7-amendment-frozen-gate, dpp-strip-final-consolidation), 2 rescued (d17-board-walkforward-view, bake-v2-5-candidate), f1-f2-pipeline-divergence, + 14 roadmap branches. (Cosmetic leftovers not deleted: the prune scratch branch, the stale prebake-v2.4-anchor.) ~~Doc-pack refresh~~ — KICKOFF v8, ROADMAP v6, DECISIONS v70, SINGLE_SOURCE_INVARIANT (5 guards) all written.

## QUEUED & TRACKED (leaves only by explicit resolution)
- V0/PVC overhaul → re-bake (the big football piece; the 14 roadmap branches feed it).
- Calibration / beat-baseline gate (forecast skill is ungated).
- Fold the 50 `_force_active` "on-a-list" flags into the store as data; recency-rule fix for 3 (Jakob Ryan/Blanck/Graham); drop 10 redundant flags.
- Where the owner's Kako corrections get typed (close the write-path loop).
- Transition model build-out (populate the dormant `future_position`).
- Weekly loop + cockpit (after overhaul); Chapter 3 (LTI, staleness dial, distribution).
- PR #35 — open as the v2.5 bake record; merge/close at owner's discretion (not required — main already carries v2.5).

## LOCKED / DONE (v2.5)
- BAKED v2.5: tag `baked-v2.5-2026-07-05` @ e1d8d78; rollback anchor `prebake-v2.5-anchor` @ 4180f06. `main` = `bbf64b4` (v2.5 + Guard 5 + ruling). Store e1b4d8bf · engine efea88e5 · board 7d1eeef8 · book seal 5799a9ce.
- DPP stripped (0 blends, 0 live transitions; `future_position` dormant). Phantoms scrubbed + calibration lift sacrificed. The 34 reassignments applied. Off-by-one games fixed. Kako 2025 folded. pvc_snapshot pinned read-only.
- Five single-source guards live on canonical (data ×4 + boot-store assertion). Both divergence halves sealed.
- "forward-eligibility ≈ $0" retired: true for the median dual, false for ~20 swingmen (Marshall $984, B.Williams $687, Petracca $619).

## STANDING REDS (not blocks — pre-attributed)
A2 (Curtis/Ward 0.821 — Luke-ruled ships-red), A3 (Rozee — data-caused, out 2026), A12 (Travaglia<Moraes — data-caused). Any NEW red beyond these blocks a bake.

## PROVENANCE
- Owner-seen / verified: the whole v2.5 board (Luke eyeballed it); baked hashes; guard tests; the A7 and Guard-5 rulings (recorded inline in the repo).
- Report-only, re-run before building on: the overhaul flag-cluster magnitudes, EVID1–5 findings — clean and engine-sourced, but re-confirm on the honest board as each phase opens.

## SEED ORDER (fresh chat)
KICKOFF v8 · HANDOVER rev111 · POSTMORTEM · SINGLE_SOURCE_INVARIANT · **DECISIONS v70 (this)** · ROADMAP v6 · OPERATING_CARD · LTI_REGISTER.
