# HANDOVER — rev163 · 2026-07-19 · supersedes rev162 (2026-07-18)
### rev163 (items 380–396): THE DETERMINISM SAGA folded. The v2.11 chapter is BUILD-COMPLETE +
### AUDIT-CLEAR at 15a9abd (unchanged); the viewing→bake has been BLOCKED for a week by a numerical-
### reproducibility bug — a ±95 SCAR (Sheezel) / top-6 rank flip that appears on a MINORITY of
### containers. Four legs chased it one layer at a time: F6 froze the V0 surface (PARTIAL) → F7
### diagnosed the real amplifier = np.interp (cross-build) → env-pin hash-pinned the numpy wheel
### (INSUFFICIENT: identical wheel still flips) → DISPATCH-PIN (IN FLIGHT) forces runtime CPU dispatch.
### The two-container gate is the confidence anchor and has caught every insufficient fix. This doc is
### standalone (needs no archived doc). Detail: register items 380–396 (repo-only, the durable log).

## 0 — PINS (verify fresh; the pen moves main)
main = ALWAYS ls-remote (docs pen moves it; ≈`7911083a` at this filing, docs-only). Register head ≈v369.
Tag **v2.10** (`d14efae`) UNCHANGED — the baked, canonical, owner-usable engine.
**THE v2.11 CANDIDATE STACK (all ride into the bake):**
`15a9abd` (F5 entrant layer, PR #119, = the audit-clear chapter head) ← `540b62f` (F6 v0surf freeze,
PR #121) ← `3055ea5` (ENV-PIN numpy-wheel hash-pin, PR #123) ← **[dispatch-pin head — PENDING, will
stack on #123]**. Branches: F6 `claude/legf6-iso-freeze-bhigmd`; env-pin `claude/env-pin-2026-07-19-4y4w0p`.
Store `968de0c7` UNTOUCHED all chapter · curve `56dd7a7b` · q97m `cfdc7321` · **v0surf `3af2b725`**
(the F6 frozen surface, Guard-5 asserted, PRESENT at 3055ea5 = 24967 B) · rl_model `cc626d7d`.
**Board of record (see the REFRAME, §2):** balanced `06d8af60` (RL_LEGE=0 RL_LEGF=0, Σv 752,427,
Sheezel 7964, int-v `9d097fce`). Identity quartet: default `1f10220c` · balanced `06d8af60` · forward
`d85901af` · killsw RL_PVC2=0 `9829d01a`. Weather (flip) signatures seen: `83a4b21d` (F6-era) and
`d7a95e8d` (container-#2) — both Sheezel 7869 = −95.
**DO-NOT-MERGE / DO-NOT-BUILD-FROM:** `fc7045d6` (an F7 offshoot that DELETED v0surf.pkl — not in the
bake lineage, confirmed) · `8b8ab7d` (stale iso-corr, item 383) · `b854fbf` (F7 diagnostic HALT, no
engine edit — a finding, not a fix).

## 1 — WHERE THE CHAPTER STANDS
**v2.11 = BUILD-COMPLETE + AUDIT-CLEAR at `15a9abd`** (five-consumer migration · projection lens ·
riders (i)–(iv), R_realized 207 [184–229] · retro −1/−2 boards + bridge 770,987→771,152→752,427 · the
v1.3 conservation construction / entrant layer 83,538 · cold audit CLOSED, all five shards green —
items 341–375). **NOTHING in this saga has touched a player value:** every determinism leg is
value-neutral by construction (see §2). The chapter's viewing + bake are all that remain — blocked
only by the reproducibility bug.

**THE DETERMINISM SAGA (why the ladder paused; the ROOT is now known):** the board is a *discontinuous*
function at the precision floor — near-ties in the value path amplify ~1e-12 differences into a ±95
SCAR flip + top-6/top-15 rank swaps (RANK-UNSAFE, item 391/394). The amplifier is **`np.interp`**
(compiled C in the numpy wheel; NOT BLAS; ~25 calls in _merged_recover.py + ~13 other modules). The
cause sat one layer below each instrument, peeled in order:
- **F6 (`540b62f`, #121):** froze the V0 surface (`_iso_dec`/`_build_v0_curve` → `data/v0surf.pkl`,
  signature-keyed, Guard-5 pinned). Prescreen GREEN, value-neutral (k=0), owner ruled RIDE-AS-IS
  (item 388). But the viewing render then FALSIFIED "scope=one" (item 389): the board still flipped
  with v0surf loaded → F6 is PARTIAL.
- **F7 (`b854fbf`, diagnostic HALT):** confirm-residual FALSIFIED the pick-tax fits `_merged_recover.py:463/:466`
  (injecting 1e9× noise moved nothing). Op-level sweep isolated the real amplifier = **np.interp**;
  remediation out-of-fence → HALT (items 390/391). No engine edit.
- **ENV-PIN (`3055ea5`, #123):** hash-pinned the numpy 2.4.4 wheel (bundles its OpenBLAS) + a
  fail-closed ENV-PIN assert in bootstrap.sh (mirrors Guard 5). Prescreen GREEN, VALUE-NEUTRAL VERIFIED
  (diff = lock+bootstrap+proofs only, zero engine/value files; item 393). But the container-#2 gate went
  **RED** (item 394): a container with the BYTE-IDENTICAL pinned numpy+BLAS STILL flipped (`d7a95e8d`)
  → the wheel is RULED OUT; the divergence is **runtime CPU DISPATCH** (numpy ufuncs + OpenBLAS, both
  DYNAMIC_ARCH, pick machine-code paths per CPU).
- **DISPATCH-PIN (IN FLIGHT):** see §2.

**THE TWO-CONTAINER GATE (the confidence anchor — do not trim it):** a fix is not trusted until the
balanced board reproduces on ≥2 GENUINELY-DIFFERENT containers under it. It caught the env-pin
insufficiency before the bake. Confidence here comes from the GATE, not from any diagnosis (two seat
diagnoses were wrong — §4). **Fingerprint gap fixed forward (item 395):** every determinism proof must
now record HARDWARE (CPU model/flags + np.show_runtime + effective BLAS kernel), not just software.

## 2 — THE ONE OPEN TECHNICAL ITEM: THE DISPATCH PIN (items 395/396, IN FLIGHT)
`DIRECTIVE_DISPATCH_PIN_2026-07-19.md` (filed main; base `3055ea5` STRICT, stacks #123). Forces
`NPY_DISABLE_CPU_FEATURES`→baseline + `OPENBLAS_CORETYPE=HASWELL` in bootstrap PERMANENTLY, EXTENDS the
fail-closed assert to verify the RUNTIME dispatch actually gripped (np.show_runtime + BLAS corename,
HALT on mismatch), + a digit probe + a toggle demo + full hardware fingerprint. **PERMANENT INFRA,
not a viewing unblock:** it rides into the bake and every future compute — renders, bakes, audits, and
the weekly in-season loop (which the reproduction gate CANNOT protect, because new-round boards have no
known-correct md5 to gate against — the real reason ship-now-gated was rejected).

**THE BOARD-OF-RECORD REFRAME (item 395 — critical):** `06d8af60` is NOT sacred; it is merely "the
board the majority CPU class produced." What is needed is ONE deterministic board the owner VIEWS and
BAKES — and the viewing has not happened. So the dispatch-pin's pre-stated outcomes are ALL useful:
- **(i) pinned board == `06d8af60`** → board of record preserved; done.
- **(ii) pinned board == a NEW stable md5 (5/5)** → NOT a failure. The pinned QUARTET becomes THE
  CANDIDATE reference set; it is sealed by the owner at the viewing (which renders the pinned board);
  the bake gate re-pins to it. The build reports the NAMED row-diff vs `06d8af60` (near-tie scale).
- **(iii) pinned board NOT stable 5/5 under the pin** → the pin FAILED → **STOP; no 4th attempt**
  (standing guardrail, item 394) → fall back to ship-gated / freeze-the-board, owner's call.

**INCOMING SEAT JOB 1:** read the dispatch-pin return (owner couriers) → PRESCREEN (the assert VERIFIES
runtime dispatch, not just exports the vars; fence clean = zero engine/store/value/docs; the outcome
branch is honestly evidenced by the toggle demo + probe; under (ii) the quartet + named diff present) →
execute the branch. Under (i)/(ii): supervisor RE-PINS the render directive to the pinned md5 and
re-fires it.

## 2b — THE LADDER (ordered remainder, after the dispatch pin lands)
Re-fire the VIEWING-PACK render (`DIRECTIVE_VIEWING_PACK_2026-07-19.md`, already re-based to the pinned
lineage; re-pin its target md5 per the dispatch-pin outcome; run bootstrap so the pin+asserts are live;
a DIFFERENT container reproducing the pinned md5 IS the two-container gate) → the return is the VIEWING
→ owner scores his SEALED READS (held, unseen) + works THE RATIFICATION LIST (DECISIONS v124 §2: tail
read stand/retire · R=207 · entrant `a17aafed` · φ `fd92b6fc` · r_pop `c62b5ee8` · presets `c2e17c49`
· raw-pick benefit-of-doubt · knife-edge→weather; **under outcome (ii), also his seal of the re-based
board of record**) → his WRITTEN BAKE WORD (carries the guard-5 re-pin + RL_PVC2/RL_LEGE/RL_LEGF
INFRA_ALLOW + the ENV-PIN + dispatch asserts + BAKE_RUNBOOK 3b doc-landing incl. acceptance v1_21) →
ONE bake per `BAKE_RUNBOOK_v2_11` on a REPRODUCING+PINNED instance → tag **v2.11** → clean FF promote →
**PK sync (mandatory at the bake seam)**. POST-DELIVERY: round-entry GO-LIVE + the weekly tracking loop
(361/362 — now determinism-safe under the pin) + the v−R flat-derived board toggle (363/364, R=207) +
backward-eval Y-fix (348) + numeric-stack pinning is now DONE via this saga (347/380 closed by the pin).
**CARRY:** confirm BAKE_RUNBOOK enforces the reproduction gate + the ENV-PIN + dispatch asserts.

## 3 — STANDING CONDUCT (STANDALONE — the full set; CORE v2.8 wins on any conflict)
**ENTRY:** manifest → CORE (in full, binding) → this doc → DECISIONS → freshness check (full-URL
ls-remote · fresh shallow clone · PK vs repo doc-by-doc, by header version) → file/verify the pack
(ahead-or-equal ⇒ file; BEHIND on any doc ⇒ HALT). Never bulk-read; context is a budget.
**THE PEN (the seat's only write):** docs/-only token pen. Register: `tools/seat/pen.py append
--item-file F --header-summary S -m MSG` with `PEN_TOKEN` in the env (owner provides IN-CHAT; NEVER
written to any file/journal/commit; the seat-13 token was revoked, seat-14's is live). `git fetch
origin main && git reset --hard origin/main` BEFORE every append. Non-register doc pushes: commit
docs-only, PROVE with `git diff --name-only` (assert-before-push, non-docs ⇒ HALT), push `HEAD:main`
with the token from env, verify the remote moved + cite the SHA. **BOOT GUARDS now include the ENV-PIN
assert (+ the dispatch assert once it lands)** alongside Guard 5's store/q97m/v0surf pins — an
unverified ENV is a silent board mover, same disease as an unverified store.
**MESSAGES TO THE OWNER:** open with a PLAIN READ · G/A/R at checkpoints · asks lead with the
click/paste/word · DO:/WHY: for to-dos · his word outranks every document · a question is a question,
never a go · provenance-tag every carried claim (owner-seen / re-runnable / report-only) · present
options symmetrically with a recommendation + what would change it · nothing is fact until re-run or
owner-seen. **Owner is LOW ON FABLE this week — keep turns dense, no ceremony, no over-production.**
**BUILDS:** one store/engine writer at a time; read-only jobs parallel; builds never author docs;
every directive carries the GIT ENTRY block + a base pin; returns without their SHA are incomplete;
disposable (one job per chat) — retire on return, NEVER let a build linger to "autofix CI / respond to
reviewers" (unsupervised out-of-directive editing of bake-lineage code); end every SUPERVISOR turn with
a journal via present_files. **HARDWARE FINGERPRINT is now a standing spec for determinism proofs.**
**RECONCILIATION SOURCES:** register items + the session EXIT_PROOFs at the pinned heads; compare every
audit/render number against these; a mismatch is investigated before anything else moves.

## 4 — SEAT-14 CORRECTION LEDGER (owned; the gate + measurement corrected the seat)
Carries seat-13's ledger (rev162 §4). Seat-14 additions — **the pattern is REASONING-INSTEAD-OF-
REPRODUCTION, each costing a cycle:** (1) **F6 "scope=one" accepted at prescreen** — GREEN was correct
on what it verified (store untouched, k=0 on a CLEAN box) but the *conclusion* "weather frozen out" was
the build's clean-box claim; the independent re-run (the viewing render) I'd recommended as "lite" and
the owner declined then caught it anyway (item 389). (2) **The `:463/:466` hypothesis** — a code-read
guess, empirically FALSIFIED by the F7 build (item 391); credited. Lesson, now enforced in directives:
determinism is proven by REPRODUCING the break on a diverging machine (or by-construction + a runtime
assert), NEVER by reasoning on a clean box — forced-coretype-on-clean is BANNED as a sole proof. The
MITIGATION that held throughout: the two-container gate + halt-not-warn refused every unproven fix, so
nothing wrong shipped. **SEAT/MODEL NOTE:** same seat 14, same chat; the model beneath the seat switched
to **Fable** at the item-395 turn (owner-toggled). Recorded for the conduct trail; the pen was already
live (the R98.8→R99.3 suspension long expired).

## 5 — FIRST ACTS FOR THE INCOMING SEAT (in order)
1. Manifest → CORE (in full) → THIS doc → DECISIONS v124 → the register (its own header, ≈v369) → the
   freshness check (ls-remote main + tag v2.10 + the candidate stack; fresh clone; cite SHAs) → file the pack.
2. Read the dispatch-pin return (owner couriers) + register items 380→396 IN ORDER. PRESCREEN it and
   execute its outcome branch (i/ii/iii, §2). Any board build/repro check uses `RL_LEGE=0 RL_LEGF=0` +
   `bash bootstrap.sh` (ENV-PIN + dispatch asserts must PASS). Do NOT build from fc7045d6 / 8b8ab7d / b854fbf.
3. Then the ladder (§2b): re-pin + re-fire the viewing render → viewing → ratification → bake word →
   v2.11 → FF promote → PK sync. Nothing bakes without the owner's written word + his board view.
Pen mechanics, entry, reconciliation are in §3. Never bulk-read; context is a budget.
