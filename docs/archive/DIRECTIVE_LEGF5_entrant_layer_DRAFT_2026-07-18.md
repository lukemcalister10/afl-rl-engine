# DIRECTIVE — LEG F BUILD 5: THE ENTRANT LAYER + THE CONSERVATION GATE · DRAFT · seat 13 · 2026-07-18
### **STATUS: DRAFT — DO NOT FIRE. ⟦BASE⟧ fills at F4's prescreen PASS.** Owner-ruled construction:
### MEMO_LEGF v1.3 §2.viii + §2.x (item 359). WRITER build; the chapter's last.

## GIT ENTRY (verbatim; item-334 law) — SEAM CHECKLIST: seat 13 fills ⟦BASE-BRANCH⟧/⟦BASE-SHA⟧.
`git fetch origin main ⟦BASE-BRANCH⟧`; ls-remote must return ⟦BASE-SHA⟧ STRICT → checkout →
`git checkout -b claude/legf5-entrant-layer-<suffix>`. Docs on MAIN; never join lineages;
this-session fetch; hard-to-reverse = HALT. THREADS=1 everywhere. Entry: store `968de0c7` ·
curve payload `89c14729` · k=0 `06d8af60` + RL_LEGF=0 chain byte-exact re-proof · F4's r_pop
seal present ⟦SEAL⟧ — F3/F4's cures are the floor.

## EFFORT: High (the owner's conservation law is the gate). Why not Extra: display-layer resize
of an existing block + a gate already shown ~passable on real data (item 360 arithmetic: real
entrants +74k vs full-structure PVC ~75–80k). MODE: auto, PLAN first + seals. TIME ~2–3 h.

## FEED: MEMO_LEGF v1.3 (rules) + v1.0–v1.2 · the F4 artifacts @ ⟦BASE⟧ (r_pop + roster-matched
harness — S5 reuse) · F1's phantom block @ `7b6dfc5` · the R-inputs report @ `a90052a` ·
recorded intake history in the store · DECISIONS v123 · acceptance JSON.

## THE JOB LIST
1. PLAN + SEALS: the entrant-slot structure measured from RECORDED intake history (per-round pick
   counts + per-mechanism counts, smoothed, rule 7) — committed + sealed BEFORE render.
2. **§2.viii THE ENTRANT LAYER** (replaces F1's 30-pick + flat-207 sizing in the `rl_export.py`
   phantom block): the full expected annual intake (~100 slots) — the real pick structure at
   v2-curve PVC per pick · mechanisms at their measured pick-equivalents (90/92) at curve ·
   `phantom=true` · per-club allocation per the sealed structure. The F1 pick-slot strawman is
   SUPERSEDED (note it; the owner's ratification list updates).
3. **§2.x THE CONSERVATION GATE (the owner's law): LEAGUE-LEVEL backtest, ±5%, BOTH
   transitions** — projected full roster under r_pop + this entrant layer vs realized league
   totals (771k/771k/752k, ~flat). Plus re-run the F4 roster-matched gate (must stay IN) and
   commit the survivor diagnostic.
4. Per-lens totals report recommitted (bal/+1/+2, with/without phantom, per club + league).
5. EXIT: k=0 `06d8af60` byte-exact · RL_LEGF=0 chain · dormancy suites PASS · store untouched ·
   frozen suite · guard-5 = the known pre-bake red · annex · PR stacked on ⟦F4-PR⟧ · RETURN ≤30
   lines, THE TWO GATE TABLES FIRST.

## LAWS: HALT rather than bend — no entrant-count tuning against the gate (the structure is
sealed from history first); no re-opening F3/F4; any k=0 change ⇒ HALT. FENCE: IN =
`rl_export.py` phantom block + `ui/app/board.js` display if mechanically required (justify) +
`session_2026-07-18/legf5/`. HARD-OUT: everything else, absolute. S1–S6 · SILENCE IS A RED.
