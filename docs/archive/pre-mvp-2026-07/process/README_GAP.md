# docs/process/ — GAP NOTE
The four process docs referenced at final-handover (PROCESS_CHANGES, CONTEXT_BUDGET_RULES,
BUILD_AGENT_FEEDBACK, SUPERVISOR_AGENT_FEEDBACK) were NOT present in the build container at cut time
(not in uploads), so the source files could not be bundled. Their BINDING content, as relayed in the
final directive, has been folded into docs/KICKOFF_PROMPT.md (five-state vocabulary; <=30-line return
template; verify-by-script-never-inspection; never print what can be grepped; long outputs to files with
verdict tails; REQUIRED_INPUTS manifest; successor first tasks). ACTION: Luke/supervisor to drop the four
original files here at next kickoff for the record.
