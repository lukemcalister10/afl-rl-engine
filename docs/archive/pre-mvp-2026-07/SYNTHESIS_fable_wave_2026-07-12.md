# SUPERVISOR SYNTHESIS — the Fable research wave · 2026-07-12 · seat 3
### Per CORE v2.3 (synthesis after each research wave). Wave = PR #64 (four measurement specs,
### head f62aea7) + PR #63 (the seven-decision ruling pack, head a3eea59, rev A). Both PRESCREENED
### PASS against committed artifacts; all pilot numbers and read-only quantifications remain
### REPORT-ONLY until the v2.9 build re-runs them under the ladder.

## 1. THE ONE-CANDIDATE LAW (the wave's headline)
Decisions 1 (PVC) · 5 (discount) · 6 (age curve) · 7 (MSD pool) share the same two budgets — the
G-COHORT margin and the board surface. Ruled separately they would each demand a refit and a
re-measure; ruled together they are ONE combined candidate, ONE refit, ONE walk-forward
G-COHORT re-measure, ONE gate suite. The deferred trio entry-switch (register v20) and the SSP
line (Decision 4) ride the same candidate. The arithmetic that forces this: age-(A) SPENDS
cohort margin; PVC (b) and a lower discount ADD it (+1.4 pts at 15% → +8.6 at 12%); the emmett
prior-cap artifact binds Decision 1's option (c) to rates ≥14. The v2.9 build directive will
mandate the combined-candidate construction with per-lever attribution (G-ATTR).

## 2. THE EMMETT CORNER (mandatory attribution)
Three independent probes now hit the same player: the discount sweep's cap artifact (−20/−22% at
≤13%) · PVC (b)'s cap movers · the MSD-pool isolation refit (−29.8%, 1177→826 — the pool was
PROPPING his ceiling, not protecting him). louis-emmett gets named per-lever attribution in the
v2.9 build; his corner is where cap-removal and pool-isolation interact and where the owner's
football-nonsense review trigger (item 11) is most likely to be exercised.

## 3. THE G-Y0 PREVIEW (the sized breach)
Composition-weighted day-one value sits 9–14% BELOW the derived pick curve across bands 1–45
(MIDs above the curve, every other position below — deviations NOT netting) and +28% above at
46–60. The frozen v3.4 assets sit ≈ at V0 (band 1–5 gap +1.4%), so ADOPTING the derived curve
raises the bar the incoming kids must meet — Decisions 1, 2 and 3 are one ruling, exactly as the
paper stages them. Breach repair law carries: raise the young side, never cut survivors, never
bend the derived curve. The binding table rides the adoption job (this preview = age-18,
historical-mix illustration).

## 4. CORRECTIONS FOLDED (claim-accuracy)
(a) Register item 11 said "107 of ~2371" MSD training rows — WRONG basis (store-wide pick
holders). The trained pool today is ND 1255 · RD 640 · MSD 29 · PSD 7 PLAYERS (MSD = 114
player-season rows). The exclusion's measured effect (668 movers · 47 ≥5% · board +0.18%) is the
committed figure. (b) Anchors move non-zero under 7(a) (bont −0.35%) — the paper's earlier
"anchors untouched" line was amended by the build itself. (c) Membership stability is now
CONCRETE: mcandrew (p12) and perez (p35) hold live pick capital and are kept out of the training
pool only by the debut≤2021 window — after the deferred trio relabel their years move past the
window, so the exclusion becomes double-locked; the v2.9 design must state which leg is load-
bearing so a future data edit cannot silently re-admit them.

## 5. STALENESS RECONCILED
PR #64's specs were designed at base 626c8378, BEFORE items 11–16 ruled. Application law for the
v2.9 build: run every spec POST-item-11(a) (the pool exclusion is part of the world the
measurements land in). SPEC_4 (scarcity design) and Decision 7's quantification are
complementary — one designs the insurance object, the other sizes the pool change; neither
supersedes the other.

## 6. THE LENS FRAME (items 12–16, woven — not in the paper by design)
The owner's ruled lens architecture (phantom pick entries on +1/+2 only · board = player ranking
· lens-total quasi-conservation as a diagnostic) consumes Decision 5's dial and Decision 1/2's
seam identity; it rides the same v2.9 export work, sequenced after the discount word. The UI is
already forward-safe (the merged #62 guard).

## 7. WHAT THE OWNER RULES, AND WHEN
Nothing now. At the v2.9 sitting: the paper's RULING SLATE — seven answers in one message —
with this synthesis beside it and the combined-candidate law wired into the build directive.
