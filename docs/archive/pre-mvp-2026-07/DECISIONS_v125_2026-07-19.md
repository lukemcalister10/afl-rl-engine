# DECISIONS — v125 · 2026-07-19 · supersedes v124 (2026-07-18)
### v125 (items 394–403): folds the determinism-pivot + bake rulings (R108.7); marks the viewing
### reads/ratification WAIVED by the owner for the v2.11 bake; replaces the stale §3 to-dos (the audit
### shards are long done; the PK-sync target is now v4.57/rev164). The register (≈v376) carries every
### verbatim — read its OWN header.

## §0 — CARRIED FORWARD (BINDING)
R98.x–R108.6 in force as at v124 §0. PLUS the seat-14 owner rulings:
- **R108.7 (items 396–402, the determinism PIVOT + BAKE):** (a) the ±95/rank flip is CROSS-MACHINE
  np.interp dispatch; the env-pin wheel-pin was INSUFFICIENT; the dispatch pin is UNPROVEN on clean
  containers → **DECOUPLED from delivery**: the BAKE is protected by the reproduction gate and never
  needed the root-fix. (b) **BAKE NOW, ROUND-14** — viewing / sealed-reads / ratification / cold-audit
  WAIVED (owner, 2026-07-19); the ONE guard kept is the reproduction gate (bake only on a container
  reproducing 06d8af60 — free + correctness, not review). (c) the weekly-loop determinism → PRE-GO-LIVE
  (very likely a non-issue on a SINGLE consistent environment; needs one confirmation). (d) the boot
  re-stamp + guard-bypass close ride the bake (value-neutral). (e) the go-live store-write + tracking
  are a REAL BUILD (never built), FIRED in parallel with the bake.

## §1 — OPEN OWNER READS (WAIVED for the v2.11 bake; revisitable post-delivery)
- **THE TAIL READ (325/332/343):** gross p1/p60 ≈5.5×; his 7.5×+ lives in the replacement adjustment.
  The owner WAIVED the viewing for this bake, so this is NOT ruled at v2.11 — it carries to the
  post-delivery entry-pricing work if he wants it. Not a bake blocker.

## §2 — THE VIEWING RATIFICATION LIST — **WAIVED** (owner, 2026-07-19)
The owner waived the viewing + sealed-reads + ratification for the v2.11 bake. The list is retained as a
record only (tail read · R=207 · entrant `a17aafed` · φ `fd92b6fc` · r_pop `c62b5ee8` · presets
`c2e17c49` · raw-pick benefit-of-doubt · knife-edge→weather). None of these blocks the bake. If the
owner later wants any ratified, they ride a post-delivery pass — do NOT re-raise them as bake gates.

## §3 — OWNER TO-DOs (current)
**DO:** run the two build blocks (bake + store-write) handed in-chat — FIRED. **WHY:** the bake → your
round-14 board; the store-write → the weekly loop, in parallel.
**DO (when the bake returns):** run the tag + FF-promote commands the build hands you (tag/promote are
yours). **WHY:** that makes the round-14 v2.11 board canonical + live.
**DO (post-bake, toward the weekly product):** the incoming seat fires the per-player rank TRACKING once
the store-write pins the snapshot shape, confirms single-env go-live determinism, and wires the weekly
loop (rounds 15→19 one-by-one). **WHY:** that is the actual remaining build — no toggle, no promised clock.
**DO (at the bake seam):** sync PK — manifest **v4.57** · HANDOVER **rev164** · DECISIONS **v125**
(CORE/SSI unchanged). **DO (post-delivery, your word):** round-entry GO-LIVE.
