# DIRECTIVE — LEG E: PROJECTION LAW + POSTURES · DRAFT · seat 13 · 2026-07-18
### **STATUS: DRAFT — DO NOT FIRE. Placeholder pins unfilled.** Becomes operative only when seat 13
### fills every ⟦SLOT⟧ at the post-migration seam and files the fireable version. Companion: the
### finalized MEMO_LEGE (v1.0, issued same seam; the v0.1 draft does not rule). Per HANDOVER rev159
### §2 ACT 2 · SPEC v1.4 §3 Leg E + §6.2 · R103.3 · R104.4/.5 · R105.4.

## SEAM CHECKLIST (seat 13 fills at issuance — the slots below)
1. ⟦BASE⟧ = the post-migration candidate head SHA + branch (after its prescreen passes).
2. ⟦DIAL⟧ = the `LENS = {now, bal, fut}` definition + consumption line numbers RE-PINNED from
   ⟦BASE⟧'s tree (the memo's `:397/:425/:444` are groundwork-base pins; the register's `:317` is
   stale; the migration edits `rl_model.py`, so every line moves).
3. MEMO_LEGE finalized → v1.0 OPERATIVE, filed, named here as the construction of record.
4. Riders (i)–(iii) return status noted (viewing prep only; nothing here depends on them).

## GIT ENTRY (verbatim; boilerplate — the item-333/334 law)
Your workspace opens on `main`; that is NOT your base. First acts, exactly:
`git fetch origin main ⟦BASE-BRANCH⟧` · `git checkout ⟦BASE-SHA⟧` · `git checkout -b
claude/lege-projection-<fresh-suffix>`. Discard any auto-created branch; push nothing from it.
Expected topology, stated: governance docs live on MAIN; the engine candidate on ⟦BASE-BRANCH⟧ —
both true at once, never joined; main merges are the owner's alone. An md5 is a content check,
never a place to stand. Every repo claim cites a this-session fetch. Any step that looks
hard-to-reverse or needs a topology decision not written here = HALT, push nothing, return ≤10
lines. Entry assertions: boot-store guard (store `968de0c7`) · curve payload `89c14729` · the
balanced board hash at ⟦BASE⟧ recorded as ⟦B-HASH⟧ for the byte-hold proofs below.

## EFFORT: High. Why not Medium: the projection law is the subtle half (R103.3 — credited
production, not the no-improvement floor) and the k=0 byte-exact invariant makes any slip loud but
only if the proofs are real. Why not Extra: display machinery over existing dials — no new model,
no new lever, values un-baked (R104.4); the balanced board cannot move by construction and that is
proven, not assumed.

## MODE: auto — first committed artifact is the PLAN, which MUST open with the memo §3 measurement
discipline: read the D5 dial family's EXACT semantics from ⟦BASE⟧'s code (which horizon each key
weights/discounts) and state it BEFORE any profile renders. TIME: one chat, ~2–4 h; S6
backgrounding; confirm band, flag >2×/<½×, report actual.

## FEED: MEMO_LEGE v1.0 (the construction of record) · DECISIONS ⟦current⟧ · CONSTRAINTS v1.19 +
`acceptance_v1_21.json` (assert `laws[LENS-PROJECTION]` and
`leg_d_placeholders.posture_2027_discounts` by key) · SPEC v1.4 §3+§6.2 · SSI v1.3.

## THE JOB LIST (per-task commits; memo v1.0 wins over this paraphrase)
1. PLAN incl. dial-semantics statement (above) + derived fence.
2. **THE PROJECTION LAW (memo §2):** +1/+2 lens = the SAME un-compressed map at the projected
   evidence state (age+k · availability-layer games accrual, LTI truncation honoured · the map's
   own cohort/growth). The Reid constraint: no lens-only growth term, no separate young
   multiplier. L-SMOOTH · L-SYMMETRY · weight-don't-gate bind inside the lens exactly as outside.
3. **POSTURES (memo §3):** contender/rebuilder as new VALUES over the existing dial vector, owner
   parameter slots taken from memo [OWNER] marks — proposed numbers rendered, NOTHING ruled; the
   owner rules at viewing.
4. **COMPOSITION LAW (memo §4):** one discount per axis; the R104.5 posture-2027 discount applied
   ONCE, final step; the committed synthetic-pick unit test proving single application per posture.
5. **INJURY × POSTURE (memo §5):** the default construction only (graded amplification via the
   existing availability haircut); no new mechanism.
6. **RETIREMENT:** the interim no-improvement-floor lens DELETED (not disabled) with its OBITUARY
   (rule 7); the UI toggle re-enabled on this landing per SPEC §3.
7. EXIT PROOFS: **k=0 lens ⇒ balanced board ⟦B-HASH⟧ byte-exact** (the memo's assertable
   invariant) · balanced board unchanged by every posture render (it alone gates/bakes/seals,
   R104.4) · no horizon cliffs (L-SMOOTH sweep committed) · store untouched · frozen suite (S4) ·
   five SSI guards · gates snapshot · candidate PR.

## FENCE (derived from the job list in the PLAN; the 322 law)
IN expected: the lens/dial/posture sites in `rl_model.py` + UI lens toggle site + the session dir
`session_⟦DATE⟧/lege/`. HARD-OUT: the store · the curve artifact · the balanced-board pricing path
(any diff that moves ⟦B-HASH⟧ at k=0 is a build defect, full stop) · `docs/` · SEASON_PROG (owner
dial). HALT on any job requiring a HARD-OUT file. Scope growth ⇒ new directive (S2).

## STANDING RULES: S1–S6 · SILENCE IS A RED · statistics law (finest resolution, smoothed, no
wide bins) wherever anything is measured.

## RETURN: ≤30 lines · branch · head SHA · PR number · actual vs band · the three narrowest
margins · plain-terms close.
