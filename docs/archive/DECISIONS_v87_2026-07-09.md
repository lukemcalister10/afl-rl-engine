# DECISIONS — v87 · 2026-07-09 · supersedes v86 · the single live tracker (rulings + owner to-dos ONLY)
### Written against manifest v4.3 (WD-4). Process = CORE · state = HANDOVER · constraint text =
### CONSTRAINTS v1.6 + acceptance v1.6. v87 delta = §26–§31: the CI landing + OQ-F + merge policy ·
### the Fable-window outputs and prescreens · the owner's Law-2 clarification · the two store
### corruptions + collision sentry + ID-keys proposal · the gate-integrity landing + re-seal ruling.
### v86 §24–§25 and v85 §16–§23 CARRY UNCHANGED. Lineage: archive/CHANGELOG.

## PLAIN READ
Your CI safety net is live on main: every push now re-runs the guards and goes red on a wrong config.
Your Brodie halving shows on the board with an owner-override tag, provably touching nothing else. All
three Fable jobs came home before the window — the UI direction, the injury spec, and the flex spec —
and you clarified in your own words what the flex law really prices: the CHANCE a player picks up a
second position's eligibility, not a march to a new position. Two birth years in the store are wrong
(the St Kilda Max King borrowed Maxwell's 2007; Ben Murphy is recorded as born in the future) — a small
fix job corrects both, hunts down the remnant that overrode your earlier Murphy correction, installs a
permanent tripwire on the King pair, and re-seals the book fingerprint at your word. The release gates
themselves were repaired: they now grade THIS version's board, not last version's. Your rulings pending:
the eighteen spec calls, and the go word on the flex-law rewrite.

## SETTLED THIS ROUND (v85 §16–§23 + v86 §24–§25 carry; adds below)
26. **CI + Brodie landing; OQ-F; merge policy.** PR #47 prescreened PASS (supervisor: 5 commits
    PLAN-first, 9 files in fence, store untouched, export wiring read line-by-line — display-only
    proven); merged by owner as merge commit **60663bd**; branch deleted; CI green on main. Narrowest
    points recorded: bake-time B4 differs by Brodie's display block until the next bake regenerates the
    baked artifact (benign, self-resolving) · time over band (flagged, compliant) · duplicate CI runs
    per event (backlog cosmetic). **OQ-F RULED: Option A** — the Project pack stays authoritative; repo
    docs/ are synced copies at each seam ("for now"; revisit when doc churn slows). **STANDING POLICY:
    candidate PRs merge as MERGE COMMITS** (never squash/rebase) — cited SHAs must remain ancestors of
    main so ancestry verification keeps working.
27. **Fable window complete (3/3), prescreens PASS.** UI direction (PR #48, head 60172dd) · injury
    derivation spec (PR #49, head d76e8dad) · positional-flex spec (PR #50, head 49a1435d) — all
    read-only fences held, all HELD OPEN to ride the next doc sweep. 18 owner rulings pending (8 injury
    + 10 flex; 3+3 flagged as the consequential ones). Flex R-3 gains an owner-prompted FLOOR sub-clause:
    friction discounts the flex bonus toward zero, NEVER negative — a dual-eligible player is always ≥
    his single-position twin (hard requirement + test in any build). SEAT NOTE: the three prescreens
    initially ran on Opus by accident (owner disclosure); the Fable re-check reconfirmed the fences and
    SHAs, UPGRADED the injury key claim to fully verified (43/43 vs store), verified the mockup banners,
    identified the mystery `00d82dde` as the owner's July-6 upload commit (old pre-bake main — the
    recurring stale-sandbox-main), and found the Ben Murphy corruption (§29).
28. **LAW-2 CLARIFIED (owner-worded, 2026-07-09 — supersedes the flex spec's transition reading).**
    Law 2 prices PROBABILISTIC FUTURE ELIGIBILITY, not positional transition: same player and producer;
    what is uncertain is whether the eligibility tag arrives; future value = the probability-weighted
    blend of the eligibility streams (JHF: ~75% pure-MID stream + ~25% MID/G-FWD stream, the MAX law
    applying within the dual stream). "80/20 meet-toward" = probability weights across streams. R-1
    RESTRUCTURED: Option A (same projection both streams; only the replacement bar differs) = the
    owner's stated intent and the DEFAULT; Option B (stream-conditional projections) = an optional
    refinement the owner concedes is "probably fair." NEW RULING REQUIRED: the probability source —
    owner register / derived base rate / derived-with-owner-override (supervisor leans the last). The
    flex spec's Law-2 section requires a re-cut (Fable revision directive AWAITING GO; window Jul 12).
29. **Store corruptions (2) + collision sentry + ID-primary keys.** Supervisor impossible-age scan
    (all 2652 rows): exactly two hits. (a) `max-king-stk` `_by`=2007, true 2000 (DOB 7 Jul 2000,
    web-verified) — owner history: ~15 cross-bleeds between the two King profiles; the 2007 King was
    renamed "Maxwell King" to stop it; stk's field was previously correct and later borrowed syd's
    value (field-bleed, not a row merge — rows verified cleanly distinct). Consequence: stk missed 2025
    (knee) so he sits in the injury derivation's 184 return cases mis-binned as ~18yo — fix lands
    BEFORE the Chapter-3 build derives; injury rulings R-i/R-v HELD until then. (b) `ben-murphy`
    `_by`=2027/`_bd`=2027-02-01 (born in the future), true early 2007 (Brisbane Irish Cat-B, 18 at
    late-2025 signing, web-verified) — owner provenance: the original submission fault WAS corrected
    and a remnant overrode the correction → the fix job includes a REMNANT HUNT so the next refresh
    cannot re-break it. RULED IN: a permanent COLLISION SENTRY (halt-not-warn self-test; named-pair
    identity assertions, King pair first; pairs added only when they bite). PROPOSED & QUEUED
    (owner-originated): **ID-PRIMARY KEYS** — a stable unique player ID (owner holds the database)
    becomes the engine's primary identity; the name/slug demotes to presentation; registers carry both.
    Supervisor sequencing rec: AFTER the Chapter-3 injury build (no key-convention churn mid-chapter);
    owner schedules; first input = his ID database export.
30. **Gate-integrity landing (PR #51) + re-seal ruling.** Prescreen PASS (supervisor: head 32db738 on
    post-CI base, 23 files in fence, store untouched, sensitive diffs read line-by-line — seal VALUE
    untouched by the build, config enforcement no-op outside bake/gate mode, matrix meta only on the
    gate-regen path, `par_redesign` revealed as a standalone mock the board never reads). The gates now
    certify the CANDIDATE: B1/B3 regenerate + hash-assert the current matrix (v2.5 = named comparator;
    the recorded v2.6 run had B1 reading the v2.5 matrix at 143; the candidate reads 130) · B2 self-runs
    its producer to a hashed, unrounded JSON cert (forged/stale/rounded certs now rejected; tol 0.5
    untouched) · PAR_RAMPS default aligned (pure hygiene) · state strings say BAKED v2.6 · one config
    manifest (39 vars) hash-stamped into boot/seal/report, ambient env cleared in bake/gate mode.
    **OWNER RULED: RE-SEAL NOW** ("updating the fingerprint makes sense"; the seal "should measure this
    version's book") — executed via the store-correction job at POST-FIX values (re-sealing pre-fix
    would be invalidated immediately). OPEN PRECISION QUESTION for the next audit: the exact metric,
    bound, and margin behind "peak 130" (if a hard ≤130-family bound, that is a zero-margin pass).
31. **Standing directive line (owner-agreed).** Every future directive carries: FETCH FRESH and verify
    local main against the live remote before citing base (two builds booted on the stale 00d82dd
    sandbox main; both recovered; the line makes it mechanical).

## OPEN — OWNER TO-DOS
- **DO:** merge PR #51 (merge commit). **WHY:** the store-fix job builds on its gate machinery.
- **DO:** fire DIRECTIVE_store_correction_reseal_v1 (fresh Opus chat; FEED: acceptance_v1_6 ·
  CONSTRAINTS_v1_6 · DECISIONS_v87). View the two players' before/after values in its return, then
  merge. **WHY:** two verified corruptions, one contaminating the injury derivation; your re-seal lands
  at the right values in the same stroke.
- **DO:** replace DECISIONS v86 with v87 in Project knowledge. **WHY:** nine rulings since the last fold.
- **DO (Fable window ends Jul 12):** go word on the flex Law-2 revision directive. **WHY:** your
  clarification supersedes the spec's Law-2 section; last Fable-budget item.
- **DO (whenever):** the 18 spec rulings — injury 8 (R-i/R-v un-hold once the store fix lands) + flex
  10 (R-3 now "B, floored" if you take the recommendation) · the 4 UI taste questions · branch sweep
  (3 branches) if not yet run.
- **AWAITED:** store-fix return · your rulings as they come.
- **DEFERRED:** ID-primary key migration (owner schedules; post-Chapter-3 recommended) · G-CONVEX
  graduation (yours whenever) · viewer partials (UI chapter).

## SUPERSEDED / CLOSED (adds; do not resurrect)
The flex spec's transition reading of Law 2 (§28 clarification governs) · re-sealing the book at
pre-fix values (sequencing ruled: seal rides the store fix) · treating the two 2007 Max King birth
years as genuine "identical DOB" collision evidence (one was corrupt) · hand-editing the seal outside
a build's proof chain.

## PROVENANCE
OWNER (this delta): OQ-F=A · merge-type question + acceptance of merge-commit rec · re-seal now + "it
should measure this version's book" · Law-2 clarification (verbatim intent §28) · Ben Murphy 2007 +
remnant history · King 15-merge history + rename context · ID-primary keys proposal · standing
fetch-fresh agreement · seat/model disclosure (Opus interlude). VERIFIED (supervisor, fresh
fetches/reads, 2026-07-09): PR #47 prescreen + merge landing 60663bd + branch deletion · the three
Fable prescreens (fences, SHAs, 43/43 keys, banners, gfut/futblend seam, harvey-thomas) · 00d82dde
identified · the impossible-age scan (2/2652) · Max King true DOB + Ben Murphy true age (web) · PR #51
prescreen (sensitive diffs line-by-line; seal value untouched; config-file var count 39). REPORT-ONLY:
build-run numbers not independently re-run (CI 13/13 visible on GitHub; B2 0.000; B1 143→130 —
precision question open). Verbatim returns: couriered in this supervisor chat, turns 7/10/11/12/17;
repo-homing at the next docs sweep (WD-2).
