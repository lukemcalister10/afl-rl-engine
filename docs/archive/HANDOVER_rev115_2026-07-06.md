# SUPERVISOR HANDOVER — rev115 · supersedes rev114 · CLEAN FORWARD PACK · Wave-0 closed, derivations next
### 2026-07-06. Read KICKOFF v10 (§0 single-source · §3B conventions incl. the new doc-sync invariant) + POSTMORTEM +
### SINGLE_SOURCE_INVARIANT first, then THIS, then DECISIONS v75. AUTHORITATIVE for current state.

## ONE LINE
v2.5 is live and independently verified. **Wave-0 (the plumbing) is CLOSED** — dead-code stripped with zero price change,
the "what moved" change-ledger built and self-proven, the anti-inflation identity-gate confirmed genuinely complete. The
measurements we could take are banked. **No decision gates anything.** Next is the model-derivation work; then a cold audit,
the owner's word, and a re-bake.

## VERIFIED / CURRENT STATE
- **Honest board = `origin/main`** (v2.5; store `rl_model_data.json` md5 `e1b4d8bf`, engine `efea88e5`, panel 10/10). HEAD has
  advanced past `7bc5726` only by OWNER DOCS-ONLY uploads (v74 committed by the owner — board untouched). Navigate by NAME + assert
  the store; never a store md5 as a place to stand.
- **Wave-0 candidates (stacked, NOT promoted — they ride to the re-bake):** 0a dead-code strip `PR #38`
  (`claude/wave0a-dpp-deadcode-strip-hcalwi`; 0 value deltas across 1,002 players, only `gf`/`fut` columns dropped, board
  `7d1eeef8`→`1d6907e8`, source unchanged) · 0b change-ledger + identity-gate confirm `PR #39`
  (`tools/change_ledger.py`; the gate at `one_source_selftest.py:83-96` + `rl_export.py:268-278` is a genuine load-vs-compute HALT
  that closes the systemic finding) · 1a measurements `PR #40` (`evidence/measurements_ledger.md`).
- **Change-ledger self-proven:** 0 moved on the Wave-0a strip (known-answer) + correctly lit up a real historical change (Emmett
  $1,361→$855, Kako's restored season) — which also re-confirmed those known-good values sit in the honest board.

## ⚠ KNOWN-STALE / OWED (read before touching the specs)
1. **Fable `OVERHAUL_SPEC_v1` (PR #37) "all LIVE" inventory is STALE** (phantoms/DPP are GONE, confirmed). Correction still owed at
   a docs pass. Do NOT treat its inventory or its owner-decision register as live.
2. **PVC spec v1 (PR #20) is a valid design core but STALE-as-executable.** Its status is in DECISIONS v75; the header-stamp +
   number-re-stamp + coordinate-re-check ride the PVC opener as step 1, BEFORE any fit.
3. **DOC-SYNC (just fixed for decisions):** the repo `docs/` had lagged to v70 while the authoritative files ran to v74 — a build
   caught it. Owner has committed v74. The rest of THIS pack (rev115, v75, ROADMAP v9, KICKOFF v10, the injury study) is synced at
   this handoff by the owner's upload. Invariant now standing (KICKOFF v10 §3B / DECISIONS v75): held files authoritative, repo swept at each seam.

## SETTLED / OPEN
Full detail in DECISIONS v75. In brief — SETTLED: injuries (return-haircut shape is a HYPOTHESIS to re-measure net of the aging
curve) · PVC (2004 window; stale-spec re-stamp first) · evidence-weighted blend · ruck (derived + calibration) · establishment-P
dead · DPP stripped · viewer (pure-view) · measuring-stick dropped + change-ledger built · **KPF is a DISTINCT loose-pricing locus
the aging fix won't reach (own treatment needed).**
OPEN / NEXT (in order): **derivations** (young-slope flatten [confirmed top lever, natural first] · PVC [re-stamp first] · aging
decline · ruck+calibration · KPF treatment — several parallel) → **injury measurement net of aging → the 3-arm fix** → **one
integration candidate (the FABLE spot — retention+floor once, injury∥aging no double-count)** → **cold audit → owner word →
re-bake**. Non-blocking: two owner METHOD-CALLS (M1 breakout follow-through validation protocol; F11 band-skew feature/yardstick).
Viewer track parallel.

## HOW THIS SEAT OPERATES (carry KICKOFF v10)
Verify against real artifacts; a build's "it passed" is "consistent with what I can see" until cold-re-run or owner-seen (the cold
audit before the bake is where candidate PRs get their independent re-run). Navigate by branch NAME + git SHA. Supervisor stays
BLIND to the repo — no supervisor-side clone; the owner + a build's prints are the eyes. Supervisor authors + updates the doc pack;
the OWNER uploads it to the repo + seeds the next chat — builds NEVER author handover docs. Provenance-tag every carried claim.
Journals every turn. **Parallelism works** (this session: two concurrent build chats — a write job + a read-only measurement job —
ran cleanly; read-only measurements parallelize with a write job; keep ~2 concurrent as the courier ceiling). **Rotate on a REAL
trigger** (context genuinely heavy OR a clean seam before a heavy phase), not a nervous gauge — this rotation is the genuine seam
(Wave-0 closed + measurements banked, before the sustained derivation stretch).

## STATE / SAFETY
Clean seam. Wave-0 closed, measurements banked, doc-sync fixed, decisions cleared. Nothing canonical in flight; nothing half-done.
Candidates PR #38/#39/#40 pending the re-bake. The heavy sustained derivation work starts fresh from here.

## SEED ORDER (fresh chat) — the CLEAN pack
KICKOFF v10 · **HANDOVER rev115 (this)** · POSTMORTEM · SINGLE_SOURCE_INVARIANT · DECISIONS v75 · ROADMAP v9 · OPERATING_CARD ·
LTI_REGISTER · INJURY_RETURN_STUDY_v2 · (measurements_ledger lives in the repo at PR #40 — reference; its numbers are in DECISIONS v75).
First action: begin the derivations (young-slope flatten is the natural first; PVC opens with the spec re-stamp).
