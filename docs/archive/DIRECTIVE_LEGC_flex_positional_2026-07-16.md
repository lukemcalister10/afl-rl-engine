# DIRECTIVE — LEG C: FLEX POSITIONAL MACHINERY · 2026-07-16 · seat 9 (Fable) · v1.1 (seat-10 pre-fire revision, item 268)
### v1.1 PRE-FIRE AUDIT FIXES (owner-requested double-check): (1) checkpoint CONVERTED to
### commit-and-proceed (owner-authorized overnight run); (2) the write count corrected 11 → the
### MEASURED THIRTEEN (the register was finalized after the item-180 count); (3) the vocab
### normalization stated (RUCK ≡ RUC — 54 primary rows write RUCK); (4) FEED DECISIONS v111 →
### v115; (5) acceptance asserted AS JSON, never prose; (6) the item-256/258/259/266 owner laws
### added to deliverables (their acceptance fold rides the pre-ladder re-seal).
### STATUS: PRE-DRAFTED — fires ONLY after (1) the Leg-B RETURN prescreens PASS and (2) the owner's
### word. TWO PLACEHOLDERS, filled at fire time: 273463e (the prescreened Leg-B candidate head)
### and 8add349 (main at issue). THE ONE STORE WRITER of the chapter. SILENCE IS A RED. S1–S6.

## EFFORT: High · MODE: auto — the PLAN is committed and pushed as the FIRST artifact (unchanged
## content: the pre-view hash manifest (this directive + acceptance v1.20 + the positioning CSV
## md5s) · the enumerated store writes (the recomputed primary-future diff + the rider = the
## COMPLETE write list, row by row) · the blend wiring site · the attribution construction) —
## then **PROCEED WITHOUT WAITING** (owner-authorized overnight run, register item 268: the plan
## is prescreened WITH the return; the ladder — cold audit · sealed reads · owner viewing —
## still precedes any merge or bake, so nothing irreversible runs unreviewed).
## TIME: 3–5 h wall-clock (confirm up front; flag >2×/<½×; report actual).

## FEED (documents verbatim — never restated)
1. `docs/SPEC_PVC_FLEX_CHAPTER_v1_2026-07-16.md` (header v1.2) — §1 THE BINDING LAW (the ruled
   stream semantics verbatim: one future position per player = a probability blend of STREAMS
   summing to 100 — a pure stream + AT MOST ONE dual primary/alternate stream; the dual stream
   NEVER revalues the player "as" the alternate — peak/curve/runway/key-premium stay keyed to the
   PRIMARY; the dual stream ONLY swaps the FUTURE REPLACEMENT BAR for the LOWER of the pair (the
   MAX law), p_dual% × the capitalised value of netting against the lower bar, floored ≥ 0) + §3
   Leg C. 2. `docs/inputs/AFFL_Future_Positioning.csv` (the owner's register; stable IDs). 3.
   `docs/acceptance_v1_20.json` (`leg_c_remeasure` + the earned-component gate + census cells). 4.
   `docs/DECISIONS_v115_2026-07-16.md` + `docs/CONSTRAINTS_v1_18` (L-AXIS · G-COHORT 1.30 · the taxonomy). 5.
   Register items 216/217 (the rider, owner verbatim). 6. This directive.

## BASE PIN
- **Engine/store base — STRICT:** the candidate branch at **273463e** exactly (the prescreened
  Leg-B head; verify by full-URL ls-remote; any other SHA ⇒ HALT). Continue THAT branch (never a
  sibling — item 202/219; this directive's inputs are all at or before 8add349, so no item-219
  contradiction exists: the base carries everything needed except docs, which are FETCHED, not merged).
- **Docs base:** main AT OR AFTER **8add349**, `git diff --name-only 8add349..main` docs/-only.

## THE JOB (kill-switch `RL_FLEX` — =0 ⇒ the 273463e board byte-exact; the RL_ISOFADE pattern)
1. **THE INGEST — VALIDATE-OR-HALT:** parse the positioning CSV — 804 stable IDs resolve · vocab
   exactly {GDEF,GFWD,KDEF,KFWD,MID,RUC} store-side AFTER NORMALIZATION — strip hyphens/spaces,
   uppercase, and **RUCK ≡ RUC** (measured at issue: the register PRIMARY column writes RUCK on 54
   rows and RUC once in ALTERNATE — normalize, do NOT halt on RUCK; the item-180 VOCAB TRAP: never
   confuse store vocab with board posCodes; state the full mapping in the PLAN); HALT only on a
   token outside the NORMALIZED vocab · at
   most ONE alternate per player · streams 0–100 summing to 100 · name→ID via the positioning file's
   own map, HALT on ambiguity (the two Max Kings are distinct).
2. **THE PRIMARY-FUTURE DIFF** → `future_position` store writes (the one authored source; SSI
   guards). RECOMPUTE the CSV-primary vs store diff at run time; the PLAN enumerates the COMPLETE
   recomputed list. Measured at issue (seat-10 audit, RUCK≡RUC, vs the 273463e store): **THIRTEEN**
   — Carroll MID→GDEF · Uwland GDEF→MID · Roberts GDEF→MID · Lindsay GDEF→MID · Thomas GFWD→MID ·
   Powell GFWD→MID · Graham GFWD→MID · Butler GDEF→GFWD · Humphrey GFWD→MID · Hall GFWD→MID ·
   Lynch GFWD→KFWD · Lloyd KFWD→KDEF · Emmett RUC→KFWD. (The item-180 count of eleven predated the
   owner finalizing the register.) HALT only if your recomputation disagrees with your own PLAN
   enumeration at write time, or with this measured list — a disagreement is a finding for the
   supervisor, not a thing to improvise past.
3. **THE RIDER (owner-ruled, items 216/217 — four writes, enumerated):** sam-flanders eligibilities
   → 'G-DEF,G-FWD' · oskar-baker eligibilities → 'G-DEF,G-FWD' · ed-langdon eligibilities →
   'G-DEF,G-FWD' · sam-flanders `present_position` MID → GDEF. NOTHING ELSE moves on these rows.
4. **THE NINETY-ONE blend rows** → the §1 ruled stream semantics wired on the YEARS-1+ leg ONLY
   (the dual stream swaps the future replacement bar for the LOWER of the pair, p_dual-weighted,
   floored ≥ 0; primary keys peak/curve/runway/key-premium — verbatim law above; no year-0 effect).
5. **THE ATTRIBUTION DELIVERABLE (item 181, owner-worded):** baseline → +current positional layer →
   +primary-future changes → +future blend — SEQUENTIAL CUMULATIVE stages (each measured on the
   previous stage's board), per-player deltas + totals, AND the interaction residual DECLARED
   (audit #21: max |Σstages − total| printed; the ordering stated). Owner-viewed at the ladder.
6. **THE CONSERVATION NOTE (audit cluster (iv)):** the aggregate flex uplift printed with the plain
   statement of its funding status — the dual stream is the OWNER'S RULED definition of the asset's
   value (the MAX law), not an unfunded bonus; the whole-system ledger carries the entry explicitly.
7. **Derived regeneration + re-pins:** store md5 MOVES (the one sanctioned store write of the
   chapter) — expected_boot re-pinned in the same commit; board/matrix/panel regenerated, stamped.

## DELIVERABLES
PLAN (then checkpoint) · A/B proof RL_FLEX=0 == 273463e board byte-exact · the ingest's
validation report (804/804 · vocab · streams) · the attribution artifact (5) · the conservation note
(6) · **the per-young-player EARNED-COMPONENT check over the 11 + Flanders (audit #27: a primary-
position write must not cut a young player's earned value at FINAL SCAR — any negative row = HALT)**
· **the acceptance blocks ASSERTED AS JSON, never this prose** (`leg_c_remeasure` — every Leg-B
acceptance statistic re-run on the post-C board, drift stated · the earned-component gate · the
census cells; acceptance v1.20, seal per the PLAN manifest) · **the post-dating OWNER LAWS (fold
rides the pre-ladder re-seal; judged at prescreen): the all-804 SINCERITY ledger CSV on the post-C
board (item-256 schema incl. rank columns) · G-COHORT judged vs the FULL BAND both sides (floor
1.08 · cap 1.30 · ideal 1.15–1.25; items 258/259) · A-PAIRS pair 2 (±15%, item 266) + pair 3
scored** · gate
snapshot by engine hash · RETURN ≤30 lines: branch · head SHA · PR · actual time.

## FENCE
IN: the store (THIS job only, the enumerated writes) · the blend wiring at the years-1+ replacement-
bar site · RL_FLEX · derived artifacts + re-pins · the deliverables. OUT (HALT): any OTHER store
field/row · rl_model.py beyond the wiring site · Leg-B's map/dial · docs/ · ui/ · gates/guards/
config manifest · the pick curve (Leg D's). Scope growth = a NEW directive (S2).
