# DIRECTIVE — LEG F BUILD 3: THE PROJECTION FIX · seat 13 · 2026-07-18
### Cures the item-352 verdict per MEMO_LEGF ADDENDUM v1.1 (the construction of record with
### MEMO_LEGF v1.0 + MEMO_LEGE v1.0). WRITER build (the one writer). Ruled ground: R103.3 ·
### item 351 (owner) · item 352 (verdict, addresses).

## GIT ENTRY (verbatim; item-334 law)
`git fetch origin main claude/legf-phantom-intake-build-dvbipz`; ls-remote must return
`7b6dfc52324e0b3a2367c9324e1acff6c8b0abaa` STRICT. Checkout it → `git checkout -b
claude/legf3-projection-fix-<suffix>`. Docs on MAIN; never join lineages; this-session fetch for
every claim; hard-to-reverse = HALT. **OPENBLAS_NUM_THREADS=1 (and OMP/MKL/NUMEXPR=1) on every
hash-bearing run (item 349).** Entry: store `968de0c7` · curve payload `89c14729` · RL_LEGF=0 ⇒
d85901af/06d8af60/9829d01a byte-exact (single-thread re-proof).

## EFFORT: High (the fix touches the projection path priced into every +1/+2 row; the acceptance
is a calibration property, not just a diff). Why not Extra: two localized sites + one rule
replacement, all pre-localized by item 352; balanced board untouchable and proven. MODE: auto,
PLAN first (derived fence + sealed strawmen: P(retire|age) table hash + any blend-decay
parameter). TIME: ~2–3 h; flag >2×/<½×.

## FEED: MEMO_LEGF ADDENDUM v1.1 (rules) + MEMO_LEGF v1.0 + MEMO_LEGE v1.0 · the item-352
investigation artifacts @ `618eb5b` (reuse its backtest harness — S5) · DECISIONS v123 ·
CONSTRAINTS v1.19 + acceptance JSON · F2's boards @ `cf94589` (backtest inputs, stamped).

## THE JOB LIST (one commit each)
1. PLAN + seals (P(retire|age) measured from recorded exits, smoothed; blend-decay if
   parameterized).
2. **§2.vi COMPLIANCE FIX:** `proj_from_peak` + the `rl_export.py:96` form anchor — the forward
   evidence state carries the SAME pedigree/evidence blend, decaying smoothly with projected
   evidence. No new multiplier, no lens-only growth term (the Reid constraint stands).
3. **§2.iii REPLACEMENT:** distributed retirement liability replaces the discrete X-bar exit
   (`rl_export.py:~526`); phantom intake re-sized to the aggregate liability.
4. **THE ACCEPTANCE RUN (the investigation's harness, frozen-form):** backtest −1→now and
   −2→−1 (±5% league) · per-cohort gradient un-inverted · totals report (bal/+1/+2,
   with/without phantom) recommitted.
5. EXIT: k=0 `06d8af60` byte-exact · RL_LEGF=0 chain byte-exact · store `968de0c7` · frozen
   suite · guard-5 = the known pre-bake red (flag, never self-pin) · annex (numeric stack,
   threads) · PR stacked on #115 · RETURN ≤30 lines: branch · SHA · PR · actual vs band · THE
   BACKTEST NUMBER FIRST · the gradient · three narrowest margins.

## CHECKPOINT LAW: any k=0 balanced change of any kind ⇒ HALT, return the diff. If the ±5%
acceptance cannot be reached WITHOUT violating the Reid constraint or hand-tuning a cohort ⇒
HALT and return the tension — never bend a law to pass a number.

## FENCE (derive in the PLAN; the touched set must be a subset — the F1 standard):
IN expected: `rl_model.py` (proj_from_peak) · `rl_export.py` (form anchor + exit construction) ·
`session_2026-07-18/legf3/`. HARD-OUT: the store · the curve · `_merged_recover.py` V0/`_iso_dec`
chain · pins/acceptance · `docs/` · SEASON_PROG · ui (unless a job mechanically requires the
toggle text — justify or HALT). S1–S6 · SILENCE IS A RED.
