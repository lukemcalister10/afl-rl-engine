# HANDOVER — rev146 · 2026-07-16 · supersedes rev145 · authored by seat 9 (Fable) PRE-ROTATION
### rev146 (same day): seg-3 RETIRED (scaffolding prescreen PASS item 237; segment-4 base = 243106b
### STRICT) · ONE chat in flight (the diagnostic) · counter-calibration conduct line added · the
### owner's pack review drove items 236/238. Written at the seam-prep: memo v1.2 pending on the
### diagnostic's return. Seat 10 opens on this + the standard prompt, runs the freshness check,
### and picks up at THE QUEUE. Seat-9 journals (turns 1–40) are the crash record; the register
### (v211, items → 238) is the durable log.

## 0 — PINS (verify fresh; the pen moves main)
main = verify fresh (full-URL ls-remote) — the pen moves it every filing (docs line). Canonical tag **v2.10** (`d14efae`) · shipped board
**790136a3** · store **b1fd0bce** · config `c2d233ae`. UI SHIPPED: #99+#101 merged (v1.2→v1.3.1,
collapse rule live) · #102 merged (LTI re-pin tool — a go-live prerequisite BANKED). Candidate
line: Leg-A `8b8ab7d` → seg-2 `c27d697` (map inert; RL_UNCOMP=0 ⇒ 8d90c9ac PROVEN) → seg-3 branch
`claude/legb-segment3-v1-rewire-5jy7f3` (MOVING — partial proceed). Pack: manifest **v4.38** ·
CORE v2.8 · DECISIONS **v112** · CONSTRAINTS v1.18 (repo-only) · acceptance **v1.18** · SSI v1.3.

## 1 — LEG B: THE THREE-ITERATION STORY (READ THIS FIRST — it explains everything in flight)
The un-compressor (English/Briggs cure, β≥0.85) has had its LAW redesigned twice, each kill
measured pre-ship: **v1.0** wired at the posval component (local elasticity ≥1 ⇒ compresses BY
CONSTRUCTION; β 0.62→0.48) and aimed at the smoothed level; **v1.1** fixed the axis to realised
output (λ_ρ 0.90-0.99 vs level_now's 0.12 — measured) but its hard games-floor MANUFACTURED FAKE
ROOKIES (a developing player's 4-9-game early seasons excluded ⇒ year-4 reads as year-1 —
OWNER-CAUGHT, the chapter's protected young cohort); **v1.2 (PENDING)** = a CAREER-PHASE-CONDITIONED
qualifying rule: keep ASCENT seasons (development), exclude INTERRUPTIONS ≤ peak (injury,
Docherty-class), POST-PEAK low-game treatment = AN OPEN OWNER RULING (may be decline signal —
excluding would inflate fading vets, the mirror defect). Machinery (β_eff = (1−w)β_c + w·λ_ρ ·
w = s·E · the price6 hook · captain-off δ pass · grid {0.55–0.70}) is SETTLED (memo v1.1 ⟪v1.1⟫
blocks + item 229 prescreen); ONLY the qualifying predicate is open.

## 2 — IN FLIGHT RIGHT NOW (exactly ONE chat)
**SEG-3 IS DONE AND RETIRED** (item 237 prescreen PASS): the rule-independent scaffolding is on the
branch at **`243106b`** — six-site removal + obituary · captain-off + δ self-test · hook blend +
ramp · `_qualifying(p, season)` stubbed (one def, one call site, unreachable while inert — proven)
· RL_UNCOMP=0 AND default-inert both ⇒ board `8d90c9ac` BYTE-EXACT (committed proof). **SEGMENT-4
BASE = `243106b` STRICT.**
**THE QUALIFYING DIAGNOSTIC (read-only, HIGH — items 230-234):** the three-phase mislabel
census (ASCENT/INTERRUPTION≤peak/POST-PEAK, BAR∈{6,10}, post-peak base+sensitivity) · cohort
deltas · λ per variant · named cases (Docherty-class · 3 young KPP · 2 post-peak vets · Reid).
Directive: docs/DIRECTIVE_DIAG_qualifying_v2 (harness PINNED at `e3ff2c7` — the branch moves).

## 3 — THE QUEUE (the path to the bake)
1. Both returns land → **MEMO v1.2** (supervisor authors: the conditioned predicate law + THE
   POST-PEAK OWNER RULING from the diagnostic's two variants) + acceptance re-seal (open, logged).
2. **SEGMENT 4 — FRESH CHAT** (seg-3 retired at 250k): one directive = fill the predicate per
   v1.2 · run the grid (smallest s clearing 0.85; empty ⇒ HALT) · the FULL item-206 deliverable
   set (w-export · donor movers · G-COHORT y4/y5/y6+fixture · ledger · censuses · killswitch
   matrix). Base = `243106b` STRICT (seg-3's prescreened halt head). → THE LEG-B RETURN.
3. Return prescreens (seat 10; the chapter's heaviest verification) → **LEG C fires**
   (docs/DIRECTIVE_LEGC_flex_positional — PRE-DRAFTED, two placeholders: <LEG-B-HEAD> + <DOCS-SHA>;
   carries the owner's four-write rider incl. Flanders present→GDEF) → D (G-Y0 population +
   numéraire + bands; consumes the w-export; R104.9 strict descent; item-204 candidates) → E
   (posture presets; movers report MUST include named LTI rows — Rozee — for the injury×posture
   ruling, item 216) → ladder → sealed reads → word → ONE bake → tag v2.11.

## 4 — STANDING CONDUCT (promoted to turn-one visibility; CORE-fold flagged for the next seam)
Substantial compute = a parallel Opus/Code job — the supervisor flags BEFORE spending Fable tokens;
light checks inline (item 209). · Builds report CONTEXT USAGE at every HALT/RETURN and REAP
background tasks before stopping (item 222) — **and self-estimates run LOW (~40%: 165k est vs 290k
app counter, seg 3) — report the APP COUNTER when visible; treat self-estimates as floors (item 238).** · EXTRA legs are multi-segment BY DESIGN; the
supervisor budgets segments; ~200k+ ⇒ retire at the next clean halt (items 212/222/232). · ONE
FIREABLE JOB = ONE DOCUMENT (item 233 — no directive+addendum pairs). · RE-VERIFY EVERY PIN AT
HANDOVER, not just at authoring (item 225 — three stale pins). · Every directive carries EFFORT
with the why-not-one-lower line (item 234). · PK-sync asks SHIP the documents same turn (item 186)
· concrete TIME estimates (item 188) · plain-read openings · DO:/WHY: · journals every turn.

## 5 — LOOSE ENDS CARRIED
Rollover: SPEC v1 FILED (five idempotent acts · season_state.json · Guard 6 · order-tolerance ·
the phantom dry-run w/ pre-signed expectation sheet) — build = a small POST-BAKE chapter (the
==2026 literals are engine). · Go-live queue (supervisor): the go-live directive pre-draft + the
weekly score-CSV format proposal. · Owner-input class ratification at go-live (item 218). ·
Ingestion store-write stays gated by the owner's v25 belt-and-braces ruling. · Book cross-CPU
hygiene (item 170n2). · The owner's Law-2 flex register (his fill, his timing). · Seat-9 token:
REVOKE at this rotation.
