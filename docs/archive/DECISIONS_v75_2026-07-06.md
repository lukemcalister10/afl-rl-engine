# DECISIONS.md — AFL RL engine — v75 · supersedes v74 · ROTATION HANDOFF
### 2026-07-06. The single live tracker. Everything here is CURRENT. For "how to operate" see KICKOFF v10; full state = HANDOVER rev115.
### v75 changes: Wave-0 CLOSED · Wave-1a measurements banked (held / method-calls / blocked) · KPF-distinct design finding ·
### DOC-SYNC invariant added (the repo doc-lag lesson). Owner has committed v74 to the repo (decisions-doc lag fixed).

## PLAIN READ
The plumbing is DONE — Wave-0 closed (dead-code stripped with zero price change · the "what moved" ledger built and proven · the
anti-inflation alarm confirmed genuinely complete). The measurements we could take are banked. From here it's the model deriving,
you reviewing the boards it produces, a cold audit, and the bake — no decisions gate any of it. Two small method-calls wait on you
(below), neither blocking. One new finding: key forwards are a separate loose-pricing problem from aging stars — the derivations
must treat them on their own.

## SETTLED — DO NOT RE-OPEN
1. **Injuries (3-arm LTI, Chapter-3, greenfield):** (a) age with the season clock (site `rl_model.py:61-62`); (b) return haircut —
   **SHAPE IS A HYPOTHESIS TO VERIFY** (`INJURY_RETURN_STUDY_v2`): re-measure on the current engine NET OF our re-derived aging
   curve (coupled → derivation phase). Survives = two hypotheses (young = forfeited growth year not ceiling dent · recovery
   age-dependent + heterogeneous) + one RULE (measure net of aging or it double-counts). (c) present-season wipe, games-remaining
   PRORATED. GT = LTI_REGISTER.
2. **PVC:** end-of-year-one value of the TYPICAL player at that slot → tightly-banded smoothed curve, loclin at pick 1. Primary
   fit from the **2004 draft**. The parked spec is a valid design core but STALE-as-executable — see PVC-SPEC-STATUS.
3. **Evidence-vs-prior blend:** gradual, evidence-weighted; convergence rate + form-conditioned aging decline MODEL-DERIVED. Pick-1 ≠ pick-50 after one game.
4. **Ruck:** derived beats the dialled 1.4× cap, WITH a cross-position sanity/replacement calibration (owner eyeballs the ruck $).
5. **Establishment-P:** dead. **DPP:** stripped (single positions; confirmed inert code stripped in Wave-0a, zero price change).
6. **Governance:** frozen gates reviewed as needed · owner dials → visible `OWNER_OVERRIDES` layer.
7. **Viewer:** ONE unified pure-view app on the cockpit, ring-fenced fixture (PR #36). Real data plugs in last (the identity-gate
   boundary — persisted-board == engine, viewer trusted to render `v` verbatim — becomes a live checkpoint HERE).
8. **Measuring stick:** beat-baseline gate DROPPED. Acceptance = owner board review + frozen PLAYER-ANCHORED gates + time. The lean
   CHANGE-LEDGER (`tools/change_ledger.py`, descriptive "what moved" — not a score) is BUILT + confirmed.
9. **Key-forward pricing (NEW — design requirement):** KPF loose-pricing is a DISTINCT locus from aging-elite (measured: KEY_FWD
   spread-ratio 6.09 vs MID 4.25; elite-KPF 24.71× value on 1.36× production; 0 KPF in the aging-fix locus). The derivations must
   give key forwards their own treatment — the aging-star fix will NOT reach them. Not a decision; a design input.

## MEASUREMENTS (Wave-1a · PR #40 · `evidence/measurements_ledger.md` · build-reported, cited)
HELD (5): **F7** games≠Σscoring — 349 disagree, 329 a systematic −1 off-by-one (n=1909); a data-class fix, not pricing · **F26**
staleness cliff +103.1% (persists) · **pedigree-floor** 53 saves / +$1,296, monotone in pedigree (pk1-10 = 38% of lift on 21% of
pop; n=590) · **KPF elasticity** (see SETTLED #9) · **OV-5 2004-window census** clean (2652 · decided cohort 1069 · RUC 64 · missing
birth-year 302). Provenance note: the Wave-1a build read repo-v70 (pre-sync); items don't depend on v70-vs-v75, so unaffected.
NEEDS-A-METHOD-CALL (owner, not blocking): **M1** breakout follow-through (hold $15,726/34 sustained; needs cohort-qual + censoring +
"realise" ruling) · **F11** band skew (needs feature matrix + distance metric + cohort/mask). BLOCKED: **injury %s** (aging-coupled) ·
**F25** per-club proration (no fixture data in repo).

## PVC DERIVATION SPEC v1 — STATUS (carry; stamp rides the PVC opener)
Branch `claude/pvc-derivation-spec-17l4ly` (PR #20). **Valid design core, STALE as executable — do NOT run P0–P8 as-is.** CARRY the
methodology; SUPERSEDED: 2006→2004 (done); CONTAMINATED: all numbers read the old inflated board (ruck-cap 1.73, V0 target,
v2.3/2.4 baselines) → RE-STAMP on v2.5; COORDINATE RE-CHECK (SCALE_PVC / pick-1=3000 / PICKEQ / build_pvc_v34 — strips may have
moved/deleted). Owner forks NOT re-opened. **ACTION:** PVC opener's FIRST step stamps the spec file header (v1/pre-v2.5/don't-run-as-is),
then re-stamps numbers + re-checks coordinates on v2.5, THEN Opus fits.

## OPEN — ACTION ORDER
1. **WAVE-0 — CLOSED.** 0a strip (PR #38) · 0b ledger + identity-gate-confirmed (PR #39) · measurements (PR #40). All candidates; main NOT promoted; they ride to the re-bake.
2. **DERIVATIONS (next — the frontier; several parallel):** young-slope flatten (confirmed top lever — natural first) · PVC (stamp
   + re-stamp + coordinate-check FIRST, then fit on the 2004 window) · form-conditioned aging decline · ruck + cross-position
   calibration · a KPF treatment (SETTLED #9). Each a build job, owner reviews the board it produces as ground truth.
3. **INJURY MEASUREMENT + FIX:** re-measure the return %s NET OF the derived aging curve (must follow aging), then the 3-arm fix.
4. **ONE INTEGRATION CANDIDATE (the FABLE spot):** retention + floor re-derive once; injury meshes with aging WITHOUT double-count; blend coheres.
5. **COLD AUDIT → owner word → RE-BAKE** via the owner codespace; promote main.
6. **OWNER METHOD-CALLS (whenever):** M1 validation protocol · F11 feature/yardstick. Non-blocking.
7. **Viewer track (parallel, non-urgent):** as before; keeper tabs need the owner roster feed.

## SUPERSEDED / CLOSED (do not resurrect)
Phantom/DPP gate (RESOLVED) · OVERHAUL_SPEC "all LIVE" inventory (STALE — correction still owed at a docs pass) · beat-baseline gate
(DROPPED) · injury haircut "settled shape" (now hypothesis) · Wave-0 build work (DONE) · DPP · establishment-P · F1/F2 bugs (the
Wave-0b identity gate closes the systemic finding) · Kako history (restored) · OD-2/3/5 + the PVC 11-fork register as "open decisions."

## PROVENANCE
- VERIFIED (owner-seen / re-runnable): v2.5 board + hashes + guards; the change-ledger's zero-delta + movement self-tests; the
  identity-gate = genuine load-vs-compute HALT.
- BUILD-REPORTED, cited (candidate PRs #38/#39/#40, not independently cold-re-run — that happens at the audit before the bake).
- REPORT-ONLY, re-measure before building: the injury study (directional hypothesis).
- FLAG (closed): local-`main` divergence was a benign stale seed stub (`f4a4d34`/`fddab37`); honest board = `origin/main`, store `e1b4d8bf`.

## STANDING CONVENTIONS (KICKOFF v10 §3B) + GUARDS
Effort ladder + mode · build does ALL git (owner: tag-push/branch-del/promote-main + doc uploads) · time estimate + confirm + actual ·
mode vs scope separate · returns end "in plain terms" · feed builds the DECIDED-STATE · run the "is this still open?" test on DECISIONS.
- **SYSTEMIC GUARD (PVC-status loss):** a warning that must survive rides ON THE ARTIFACT (header stamp), not only a side tracker;
  when folding a delta into a new doc version, DIFF the delta against the new version — don't trust the compression.
- **DOC-SYNC INVARIANT (repo doc-lag lesson):** the doc pack has ONE authoritative home (the owner's held files, kept current by the
  supervisor); the repo `docs/` is synced to match at each seam (owner upload — as done for v74 — or a doc-sweep) so no build ever
  reads stale decided-state. The same "no stale divergent copy" rule that protects the board, applied to the docs.
