# DECISIONS — v90 · 2026-07-10 · supersedes v89 · the single live tracker (rulings + owner to-dos ONLY)
### Written against manifest v4.3 (WD-4). Process = CORE · state = HANDOVER rev127 · constraint text =
### CONSTRAINTS v1.6 + acceptance v1.6. v90 delta = §36–§38: R-i RULED ADVANCE (flips the provisional
### pause) + #56 merged + reads sealed + bake ladder STARTED · the Matchday theme ruling + three
### amendments (folded from 2026-07-09/10) · the G-DEF pattern note. §16–§35 CARRY UNCHANGED.

## PLAIN READ
The injury chapter is merged and your word started the bake ladder. One flip first: you ruled the
young-player runway clock ADVANCES during injury (it keeps ticking while a kid sits out), which is the
opposite of what the build shipped as its default — so a tiny job commits your ruling as the code
default and re-prices the affected youngsters (O'Farrell and Gibcus drop by the amounts you saw in the
table) before anything is audited or baked. Then: a blind audit in a fresh incognito chat, then you
view the board and book against the reads you sealed — that's also where you rule whether Darcy's
availability-only drop reads right — and only your written word after that viewing bakes v2.7. On the
UI: Matchday is the locked look, with full names, comma numbers, and the card bug fixed. The
Patterson/Travaglia read fails are recorded as one pattern — young high-pick general defenders
underpaid as a cell — for the reads chapter to investigate properly.

## SETTLED THIS ROUND (§16–§35 carry; adds below)
36. **R-i RULED: ADVANCE (owner, 2026-07-10) — supersedes the §33 provisional pause.** The L1c
    fade-clock ADVANCES during injury. Owner viewed the build's pause-vs-advance named table
    (O'Farrell −206, Gibcus −17; Darcy/Motlop/Flanders past the credit window, Δ0) and ruled advance.
    OPERATIVE FORM (R3 doctrine): the code DEFAULT flips to advance + a RULING-CONFIG assertion pins it
    (no env-override bakes); the flip job regenerates the candidate. PR #56 MERGED (merge commit
    99941f1, supervisor live-verified, c04dfc5 ancestor). READS SEALED (owner-attested, pre-viewing,
    per OQ-D). "Let's bake" = the LADDER IS STARTED; the owner's FINAL written bake word comes AFTER
    the board+book viewing (rule 3 unchanged). A-DARCY on-sight call (availability-only drop
    4470→3825; ceiling loci byte-identical; return_hc=0) is TAKEN AT THE VIEWING. Ladder: R-i flip job
    → prescreen → COLD AUDIT (incognito, outside the Project, blind seed: branch + pinned head + store
    md5 + acceptance JSON + checklist incl. the 142-count re-derivation · young h=0 verification ·
    4-unmoved-register-names check · B1 exact metric/bound/margin [closes the §30 precision question]
    · R-i=advance config assertion) → reconcile → owner viewing vs sealed reads + Darcy sight →
    WRITTEN word → bake v2.7 tag + promote → SEAM (docs fold · PR sweep #48/49/50/53/54/55 · WD-2
    repo-homing · seat rotation).
37. **UI: MATCHDAY WINS (owner, 2026-07-09/10) + three amendments — DELIVERED and locked.** Amendments:
    full player names everywhere (the first-name-dropping affectation and the silent ellipsis
    truncation both killed); comma digit grouping ("3,462" never "3 462"); the working-card defect
    (a `.zero` CSS class collision drawing a full-page hairline and shifting the waterfall grid) fixed
    as `zline`. Delivered: `docs/ui_styles/theme_03_matchday_FINAL/` (PR #55, prescreened PASS,
    render-verified) + LOCK.md — THE visual law for the UI implementation chapter. Cockpit's card
    defect recorded, moot (non-winner). Fable window FULLY SPENT (5 jobs: injury spec · flex spec +
    v1.1 rewrite · UI direction · 4 pitches · polish).
38. **G-DEF pattern (owner, 2026-07-10).** The high-pick G-DEF undervaluation TIES INTO the
    dylan-patterson + tobie-travaglia standing read fails (keys verified: pick 5/2025 and pick 8/2024,
    both GDEF, both young). Recorded as ONE cell-level pattern hypothesis (young elite G-DEF underpaid
    as a class) for the READS/ORDERING chapter — investigate the mechanism (does high draft capital in
    a low-scoring position cell get its pick signal crushed by the position level?), never patch the
    two players individually.

## OPEN — OWNER TO-DOS
- **DO:** replace DECISIONS v89 with v90 in Project knowledge. **WHY:** the flip job's FEED must carry
  R-i=advance.
- **DO:** paste the R-i FLIP directive (couriered 2026-07-10) into a fresh Opus chat; courier its
  return. **WHY:** its head = the audit-pinned candidate.
- **DO (next, on my hand-off):** paste the COLD-AUDIT directive into a fresh INCOGNITO chat OUTSIDE
  this Project. **WHY:** memory-free + blind seed is what makes its agreement evidence.
- **THEN:** view board+book vs your sealed reads (+ the Darcy sight call) → your WRITTEN bake word →
  tag v2.7 + promote main.
- **DO (whenever):** branch sweep (original 3 + merged session branches) · max-king-stk DOB day/month
  eyeball (low).
- **AWAITED:** flip return · audit return · your viewing verdicts.
- **DEFERRED:** reads/ordering chapter (NEXT in queue post-seam; carries §38) · flex build (after
  dead-code strip) · PVC re-derivation · measurement-integrity · ID-primary migration · G-CONVEX
  graduation.

## SUPERSEDED / CLOSED (adds; do not resurrect)
The provisional R-i pause (§33 — ADVANCE ruled) · treating "let's bake" as the final bake word (it
started the ladder; the written word follows the viewing) · the pause default in merged code (flip job
re-defaults it).

## PROVENANCE
OWNER (this delta): R-i=advance · "merge done" · "reads sealed" · "let's bake" · the G-DEF/Patterson/
Travaglia tie-in · Matchday amendments (2026-07-09/10). VERIFIED (supervisor, fresh fetches,
2026-07-10): main 99941f1 = #56 merge, c04dfc5 ancestor · patterson/travaglia store rows · the
matchday-FINAL amendments in source (names, commas, zline fix; SVG-coordinate false-positives ruled
out) · #56 prescreen (store byte-identical; 9 commits; artifacts present; 142-count declaration read
from the build's PLAN). BUILD-REPORTED: #56 suite numbers (audit re-derives) · the pause-vs-advance
table deltas (audit spot-checks under the flipped default).
