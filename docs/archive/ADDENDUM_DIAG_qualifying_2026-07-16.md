# ADDENDUM — QUALIFYING-RULE DIAGNOSTIC (rides DIRECTIVE_DIAG_qualifying_rule) · 2026-07-16 · seat 9 (Fable)
### Paste into the diagnostic chat (before or during its run — additive, nothing retracted).

1. **THE THIRD CATEGORY — DECLINE (the gap):** the interruption test (games < BAR ∧ prior higher
   level) also captures POST-PEAK low-game seasons, where the low count may be genuine decline
   evidence, not an interruption — excluding it would OVERSTATE fading veterans (the mirror of the
   fake-rookie defect). REQUIRED: split every interruption-classified season by career phase at the
   season's age vs PEAK_AGE[pos] — PRE-PEAK / AT-PEAK / POST-PEAK — and report counts + the measure
   delta for post-peak exclusions separately, with the top-10 affected veterans named (age, pos,
   the window each rule picks). The memo will RULE on the post-peak treatment; the diagnostic only
   sizes it.
2. **BAR DECLARED:** measurement 2's interruption test runs at BOTH BAR = 6 and BAR = 10; report
   the conditioned rule's results per BAR (mislabel counts, cohort deltas, λ).
3. **BASE PIN:** read at main ≥ `c2e91fe` or the candidate head `c27d697`; STATE the exact SHA(s)
   read in the findings file (reproducibility).
