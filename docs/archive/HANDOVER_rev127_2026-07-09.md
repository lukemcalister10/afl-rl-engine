# HANDOVER — rev127 · 2026-07-09 · supersedes rev126
### State + the SINGLE action order. Read after CORE. Constraints by ID (registry v1.6 + acceptance
### v1.6). rev127 = the post-v2.6 hardening: CI live on main → external-review-driven gate repair →
### two store birth-year corrections + collision sentry + owner-ruled re-seal → all specs ruled.
### Rulings detail: DECISIONS v89. Verbatim returns: couriered in the supervisor chat (turns 7–26),
### repo-homing at the next docs sweep (WD-2).

## ONE LINE
**v2.6 remains the baked board; main has moved 4 merges PAST the tag on process/data-fix work (NO
re-bake):** main = `b30379559b29008ba7f802958a24f9a3fc6fbee2` (#52 merge) · tag v2.6 still = d4e8f6dc ·
store `a2fbc9a0` (2652 rows; King/Murphy birth years CORRECTED) · board `fd90472c` · book seal
`ff6cf9d8` (owner-ruled re-seal, DECISIONS §30) · engine `4b08796c` · CI guards LIVE on every push ·
collision sentry LIVE. Three jobs in flight (2 Fable + injury build).

## ROTATION NOTE
This seat (opened at the v2.6 seam) continues. PLANNED ROTATION: at the **injury-chapter seam** — after
the three in-flight returns are prescreened, the owner confirms R-i on the named table, the injury PR
merges, and the chapter's bake decision is taken; docs sync then and the seat rotates clean. Rotate
EARLIER only on a real trigger (context degradation: misremembered rulings, wrong SHAs — the seat
self-reports, the owner should also watch). This rev makes any rotation cheap. FABLE BUDGET: window
ends **Jul 12** — the two in-flight Fable jobs are the last of it.

## STATE
- Since rev126, merged to main (all merge commits, standing policy §26): PR #47 CI guards + Brodie
  display override (60663bd) · PR #51 gate integrity — B1/B3 certify the CANDIDATE, B2 self-runs to a
  hashed unrounded cert, config manifest (39 vars) in artifact identity, state strings say BAKED v2.6
  (900e4bf) · PR #52 store correction (max-king-stk _by→2000, ben-murphy _by→2007) + collision sentry +
  re-seal (b303795). Supervisor verified each merge live; only the two corrected players' age features
  moved (board shielded by the age floor; the injury derivation was the real beneficiary).
- Expected reds in the frozen suite: **A2/A3/A12 ONLY** (unchanged; A3=Rozee resolves as
  permanent-DC-with-annotation per ruling R-A3).
- OPEN PRECISION QUESTION (next audit): exact metric/bound/margin behind B1's "peak 130" — if a hard
  ≤130-family bound, that is a ZERO-margin pass (DECISIONS §30).
- Two GPT external reviews were triaged (§24): 4 mechanical defects FIXED (the gate-integrity job);
  deep findings folded as requirements into the queued measurement-integrity + PVC chapters; owner
  REJECTED pre-registered statistical acceptance gates (§25) — the bar stays owner reads + hard guards
  + anchors.
- Owner-supplied `authoritative_universe.csv` v2 = the ID-primary migration input (804 stable IDs,
  clean; §29 proposal, post-Chapter-3 sequencing). Eligibilities column = THIS-SEASON facts only.
- PRs HELD OPEN for the next docs sweep: #48 (UI direction) · #49 (injury spec) · #50 (flex spec v1;
  v1.1 supersedes on the revision's landing).
- Standing lines now in every directive: FETCH FRESH + verify main vs live before citing base (§31) ·
  merge-commit policy (§26) · two-tier UI law (§32).

## OPEN — THE ACTION ORDER
1. **In flight (returns → owner couriers → supervisor prescreen):** UI STYLE EXPLORATION (Fable; owner
   judges 3–4 pitches in a browser) · FLEX LAW-2 REVISION (Fable; spec v1.1) · CHAPTER-3 INJURY BUILD
   (Opus; the big one — its return carries the R-i pause-vs-advance named table and the exemplar
   before/afters for the owner's eyes BEFORE any merge word).
2. **At the injury landing:** owner confirms/flips R-i on the table → merge → **chapter bake decision**
   (the full ladder applies: prescreen with three narrowest margins → cold audit incognito → owner
   views board+book scored against his sealed held-out reads (OQ-D) → his written word → tag+promote).
   Docs sync + seat rotation at that seam (manifest bump rides it).
3. **Fable window closes Jul 12** — nothing new on Fable after the two in-flight jobs return.
4. **Then, the queue (owner fires in order, supervisor writes each directive):** flex BUILD (needs the
   Law-2 revision landed AND the dead-code strip — strip first) · reads/ordering chapter (five standing
   read fails + late-entry premium + Pickett/Berry locate-first + lens archaeology) · PVC re-derivation
   (P0–P8 + consumer requirements; heaviest ruling load) · measurement-integrity chapter (F4/F5/F11 +
   the §24 review requirements: refit-by-origin, out-of-sample young-credit target, board-vs-backtest
   split, pick/player lifecycle identity, uncertainty publication, fail-closed exceptions) · dead-code
   strip (+F17/F24; NOTE: the flex seam looks dead — future==present except Harvey Thomas post-ruling —
   and MUST SURVIVE the strip) · ±1/±2 lenses · Contender/Balanced/Developing lenses · round-score
   ingestion · UI implementation chapter (on the style winner; two-tier law binds; per-player cards
   template-rendered; hover-peek + click-through) · ID-primary migration (owner schedules; input held).
5. **Owner smalls (whenever):** branch sweep (original 3 + any lingering merged session branches) ·
   the max-king-stk DOB day/month eyeball (9 Jan coincidence with syd — low) · WD-2 docs sweep (this
   round's couriered returns → repo).

## PROVENANCE
- OWNER-SEEN/RULED: all §26–§35 rulings (DECISIONS v89) · the three merges (his clicks; supervisor
  live-verified each) · the two corrected players' before/after values (viewed pre-merge).
- VERIFIED (supervisor, fresh fetches/reads, 2026-07-09): every merge landing (60663bd, 900e4bf,
  b303795, ancestry checks) · the #52 store diff row-by-row (exactly 2 rows/4 fields; max-king-syd
  byte-identical) · re-seal values · sentry content+wiring · all four prescreens' fence checks · the
  impossible-age scan (2/2652) · both true birth dates (web) · universe csv v2 (804 IDs clean).
- BUILD-REPORTED (accepted on proofs/gates): guard-suite run numbers · the age-floor shielding
  mechanism (rl_model.py:365) · B2 0.000 · CI 13/13 (runs visible on GitHub). REPORT-ONLY: the GPT
  reviews' own runtime numbers (§24).
