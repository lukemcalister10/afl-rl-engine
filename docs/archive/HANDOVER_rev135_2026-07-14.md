# HANDOVER — rev135 · 2026-07-14 · supersedes rev134
### State + the single action order. Read after 00_MANIFEST and CORE. Constraints only when judging a board.

## ⚠⚠ THE ONE THING THAT OUTRANKS EVERYTHING ELSE
**MAIN IS RED, AND WE DO NOT YET KNOW WHY.**
CI's panel FAILS: all ten players mismatch. **PR #74 completed the config manifest (the supervisor's
diagnosis) and the panel STILL mismatches** — its own log reads `config manifest (gate mode) LOADED hash
c2d233aec104 (47 model vars pinned; ambient cleared)` and then prints the same ten mismatches.
**The missing levers were never the cause. That diagnosis was WRONG.**

**AND THE SAME COMMIT DISAGREES WITH ITSELF.** On PR #74 the `pull_request` check PASSED and the `push`
check FAILED. The workflow is byte-identical for both events. **Two runs of effectively the same tree gave
opposite verdicts.**

**THE MISMATCH SIGNS ARE MIXED** — Daicos 7640 vs 7667 (down) · Josh Ward 1679 vs 1649 (**up**) · Will
Green 660 vs 655 (**up**) · Gawn 2390 vs 2393 (down). A missing lever pushes ONE way. **Mixed, small,
both-directions movement is the signature of a MODEL REFITTED WITH A DIFFERENT DRAW.**

**THE LIVE HYPOTHESIS (unconfirmed — a job is measuring it):** `_merged_recover.py:31` constructs a
`GradientBoostingRegressor` and **`.fit()`s it AT IMPORT TIME** — every board build, every gate run, every
panel run. It is seeded, **but a fitted model is only as reproducible as the library that fits it.**
`wire_redesign.py` asserts *"cache == retrain byte-for-byte"* — **in a COMMENT, never tested.**
**IF THE BOARD IS NOT DETERMINISTIC, EVERY GUARD, GATE AND BAKE IN THIS REPO HAS BEEN COMPARING AGAINST A
NUMBER THAT WAS NEVER STABLE.** That would reframe the entire chapter and it is the first thing the
incoming seat must resolve.

## ⚠⚠ FABLE'S FIRST TWO ACTS — DO THESE BEFORE ANYTHING ELSE, INCLUDING THE DETERMINISM RETURN

**ACT 1 — FILE THE PACK YOU WERE HANDED (R98.10).** You received the seam pack from Project knowledge.
**File the pack you were handed, as your first act** — **the manifest names the current versions** —
archiving superseded versions to `docs/archive/`. **CORE is already in the repo, unchanged; check the
manifest before you assume it is part of the filing.**
**DIRECTIONAL GUARD (BINDING):** compare PK against the repo **doc by doc, by header version.** PK ahead
or equal ⇒ file and proceed. **PK BEHIND on ANY doc ⇒ HALT and tell the owner.**

**ACT 2 — FOLD CORE → v2.6. THIS IS YOURS BY OWNER RULING, AND IT IS URGENT.**
**Two BINDING rulings are NOT YET IN CORE, and until they are, CORE v2.5 does not describe the rules you
are operating under:**
- **R98.9 — THE PEN IS RESTORED.** CORE v2.5 rule 8 says the docs/-only token pen files directly. That was
  **suspended** for seat 5 by R98.8 and is **restored** for you. **CORE v2.6's changelog must record both
  the suspension and the restoration**, so no future seat reads the gap and thinks doctrine flip-flopped.
- **R98.10 — THE HANDOVER EXCEPTION.** CORE v2.5's FRESHNESS CHECK still says, flatly, *"disagree ⇒ the
  repo wins."* **Under R98.10 that is WRONG at a handover — and your first act IS a handover.**
  ⚠ **A seat that holds CORE v2.5 in full and obeys it literally would throw away the newer pack it was
  just handed.** Fold this before you trust the freshness check again.

**THEN:** the determinism return. Then everything else.

## THE SINGLE ACTION ORDER
**Read the DETERMINISM return the moment it lands. Nothing else matters until it does.**
- **Board builds twice to the same md5 ⇒** the CI mismatch is cross-environment (library versions);
  proceed to fix that, then merge #74 → #73, main goes green, and the pricing chapter wires.
- **Board does NOT reproduce ⇒ STOP THE CHAPTER.** No bake, no wiring, no gate verdict is trustworthy.
  Escalate to the owner immediately. This outranks every other item in this document.

## STATE (verify it yourself — fresh clone, cite SHAs; CORE rule 2)
- **main = `7989d21`** (register v80). PR #72 merged at `7e2e1ed`; the two commits since are docs-only pen.
- **TAGGED BOARD OF RECORD: v2.9 = `9f8ae76`** · board **`81e48293`** · store **`b0c39d78`** · pick-1 = 3000.
- **WORKING HEAD:** store **`340a7a32`** · board **`3dc19fbb`** (post-item-20; differs from the tag by ONE
  SCAR — bramble 92→93 — plus identity fields). Engine head **`2030e5df`** (`_merged_recover.py`).
  `rl_model.py` = `952ddb3d`.
- **Pack:** manifest **v4.15** · CORE **v2.5** · HANDOVER **rev135** · DECISIONS **v98** · CONSTRAINTS
  **v1.11** + `acceptance_v1.11.json` · SSI **v1.3** · OPEN_ITEMS_REGISTER **v81**.
- ⚠ **THE SUPERVISOR TOKEN IS DUE FOR ROTATION** (pasted in plaintext in the seat-5 chat).

## OPEN PRs
| PR | head | state |
|---|---|---|
| **#73** — one harness (canonical runner · code lookalike tripwire · masking lint) | `2c0fe48` | **PRESCREENED PASS.** Blameless. Blocked only on a green main. |
| **#74** — config manifest completed 40→47 vars | `ed13177` | **PRESCREENED PASS — but it does NOT fix CI. HOLD.** Merging it as "the CI fix" would leave main red while we tell ourselves it is green. |

## IN FLIGHT (both read-only, both fired 2026-07-14)
1. **DETERMINISM** — *is the board reproducible?* Builds the board twice and byte-compares; attributes the
   CI mismatch player-by-player; enumerates every runtime-fitted model and every unstamped artifact.
   **Reports D1 (the two md5s) immediately, ahead of the rest.**
2. **SMOOTH THE THREE HARD EDGES** — attribution first, then three surgical fixes, **each ABLATED alone**.
   Carries a BASELINE-INTEGRITY clause: it must rebuild `81e48293` exactly before measuring any delta, and
   **STOP** if it cannot.

## LOCKED, NOT YET WIRED
**THE CAPTAIN CURVE — owner-approved 2026-07-14.**
`credit(L) = G · ∫[BAR → L] P(a) da`, P logistic. **BAR 105.0 · M 109.5 · W 1.85 · G 1.00.**
Credit **= your projected level − 109.66** once clear of the knee; per-point rate climbs 0.10 → 0.50 → 1.00 and
**reaches 1:1 at 120** (0.997 — a logistic approaches 1 and never touches it; do not gate on "exactly"). Gawn **16.34** · Bont **9.85** · Daicos **4.96** — gaps
**6.49 / 4.89** against the owner's read of 6.5 / 5.0. Fitted to the realized order-statistic armband
record (rank1→2 = 5.31, rank5→6 = 1.09), **held-out ranks reproduced**, marginal never exceeds 1.0.
Replaces the live saturating `capt_prem` (hard 18-pt cap; marginal 0.34 **and falling** at the top).
⚠ **WIRING REQUIRES A REBUILT + RE-SEALED BOOK** (BOOK-PARITY LAW) and re-measurement of every guard —
**the earlier "self-funds" result does NOT carry over** (that was a gain change on the saturating curve;
this is a shape change, far larger at the top). It also lifts young players' upper tails, so **pair 3 may
get WORSE. Measure; do not assume.**

## THE PRICING CHAPTER (one bake, CYCLE law)
Captain curve · the smooth-edges fixes · the flattery decay (Fable) · the lens rebuild → **ONE candidate,
ONE ladder, ONE bake.** Then G-Y0 ratification → PVC re-derivation (where pair 3 actually lives) → FLEX →
ingestion go-live (owner's switch; provision built and merged at PR #66, apply-switch welded off).

## NOT GOT TO
lens rebuild · G-Y0 ratification · **PVC re-derivation** · the flattery decay itself · the dead-code strip
(blocked on nothing now — the June-28 Stage-2 gate was traced and is stale; see register item 60) ·
Dylan Moore's read (his suspected mechanism measured SMALL: +0.69% incremental R²; his read STANDS) ·
FLEX · round-score ingestion · team-context lenses · the stale-config stamps on the committed baked
matrices (historical comparators; misleading identity, not live).

## FABLE'S QUEUE
0. **THE CORE v2.6 FOLD** (R98.9 + R98.10 — see FABLE'S FIRST TWO ACTS) and **filing THE CAPTAINCY LAW and
   THE SMOOTHNESS LAW into CONSTRAINTS.** Both laws are BINDING, both are ABSENT from the registry, and
   both will govern every fix in the next chapter. The owner ruled the wording deserves a fresh seat, not
   a transcription pass — so seat 5 left them unwritten. **They are yours.**
1. **The completeness architecture** (register item 67) — our guards check CONSISTENCY; they have never
   checked COMPLETENESS. The config manifest was perfectly consistent with itself and simply missing five
   variables, and nothing was ever going to say so.
2. **F1 / F2** — the G-PEAK collision and the reference frame.
3. The flattery decay's functional form (now much sharper — see register item 65).

## SEAT-5 CORRECTION LEDGER (read this; it is why the directives are written as they are)
Four material errors, **all caught by the owner:**
1. Claimed an owner ruling had been "silently reversed" — the old ruling attached to an old engine.
2. Claimed the gate did not assert `config_sha256` — **it did, and had since PR #66.** I "verified" it by
   **printing my own conclusion inside a script and reading it back as a result.**
3. Told the owner the nqual cliff was fixed — **the smooth JUH path I quoted was produced by λ**, the lever
   he had just rejected. The build's own ablation contained the disproof and I did not check it.
4. Spent hours on the `PROVEN_N` cliff as Jamarra's mechanism. **His nqual never crosses 4.** His collapse
   is `_lvlcurr`: a 3-game cameo carrying **35%** of his level estimate.
**The pattern is one thing: REASONING FROM A PLAUSIBLE MECHANISM INSTEAD OF MEASURING THE REAL ONE.**
Every directive now leads with **ATTRIBUTION BEFORE DESIGN**, and every multi-lever job must **ABLATE**.
