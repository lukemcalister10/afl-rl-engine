# DIRECTIVE — LEG F BUILD 2: RETROSPECTIVE BOARDS (−1/−2) · seat 13 · 2026-07-18
### Owner-ruled item 346. MEMO_LEGF v1.0 §3 = the construction. READ-ONLY (Tier 3), runs
### PARALLEL to F1 (S3; disjoint files — writes ONLY its session dir).

## GIT ENTRY (read-only variant — the item-338 law)
`git fetch origin main claude/lege-projection-law-postures-63u283`; ls-remote must return
`cc58570…5adc` STRICT. Branch parent = MAIN (expected); provenance = stamps asserted at load,
HALT-on-mismatch: store `968de0c7` · curve payload `89c14729`. Engine imported READ-ONLY (the
rider pattern); never edit engine files.

## EFFORT: High (historical evidence-state reconstruction is where silent errors live — §5.9 of
the filed failure-mode list: `games` is CUMULATIVE; entry-year span defects known). Why not
Extra: read-only, stamped, byte-reproducible. MODE: auto, PLAN first. TIME: ~1–2 h.

## THE JOB LIST
1. PLAN: enumerate the recorded history the store actually carries for 2024/2025 list membership
   + per-season evidence (vpath, games-cumulative handling per §5.9). **If ANY required record is
   missing: HALT and return the exact gap — reconstruct nothing by guess.**
2. **THE −1 BOARD (2025)** then 3. **THE −2 BOARD (2024):** the full board re-rendered at each
   season's recorded evidence state (list membership as recorded; NO phantom layer — history
   happened). Stamped DERIVED artifacts (store md5 · code SHA · asof year) at a documented path
   for F1's UI tab.
4. **THE BRIDGE REPORT:** −2 → −1 → now totals + top-20 movers each step (the owner's sanity
   view: does the recent past look like the past?). Per-pick smoothing where curves appear; no
   wide bins.
5. EXIT: README (artifact→stamp map) · PR REPORT-ONLY/DO-NOT-MERGE · RETURN ≤25 lines (branch ·
   SHA · PR · actual vs band · the one most decision-relevant finding per board).

## FENCE: IN = `session_2026-07-18/legf2/` ONLY. HARD-OUT: every engine file · the store · the
curve · `docs/` · F1's session dir. HALT on any HARD-OUT need. S1–S6 · SILENCE IS A RED ·
findings not verdicts — these boards never gate and never re-litigate a sealed read.
