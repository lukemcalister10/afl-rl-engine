# DIRECTIVE — FORWARD-LENS CALIBRATION INVESTIGATION · seat 13 · 2026-07-18
### Owner-ruled "Yes" (item 351) on the item-350 red flag, ENRICHED with his conservation model +
### the distributed-retirement hypothesis. READ-ONLY (Tier 3) · report-only · findings not
### verdicts · changes NOTHING. Gating: the ladder holds on its result.

## GIT ENTRY (read-only variant — item-338 law)
`git fetch origin main claude/legf-phantom-intake-build-dvbipz`; ls-remote must return
`7b6dfc52324e0b3a2367c9324e1acff6c8b0abaa` (STRICT). Branch parent = MAIN; provenance = stamps
asserted at load, HALT-on-mismatch: store `968de0c7` · curve payload `89c14729`. Also read F2's
retro boards @ `cf94589` (−1 `board_minus1_2025.json` d223d8f6 · now `board_now_2026.json`
ad0c6610). **OPENBLAS_NUM_THREADS=1 on every run (item 349).** Engine imported READ-ONLY; never
edit engine/store/curve/docs.

## THE QUESTION
The forward board totals collapse (now 752,427 → +1 618,248 → +2 485,937 WITH phantom) while
backward is flat (−2/−1/now ≈ 771/771/752k). Is the forward lens CALIBRATED (decline is real and
expected) or does it SYSTEMATICALLY UNDER-PROJECT? Decompose and settle it.

## THE JOBS (read-only; one commit each; smoothed per-cohort, no wide bins)
1. **THE BACKTEST (decisive):** take F2's −1 board (end 2025, the ACTUAL list as recorded) and
   project it FORWARD one year through the SAME forward lens. It should reproduce "now" = 752,427
   (± a stated tolerance). Report the reproduction total + error. **A material under-shoot (e.g.
   → ~620k) is the smoking gun for systematic under-projection.** Also backtest −2 → −1.
2. **THE now→+1 DECOMPOSITION** (the owner's conservation model, tested): split the Δ by cohort —
   (a) VETERAN decline (age ≥ ~28): expected NEGATIVE (correct). (b) DEVELOPING players (age ≤ ~23
   / pre-peak): the owner's claim is these should HOLD OR RISE and offset (a); **if this cohort is
   NEGATIVE, that is the defect.** (c) EXITS removed. (d) PHANTOM intake added. Report each as a
   signed total + per-cohort mean.
3. **THE PEDIGREE-PREMIUM-CARRY TEST (the likely root):** the CURRENT board anchors zero/low-
   evidence young players on PEDIGREE (the Leg-D V0 = curve entry value); does the forward lens
   re-price them on thin PRODUCTION and thereby STRIP the premium? For high-pick, low-games,
   young rows: report now-price (pedigree-anchored) vs +1-price, and the premium lost. Quantify
   how much of job-2(b) this explains.
4. **THE DISTRIBUTED-RETIREMENT ALTERNATIVE (the owner's steer):** F1 uses a DISCRETE exit
   (value < X=207 ⇒ named player exits, slot refills). Model the alternative: a retirement
   LIABILITY spread PROBABILISTICALLY across the age-eligible cohort (you cannot know who retires),
   per-player haircut = P(retire|age) · value, no named exits. Report the aggregate +1/+2 totals
   under this construction beside F1's discrete rule — is the discrete rule mis-stating the exit
   side, and by how much?
5. **VERDICT ARTIFACT:** the four findings + a one-paragraph read: is the forward decline
   CALIBRATED, or is it (b)/(3) under-projection and/or (4) exit-mis-statement? Localize by
   engine site (address, not fix). NO fix in this build.

## FENCE: IN = `session_2026-07-18/fwd_calib/` ONLY. HARD-OUT: every engine/store/curve/docs file.
HALT on any HARD-OUT need or any construction choice the owner's model does not settle. S1–S6 ·
SILENCE IS A RED. RETURN ≤25 lines: branch · SHA · PR (report-only) · the backtest number first ·
per-cohort decomposition · verdict.
