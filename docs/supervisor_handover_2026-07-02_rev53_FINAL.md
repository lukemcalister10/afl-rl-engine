# SUPERVISOR HANDOVER — AFL SuperCoach RL valuation engine — 2026-07-02 (rev53 · FINAL · SUPERSEDES ALL)
### For the incoming SUPERVISOR (new model). Read `supervisor_KICKOFF_2026-07-02_v5.md` FIRST (how to operate + the 8 binding directives + the map); THIS is the canonical STATE. This chat is retired. Binding on you: `SUPERVISOR_AGENT_FEEDBACK_2026-07-02.md`, `PROCESS_CHANGES_2026-07-02.md`, `CONTEXT_BUDGET_RULES_2026-07-02.md` (all by filename below). Nothing bakes without Luke's explicit go.

## §1 — ANCHOR STATE (the build's final VERIFIED checkpoint, 2026-07-02)
- **Engine head `8aed420a`** — UNCHANGED this session (diagnostic-only; nothing baked).
- **Store `644d1254` · band `34faa865`.**
- **Bundle `rl_complete_8aed420a_final2026-07-02.tar.gz`, md5 `414706b0eb8b90d32c05f96c323dbc7b`** (573 files, 5.35 MB). **This supersedes the old bundle `89459a9b`** (do not reference 89459a9b as current).
- **RESTORE-VERIFY = VERIFIED by the build's cold run** (`verify_restore.sh`, clean extraction, tarball's own vendor): head `8aed420a` / store `644d1254` / band `34faa865` PASS; Maric ev(2026)=**1409** PASS; Langdon ev(2026)=**593** PASS; harnesses present (`gate1_wf`, `gate1_picksplit`, `s4_matrix_M1v7`, `s4_render_M1v7`) PASS; `run_panel.sh` **10/10** PASS → **9 PASS / 0 FAIL**. `doc_lint.py` **0 FAIL**. (Supervisor cannot re-run from its seat; this is the build's cold-reproduction artifact — the five-state VERIFIED bar.)
- **ENV (every command):** `PYTHONHASHSEED=0 RL_GAMMA=0.85 RL_PICK1=3000 RL_RECENCY_DECAY=0.72 RL_PRIOR_TREES=400 PAR_RAMPS=22` (RUCK_TAX=0.25 is a DEAD relic; PAR_RAMPS default 14 is WRONG). **One engine load per Python process** (double-load in one process throws a FALSE RecursionError — harness artifact, not an engine bug).
- **Lineage (history — NOT current heads):** `e0ac9c` (par-centred, last committed canonical) → `8c6d5582` (step-3 dials) → `55e3c3a9` (decline shed) → `7147b824` (unearned-upside) → `8aed420a` (no-games anchor + 34 positions reconciled). `8aed420a` is the working candidate; it has NOT been committed as the new canonical (that is not a pending action — the arc's next head-changing event is the M1+v7 bake, below).

## §2 — FIVE-STATE VOCABULARY (from PROCESS_CHANGES §1 — "closed"/"done" are BANNED)
`PROPOSED → DERIVED → WIRED → VERIFIED → BAKED`. Every workstream/decision claim carries exactly one. **VERIFIED** requires naming the cold-reproduction artifact (script + number). **BAKED** requires Luke's explicit go. Migrate any inherited "closed/done/bake-ready" language on contact.

## §3 — WORKSTREAMS (each stamped)
- **M1 + refined-v7 (the mediocre-overvaluation fix, Luke's #1)** — **DERIVED.** Params derived (M1: TOL_M1=5, G_ADQ=12, WIN=2, S_M1=0.46; v7: cB=0.47·clip((e−1)/3), q97 width aSc by age, tail 0.10) and validated in the 606-player combined backtest (right convexity gradient; both systemic flags resolved). NOT WIRED into `8aed420a` (it is an applyable prototype patch, separate from the live head). Path: Luke read-pass → WIRE into `_merged_recover.py` → re-cut → VERIFY cold (panel 10/10 + Maric 1409/Langdon 593 + the M1+v7 numbers) → BAKE. **M1+v7 × current-season interaction is FAVOURABLE** (it compresses inflated riser peaks; Richards' 2025→26 drop 41%→19%) — so it is SEPARABLE from the current-season-drop thread and improves it.
- **1-5g cliff blend (Lombard fix)** — **PROPOSED.** Luke's design: 0g = full sit-out anchor; 1-5g = blend anchor→production with `w` reaching 1 AT 6g (`w=games/6`; production endpoint = the exact ns≥1 value → clean seam); 6+g unchanged. Relay `to_build_lowgames_1to5_cliff_blend_2026-07-01.md` drafted + ready; not yet sent/built. (+0.1 retain raise is DROPPED — the re-derivation showed the curve is not too harsh, so +0.1 is wrong-direction.)
- **Retain re-derivation** — **PROPOSED.** On the correct daEV/PVC ruler (drop-in, not the raw_ev proxy); pooled/sample-weighted; RUC ceiling derived not picked. Rides inside the cliff-blend relay. Sets the 0-game anchor accurately.
- **Current-season drop (thin-2026 board depression)** — **DIAGNOSIS UNRESOLVED; no fix DERIVED.** Symptom (solid): the g<6-in-2026 cohort column drops −18% to −35% UNIFORMLY every cohort; on-pace players follow the normal arc; leaguewide scoring FALSIFIED. **Two mechanism diagnoses FALSIFIED** (reliability-shrink — only ~10% shrink-regime; "correct aging" — cannot produce a uniform drop). **`connor-rozee` confirmed UNDERPRICED by Luke (2g/80 in 2026, valued 2866) → a REAL artifact exists.** Leading hypothesis **H1 = UNTESTED:** `_lvl_wt` (level-weighting on the 2025-vs-2026 avg gap) over-reacts to a thin, below-par 2026 sample. Pin-mechanism relay `to_build_currentseason_drop_pin_mechanism_rozee_2026-07-01.md` OUT (Rozee decomposition + cross-cohort channel split = the decisive uniform-vs-cohort-varying test). **NO fix design until H1 is CONFIRMED** (two prior fixes were built on unconfirmed mechanisms; one would have mispriced a third of the board).
- **Decay proration (thin-games)** — **FALSIFIED / dead.** `_swt` is a global recency lever (35% of on-pace players moved >2%, ±20%; inactive on 90% of thin players). Inert-at-f=1 held (byte-exact history), but the lever is wrong. Do not revive.
- **Mid-season availability proration** — **FALSIFIED** (partial-games count explained <15%; incoherent for decliners). Dead.
- **Pick value curve (PVC)** — **PROPOSED** (design `pick_value_curve_FINAL_DESIGN_2026-06-29.txt`; within-cohort-shape / cross-cohort-consistency stages). **BLOCKED by SHIP_GATES.md** (see §4) — do not start PVC work until the acceptance suite is frozen.
- **Position work + 103-player dev-template** — dev-template **DERIVED** (`AFL_RL_DEVELOPMENT_position_template__1_.xlsx`, built, NOT wired). Folding = **PROPOSED**. NOT blocked (the "recursion blocker" was a harness double-load artifact — do not treat as an engine bug; do not manufacture it as a blocking step). Three-track position model: drafted `pos` (frozen, pedigree/cohort) / development `_fut` (feeds the pole) / SC-eligibility (feeds replacement, deferred).
- **`_fut` data artifacts** — **PROPOSED** cleanup (Coe/Puncher/Knevitt/Tunstill keyed by ID; subsumed into low-games work). Langdon 593 RESOLVED (2×2+Shapley: ~100% the present-position bar, `_fut` pole ~0%).
- **DOBs + display; relic cleanup (drop dead RUCK_TAX); hygiene re-cut** — **PROPOSED** backlog. DOB guards: "Max King" = TWO real players, `_by`=2000, "Maxwell King" rename is the guard (never merge on name); 8 more name-collisions key by pick/cohort/ID. Real DOBs recorded in prior notepads.
- **Audit** — **VERIFIED** (external, shares no context): GATE-1 harnesses re-run at head PASS; Maric/Langdon `_pos_now` fixed; the four book bugs fixed; all reproduced in the checkpoint. Cadence: a fresh cold audit is REQUIRED before any bake and after any rotation (§5).

## §4 — DECISIONS (awaiting Luke — default action · supervisor's last lean)
1. **M1+v7 read-pass gate.** Luke reads the walk-forward book on the complete-season / on-pace / proven rows (a fixed named panel) — DISREGARD the whole 2026 column (confounded by the current-season drop, a separate issue). Default: stays DERIVED/unbaked. **Lean: BAKE on an endorsing read** — validated on the plateau/decline population, and M1+v7 improves the on-field risers; it is independent of the current-season drop. Bake ritual gated on BAKE_CHECKLIST.md (item 6).
2. **Send the 1-5g cliff-blend relay.** Default: not sent → cliff stays. **Lean: send it** (Luke's own design; the correct, seam-clean fix).
3. **Current-season-drop rework direction.** Default: the pin-mechanism relay (Rozee) is the next step; no fix until H1 is confirmed. **Lean: pin the mechanism first** — anchor on Luke's reads + the cross-cohort channel test, not another mechanism story; H1 (`_lvl_wt` thin-sample over-reaction) fits best but is UNTESTED. If confirmed, the fix is targeted (discount a thin recent sample in the level-weighting), NOT a global lever.
4. **The 1.19× uniform retention lift.** Default: do NOT apply (a denominator bandaid; matches the Part-A absolute-vs-indexed split). **Lean: keep PARKED; revisit at the PVC stage.**
5. **SHIP_GATES.md** (PROCESS_CHANGES §3) — **OPEN, awaiting Luke.** Template exists; Luke picks ~15-20 named relativities he'd stake his league record on + the cohort-growth gate + book gates + JS parity, FROZEN on creation. **This BLOCKS the PVC directive** — the stopping rule must exist before the most expensive stage begins.
6. **BAKE_CHECKLIST.md** (PROCESS_CHANGES §8) — **OPEN, build task, required BEFORE the first bake (M1+v7).** Cold audit at head → Luke read-pass on the fixed panel → bake → full re-verify (md5s, 10-panel, named players, book, parity) → tarball with doc_lint pass → send. Must be written before its first use.
7. **Walk-forward book summary metric** — minor: confirm the %-appreciation denominator is year-1 (default assume year-1, not the PVC baseline).

## §5 — PROCESS MECHANISMS NOW IN FORCE (fold into practice; full text in the named files)
From `PROCESS_CHANGES_2026-07-02.md`: five-state vocab (§2 above); **`doc_lint.py`** gates every tarball cut (greps superseded hashes + stale status words outside a SUPERSEDED banner); **SHIP_GATES.md** before PVC; **≤30-line build-return template — supervisor BOUNCES nonconforming returns** rather than ingesting them; **state-stamped, latest-only relays**; **hypothesis register + causal vocabulary** (no strategy/blocker on an UNTESTED mechanism, enforced on FAILURE reports too; steelman a falsified Luke read in its strongest form); **REQUIRED_INPUTS manifest** in every directive (build reports gaps turn one); **BAKE_CHECKLIST.md**; **DECISIONS.md** (defaults + expiries, one batched decision turn/session). Cadence: rotate heavy chats at **70-80%**; cold audit before bake / after rotation / on VERIFIED→BAKED; **WIP cap = 2 live tracks.** From `CONTEXT_BUDGET_RULES_2026-07-02.md`: verify by SCRIPT not inspection; summary-only supervisor ingestion; docs-inside-the-tarball (text uploads inject in full — costly); burn-report heavy turns.

## §6 — FILE MAP (`/mnt/user-data/outputs/`)
- **THIS rev** (canonical state) · **`supervisor_KICKOFF_2026-07-02_v5.md`** (operating manual — read first) · **`supervisor_notepad_turn71_2026-07-02.md`** (this turn).
- **Binding governance:** `SUPERVISOR_AGENT_FEEDBACK_2026-07-02.md`, `BUILD_AGENT_FEEDBACK_2026-07-02.md`, `PROCESS_CHANGES_2026-07-02.md`, `CONTEXT_BUDGET_RULES_2026-07-02.md`.
- **Live relays (drafted, latest-only):** `to_build_lowgames_1to5_cliff_blend_2026-07-01.md` (cliff blend + retain re-derivation) · `to_build_currentseason_drop_pin_mechanism_rozee_2026-07-01.md` (pin the drop mechanism). All earlier relays are SUPERSEDED history.
- **Design:** `pick_value_curve_FINAL_DESIGN_2026-06-29.txt`; dev-template `AFL_RL_DEVELOPMENT_position_template__1_.xlsx`.
- **Bundle:** `rl_complete_8aed420a_final2026-07-02.tar.gz` (md5 `414706b0…`).
- **ENVIRONMENT NOTE:** this supervisor chat typically holds ONLY the docs; `/mnt/transcripts/` may be EMPTY on a fresh resume. Luke pastes build returns. **If a load-bearing fact is not in the docs, ask Luke — do not invent.**

## §7 — PATH TO SIGN-OFF (objective-anchored — the governing objective is the COHORT-COMPOUNDING GROWTH LAW, not per-cell draft prices)
Spine (each step moves a top-gate metric): (1) Luke read-pass → **BAKE M1+v7** (write BAKE_CHECKLIST first; cold audit at head). (2) **Cliff blend + retain re-derivation.** (3) Commit the combined head as canonical. (4) Freeze **SHIP_GATES** (Luke) → then **BUILD THE PVC.** (5) Position work + dev-template fold-in. (6) DOBs/display → relic cleanup (drop dead RUCK_TAX) → hygiene → sign-off against SHIP_GATES. **Off-spine (does NOT block the bake): the current-season-drop thread** (pin H1 → decide fix) — a live-board issue, separable. WIP cap 2 tracks. Once per session ask: is the active thread the binding constraint on the growth law, or the most tractable thing the build surfaced?
