# HANDOVER — rev162 · 2026-07-18 · supersedes rev161 (same-day)
### rev162 (item 387): THE F6/DETERMINISM SEAM + FULL AUDIT CLOSED. Chapter BUILD-COMPLETE + AUDIT-CLEAR
### at 15a9abd; the ONLY open work is the F6 determinism fix (a display-row weather flip) + then the
### viewing → bake. Standalone (needs no archived doc). The v2.11 chapter is
### BUILD-COMPLETE; the cold audit is IN FLIGHT (five shards, owner-couriered). This doc is the
### rotation insurance: an incoming seat continues the ladder from here + the register (v340+).

## 0 — PINS (verify fresh; the pen moves main)
main = ALWAYS verify fresh by ls-remote (the pen moves it; ≈`697779cb` at this filing, docs-only). Register head ≈v359. Tag **v2.10** (`d14efae`)
UNCHANGED — the baked, canonical, owner-usable engine. **THE FINAL CANDIDATE:** …→ `e4177c2` →
`a90052a` (five-migration, PR #111 line) → `cc58570` (Leg E, #113) → `7b6dfc5` (F1 phantom,
#115) → `bccc231` (F3 pedigree fix, #117) → `a9570cb5` (F4 calibration, #118) → **`15a9abd`
(F5 entrant layer, #119) = THE CHAPTER HEAD**, branch `claude/legf5-entrant-layer-conservation-
p4susl`. Report-only PRs: #110 (riders i–iii) · #112 (rider iv) · #114 (retro boards) · #116
(calibration investigation). Store `968de0c7` UNTOUCHED all chapter · curve `56dd7a7b`/payload
`89c14729` · engine at head: rl_model `cc626d7d` · _merged_recover `caf013e2` · SEASON_PROG 0.58.
Boards (single-thread, canonical-container labels): balanced `06d8af60` · +lens `d85901af` ·
RL_PVC2=0 `9829d01a` · +phantom `e613ca58`-era → F5 head recomputed at audit. Seals: presets
`c2e17c49` · φ `fd92b6fc` · r_pop `c62b5ee8` · s(age) `efe97ee3` · entrants `a17aafed`.

## 1 — WHERE THE CHAPTER STANDS (v2.11, BUILD-COMPLETE + AUDIT-CLEAR at `15a9abd`)
ALL builds GREEN + prescreened (register items 341–366): five-consumer migration · projection
lens (k=0 byte-exact) · riders (i)–(iv) (R_realized **207 [184–229]** contains the owner's 220;
R_curve 471) · retro −1/−2 boards + bridge (770,987→771,152→752,427) · **the v1.3 CONSERVATION
CONSTRUCTION (owner-worded): full entrant layer 83,538 sealed-from-history; ONE population carrier
r_pop `c62b5ee8` (§2.iii retired w/ obituary); the L-symmetry damper**. Gates: conservation
−1.2%/+1.7% IN · roster-matched −2.6%/+0.3% IN · gradient monotone-correct · G-Y0 1.100%.
**THE COLD AUDIT IS CLOSED — all five shards GREEN, NO candidate defect (items 371–375):** A
determinism/guards · B gates inside bounds · C the kill-switch equivalence BYTE-EXACT + integer-v
invariant proves the curve is the SOLE value-mover · D projection chain reproduced + k=0/balanced
invariants confirmed BY CODE-READING · E the curve RE-DERIVES BYTE-IDENTICAL from raw inputs.
Two OWNED docs-lineage findings (both immaterial, cured at bake): `acceptance_v1_21.json` + the §5
file are on docs/MAIN not the candidate engine lineage → they LAND at the bake (BAKE_RUNBOOK 3b);
candidate carries acceptance **v1_20** (v1.20→v1.21 delta verified harmless to every gate).

## 2 — THE ONE OPEN TECHNICAL ITEM: F6, THE `_iso_dec` DETERMINISM FIX (items 380–386)
**What it is:** a live `IsotonicRegression().fit()` at `_merged_recover.py:1222` (the one fit the
2026-07-14 q97m freeze missed) whose PAVA near-tie resolution can flip ONE display row (Sheezel,
−95: 7869 vs the clean 7964) under DYNAMIC_ARCH BLAS 1-ULP differences. It touches the balanced
board (V0 feeds year-0) so it is a real, if 1-row display-scale, value-path item. **Integer-v
invariant holds (item 374): ranks/valuations are NOT affected; the curve is the sole value-mover.**
**Current state (items 385/386 — read BOTH; 386 corrects 385):** the flip is OCCASIONAL not
every-run — with `RL_LEGE=0 RL_LEGF=0` the balanced board reproduces `06d8af60` BYTE-EXACT (Σv
752427, Sheezel 7964); it flipped once in three runs. **A prior supervisor precondition compared
the WRONG board (default `RL_LEGE=1` ⇒ `d85901af` the lens board, not the LEGE=0-gated `06d8af60`)
— any F6 base/reproduction check MUST set `RL_LEGE=0 RL_LEGF=0` to build the balanced board.**
As of this filing the F6 build (a FRESH legf6 chat, base `15a9abd`, hardened base-proof per item
384) is STILL RUNNING — resolving whether run-1 was genuine-rare weather or a first-run artifact.
**DO NOT INTERRUPT IT; await its verdict.**
**THE OWNER DECISION it resolves to (A vs B):** (A) DEFER F6 — ship v2.11 under the bake-container
reproduction gate (the SHIPPED board is correct because the bake only runs on an instance
reproducing `06d8af60`; the flip stays a viewing/bake retry cost) · (B) the SOURCE ORDER-FIX —
make `_iso_dec` tie-breaks BLAS-invariant (rational-quantise the input grid), deterministic
everywhere, its own small build + 1-row re-audit. The original "freeze a clean surface + pickle"
path (item 380) is viable IF clean surfaces are drawable (they are, most runs) but needs the
corrected LEGE=0 precondition. **INCOMING SEAT JOB 1: read the F6 verdict, rule A/B WITH the
owner, execute.** NOTE: the stale branch `claude/iso-corr-evidence-fade-jnda76` (`8b8ab7d`) is an
ABANDONED wrong-base build (item 383) — do NOT merge or build from it.

## 2b — THE LADDER (ordered remainder, after F6 lands)
Re-fire the VIEWING-PACK render (`docs/DIRECTIVE_VIEWING_PACK_2026-07-18.md`, read-only, base =
the F6 head once it exists else `15a9abd`; **its reproduction precondition needs the same LEGE=0
correction**) → the return is the VIEWING → owner scores his SEALED READS (held, unseen) + works
THE RATIFICATION LIST (§2 of DECISIONS v124: tail read stand/retire · R=207 · entrant `a17aafed` ·
φ `fd92b6fc` · r_pop `c62b5ee8` · presets `c2e17c49` · raw-pick benefit-of-doubt · the knife-edge
note now downgraded to weather) → his WRITTEN BAKE WORD (carries the guard-5 re-pin + RL_PVC2/
RL_LEGE/RL_LEGF INFRA_ALLOW + BAKE_RUNBOOK 3b doc-landing) → ONE bake per `BAKE_RUNBOOK_v2_11` on a
REPRODUCING instance → tag **v2.11** → clean FF promote. POST-DELIVERY: round-entry GO-LIVE (one
owner word) + the weekly tracking loop (361/362) + the v−R flat-derived board toggle (363/364,
R=derived 207) + backward-eval Y-fix (348) + numeric-stack pinning option (347/380).

## 3 — STANDING CONDUCT (STANDALONE — the full set; CORE v2.8 wins on any conflict)
**ENTRY:** per CORE/R100.10 as ever — manifest → CORE (in full, binding) → this doc → DECISIONS →
freshness check (full-URL ls-remote · fresh shallow clone · PK vs repo doc-by-doc) → file/verify
the pack. Never bulk-read; context is a budget.
**THE PEN (the seat's only write):** `tools/seat/pen.py append --item-file F --header-summary S
-m MSG` with `PEN_TOKEN` in the shell env — the owner provides the token IN-CHAT; it is NEVER
written to any file, journal, or commit (verified repo-wide clean at item 370). `git fetch
origin main && git reset --hard origin/main` BEFORE every append (the non-FF guard fired twice
on stale bases — correctly both times). Non-register doc pushes: commit docs-only, prove with
`git diff --name-only`, push `HEAD:main` with the token from env, verify the remote moved.
Filing transforms: assert-before-write + &&-chained ops (the turn-15 swallowed-failure cure —
later PROVEN firing correctly at item 365).
**MESSAGES TO THE OWNER:** open with a PLAIN READ · G/A/R at checkpoints · asks lead with the
click/paste/word · DO:/WHY: for his to-dos · his word outranks every document · a question is a
question, never a go · provenance-tag every carried claim (owner-seen / re-runnable /
report-only) · nothing is fact until re-run or owner-seen.
**BUILDS:** one store/engine writer at a time; read-only jobs parallel (branch parent = main,
provenance = the stamps, item 338); builds never author docs; every directive carries the GIT
ENTRY block; returns without their SHA are incomplete (recover by ls-remote, note it); fences
derived-from-jobs and kept honest; end EVERY turn with a journal via present_files.
**RECONCILIATION SOURCES (the live task):** build-claimed numbers = register items 341 · 343 ·
344 · 348 · 349 · 352 · 355 · 365 · 366 + the session EXIT_PROOFs at the pinned heads. Compare
EVERY audit-computed number against these; a mismatch is investigated before anything else
moves. The auditor annexes decide container comparability (item 347/349/366 doctrine).

## 4 — SEAT-13 CORRECTION LEDGER (owned; measurement + the owner + the builds corrected the seat)
(1) the half-shipped Leg-E filing (swallowed python failure; cured, cure later fired correctly).
(2) the rider-(iv) circularity premise (refuted by the build with code evidence; credited).
(3) the item-351/352 backtest construction (roster-vs-league conflation — the owner's conservation
law exposed it; owned at 357). (4) the 357/358 entrant-value conflation (corrected at 360). (5) the
item-339 over-stuffed fence (an 8-h hang; cured by right-sizing + checkpoint gating). **(6) item
382 — a CONFIDENT WRONG diagnosis** ("F6 fired into the wrong chat") before verifying branch
ancestry; the owner pushed back; corrected at 383 (the true cause = a stale-base RESUME, cured by
the item-384 base-proof-as-commit-1 hardening). **(7) item 385 — OVERSTATED** "the board isn't
run-to-run stable, the freeze path is dead"; the build corrected it at 386 (the flip is occasional;
my own precondition compared the wrong board — LEGE=1 vs the LEGE=0-gated 06d8af60). **THE PATTERN
(6)+(7): a long seat over-concluding on determinism minutiae under owner frustration — this is the
ROTATION SIGNAL, acted on here.** Mechanical: recurring pen non-FF on a stale local base (guard
held every time — `git reset --hard origin/main` before each append).

## 5 — FIRST ACTS FOR THE INCOMING SEAT (in order)
1. Manifest v4.55 → CORE (in full) → THIS doc → DECISIONS v124 → the register (its own header) →
   the freshness check (ls-remote main + tag + candidate; fresh clone; cite SHAs) → file the pack.
2. Check the F6 build's latest return (owner will courier it). Read register items 380→386 IN
   ORDER (386 corrects 385). Rule F6 A vs B WITH the owner; execute; **any board build/repro check
   uses `RL_LEGE=0 RL_LEGF=0`.**
3. Then the ladder (§2b): viewing render → viewing → ratification → bake word → v2.11 → promote.
Pen mechanics, entry, and reconciliation sources are in §3. Never bulk-read; context is a budget.
