# DIRECTIVE — LEG F BUILD 1: THE PHANTOM INTAKE LAYER · seat 13 · 2026-07-18
### Owner-ruled item 346 (Option B). MEMO_LEGF v1.0 = the construction of record. WRITER build
### (the one store/engine writer); F2 runs read-only in parallel (disjoint files).

## GIT ENTRY (verbatim; the item-334 law)
`git fetch origin main claude/lege-projection-law-postures-63u283`
ls-remote must return `cc58570a65616a643a5dcfc955a79f5a44dd5adc` for that branch — STRICT, HALT on
mismatch. `git checkout cc58570…` → `git checkout -b claude/legf1-phantom-intake-<suffix>`.
Discard auto-branches · docs live on MAIN, never join lineages · every repo claim cites a
this-session fetch · "hard-to-reverse" = HALT. Entry assertions: store `968de0c7` · curve payload
`89c14729` · RL_LEGE=0,RL_PVC2=1 ⇒ `06d8af60` · RL_LEGE=1 ⇒ `d85901af` · RL_PVC2=0 ⇒ `9829d01a`.

## EFFORT: High (new view-law machinery over the lens; the exit rule is the subtle part). Why not
Extra: report/view layer only — the balanced board is untouchable by construction and proven each
commit. MODE: auto, PLAN first (derived fence + the SEALED strawman parameter vector, hash
recorded BEFORE any phantom render — the §6 law). TIME: ~3–4 h; flag >2×/<½×; report actual.

## FEED: MEMO_LEGF v1.0 (rules on any conflict with this paraphrase) · DECISIONS v123 ·
CONSTRAINTS v1.19 + acceptance JSON · the rider-(iv) artifacts @ `472a5fa` (the three R
candidates) · the R-inputs report @ `a90052a` · SPEC v1.4.

## THE JOB LIST (one commit each)
1. PLAN + seal (strawmen: free-intake R=207 · exit bar X=207 · natural-order slots where
   ownership unrecorded — hash committed before render).
2. **DRAFT CAPITAL at +k** (memo §2.i): club season-Y+k picks, v2-curve GROSS, `phantom=true`.
3. **FREE INTAKE at +k** (§2.ii): per-club per-season mechanism expectation at sealed R.
4. **EXITS** (§2.iii): list-size conservation trim at the sealed bar; residuals leave club
   totals; slots refill phantom. HALT-AND-CHECKPOINT if any construction choice arises the memo
   does not answer.
5. **TOTALS REPORT** (§2.v): per-lens totals (bal/+1/+2) WITH/WITHOUT the phantom layer, per club
   + league; smoothed per-pick where curves appear; no wide bins.
6. **UI:** phantom rows visibly flagged in +1/+2; lens control gains −1/−2 tabs reading F2's
   artifact path (stamp-asserted at load; empty-state safe if F2 not yet landed).
7. **GATE RL_LEGF (default ON):** =0 ⇒ `d85901af` AND `06d8af60` byte-exact — proven.
8. EXIT: frozen suite (S4) · SSI guards (guard-5 red = the known pre-bake class; flag, never
   self-pin) · k=0 balanced `06d8af60` byte-exact FINAL proof · store `968de0c7` · gates
   snapshot · PR on the branch · RETURN ≤30 lines (branch · SHA · PR · actual vs band · three
   narrowest margins).

## CHECKPOINT LAW (BINDING)
The balanced board cannot move: any k=0 change of any kind ⇒ HALT, commit nothing further, return
the diff to the supervisor. Phantoms appear ONLY at +k under RL_LEGF=1.

## FENCE (derive in the PLAN — the 322 law; three misses this chapter, derive it HONESTLY from
the jobs including every conduit and UI file, then HALT rather than exceed it)
IN expected: `rl_model.py` · `rl_export.py` · `engine/forward_valuation/distribution_pricing.py`
· `ui/app/board.js` · `session_2026-07-18/legf1/`. HARD-OUT: the store · the curve ·
`_merged_recover.py` `:1121-1171` · pins/acceptance · `docs/` · SEASON_PROG · F2's session dir.
S1–S6 · SILENCE IS A RED.
