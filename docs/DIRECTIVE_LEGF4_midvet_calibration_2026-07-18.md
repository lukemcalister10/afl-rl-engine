# DIRECTIVE — LEG F BUILD 4: MID/VETERAN FORWARD CALIBRATION · seat 13 · 2026-07-18
### The item-354 re-scope: the residual ~11pt board-wide forward-vs-backward asymmetry
### (composition forward −19.9% vs realized backward −9.0%), living in the mid/veteran PRODUCTION
### cohorts (φ=0, untouched by F3). **THE TRANSFERRED ACCEPTANCE: the ±5% backtest, UNWAIVED.**
### WRITER build. CHECKPOINT-GATED: no engine edit before the supervisor answers the mechanism.

## GIT ENTRY (verbatim; item-334 law)
`git fetch origin main claude/legf3-projection-fix-tbvxu5`; ls-remote must return
`bccc23145e154f5b83385ffe8c06a54beab09030` STRICT. Checkout it → `git checkout -b
claude/legf4-midvet-calibration-<suffix>`. Docs on MAIN; never join lineages; this-session fetch
for every claim; hard-to-reverse = HALT. **THREADS=1 (OPENBLAS/OMP/MKL/NUMEXPR) on every
hash-bearing run.** Entry: store `968de0c7` · curve payload `89c14729` · k=0 `06d8af60` +
RL_LEGF=0 chain byte-exact re-proof · the F3 seals present (φ `fd92b6fc` · P(retire|age)
`6100f121`) — F3's cures are the floor, never re-opened.

## EFFORT: High (the last calibration between the chapter and the ladder; the mechanism is
un-localized). Why not Extra: the harness exists (S5 — reuse F3's `scripts/backtest.py` +
probes), the cohort is pinned, and the checkpoint gate caps wandering. MODE: auto, PLAN first.
TIME: ~2–3 h; flag >2×/<½×.

## FEED: F3's committed artifacts @ `bccc231` (CHECKPOINT/CHECKPOINT2/EXIT_PROOF + probes — the
residual's characterization of record) · MEMO_LEGE v1.0 (the L-SMOOTH/L-SYMMETRY laws + R103.3)
· MEMO_LEGF v1.0+v1.1 · DECISIONS v123 · CONSTRAINTS v1.19 + acceptance JSON.

## THE JOB LIST
1. **DIAGNOSE (read-only; reuse the harness):** localize WHY production-cohort (φ=0) players
   project forward ~2× steeper than their realized backward path — per-mechanism decomposition
   of the forward decline (the decline-curve application · runway/horizon effects · availability
   layers · anything else measured, not guessed), with the specific `_merged_recover.py`
   addresses. Composition-controlled, per-cohort, smoothed, no wide bins.
2. **CHECKPOINT (mandatory; no engine edit before the answer):** return to the supervisor ≤15
   lines — the mechanism · the addresses · the §-law-compliant fix construction you propose ·
   what it CANNOT reach without bending a law. The supervisor answers with the fence grant + a
   MEMO addendum v1.2 carrying the construction; only then:
3. **THE FIX** per the granted memo law, one commit per site class, seals-before-render for any
   new parameter.
4. **THE ACCEPTANCE RUN:** backtest −1→now AND −2→−1 within **±5%** (the transferred bar) · the
   F3 gradient stays un-inverted (developing shallowest) · per-lens totals recommitted.
5. EXIT: k=0 `06d8af60` byte-exact · RL_LEGF=0 chain byte-exact · k=0 dormancy tests (F3's +
   yours) PASS · store untouched · frozen suite · guard-5 = the known pre-bake red · annex ·
   PR stacked on #117 · RETURN ≤30 lines, THE BACKTEST NUMBERS FIRST.

## THE LAWS (BINDING)
HALT rather than bend: no Reid violation, no lens-only growth/decay terms, no cohort hand-tuning
to hit the aggregate, no re-opening F3's pedigree carry to compensate (the item-354 self-rejected
shortcut stays rejected). Any k=0 change of any kind ⇒ HALT. If honest law-compliant maths cannot
reach ±5%, HALT AND RETURN THE TENSION — that outcome goes to the OWNER (it would mean the ±5%
expectation itself needs his re-ruling, which only he can give).

## FENCE: pre-checkpoint IN = `session_2026-07-18/legf4/` ONLY (diagnosis is read-only).
Post-checkpoint IN = exactly the granted addresses + the session dir. HARD-OUT always: the store
· the curve · V0/`_iso_dec` `:1121-1171` · `rl_model.py` · pins/acceptance · `docs/` · ui ·
SEASON_PROG. S1–S6 · SILENCE IS A RED.
