# ROADMAP — AFL SuperCoach RL valuation engine — v9 · 2026-07-06 · supersedes v8
### Clean forward view. Companion to DECISIONS v75 (live tracker) + HANDOVER rev115 (current state).

## STATUS (read first)
v2.5 is live and independently verified. **Wave-0 (the plumbing) is DONE** — dead-code stripped (zero price change), the
change-ledger built + self-proven, the anti-inflation identity-gate confirmed genuinely complete (a real load-vs-compute HALT).
The measurements we could take are banked. **No decisions gate anything** — what remains is model-derivation re-fits, a cold audit,
and a re-bake. The plumbing is honest; the model is the frontier.

## THE CONCEPT (unchanged)
Price every player and draft pick as an expected-future-value asset in one SCAR currency. The board reads the engine; nothing
outside the engine re-values.

## THE SEQUENCE

### GATE + WAVE 0 — DONE
Phantom/DPP base confirmed clean · dead-code stripped (PR #38, zero delta) · change-ledger built + proven (PR #39) · identity-gate
confirmed complete (closes the F1/F2 systemic finding) · measurements banked (PR #40).

### WAVE 1 — Measurements — DONE (with two owner method-calls + one deferral)
Held: games off-by-one, staleness cliff (+103%), pedigree floor, KPF elasticity, 2004-window census. Await an owner METHOD-CALL:
M1 breakout follow-through, F11 band skew. DEFERRED: the return-from-injury %s (must be measured NET OF the re-derived aging curve —
they land in the derivation phase).

### WAVE 2 — Derivations (the frontier; several parallel)
- **Young-slope flatten** — the confirmed top lever (19–22 over-optimism). Fairly independent → natural first.
- **Pick-value curve** — the opener STAMPS + re-stamps + coordinate-checks the parked v1 spec on v2.5 FIRST, then fits on the 2004 window.
- **Form-conditioned aging decline** — must precede the injury measurement (they're coupled).
- **Ruck** — derived + cross-position sanity calibration (owner eyeballs the ruck $).
- **Key-forward treatment** — DISTINCT from aging-elite; the aging fix won't reach KPFs (measured). Own derivation target.
The model derives; the owner reviews each board as ground truth (expect healthy re-fit loops where a board looks wrong to him).

### WAVE 3 — Injury measurement + the 3-arm fix
Re-measure the return %s net of the derived aging curve → age-with-clock + graded present-season wipe + the return haircut in its
correct shape (young = lost-growth-year slide · prime = small lasting penalty · old = let the aging curve own it, no double-count).

### WAVE 4 — One integration candidate  ← the Fable-budget spot
Retention + floor re-derive ONCE, injury meshing with aging without double-counting, blend coherent. The one genuinely hard
reasoning step — reserve the 4% Fable here.

### WAVE 5 — Cold audit → closure → re-bake
Independent cold-seat re-runs the candidate PRs' key checks (the postmortem rule — no bake on a single chat's word). Then the owner
views the candidate board + book, gives the word, we re-bake via his codespace and promote main.

## PARALLEL — THE VIEWER (not blocking)
PR #36 pure-view core → refinements → merge → companions (published-stamp fix, board-app retirement) → analytics cards → 3 future
tabs. Real data plugs in LAST — the point where the identity-gate's "viewer renders `v` verbatim" boundary gets its explicit tick.

## PATH (one line)
v2.5 → gate+Wave-0 DONE → measurements DONE → derivations (young · PVC[re-stamp first] · aging · ruck · KPF) → injury (net of aging)
→ one integration candidate (Fable) → cold audit → owner word → re-bake. No decisions left; the model derivations are the work.
