# DIRECTIVE — LEG B SEGMENT 5: THE ACCEPTED LAW → THE GRID → THE RETURN · 2026-07-16 · seat 9
### FRESH Claude Code (Opus) chat — the whole brief. Segments 1-4 retired (seg-4's law wiring +
### diagnostics live on the branch). Every number in this job is MEASURED or OWNER-SET (R105.6:
### "Accept" — quarter fade · bar 0.80). SILENCE IS A RED. S1-S6. Report the APP counter; reap tasks.

## EFFORT: Extra (the chapter's crux ships; thinner validation is a refused lever). MODE: auto with
## a MINI-CHECKPOINT: commit the constant-flip + the two new self-tests + pre-run assertions, HALT
## ≤5 lines with the diff SHA, await PROCEED (the design freedom is zero; the checkpoint is cheap
## insurance only). TIME: ~15 min to checkpoint · 2.5-4 h after.

## FEED (fetch fresh; VERIFY seals — mismatch ⇒ HALT)
1. docs/MEMO_LEGB_functional_form_2026-07-16.md — header **v1.3** — md5 == `cf6c00804538a73234a821c5472d1a8f`. THE LAW.
2. docs/acceptance_v1_20.json — md5 == `6b83e3368ecd19843663ffd9b1c41695`. Assert every leg_b entry (decay 0.25 · threshold 0.80 ·
   recency_invariant · movement_ledger · the grid · everything carried).
3. docs/DIRECTIVE_LEGB_uncompress_2026-07-16.md — the full deliverable set + fence where not
   superseded by memo ⟪v1.1⟫/⟪v1.2⟫/⟪v1.3⟫. 4. Register items 240/241/245-248.

## BASE PIN — STRICT: branch `claude/legb-segment4-v1-2-law-grid-2l4dhv` at **`ef6bf1a`** exactly
## (full-URL ls-remote; other head ⇒ HALT). CONTINUE it. Docs: main at-or-after **`0163ed5`**,
## docs/-only diff. Store `b1fd0bce` + config `c2d233ae` untouched.

## THE JOB
0. **THE FLIP + THE GUARDS (the checkpoint's content):** UNCOMP_DECAY 0.5 → **0.25** (owner-set,
   one declared constant) · implement **the R105.5 L-RECENCY self-test** (per-game weight strictly
   non-increasing in years-back across the store's season range; HALT-NOT-WARN; suite-wired) ·
   assert the R105.4 forbidden-list (no exclusion/floor/phase test anywhere in rho_out). HALT.
1. **A/B re-prove at your head:** RL_UNCOMP=0 AND default-inert ⇒ board `8d90c9ac` BYTE-EXACT.
2. **THE GRID:** frozen fit_beta per point over {0.55, 0.60, 0.65, 0.70}; **s = the SMALLEST
   clearing β ≥ 0.80 (OWNER-SET)**; EMPTY ⇒ HALT with the table (never extend, never re-tune).
   Selected s ⇒ UNCOMP_S_DEFAULT.
3. **THE FULL RETURN** (item-206 set, unchanged) **+ THE ALL-804 MOVEMENT LEDGER at the chosen s**
   (id · player · pos · price off/on · Δ SCAR/% · rho · w · games-in-window — committed CSV) —
   E/B captain-in vs the owner's HARD 1.75 · G-COHORT y4/y5/y6 vs 1.30 + fixture · ≤22 slope ·
   censuses · L-SMOOTH · ledger · value-flow + R104.8 · the w-export · killswitch matrix · gate
   snapshot · panel/self-tests (now incl. L-RECENCY).

## FENCE
IN: the one constant · the two guards · the grid/selection · deliverables · derived regeneration
(stamped). OUT (touch = HALT): the STORE · docs/ · ui/ · acceptance · gate/guard code beyond the
named self-tests · config · the wired law itself (it is BUILT — you flip one constant, you do not
redesign). RETURN ≤30 lines + plain-terms close: branch · head · PR · chosen s · β at s · E/B ·
G-COHORT y4/y5/y6 · ledger headline (biggest up/down) · actual time · APP counter.
