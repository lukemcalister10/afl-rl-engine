# HANDOVER — rev159 · 2026-07-17 · supersedes rev158
### rev159 (item 329): THE SEAT-12 ROTATION FILING. LEG D COMPLETE AND COLD-AUDITED (PR #109
### `e4177c2`, prescreen item 322, shard reconciled GREEN item 328). The seat's full record =
### register items 313–328. Seat-13 queue = the FIVE-MIGRATION directive → Leg E → the ladder →
### ONE bake → v2.11. DECISIONS → v123 · manifest → v4.52. SEAT 12 CLOSES HERE.
### AMENDED same filing (item 330, owner-requested recheck): the five-consumer MAP OF RECORD is the
### committed SITE_CENSUS (paraphrased lists deprecated); the unfired 319-superseded review pack
### archived; docs/ root's completed prior-seat directives noted as standing practice (optional
### hygiene sweep for a future seam, NOT a trap — nothing in them is operative over the manifest).

## 0 — PINS (verify fresh; the pen moves main)
main = verify fresh. Tag **v2.10** (`d14efae`, board 790136a3) unchanged. **CANDIDATE LINE:**
…→ `33c8b52` (PR #105, Leg C) → ACT-1 checkpoint `12a0761` (branch
`claude/legd-pvc-rederivation-o290ye`, WRITE-RETIRED, unmoved-verified) → **`e4177c2` (PR #109,
branch `claude/legd-pvc-rederivation-act2-l2hqpl`, stacked on #105) — PRESCREEN item 322 ·
SHARD-AUDITED GREEN item 328. Board `9829d01a` → `270a2c5f` · store `968de0c7` UNTOUCHED · curve
`pvc_curve_v2.json` (payload stamp `89c14729`, file `56dd7a7b` — a KAT names WHAT it hashes) ·
kill-switch RL_PVC2 (=0 ⇒ 9829d01a byte-exact, twice-proven).** TOOLS ON MAIN: + the round-entry
tool (PR #108 owner-merged `52da0fad`; dry-run only; go-live = post-delivery owner word).
GROUNDWORK FEED: `claude/legd-groundwork` @ `9845180`. Pack: per the MANIFEST (v4.52) · CORE v2.8 ·
DECISIONS **v123** · CONSTRAINTS v1.19 + acceptance v1.21 (sealed, item 314) · SPEC **v1.4
OPERATIVE** (item 313) · SSI v1.3.

## 1 — WHERE THE CHAPTER STANDS
**LEG C COMPLETE** (rev157/158 record). **LEG D COMPLETE + AUDITED:** the COMPOSED PATHWAY curve
(owner-ruled; busts full weight; 2-D per-exact-pick non-median kernel; PVC = the year-0 point;
entry closure asserted; memo-C fallback measured 9.486% and rejected on evidence). Gates: pooled
G-Y0 **1.10%** ≤ 2% HARD (shard-recomputed 1.100%) · strict descent 0 violations (15 old plateaus
cleared) · curve(1)=3000 · R104.5 exact. Curve shape: picks 2–40 down ~2–8%, the deep tail
LIFTED (p50 473→589 · p60 355→542 · p80 342→494 · pathway slots p84 340→485 · p90 334→473).
**MOVERS: 22, ALL RUCKS, ALL UP** (late-pick/pickless deep-tail anchoring; max +208/+50%,
pickless pk90 rucks); every non-ruck and every established row HOLDS byte-exact. Pair 3 = Bont
ahead +6.2%, INSIDE the 0–10% band on this candidate (formally scored at the ladder). **OPEN
OWNER READ (item 325, operative): "no world where p1 is only 6x p60"** (curve reads 5.5x GROSS)
— tested at the ladder by riders (i)+(iv); if standing, the cure = the ENGINE entry-pricing axis,
post-v2.11, never a hand-bent curve. **ADOPTED RIDERS (325, report-only ladder viewing):** (i)
realized-outcome cohort-holdout calibration (+ washout-exit calibration) · (ii) cohort-bootstrap
tail influence (+ short-draft-era caveat) · (iii) uncertainty grading past ~p50 · (iv) the
REPLACEMENT-ADJUSTED VIEW (gross beside v−R, measured R). The GPT clean-room competitor: assessed
item 324 (4 independent convergences; target-definition tail divergence not adopted; its §5
failure-mode list = ladder-audit seeding material, addresses only). **SEALED READS: DONE**, held
by the owner, unshown until the ladder's scoring step. SEASON_PROG 0.58 (item 297).

## 2 — SEAT 13's ORDERED QUEUE
**ACT 1 — THE FIVE-MIGRATION DIRECTIVE** (first pen job; released item 328): migrate the five
`rl_model.py` MA.PVC consumers off the frozen v3.4 curve onto `pvc_curve_v2` — **THE MAP OF
RECORD = `session_2026-07-17/legd_derivation/SITE_CENSUS.md` on the act2 branch (`e4177c2`),
sites named from the code with line numbers; any list in prose (including this doc's earlier
drafts) is a paraphrase, the census wins** — ONE
CONSUMER AT A TIME with per-consumer before/after proofs; **`pvc_snapshot`→peak-model MEASURED
FIRST** (trained on the old snapshot; post-bake fallback if it needs retraining); base = `e4177c2`
STRICT; **the fence DERIVED MECHANICALLY FROM the job list (the item-322 lesson — never
parallel-maintained)**; rider: emit the engine-side R inputs (the free-intake pick-equivalents)
report-only for items 326/327. **ACT 2 — LEG E:** finalize
`docs/MEMO_LEGE_projection_construction_DRAFT` on the post-migration base (re-pin the dial site;
the lens reads the NEW curve), issue, build, prescreen. **ACT 3 — THE LADDER:** S6-sharded cold
audit (seed the GPT §5 checklist by address) · the owner's sealed reads scored · his viewing =
board + book + the chapter ledgers + riders (i)–(iv) · **the RL_PVC2 manifest/INFRA_ALLOW entry
rides the bake** (the RL_FLEX pattern, shard-noted 328) · his written word → **ONE bake → v2.11 →
clean promote.** POST-DELIVERY: round-entry GO-LIVE (owner-worded, GO_LIVE runbook) ·
SEASON_PROG advances per-round · the post-v2.11 design queue = **326/327 the incremental
currency** (orthogonal axes ruled: production bars ≠ list-space R; trade-math list-spot term) ·
**307/308 season leverage** · **the 325 tail question if the read stands**. PARKED: hygiene
(i)/(iii) ride the next natural engine build.

## 3 — STANDING CONDUCT (adds to CORE + rev158 §3; the seat-12 additions)
`git fetch origin main` OPENS EVERY PRESCREEN — a merge-base against a stale tracking ref
mis-scopes (321, demonstrated again by the pen's non-FF halt) · a KAT NAMES WHAT IT HASHES
(payload vs file, 322/328) · FENCE LISTS ARE DERIVED FROM JOB LISTS mechanically (322: my fence
omitted the site my own job 8 required) · assertion patterns are LIVE-REGION / VERSION-CELL
scoped — historical changelogs legitimately carry superseded strings (314) · clean-room /
audit seeding carries a mechanical LEAK-GUARD (319: our vocabulary; 323: build-reported numbers)
· checkpoint chats are WRITE-RETIRED the moment a successor exists; the successor's entry
ls-remote asserts the head unmoved (320) · plus all carried rev158 §3 laws.

## 4 — SEAT-12 CORRECTION LEDGER (owned; the pattern: a DESIGN-DIALOGUE seat — the owner's
corrections improved the record each time; verify-then-file held throughout)
(1) item-318 scope misread (a "review of ours" where the owner wanted the clean-room competitor;
superseded 319). (2) the ACT-2 directive's fence omitted `rl_export.py` while its own job 8
required it — the item-322 trigger; cured by the shard + the derived-fence law. (3) the
stale-fetch merge-base (321). (4) the decile-bands diagnostic proposal — a CORE-rule-7 violation
the OWNER caught (325). (5) the 326 "double-count trap" over-claim, dissolved by the owner's
orthogonal-axes split (327). Mechanical (all loud, none silent): the turn-3 token-expansion
defect · the turn-12 heredoc ordering · the turn-11 non-FF (the guard working). Three shard/audit
instruments fired this seat (the ACT-2 shard · the clean-room leak-guard · the pen's own gates)
— each found what it was pointed at or proved the target clean.
