# HANDOVER — rev151 · 2026-07-17 · supersedes rev150 · SEAT 10 ROTATION-READY
### rev151 (same day, item 279): the SECOND self-audit — five more defects (the manifest's own
### table row and STATE heading still v4.42 · the DECISIONS PK list naming superseded versions ·
### two live pointers at the archived rev149 · stale terminal item-counts AGAIN). Durable cure:
### TERMINAL ITEM-COUNTS ARE REMOVED from every pack doc — the register's own header is the only
### source for its count, ever.
### rev150 (same day, item 277): the OWNER-PROMPTED SELF-AUDIT of the rev149 cascade found four
### defects (stale register refs · the spec v1.2-header lookalike · §1b at the spec tail · the
### missing checkpoint-paste note) — all corrected here; rev149 archived. Nothing else moved.
### Seat 10 closes at the RELAY-RETURN seam (recommended; 43 turns deep, correction ledger ×3).
### The incoming seat: freshness check FIRST (R100.10), file the handed pack, then §2 — the
### relay MINI-CHECKPOINT prescreen is the first act. Register is the log — read its OWN header for version and item count.

## 0 — PINS (verify fresh; the pen moves main)
main = verify fresh (full-URL ls-remote). Tag **v2.10** (`d14efae`, board 790136a3, store
b1fd0bce). CANDIDATE LINE (one straight history): Leg-A `8b8ab7d` (Iso Evidence Fade, board
8d90c9ac) → seg-3/4/5 → selection `273463e` (**s=0.10 OWNER-SET wired; board `f2f077b2`;
baseline y4/y5 breach HEALED 1.2667/1.2727/1.2300; PR #103 open, merges at chapter end**) →
**THE RELAY IN FLIGHT** (base 273463e STRICT; FIRST COMMANDS mandatory). QUARANTINED:
`2b64630` (Leg-C content reference — sound content, broken topology; NEVER a base, NEVER
merged). Pack: manifest **v4.44** · CORE v2.8 · DECISIONS **v118** · CONSTRAINTS v1.18
(re-seal QUEUED, cargo in §3) · acceptance v1.20 (same) · SPEC **v1.3** (§1b beside §1; header corrected) · SSI v1.3.

## 1 — THE SEAT-10 STORY (items 250–275; what every seat must know)
**LEG B RESOLVED BY MEASUREMENT + OWNER LAW:** the 0.80 grid came back EMPTY (sanctioned HALT);
the gap diagnostic reconciled the 0.14 prediction miss (memo v1.3 carried TWO defects: the
conservation renorm C OMITTED from its β formula; a borrowed λ 0.92 vs realised 0.72). The
owner then ruled: per-position conservation is NOT his law (pools may re-rate; unfunded
permitted); the guards are G-COHORT + **THE SINCERITY LAW** (relative claims judged in relative
terms; ledgers carry RANKS). The unfunded road measured DEAD under his **COHORT HEALTH BAND**
(floor 1.08 · cap 1.30 hard · ideal 1.15–1.25 — a sub-floor reading is a FAILURE; edges move
only via explicit benefit/tradeoff conversation). The conserved family's landing zone measured;
**the owner locked s = 0.10** (bar/grid machine-selection RETIRED) and it is WIRED at 273463e.
Pair 2 re-banded ±15% (passes at 0.10); pair 3 passes. **LEG C:** flex machinery BUILT (11
future writes + 4-rider + blends) with content verified SOUND but topology BROKEN (wrong base,
2nd in 24 h ⇒ FIRST COMMANDS law); the earned-component gate HALTed on Carroll/Emmett — owner
ADJUDICATED "Accept." (two-row NAMED waiver, gate stays binding elsewhere). **THE CURRENT-
SEASON DPP MISS:** the owner's long-discussed design was never captured into the spec — now
**§1b (spec v1.3, BINDING)**: post-collapse eligibilities (K-X drops every G), remaining-season
bar = the more valuable of the dual pair, level path keyed to present_position, year-0 only.
Driscoll register-corrected to 100% MID (blend rows = 90). **THE RELAY** (in flight) closes
items 270–275 in one build.

## 2 — IN FLIGHT (exactly ONE writer): THE RELAY (docs/DIRECTIVE_LEGC_relay_2026-07-17.md)
Mini-checkpoint after its wiring commit (§1b year-0 site + guard update + fut-label fix) —
the supervisor prescreens the diff, PROCEED via the owner. Then: re-ingest (corrected register;
expect ELEVEN primary changes — the item-269 list — and NINETY blends) · RL_FLEX gates ALL of
Leg C (=0 ⇒ f2f077b2 byte-exact) · full battery incl. band BOTH SIDES · sincerity ledger ·
pairs · the two-row waiver scored-never-flagged · attribution with a §1b stage
(Petracca/Sheezel/Jagga named). `b4c67bb6` non-reproduction is EXPECTED (Driscoll + §1b).

## 3 — THE QUEUE (the path to the ONE bake)
Relay RETURN prescreen (rank-ledger scans · band both sides · earned gate ex-waiver · labels
assertion) → **THE PRE-LADDER ACCEPTANCE RE-SEAL** (one pen job; cargo: L-SINCERE · the
two-sided G-COHORT band · pair-2 0.15 · the s=0.10 owner-set law · `s_dial_selection`
RETIREMENT (it still encodes the dead bar/grid — the ladder must never assert it) · §1b
entries · the item-272 waiver rows as standing_fails-style entries) → **LEG D** (pick curve;
R104.9 strict descent; consumes the w-export; G-Y0 lands here) → **LEG E** (posture presets;
named LTI movers incl. Rozee) → the LADDER (cold audit · sealed reads · owner viewing) → his
word → ONE bake → **v2.11**.

## 4 — STANDING CONDUCT (seat-10 additions; turn-one law with rev148 §4's)
**FIRST COMMANDS** on every build (checkout -b from BASE-SHA + merge-base assert, printed;
"content moved to match" is never conformance — item 270). · Identity resolution by STABLE ID
only; name-matching over the 2,652-row store FORBIDDEN (269 — retired namesakes). · A guard
protects a RANGE; "clears with margin" toward an unguarded side is a WARNING (258). · Ledgers
carry RANKS (256). · Discussed owner designs are captured to SPEC verbatim or they do not
exist (274). · Pre-fire directive audits are mandatory AND fallible — the item-268 audit fixed
three real hazards and manufactured one false count (269); verify the auditor too.

## 5 — SEAT-10 CORRECTION LEDGER (owned, register-filed)
(1) The phantom 13-count (name-collision; item 269). (2) The R105.1 over-application
("eligibility never values" — its true scope is K-listings' G; item 274). (3) The ledger
base-column mislabel (post-Leg-A board labelled v2.10; item 273). All owner- or build-caught;
the pattern to guard: answering from a plausible construction instead of the resolved record.
