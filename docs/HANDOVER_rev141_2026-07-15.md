# HANDOVER — rev141 · 2026-07-15 · supersedes rev140 (THE PRE-AUDIT SEAM handover:
### the chapter's last writer has returned; the outgoing seat rotates so a FRESH seat runs the audit+bake.
### To the INCOMING SEAT (Fable OR Opus — the seat is model-agnostic by design). Read after 00_MANIFEST
### (v4.23) + CORE (v2.8). **The register (v130+) is the durable log — trust it over this summary.**
### This refresh exists because rev139 predated a FULL DAY of work (items 118–145). State is fully updated.

## FIRST ACTS (CORE v2.8, BINDING ORDER)
Freshness check FIRST — full-URL `ls-remote` · register header · docs/ list · PK vs repo DOC BY DOC —
then file the handed pack. **PK behind on ANY doc ⇒ HALT and tell the owner.** Adopt `tools/seat/` from
turn one (on main: orient.sh · prescreen.sh · gates_score.py — after orient, the register step now reads
docs/OPEN_ITEMS_REGISTER.md, Fix 1 merged item 124).

## STATE (verify live; cite your own fetches)
- Pack: manifest **v4.23** · CORE **v2.8** · DECISIONS **v102** · CONSTRAINTS **v1.15** + acceptance_v1_15.json
  (REPO-ONLY) · SSI v1.3 (docs/) · **register — READ ITS OWN HEADER** (v135+, items 118–151 are today).
- Tag of record **v2.9 = `9f8ae76`** · **pick 1 = 3000, permanent numéraire.** Main CI red = EXPECTED
  (goes green when this chapter promotes). Main is the pen's docs line; base pins are "at-or-after + docs-only".
- **THE CHAPTER — "evidence over calendars" — owner-ruled EXTEND then SPLIT (items 122·133).** Candidate
  line, one straight history, every base STRICT:
  `#82 (Fix1+absence e7d980eb) → #83 (determinism a2a06c7) → #85 (evidence-fade 62352729) →
   #89 (evidence-weight 557297c) → #90 (improver 2c7b905) → PR#91 (rl_model pin 1073f633) → TRIO (in flight)`.
- **THE CHAIN IS COMPLETE — NO WRITER IN FLIGHT.** pin `1073f633` (item 142) → trio `0cf723af`
  (item 149; store `340a7a32→b1fd0bce`, board `800d0399`) → **CAPTAINCY = THE FINAL HEAD:
  `9be07b8e5939eeade71106ef1eaee112df183441` (PR #94, prescreen item 151 — board `790136a3`)**. Nothing else
  writes before the bake.
- **THE TRIO LANDED CLEAN (item 149):** 106 active movers all-MSD (PICKEQ 60→90, owner-desired) + 12
  back-section fringe; cohort ratios under even the UN-waived 1.30 (y5 1.2995); **Newcombe 4302→3975 —
  his THIRD move today, the owner watches him at viewing.** B1 certified on the frozen suite.
- **THE 1.335 WAIVER (item 143, acceptance v1.15 `cohort_waiver`):** G-COHORT bound 1.30→**1.335 THIS
  BAKE ONLY**, reverts next chapter. >1.335 = HALT, never a waived pass. (Improver left y5 at 1.2998;
  captaincy will eat into it — MEASURE, the directive requires both-bound reporting.)
- **A9 (Ginnivan>Ward): RESOLVED-IN-CANDIDATE** by the improver's two-directional `_eo` (item 140) —
  scored, not skipped. Standing fails: A2/A3/A12 (data-caused) · A-PAIRS pair 3 (expected until PVC).

## YOUR QUEUE, IN ORDER
1. **HAND THE OWNER THE FILLED AUDIT SEED** (v2, couriered at rotation — acceptance v1_15 scoring frame,
   RL_CAPT in the ablation chain, the trio-store ablation subtlety, the Newcombe watch) → he runs it in
   an incognito chat OUTSIDE the Project.
2. **RECONCILE measured-vs-claimed** shard by shard against register items 134·140·142·149·151
   (ITEM-74 LEG 2 clears on this reconcile). Any unexplained delta HALTS the ladder.
3. **The owner views board+book vs his SEALED READS** (written, on paper — OQ-D satisfied) → his WRITTEN
   bake word → BAKE (tag; the bake build re-pins the panel, regenerates q97m per the freeze ruling,
   commits the snapshot) → promote main (clean fast-forward; CI green) → SEAM: docs archive, pack sync.
4. **AT THE SEAM:** fire the BOOK-CALIBRATION v1.1 on the bake head (directive couriered, base
   placeholder — it was NEVER FIRED; rev140's "in flight" claim was WRONG, corrected here) → open the
   PVC chapter (items 130–132 verbatim law; the calibration is its scoring baseline; the tail ruling —
   keep 0.11 — and the snapshot-naming hygiene item ride along).
4. **POST-BAKE (queued, not now):** L-CAPTAIN is fully unlocked at the bake (all 3 prereqs clear); the
   PVC/pick-curve chapter opens on the reworked spec carrying items 130–132 VERBATIM (no young net-strip ·
   cure on the axis where the defect lives, LIFT-led · iso_corr evidence-fade = the English leg) + the
   book-calibration baseline (v1.1, may still be in flight). Then ingestion (blocked on item 75 cm_400 +
   item 67 completeness) · UI · positions.

## OPEN OWNER WORDS (no action until given)
U-curve <20 clamp (R100.11's last open assumption, unruled) · THE AXIS RULE (PENDING-OWNER, CONSTRAINTS
v1.15) · the tail is CLOSED (item 141, keep 0.11).

## HOW TO WORK WITH THE OWNER (empirical record — UNCHANGED, load-bearing)
His reads are ground truth until HE waives them — he has found pricing defects from the board repeatedly
(the flattery net-strip, the English/Briggs axis, the iso non-fade, the trio premise) and corrected
supervisor framings with one sentence. **Give him numbers, never theories**; symmetric options WITH a
recommendation; DO:/WHY: asks; plain-read openings; no over-production. **The repeating seat failure: a
plausible mechanism written as a measured fact — RUN THE COMMAND.** Provenance-tag every carried claim;
never silently upgrade. Prior seats' judgements are hypotheses; verify before building.
**⚠ CLIENT QUIRK (this chat): text BEFORE/BETWEEN tool calls did not render for the owner — put the full
response AFTER the last tool call. Journals carried the record when the chat dropped it.**

## CONTEXT ECONOMY (owner-agreed)
Chat = plain read + the decisions the owner needs; detail in the register + the per-turn journal
(MANDATORY, crash insurance). Verify in FEW targeted commands; grep, never read wholesale. CONSTRAINTS +
acceptance are REPO-ONLY — fetch and grep when judging a board.

## RESIDUAL RISK MAP
The out-of-fence `np.average` site · `_iso_dec` at `_merged_recover.py:801` — look there first if CI reds
on the fixed line; neither is a failed fix. `cm_400.pkl` is pinned-but-not-rederivable (item 75) — blocks
ingestion, not this chapter.
