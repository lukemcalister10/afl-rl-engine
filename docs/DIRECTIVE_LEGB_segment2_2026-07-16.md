# DIRECTIVE — LEG B SEGMENT 2 (IMPLEMENTATION) · 2026-07-16 · seat 9 (Fable)
### FRESH CHAT (S2 seam: the plan chat reached ~187k tokens producing the PLAN; the PLAN is now a
### committed artifact, so implementation runs in a fresh chat seeded from the repo — the disposable-
### chat doctrine, applied at the checkpoint seam). The plan chat's job ended at the amended-PLAN
### commit; it is RETIRED. This chat implements. SILENCE IS A RED. S1–S6 operative.

## EFFORT: EXTRA · MODE: auto (the PLAN already exists — do NOT re-plan; first act = read it)
## TIME: 3.5–6 h wall-clock (segment 2 of the original band; confirm up front; report actual)

## BASE PIN — STRICT
Branch `claude/legb-output-price-decompress-3vo8y1` at the AMENDED-PLAN commit:
`git ls-remote https://github.com/lukemcalister10/afl-rl-engine claude/legb-output-price-decompress-3vo8y1`
must print the amended-PLAN head (the commit whose message carries the item-211 amendments; its
parent chain is amended-PLAN → 12cb206 → 8b8ab7d). Any other head ⇒ HALT-AND-ASK. Continue THIS
branch — never a sibling (the item-202 lesson).

## THE DESIGN OF RECORD (read in this order, in full — then implement, never redesign)
1. `session_2026-07-16/uncompress/PLAN.md` AT THE HEAD — the plan WITH the two item-211 amendments
   (site 7 `_nv_bwd` NOT-WIRED · captain-preserved-additively at ALL SIX SITES — item 213: `base=lev+capt_prem(lev)` feeds every wrapped posval, so the v′=v0′+δ construction and the δ byte-identity self-test apply at 1-6; ρ stays pre-captain).
   The plan governs; its §13 flags are RATIFIED per register item 211.
2. `docs/MEMO_LEGB_functional_form_2026-07-16.md` + `docs/acceptance_v1_17.json` — verify their md5s
   == the PLAN §1 pre-view manifest (3624ce55… / c3d5008…). Mismatch ⇒ HALT (the seal).
3. `docs/DIRECTIVE_LEGB_uncompress_2026-07-16.md` — deliverables/fence live there unchanged.

## THE JOB
Execute the amended PLAN §§2–10 exactly: the six wired sites (+ site 7 not-wired) · ρ/E per-leg ·
V_ref median construction · Δ=6.0 onset · captain-additive at ALL SIX sites (item 213) · production-side per-position
renorm · RL_UNCOMP + the A/B 8d90c9ac byte-identity · frozen fit_beta s-grid with the smallest-s
rule (HALT-AND-ASK on an empty grid) · hygiene rider (375–389, verify-dead-then-delete, obituary) ·
every deliverable in the original directive incl. the w-export and the donor-side mover report.

## FENCE — as the original directive, verbatim (engine sites only; store/docs/ui/gates OUT).
RETURN ≤30 lines + plain-terms close: branch · head SHA · PR number · actual time.
