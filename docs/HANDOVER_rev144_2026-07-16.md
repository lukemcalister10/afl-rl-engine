# HANDOVER — rev144 · 2026-07-16 · supersedes rev143 · authored by seat 8 (Fable) MID-CHAPTER
### Written for ROTATION-READINESS while two builds run (the seat compacted once; the owner and seat
### agreed to refresh the pack early). A successor seat opens on this + the standard opening prompt,
### runs the freshness check (orient.sh), and picks up at "THE QUEUE" below. Seat-8's per-turn journals
### (turns 1–11) are the owner's crash record; the register (v166, items → 188) is the durable log.

## 0 — PINS (verify fresh, never trust this file's copy)
main = `f22149d` at writing (DOCS LINE — the pen moves it; base reads are at-or-after + docs/-only
diff). Canonical tag **v2.10** (`d14efae`) · shipped board **790136a3** · store **b1fd0bce** · engine
tree **fc7045d6** · config c2d233aec104. Pack: manifest **v4.32** · CORE **v2.8** · DECISIONS **v107**
· CONSTRAINTS **v1.18** (repo-only) · register **v166** · SSI v1.3. CI green on the merge head (run
443); the board-md5 print lives INSIDE A COLLAPSED STEP on the run page (expand it; item 175).

## 1 — THE CHAPTER (PVC+FLEX, R103.2 merged) — IN FLIGHT
Spec of record: `docs/SPEC_PVC_FLEX_CHAPTER_v1_2026-07-16.md` (**v1.1** header; filename keeps the v1
path deliberately — the Leg-A FEED cites it). Five legs A→E, sequential kill-switched writers, ONE
candidate line/ladder/bake → tag v2.11. Per-leg EFFORT + seat allocation in §3 (B is EXTRA and
Fable-designed; builds are Opus). **ALL SIX §6 RULINGS ARE IN** (DECISIONS v107):
R104.3 English/Briggs priced-ratio HARD FLOOR 1.75 (owner flexibility AT THE VIEWING RUNG only; β set
rides: proven-27+ ≥0.85, CI∋1.0, ≤22 slope risen from 0.111) · R104.4 posture sketch adopted,
BALANCED CANONICAL (the only board that gates/bakes/seals) · R104.5 2027 pick discount per-posture
0.10/0.15/0.05 · R104.6 sealed reads WRITTEN (OQ-D) · R104.7 **G-Y0 BINDING, POPULATION-LEVEL**
("over the wider sample, on average" — V0 is positional; single-class top-heaviness is NOT a breach;
effective at Leg D; CONSTRAINTS v1.18) · L-AXIS BINDING (v1.17, carve-out + owner-only waivers).

## 2 — IN FLIGHT RIGHT NOW (two lanes, disjoint per S3)
**LANE 1 — LEG A (writer):** iso_corr evidence-fade on the v2.10 `w` + ISO monotonization.
Directive: `docs/DIRECTIVE_LEGA_isofade_2026-07-16.md`. **PRESCREEN ITS RETURN AGAINST:** the item-130
value-flow check (ΣΔ · age/cohort distribution · over-performer scan — ANY young net-strip or ANY
above-projection young cut = HALT) · RL_ISOFADE A/B BYTE-IDENTITY proof · census-v2 gauge ≤ +15,612 ·
L-SMOOTH census · G-COHORT y1–y5 < 1.30 · A-PAIRS scored · the English/Briggs ratio PRINTED (chapter
floor 1.75 — Leg A alone need not reach it) · fence held (no store, no map, no docs) · the two hygiene
riders landed (SGC_* strip · `_EVW` obituary). Expect PURE LIFTS concentrated in proven mid-rounders.
**LANE 2 — UI v1.2 (ui/-only):** directive `docs/DIRECTIVE_UI_v1_2_club_valuation_2026-07-16.md`,
estimate 1.5–3 h. Based on the SHIPPED board 790136a3 (never the candidate). Prescreen vs the
directive: fence 100% ui/ · ring-fence pin asserted · values NEVER from the owner's sheet (live pick
curve STAMP-ASSERTED; mean over band; 2027 × 0.90) · picks visible ONLY in club-valuation context ·
Best-23 exact greedy · the HOW_TO_UPDATE_INPUTS one-pager ships · posture toggles + +1/+2 re-enable
DEFERRED-FLAGGED, not built. Merges on the owner's click after his viewing (Tier 3, off the ladder).

## 3 — THE QUEUE (after the two prescreens file)
1. **LEG-B FUNCTIONAL-FORM MEMO (Fable, EXTRA — the chapter's crux).** The memo DECIDES, before any
   build: (a) the functional family replacing the concave top (candidates to weigh: piecewise-linear
   in log-space above a fitted knot · power-law blend β→1 with evidence · isotonic re-fit under a
   convexity floor); (b) the CONSERVATION mechanism (load-time-calibration renormalisation vs explicit
   budget transfer — mind the cohort-ratio denominators); (c) the REID EXTENSION mechanics (the same
   map applied to projected years — where, exactly, in the valuation path); (d) the acceptance mapping
   (pre-sim on the frozen suite: which settings clear β ≥ 0.85 and ratio ≥ 1.75 UNDER G-COHORT 1.30
   with the captain lift in); (e) interactions: captain curve · L-SMOOTH · census gauge. Then the
   Leg-B directive + **acceptance v1.17 JSON** (entries enumerated in spec §5; add G-Y0's population
   tolerance placeholder for Leg D).
2. Leg-C directive (semantics fully ruled — item 20b; the 11 changes + 91 blends + THE ATTRIBUTION
   deliverable, item 181) — can be pre-drafted ANY TIME, it does not depend on B's design.
3. Leg-D directive (G-Y0 population construction + numéraire + band wiring) → Leg-E (R103.3
   projection construction + posture profiles; movers report for preset ratification, R104.4).
4. The ladder: prescreen → cold audit (S6-sharded) → owner sealed-reads viewing → word → ONE bake →
   tag v2.11 → promote. THEN: post-Leg-E UI pass (posture toggles + +1/+2 re-enable).

## 4 — OWNER-AVAILABLE PARALLEL ITEMS (offered, none blocking)
**A-PAIRS pair 2** (`young_gun_tbd` vs Bontempelli) has NEVER been resolved — an owner decision that
can land any time and joins the acceptance set. · **Cross-model adversarial review of the spec**
(precedented): the supervisor authors a GPT prompt, the owner runs it independently — cheap QA on the
chapter's crux while builds run. · Flex positioning register revisions re-courier ONLY if he changes
them (the filed CSVs are the sources).

## 5 — SUPERVISOR CONDUCT (recent standing rules — carry verbatim)
Every PK-sync ask SHIPS the referenced documents via present_files IN THE SAME TURN (item 186,
owner-worded; the miss is on record). · Every directive carries a CONCRETE wall-clock TIME estimate
(item 188, owner-worded). · Owner token: provided in-chat when needed; NEVER written to any
file/journal/commit; advise revocation when a seat's need ends. · Vocab trap: the store's
future_position vocabulary is GDEF/GFWD/KDEF/KFWD/MID/RUC — NOT the board posCodes (item 180's 556
false diffs). · The plain-read opening, DO:/WHY: asks, one build chat per lane, journals every turn.

## 6 — LOOSE ENDS CARRIED (unchanged)
Book cross-CPU stable-sha hygiene (item 170 note 2) · ingestion go-live gated separately (804-CSV
migrated; NOT this chapter) · branch posture: census-v2 `cfe4a14` + cm_400 diagnosis `796f371` stay
standing branches by default · the owner's Law-2 flex register (his fill, his timing).
